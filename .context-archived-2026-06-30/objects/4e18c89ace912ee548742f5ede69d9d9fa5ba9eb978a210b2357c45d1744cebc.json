{
  "title": "install --update clobbers user schema customizations; no customization-preserving merge",
  "status": "closed",
  "priority": "P2",
  "package": "pi-context",
  "description": "install --update overwrites each installed schema with the catalog copy — verbatim on a same-version resync, migrate-or-refuse on a version gap — so a user's edits to a shipped block schema are lost on update. Block item data is preserved (S1) and a user-added custom block kind is untouched (no catalog counterpart), but edits to a catalog-shipped schema have no merge path: update is replace.",
  "evidence": [
    {
      "file": "packages/pi-context/src/index.ts",
      "reference": "resyncSchema overwrites the installed schema with the catalog version on install --update; same-version resync is a verbatim overwrite, version-gap is migrate-or-refuse — neither reconciles local schema edits"
    }
  ],
  "impact": "A non-developer user who tailors a shipped schema must choose: take the update and lose the customization, or run plain install (never-clobber) and forgo the update. Tailoring a catalog schema is fragile against the next --update.",
  "canonical_vocabulary": "three-way merge on update (customization-preserving upgrade)",
  "proposed_resolution": "Two candidate routes. (1) Deterministic three-way merge on install --update: reconcile the catalog schema change against the user's local edits (base = the previously-installed catalog version), preserving non-conflicting edits and surfacing conflicts instead of overwriting; or an overlay that re-applies recorded customizations after the catalog refresh. (2) Agent-mediated update in pi: for users tracking the samples, a mechanism by which the pi agent reconciles the user's local schema against the updated samples — applying the catalog change while preserving the user's customizations by judgement rather than a deterministic merge."
}