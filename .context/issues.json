{
  "issues": [
    {
      "title": "resyncSchema refuse path leaves stray migration decls in migrations.json",
      "body": "Found by the S4 adversarial audit (TASK-026 / FGAP-029) before commit. On a version-bump re-sync, resyncSchema appends the shipped migration declarations into the substrate's migrations.json before it forward-validates the block. When the migrated items would fail the new schema, validation throws and the outcome is 'blocked'; the catch block restores the schema file bytes and invalidates the in-process registry cache, but it does not undo the decls already written to migrations.json. A refused re-sync therefore leaves stray migration entries on disk, contradicting the function docstring's claim that a 'blocked' outcome leaves files byte-identical to their pre-call state. Latent in current project state (the idempotency guard prevents a collision crash on re-run, and no live re-sync currently reaches the failing-items path), but a real correctness bug in the refuse path. Planned fix: capture migrations.json raw bytes before the decl-append loop and restore them (or unlink if the file did not exist pre-call) in the existing catch, mirroring the originalSchemaBytes capture/restore already in the same function; add a migrations.json-byte-unchanged assertion to the two 'blocked' tests. Fixed and closed within TASK-026 (an S4 sub-defect, not a separate arc).",
      "location": "packages/pi-context/src/index.ts:549",
      "status": "resolved",
      "category": "issue",
      "priority": "medium",
      "package": "pi-context",
      "source": "agent",
      "id": "issue-001",
      "oid": "8585cfde13ec83732bbaecd9878b9ff6",
      "content_hash": "1404265861f31b42b98801b5a3a2efd504e035eeea79d356b7165acf8cdfbadc",
      "resolved_by": "0fcba9c — resyncSchema captures migrations.json raw bytes before the decl-append loop and, on the blocked refuse path, restores them (or unlinks the file if it did not exist pre-call) in the catch, so a refused re-sync leaves migrations.json byte-unchanged; the two blocked-path tests in install-subcommand.test.ts assert migrations.json byte-identity after a blocked outcome.",
      "content_parent": "d6d89572ab328fd870734022441fbefb361e92f1f3e085c976acaad273852a5d"
    },
    {
      "id": "issue-002",
      "title": "layer-plans schema nests id-bearing arrays (plans.layers, plans.migration_phases) instead of top-level entities + membership edges",
      "body": "plans[].layers[] and plans[].migration_phases[] are nested id-bearing arrays; id-bearing items must be top-level entities joined by membership edges (edges-not-inline). context-validate flags both (code nested_id_bearing_array). Fix (schema-only; layer-plans has 0 items, no data migration): promote layers and migration_phases to top-level entity kinds, link each to its parent plan via a membership relation_type, drop the nested arrays from the layer-plans item schema; apply to the samples catalog and the live installed copy.",
      "location": "packages/pi-context/samples/schemas/layer-plans.schema.json",
      "status": "open",
      "category": "cleanup",
      "priority": "low",
      "package": "pi-context",
      "source": "agent",
      "oid": "0c48b7f89b4e10b192908e90e6001fe8",
      "content_hash": "04e269c0617a2a9d99f1a9e9a90d63e9c06beade247aff2d14d7bf47f52afbd8",
      "content_parent": "fdec4278d966ea73b00914ccb0d6adf6dbd8bf58dba51e4a92d3a90b48362658"
    },
    {
      "title": "update refused conventions items that by inspection satisfy the target schema",
      "body": "A conventions 1.0.0->1.0.1 forward-migration blocked items that, by the operator's inspection, satisfy 1.0.1: every rule carries description, enforcement, and severity; all enforcement values are within {lint,test,review,manual} and all severity values within {error,warning,info}; the migration is identity. dry-run predicted resynced; the live forward-migrate + re-validate refused, leaving schema and block byte-unchanged. Root cause undetermined. Candidates: the forward-migration path faulting; a schema-parse difference between the dry-run and live paths; an item value that passes inspection but fails a pattern, type-coercion, or JSON-Schema edge case. Reproduction: the operator's substrate (conventions installed at 1.0.0, 45 rules). Not reproducible against this repo's .context, where conventions is in-sync at 1.0.1 and validates cleanly. Confirmation is gated on the per-item schema-validation diagnostic that would name the failing item/field, or an independent reproduction of the operator's substrate.",
      "location": "packages/pi-context/src/index.ts:599",
      "status": "open",
      "category": "issue",
      "priority": "medium",
      "package": "pi-context",
      "source": "agent",
      "id": "issue-003",
      "oid": "112cebdeb44629475e8e58999a9905e8",
      "content_hash": "f1cc163943ec7bbba31fb9d224477c360003c66fcdfb84729ca8a8abe82c7172"
    },
    {
      "title": "npm link couples the operator pi-context binary to the dev dist/ — a dev build mutates/bricks the live binary",
      "body": "The global pi-context is an npm link symlink to packages/pi-context-cli/dist/bin.js; the package build is `rm -rf dist && tsc -p tsconfig.build.json && chmod +x dist/bin.js`. So npm run build repoints — and transiently (rm -rf) removes — the live-resolving operator binary: the dogfooding/operator binary IS the dev build target, with no separate installed copy. Incident: the TASK-020 config-schema bump rebuilt the linked binary to a new schema while live .context/config.json was still the old shape, so every config-loading op exit-5 bricked until the change was reverted. This is the binary-identity root coupling of that incident, distinct from the validation/migration side (FGAP-095 no load-time config migration / FGAP-096 validate-before-repair deadlock / FGAP-097 no additive discipline) and from FGAP-089 (PreToolUse hook scope). Structural fix (analysis/2026-06-18-proving-ground-prior-art-research.md): make the operator binary a COPY via npm pack + npm i -g <tarball> (or a custom prefix), refreshed by an explicit publish-free local promote step — symlink-free, so a routine dev build cannot touch it; npm link also does not validate packaging (files/exports/bin), so a packed-copy additionally catches packaging drift. Verified mechanics + derivation in analysis/2026-06-18-proving-ground-prior-art-research.md and analysis/2026-06-18-isolated-proving-ground-method.md.",
      "location": "packages/pi-context-cli/package.json:31",
      "status": "open",
      "category": "issue",
      "priority": "critical",
      "package": "pi-context-cli",
      "source": "human",
      "id": "issue-004",
      "oid": "0b63971e2c1396b97c644cbe71521979",
      "content_hash": "dddd78d9254eb3cbd215d8703032b7873690c3a5646df32405a19626604d710d"
    },
    {
      "title": "update same-version resync verbatim-overwrites the installed schema with no block-item re-validation — a narrowing catalog change silently invalidates existing items",
      "body": "update routes a catalog-ahead schema whose body changed at the SAME version through resyncSchema's verbatim-overwrite arm (index.ts:1066): the installed schema file is replaced with the catalog body and no existing block item is re-validated against it. A catalog change that narrows validity at the same version — dropping a property under additionalProperties:false, adding a required field, narrowing an enum — silently leaves existing block items invalid, with no refusal and no warning. This is asymmetric with the version-bump path, which forward-migrates + re-validates the items and refuses (blocked, byte-unchanged) when they would fail. Concretely: the catalog story schema omits user_kind under additionalProperties:false while 14 live story items carry user_kind; update --dryRun classifies that schema resync (not blocked), so applying it invalidates those items.",
      "location": "packages/pi-context/src/index.ts:1066",
      "status": "open",
      "category": "issue",
      "priority": "high",
      "package": "pi-context",
      "source": "agent",
      "id": "issue-005",
      "oid": "b9c214a91ec020a4f2461fc9bd23091e",
      "content_hash": "0af9de1a04c09bd55da9e36ec3c198f0b93acb15cde8d8c0ba0aafee10fecba4"
    },
    {
      "title": "promote-cli runs `npm rm -g` against a poisonable, unvalidated target prefix before any guard",
      "body": "scripts/promote-cli.mjs resolves the install target via `npm prefix -g` (resolveTargetPrefix, line 107), which echoes an inherited `npm_config_prefix` rather than the true global; an invocation that set `--prefix` (e.g. `npm --prefix <dir> run`) exports `npm_config_prefix` into the child, so the resolved target becomes that value. `isRealGlobal` (line 116) is derived from the source label alone, which survives the poisoning, so a poisoned prefix is classified as the real global. When `isRealGlobal` is true the script runs the destructive `npm rm -g @davidorex/pi-context-cli` (line 153) before installing and before any validation of the target. The only validation — the realpath `startsWith(REPO_ROOT)` check (line 177) — is post-install, too late to prevent the rm. Effect: a global removal runs against an unvalidated prefix. Reproducing condition: `npm --prefix <repo> run promote:cli` resolves the target to the repo root, classifies it `isRealGlobal` true, and installs under the repo (`bin/`, `lib/node_modules/`); the post-install guard then aborts (exit 1). The destructive `npm rm -g` is aimed at the same poisoned prefix, so it does not touch the real global in this path — but the rm still runs unguarded against an unvalidated target. Class: a destructive operation executed before its precondition is validated, compounded by trusting a poisonable ambient environment for a safety-critical target. Fix: resolve the real global from a source that does not honor an inherited `npm_config_prefix` (or refuse the implicit real-global arm when the prefix is inherited); hoist a pre-destruction validation before line 153 carrying the `REPO_ROOT` check plus an intended-global assertion, so no destructive op runs until the target is validated; retain the post-install guard as defense-in-depth. Post-condition: no destructive op executes against an unvalidated target.",
      "location": "scripts/promote-cli.mjs:153",
      "status": "open",
      "category": "issue",
      "priority": "critical",
      "package": "pi-context-cli",
      "source": "agent",
      "id": "issue-006",
      "oid": "2158379b05144b5ed8c9eb44fd67b295",
      "content_hash": "c9c65f34ac8c19346d6c4d178b6ee5c98dc7bd69bfe6da36b1c553ea5b3f6b78"
    }
  ]
}