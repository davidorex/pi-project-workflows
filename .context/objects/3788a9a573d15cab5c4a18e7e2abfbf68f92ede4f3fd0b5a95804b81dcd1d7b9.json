{
  "enforcement": "review",
  "severity": "error",
  "description": "Intel and context are captured durably by the agent that produces them, at the moment of production — never left in the ephemeral transient-output state for the orchestrator to re-render to file (a re-rendering step that risks signal loss).\n1. An investigating/exploring agent WRITES ITS OWN report file (analysis/<date>-<slug>.md) as part of its task — the durable artifact is the agent's output, not a summary the orchestrator transcribes afterward.\n2. A gap-exploring agent (Experience-Gap Handling) writes the investigation file AND, when the investigation is research-grade (grounded root cause / repro / class verdict), files the rhetorical-register-compliant research item itself (findings_document pointing at its md, findings_summary self-contained) — so the substrate filing carries the investigator's full signal verbatim.\n3. The agent's transient return to the orchestrator is a pointer + verdict (path, 2-3 sentence conclusion), not the content: the content already persists.\n4. The orchestrator verifies (reads back the file/filing, spot-checks claims) and commits — it does not author the artifact from the agent's summary.\nEnforcement: at review of agent briefs (a brief for investigation work that lacks the write-your-own-report instruction is incomplete) and of the resulting artifacts (an investigation whose only trace is the agent's transient return violates this).",
  "status": "active"
}