{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "pi-context://schemas/tasks",
  "version": "1.1.0",
  "title": "Tasks",
  "description": "Discrete units of work with status lifecycle (planned, in-progress, completed, blocked, cancelled). The operational layer between gaps/requirements and code changes.",
  "type": "object",
  "required": [
    "tasks"
  ],
  "properties": {
    "schema_version": {
      "type": "string",
      "description": "Semver of the block schema this envelope was last written against. Stamped by the write path to the schema's current version; when it differs from the installed schema's version, the read/write gates walk the block through the registered migration chain (or fail loud when no chain reaches the current version). Absent on blocks not yet written by a stamping write path."
    },
    "tasks": {
      "type": "array",
      "items": {
        "type": "object",
        "required": [
          "id",
          "description",
          "status"
        ],
        "properties": {
          "id": {
            "type": "string",
            "pattern": "^TASK-\\d{3,}$"
          },
          "description": {
            "type": "string",
            "x-prompt-budget": {
              "tokens": 1000,
              "words": 800
            }
          },
          "status": {
            "type": "string",
            "enum": [
              "planned",
              "in-progress",
              "completed",
              "blocked",
              "cancelled"
            ]
          },
          "files": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Files this task creates or modifies"
          },
          "acceptance_criteria": {
            "type": "array",
            "items": {
              "type": "string",
              "x-prompt-budget": {
                "tokens": 1000,
                "words": 800
              }
            }
          },
          "assigned_agent": {
            "type": "string",
            "description": "Agent name if assigned"
          },
          "notes": {
            "type": "string",
            "x-prompt-budget": {
              "tokens": 1000,
              "words": 800
            }
          },
          "oid": {
            "type": "string",
            "pattern": "^[0-9a-f]{32}$",
            "description": "Content-independent substrate-stable item identity (content-addressed substrate identity, Cycle 3). Minted once at item birth via mintOid(substrate_id); immutable across content versions. Optional in the schema so pre-Cycle-3 items validate; stamped on next write."
          },
          "content_hash": {
            "type": "string",
            "pattern": "^[0-9a-f]{64}$",
            "description": "SHA-256 (hex) of the RFC-8785-canonical content projection of this item (metadata fields excluded). Recomputed on every stamping write; moves with content, stable across metadata-only churn."
          },
          "content_parent": {
            "type": "string",
            "pattern": "^[0-9a-f]{64}$",
            "description": "content_hash of the immediately-prior version of this item; set on a content-changing update, absent on the first (v1) version. Forms the per-item content version chain."
          }
        },
        "x-rhetorical-criteria": {
          "downstream_consumer": "The implementing agent dispatched to execute the task: `description` and acceptance_criteria are composed verbatim as the work brief it acts on, so they must be self-sufficient and literally correct.",
          "register_notes": "`description` states what to do and its scope at implementation-decision granularity — the operational bridge from a gap / requirement to a code change — self-contained and actionable without external context; `notes` carries terse working intel, not a running log. State the task's current shape and scope; no narration of prior task framing or status history (the status field and edges carry lifecycle).",
          "prohibited_patterns": []
        }
      }
    }
  }
}