{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "pi-context://schemas/research",
  "version": "1.2.0",
  "title": "Research",
  "description": "Factual and analytical substrate under decisions — each entry captures what was investigated, how, what was found, what grounds the findings, and what stale conditions invalidate them.",
  "type": "object",
  "required": [
    "research"
  ],
  "properties": {
    "schema_version": {
      "type": "string",
      "description": "Semver of the block schema this envelope was last written against. Stamped by the write path to the schema's current version; when it differs from the installed schema's version, the read/write gates walk the block through the registered migration chain (or fail loud when no chain reaches the current version). Absent on blocks not yet written by a stamping write path."
    },
    "research": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "id",
          "title",
          "status",
          "layer",
          "type",
          "question",
          "method",
          "findings_summary",
          "created_by",
          "created_at"
        ],
        "properties": {
          "id": {
            "type": "string",
            "pattern": "^R-\\d{4}$"
          },
          "title": {
            "type": "string"
          },
          "status": {
            "type": "string",
            "enum": [
              "planned",
              "in-progress",
              "complete",
              "stale",
              "superseded",
              "revised"
            ],
            "description": "planned = question framed, method chosen, no work started. in-progress = investigation underway. complete = findings_summary and citations finalized. stale = a stale_condition fired; findings may still be correct but grounding is no longer authoritative. superseded = a newer research entry replaces this one. revised = transient state during re-investigation against updated grounding."
          },
          "layer": {
            "type": "string",
            "enum": [
              "L1",
              "L2",
              "L3",
              "L4",
              "L5"
            ],
            "description": "Which architecture layer the research informs. L1 identity/domain; L2 specification; L3 work; L4 execution; L5 memory."
          },
          "type": {
            "type": "string",
            "enum": [
              "investigative",
              "comparative",
              "empirical",
              "historical",
              "audit",
              "landscape",
              "feasibility",
              "curation"
            ],
            "description": "Kind of research, per the research:* skill taxonomy. Affects reliability characteristics and staleness behavior."
          },
          "question": {
            "type": "string",
            "description": "The question being investigated.",
            "x-prompt-budget": {
              "tokens": 1000,
              "words": 800
            }
          },
          "method": {
            "type": "string",
            "description": "How the investigation was conducted — e.g. 'code-inspection', 'empirical-test', 'web-fetch', 'curation of external document', or a combination.",
            "x-prompt-budget": {
              "tokens": 1000,
              "words": 800
            }
          },
          "scope": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "What was in scope — versions, branches, revisions, URLs, documents examined."
          },
          "findings_summary": {
            "type": "string",
            "description": "Self-contained, prompt-injectable summary of what was found. Target ~300-800 words. Must stand alone without the full findings_document.",
            "x-prompt-budget": {
              "tokens": 1000,
              "words": 800
            }
          },
          "findings_document": {
            "type": "string",
            "description": "Path to the full rich document (typically in analysis/) for deep reads. May be null for research entries whose summary is sufficient or whose investigation has not yet produced a document."
          },
          "grounding": {
            "type": "object",
            "additionalProperties": false,
            "properties": {
              "dependencies": {
                "type": "array",
                "items": {
                  "type": "string"
                },
                "description": "Version-pinned dependency strings (e.g. '@mariozechner/pi-ai@0.63.1') whose behavior is load-bearing for the findings."
              },
              "revisions": {
                "type": "array",
                "items": {
                  "type": "string"
                },
                "description": "Git SHAs, branch snapshots, or repo-at-date references."
              },
              "external_refs": {
                "type": "array",
                "items": {
                  "type": "string"
                },
                "description": "Dated external references — URLs with retrieval timestamps, academic papers, external spec documents."
              }
            }
          },
          "grounded_at": {
            "type": "string",
            "description": "Timestamp at which the grounding was current."
          },
          "stale_conditions": {
            "type": "array",
            "items": {
              "oneOf": [
                {
                  "type": "string"
                },
                {
                  "type": "object",
                  "additionalProperties": false,
                  "required": [
                    "kind",
                    "item",
                    "bucket"
                  ],
                  "properties": {
                    "kind": {
                      "const": "item-status"
                    },
                    "item": {
                      "type": "string",
                      "description": "Selector of the substrate item whose status this condition watches (a reference predicate, not an embedded entity — deliberately not named 'id')."
                    },
                    "bucket": {
                      "type": "string",
                      "description": "Status bucket that fires the condition when the watched item's status maps to it."
                    }
                  }
                },
                {
                  "type": "object",
                  "additionalProperties": false,
                  "required": [
                    "kind",
                    "path"
                  ],
                  "properties": {
                    "kind": {
                      "const": "file-changed"
                    },
                    "path": {
                      "type": "string",
                      "description": "Project-root-relative path whose content change fires the condition."
                    },
                    "lines": {
                      "type": "string",
                      "description": "Optional comma-separated 1-indexed line numbers / inclusive ranges (e.g. '483-492,597-614,651-652'), the same shape citations[].lines carries. Scopes the condition to exactly the cited lines: baseline_hash is stamped and compared over those lines only, so an edit elsewhere in the file does not fire it. Keyed by line number, not a content anchor — an earlier edit that shifts line numbers can still false-fire or false-pass. Absent means whole-file."
                    },
                    "baseline_hash": {
                      "type": "string",
                      "pattern": "^[0-9a-f]{64}$",
                      "description": "SHA-256 at grounding time — of the file's bytes, or of exactly the cited lines when 'lines' is present. Stamped by the write path when absent and the file is readable; the condition fires when the current hash differs (or the file is gone)."
                    }
                  }
                },
                {
                  "type": "object",
                  "additionalProperties": false,
                  "required": [
                    "kind",
                    "ref"
                  ],
                  "properties": {
                    "kind": {
                      "const": "revision-moved"
                    },
                    "ref": {
                      "type": "string",
                      "description": "Git ref whose movement fires the condition."
                    },
                    "baseline_sha": {
                      "type": "string",
                      "description": "Commit the ref pointed at when grounding was current. Stamped by the write path when absent and the ref resolves; the condition fires when the ref moves."
                    }
                  }
                }
              ]
            },
            "description": "Conditions under which the research is no longer authoritative. Bare strings are human-judged only. Typed variants are machine-evaluated: context-validate flags a complete item whose condition fired as a staleness candidate, and context-reconcile (or a human) applies the complete-to-stale transition — never silently."
          },
          "citations": {
            "type": "array",
            "description": "Labeled source pointers grounding specific claims in the findings. Every factual claim in findings_summary should trace to a citation.",
            "items": {
              "type": "object",
              "additionalProperties": false,
              "required": [
                "label"
              ],
              "properties": {
                "label": {
                  "type": "string"
                },
                "path": {
                  "type": "string"
                },
                "lines": {
                  "type": "string"
                },
                "url": {
                  "type": "string"
                },
                "retrieved_at": {
                  "type": "string"
                }
              }
            }
          },
          "conducted_by": {
            "type": "string",
            "description": "Alias for created_by; retained for research-specific provenance where 'conducted' reads more naturally than 'created'."
          },
          "conducted_at": {
            "type": "string",
            "description": "Alias for created_at."
          },
          "created_by": {
            "type": "string"
          },
          "created_at": {
            "type": "string"
          },
          "modified_by": {
            "type": "string"
          },
          "modified_at": {
            "type": "string"
          },
          "oid": {
            "type": "string",
            "pattern": "^[0-9a-f]{32}$",
            "description": "Content-independent substrate-stable item identity (content-addressed substrate identity, Cycle 3). Minted once at item birth via mintOid(substrate_id); immutable across content versions. Optional in the schema so pre-Cycle-3 items validate; stamped on next write."
          },
          "content_hash": {
            "type": "string",
            "pattern": "^[0-9a-f]{64}$",
            "description": "SHA-256 (hex) of the RFC-8785-canonical content projection of this item (metadata fields excluded). Recomputed on every stamping write; moves with content, stable across metadata-only churn."
          },
          "content_parent": {
            "type": "string",
            "pattern": "^[0-9a-f]{64}$",
            "description": "content_hash of the immediately-prior version of this item; set on a content-changing update, absent on the first (v1) version. Forms the per-item content version chain."
          }
        }
      }
    }
  },
  "x-lifecycle": {
    "field": "status",
    "states": [
      "planned",
      "in-progress",
      "complete",
      "stale",
      "superseded",
      "revised"
    ],
    "transitions": [
      {
        "from": "planned",
        "to": "in-progress",
        "required_authority": "any"
      },
      {
        "from": "in-progress",
        "to": "complete",
        "required_authority": "any",
        "note": "Agent may mark own research complete when findings_summary and citations are finalized."
      },
      {
        "from": "complete",
        "to": "stale",
        "required_authority": "any",
        "note": "Any party marks stale when a stale_condition fires. A future staleness engine fires this automatically."
      },
      {
        "from": "stale",
        "to": "in-progress",
        "required_authority": "user",
        "note": "Reopening for revision; requires user authority if the research is currently cited by an enacted decision because revising may invalidate the decision's grounding."
      },
      {
        "from": "complete",
        "to": "superseded",
        "required_authority": "any",
        "precondition": "superseding entry exists and points to this one via a research_supersedes_research edge"
      },
      {
        "from": "in-progress",
        "to": "revised",
        "required_authority": "any"
      },
      {
        "from": "revised",
        "to": "complete",
        "required_authority": "any"
      }
    ],
    "note": "AJV draft-07 cannot enforce transition validity or required_authority; this block is metadata reserved for future framework-level state-machine validation and authorship attestation. A future staleness engine reads stale_conditions and fires the complete→stale transition automatically when conditions are met."
  }
}