{
  "research": [
    {
      "id": "R-0001",
      "title": "Shared CLI core across monorepo packages — commonalities, layering, and scope",
      "status": "complete",
      "layer": "L2",
      "type": "feasibility",
      "question": "What are the underlying commonalities any monorepo package CLI needs (pi-context-cli today; future pi-workflows / jit-agent-dispatch CLIs) so we do not duplicate code — a single source for what is best single-source-editable, with each package adding its particulars — from a minimalist best-practices perspective?",
      "method": "Code-inspection via parallel Explore agents: mapped pi-context-cli (bin.ts/cli.ts) generic-vs-package-specific machinery, and the op/tool registration shapes + execution models of pi-workflows, pi-agent-dispatch, pi-behavior-monitors, pi-jit-agents.",
      "scope": [
        "pi-project-workflows monorepo at v0.30.0 (HEAD be16688)",
        "packages/pi-context-cli/src/bin.ts + cli.ts",
        "packages/pi-context/src/ops-registry.ts + dispatch-context.ts + read-element.ts",
        "src/ tool registration of pi-workflows / pi-agent-dispatch / pi-behavior-monitors / pi-jit-agents"
      ],
      "findings_summary": "The duplication risk for monorepo package CLIs is real but narrower than \"share OpDefinition.\"\n\nCorrective finding: OpDefinition is pi-context's INTERNAL wrapper (ops-registry.ts), not a shared contract — registerAll wraps each OpDefinition into pi.registerTool. The contract already single-sourced in-pi is pi-mono's pi.registerTool / AgentToolResult, which all four extensions use identically; there is no in-pi duplication to remove. The risk is entirely in the CLI layer.\n\nWhat is genuinely common (all generic in pi-context-cli today): argv->params coercion driven purely by the op's typebox parameters schema (--field, @file, scalar/enum/JSON, required-checking); global flags --cwd/--json/--yes/--help; the auth-DECISION logic (authGated + --yes + TTY); help generation; exit codes; and the output layer — OpResult (string|{json}|{read}), renderOpResultText, boundedJsonOutput, and the 50KB cap enforced at the output boundary. That cap was bug-prone this arc (FGAP-013/015: the {json}-channel bypass + the read-.data leak); single-sourcing it stops that class recurring per package. Package-specific is small: the ops registry, a buildContext callback (pi-context's DispatchContext + resolveIdentity + WriterIdentity), the writer-kind vocabulary, and which ops are authGated.\n\nMinimalist shape: one small cli-core package generic over a context type Ctx and a minimal CliOp shape ({name, description, parameters: TSchema, surface, authGated?, run(cwd, params, ctx)}); each package bin ~= runCli({programName, ops, buildContext}). Layering nuance (load-bearing): OpResult/render/cap are used by BOTH the CLI and pi-context's in-pi registerAll (tool text), so they belong in a base both depend on — a tiny op-io package — NOT inside the CLI package (that would invert the extension->CLI dependency). op-io is also where the cap-at-boundary invariant becomes structurally un-bypassable for any surface.\n\nHonest scope limit: a reflection CLI serves only request->response ops. pi-context fits today. pi-workflows fits PARTIALLY (simple ops reflect; workflow-execute/resume are long-running subprocess + checkpoint state + TUI — bespoke, not reflection). pi-agent-dispatch does NOT fit as-is (gating is a pi-runtime pi.on('tool_call') handler). pi-behavior-monitors does NOT fit (tools read runtime state mutated by agent_end handlers). pi-jit-agents is a pure library (no tools, no CLI). So cli-core is the single source for the request->response CLI projection — pi-context now, the request->response subset of pi-workflows later — NOT a uniform four-package CLI. Consistent with the per-package-CLI decision (design-ledger 1c).\n\nOnboarding seam: a package provides an adapter tools->CliOp[]; pi-context's OpDefinition already conforms; pi-workflows wraps its request->response tools' execute->run. The cli-core/op-io split is a PROPOSAL — six open questions (op-io boundary, Ctx-genericity vs a shared identity model, the workflows long-running split, an auth-policy hook, generalizing scripts/parity-check.ts to any cli-core adopter, package count) are recorded in the findings_document; pre-implementation it goes through the canonical pipeline.",
      "findings_document": "analysis/2026-06-05-shared-cli-core-design.md",
      "grounding": {
        "dependencies": [
          "@earendil-works/pi-coding-agent — pi.registerTool / AgentToolResult is the actual shared in-pi tool contract used identically by all four extensions"
        ],
        "revisions": [
          "be16688 (v0.30.0)"
        ]
      },
      "stale_conditions": [
        "pi-workflows / pi-agent-dispatch / pi-behavior-monitors change their tool registration shape away from the pi-mono pi.registerTool interface.",
        "OpResult / renderOpResultText / boundedJsonOutput / the 50KB boundary cap relocate or change semantics (currently in packages/pi-context/src/ops-registry.ts + read-element.ts).",
        "pi-context-cli's reflection machinery (parseOpArgs / authDecision / the envelope emission) is refactored such that the generic-vs-specific seam shifts.",
        "The per-package-CLI decision (design-ledger 1c) is revisited toward a single meta-CLI.",
        "pi-mono's registerTool / AgentToolResult interface changes."
      ],
      "citations": [
        {
          "label": "pi-context-cli generic reflection engine (parse/coerce/flags/auth/envelope)",
          "path": "packages/pi-context-cli/src/cli.ts"
        },
        {
          "label": "OpDefinition contract — pi-context-internal, not shared",
          "path": "packages/pi-context/src/ops-registry.ts",
          "lines": "201-210"
        },
        {
          "label": "output cap machinery: OpResult / renderOpResultText / boundedJsonOutput / 50KB boundary cap",
          "path": "packages/pi-context/src/ops-registry.ts",
          "lines": "104-210"
        },
        {
          "label": "DispatchContext / WriterIdentity — package-specific run-context",
          "path": "packages/pi-context/src/dispatch-context.ts"
        },
        {
          "label": "pi-workflows direct pi.registerTool registration + long-running executors (divergent model)",
          "path": "packages/pi-workflows/src/index.ts"
        },
        {
          "label": "pi-jit-agents is a library, not an extension (no CLI surface)",
          "path": "packages/pi-jit-agents/README.md"
        },
        {
          "label": "per-package-CLI decision (1c)",
          "path": "analysis/2026-06-03-pi-context-cli-design-ledger.md"
        },
        {
          "label": "full design doc (deep read)",
          "path": "analysis/2026-06-05-shared-cli-core-design.md"
        }
      ],
      "created_by": "human/davidryan@gmail.com",
      "created_at": "2026-06-05T00:00:00.000Z",
      "modified_by": "human/davidryan@gmail.com",
      "modified_at": "2026-06-04T21:57:33.590Z",
      "oid": "a0c0c826114705c8bf5bfb50d4f5189e",
      "content_hash": "98c0b0c6cc1f700fb0f673a4e7e648cdb701f18edd2e4787f15b577f79a3a992"
    },
    {
      "id": "R-0002",
      "title": "Substrate portability — export/import bundles vs an external/global substrate dir",
      "status": "complete",
      "layer": "L2",
      "type": "feasibility",
      "question": "How can a pi-context substrate be made portable via a `pi-context-cli export / import` command (e.g. encode a project-management model once and import it into other projects so it 'just works'), and/or via a switchable-to substrate that lives at an external `path/to/.substrate-dir` global location — which is feasible, what already exists, and what must be built?",
      "method": "Code-inspection via three parallel read-only Explore agents over packages/pi-context/src, packages/pi-context-cli/src, packages/pi-workflows/src, and scripts/ at branch context-jit-spec-v2 (post-v0.30.0, HEAD be16688). Agents mapped: (a) the existing model-install/catalog machinery (init/accept-all/install + samples catalog), (b) substrate-dir resolution + switching + external-path feasibility + content-identity, (c) the CLI op-registry reflection surface + the parity gate + the pi-workflows workflow-discovery prior art.",
      "scope": [
        "pi-project-workflows monorepo, branch context-jit-spec-v2, post-v0.30.0 (HEAD be16688)",
        "packages/pi-context/src/{context.ts, context-dir.ts, index.ts, block-api.ts, ops-registry.ts, content-hash.ts, object-store.ts, samples-catalog.ts}",
        "packages/pi-context/samples/{conception.json, schemas/, blocks/} + schemas/bootstrap.schema.json",
        "packages/pi-context-cli/src/{bin.ts, cli.ts}",
        "packages/pi-workflows/src/workflow-discovery.ts",
        "scripts/parity-check.ts"
      ],
      "findings_summary": "The prompt holds TWO non-interchangeable mechanisms. Fork 1 — export/import (file bundle): transport a COPY; materialize an independent instance in the target project. Fork 2 — external/global path/to/.substrate-dir: share ONE live substrate across projects. The user's PM-import example is Fork 1, and specifically a MODEL export (vocabulary), not instance content.\n\nSecond load-bearing split: a substrate = MODEL (portable: config.json registries — block_kinds, relation_types, hierarchy, lenses, invariants, layers, status_buckets, display_strings, naming, installed_schemas/blocks, tool_operations; plus schemas/*.schema.json) vs INSTANCE (not portable: each block's items, relations.json, substrate_id, root). ConfigBlock at context.ts:39; REGISTRY_DESCRIPTORS at :942.\n\nFork 1 — import is ALREADY HALF-BUILT, just hardcoded to the packaged catalog. /context init (initProject, index.ts:250) + /context accept-all (adoptConception, context.ts:873, reads samples/conception.json, never-clobber guard :882) + /context install (installContext, index.ts:334, copies installed_schemas/blocks from samples/ with skip-if-exists unless --update :387) IS 'import from a bundle'. Directionality is strictly one-way today (grep finds no export producer). The seam is two changes: (a) parameterize the source — adoptConception(cwd,{conceptionPath?}) + installContext(cwd,{schemasRoot?,blocksRoot?}), default packaged (unchanged), override to a bundle dir; then /context import <bundle> is thin orchestration inheriting never-clobber + --update. (b) Add net-new exportSubstrate(cwd,outDir): strip root + substrate_id, copy declared schemas, emit starter (or full) blocks, write a samples/-shaped conception.json. CRITICAL constraint: the 50KB output cap (DEFAULT_MAX_BYTES, truncate.ts:12; enforced in renderOpResultText + boundedJsonOutput, ops-registry.ts:127-199) means a whole-substrate bundle (100KB-1MB) CANNOT flow through the {json} channel — export MUST be a file-write op returning {read}/prose metadata (parity-check checkJsonContentCap would hard-fail {json:<content-read>}). import fits cleanly as a request->response write op (template amend-config, ops-registry.ts:1083). CLI exposure is automatic: useOps filters surface:'use' (cli.ts:44), zero CLI edits per op.\n\nFork 2 — NOT RESOLVABLE TODAY. resolveContextDir is unconditionally path.join(cwd, contextDir) (context-dir.ts:142) with no isAbsolute check and no ~ expansion; an absolute contextDir gets silently NESTED under cwd (path.join('/base','/abs')='/base/abs'), and bootstrap.schema.json puts no pattern on contextDir so it fails silently. Every path builder chains through that resolver (config/relations/schemas/migrations/agents/templates/blocks/object-store/locks/tmp-writes), so all inherit the breakage. listSubstrates only scans cwd by bare dir-name (index.ts:692,:709) so external dirs are invisible to switching. Enabling Fork 2 (no debt): isAbsolute branch in the resolver; a bootstrap-schema absolute-path policy; external-target handling in switchToExisting/switchAndCreate; an external-substrate discovery/registry; AND a substrate-level lock — the real gap: per-block locks (withBlockLock, block-api.ts:45) serialize on the absolute block path (cross-project-safe), but config.json/relations.json/migrations.json are written WITHOUT lock, so concurrent multi-project mutation races.\n\nIdentity crux (governs moving instance content): oid = sha256([substrate_id, nonce]) (mintOid, block-api.ts:624) is substrate-SCOPED — re-import yields a different oid; content_hash = sha256(content) (content-hash.ts:65) is GLOBALLY stable — enables cross-substrate dedup + integrity. So Fork 1 MODEL export is identity-clean; content export and Fork 2 sharing both need an oid-reconciliation policy (re-mint under target substrate_id, key on content_hash).\n\npi-workflows reality-check: 'import workflows' is actually directory-scan DISCOVERY (discoverWorkflows, workflow-discovery.ts — project .workflows/ > user ~/.pi/agent/workflows/ > bundled, dedup-by-name), NOT an import command or bundle transport. It is prior art for Fork 2's discovery angle, not Fork 1.\n\nThe forks are separable and need not land together. Fork 1 model export/import is lowest-risk (mostly generalization of existing machinery); Fork 2 touches the resolver everything chains through and needs the locking + identity work before it is safe.",
      "findings_document": "analysis/2026-06-05-substrate-portability-export-import.md",
      "grounding": {
        "revisions": [
          "be16688 (v0.30.0); branch context-jit-spec-v2"
        ],
        "dependencies": [
          "Node path.join POSIX semantics — path.join(cwd, absolutePath) nests rather than resets, which is why an absolute contextDir fails silently in resolveContextDir"
        ]
      },
      "stale_conditions": [
        "resolveContextDir (context-dir.ts) gains an isAbsolute / ~-expansion branch — Fork 2's primary blocker is removed and the 'not resolvable today' verdict no longer holds.",
        "An export producer or /context import command lands — Fork 1's 'strictly one-directional / half-built' framing is superseded.",
        "adoptConception / installContext change signature away from the hardcoded samples/ source assumed by the seam analysis.",
        "The 50KB output cap (truncate.ts DEFAULT_MAX_BYTES) or the boundedJsonOutput / renderOpResultText boundary relocates or changes semantics — the 'export must be file-write' constraint may shift.",
        "scripts/parity-check.ts changes its writer-classification / ctx-forwarding / {json}-content-cap gates — the parity obligations for new export/import ops change.",
        "withBlockLock scope changes or a substrate-level lock is added — the Fork 2 cross-project concurrency gap is closed.",
        "The oid/content_hash identity model (mintOid substrate-scoping; content_hash global stability) changes — the content-portability reconciliation requirement changes."
      ],
      "citations": [
        {
          "label": "ConfigBlock interface — model vs instance fields",
          "path": "packages/pi-context/src/context.ts",
          "lines": "39-64"
        },
        {
          "label": "REGISTRY_DESCRIPTORS — the portable registries",
          "path": "packages/pi-context/src/context.ts",
          "lines": "942"
        },
        {
          "label": "adoptConception — reads hardcoded samples/conception.json, never-clobber guard",
          "path": "packages/pi-context/src/context.ts",
          "lines": "873-909"
        },
        {
          "label": "installContext — copies installed_schemas/blocks from samples/ with skip-if-exists",
          "path": "packages/pi-context/src/index.ts",
          "lines": "334-410"
        },
        {
          "label": "initProject — bootstrap pointer + skeleton config + substrate_id",
          "path": "packages/pi-context/src/index.ts",
          "lines": "250"
        },
        {
          "label": "resolveContextDir — unconditional path.join(cwd, contextDir), no isAbsolute (Fork 2 blocker)",
          "path": "packages/pi-context/src/context-dir.ts",
          "lines": "104-143"
        },
        {
          "label": "listSubstrates — cwd-bound bare-name discovery",
          "path": "packages/pi-context/src/index.ts",
          "lines": "675-712"
        },
        {
          "label": "withBlockLock — per-block lock; no substrate-global lock for config/relations/migrations",
          "path": "packages/pi-context/src/block-api.ts",
          "lines": "45-55"
        },
        {
          "label": "mintOid — substrate-scoped oid",
          "path": "packages/pi-context/src/block-api.ts",
          "lines": "624-627"
        },
        {
          "label": "computeContentHash — globally stable content hash",
          "path": "packages/pi-context/src/content-hash.ts",
          "lines": "65-67"
        },
        {
          "label": "OpResult + 50KB cap at the output boundary",
          "path": "packages/pi-context/src/ops-registry.ts",
          "lines": "118-199"
        },
        {
          "label": "CLI reflection — useOps filters surface:'use'",
          "path": "packages/pi-context-cli/src/cli.ts",
          "lines": "44"
        },
        {
          "label": "parity-check gates — classification / ctx-forwarding / {json}-content-cap / dual-surface params",
          "path": "scripts/parity-check.ts",
          "lines": "423-827"
        },
        {
          "label": "workflow-discovery — directory-scan, the real 'import workflows' prior art",
          "path": "packages/pi-workflows/src/workflow-discovery.ts"
        },
        {
          "label": "full design doc (deep read)",
          "path": "analysis/2026-06-05-substrate-portability-export-import.md"
        }
      ],
      "created_by": "human/davidryan@gmail.com",
      "created_at": "2026-06-05T00:00:00.000Z",
      "modified_by": "human/davidryan@gmail.com",
      "modified_at": "2026-06-04T22:19:52.083Z",
      "oid": "8e68cdb846eaf93da1751ec989d11a9d",
      "content_hash": "34a477b940a36cdf8cf6233aa278959d3b4b47526b0e63a7a1ac11d63126796d"
    },
    {
      "id": "R-0003",
      "title": "CLI↔ops↔scripts parity survey — categorical CLI-side parity under frozen in-pi ops",
      "status": "complete",
      "layer": "L2",
      "type": "audit",
      "question": "Given pi-context's three surfaces over one library (the 48 surface:\"use\" ops, the CLI that reflects them 1:1, and the orchestrator-script twins), why do CLI defects keep surfacing, and can the CLI be brought to categorical capability-parity with the scripts WITHOUT changing in-pi op behavior — enumerating every script capability and the CLI seam that closes each gap?",
      "method": "Agent-produced comprehensive read-only survey (general-purpose, opus) over packages/pi-context/src (ops-registry.ts, read-element.ts, context.ts, block-api.ts), packages/pi-context-cli/src (bin.ts, cli.ts), scripts/orchestrator/*.ts, and scripts/parity-check.ts. Enumerated all three surfaces; built a capability matrix; categorized each parity gap under the hard constraint that in-pi op behavior is frozen. Two known gaps (FGAP-019, FGAP-020) reproduced empirically against the live .context substrate. Orchestrator independently re-verified the load-bearing counts (48 use-ops confirmed via CLI --help) and read the verdict/categorization sections directly.",
      "scope": [
        "pi-project-workflows monorepo, branch context-jit-spec-v2, HEAD 9ef6e3d",
        "48 surface:\"use\" OpDefinitions in packages/pi-context/src/ops-registry.ts",
        "packages/pi-context-cli/src/{bin.ts,cli.ts} reflection layer",
        "scripts/orchestrator/*.ts — 53 files: 24 op-twin parity targets + 16 composer + 12 runtime-demo + 1 launch-support (reconciled; the survey prose's earlier '52' was an arithmetic slip, enumeration is complete and the 24 op-twin set is correct)",
        "scripts/parity-check.ts static dual-surface gate"
      ],
      "findings_summary": "Three surfaces sit over the same block-api/context-sdk library: the in-pi OPS (48 surface:\"use\" OpDefinitions), the CLI (a 1:1 runtime reflection of the ops, cli.ts:44), and the orchestrator SCRIPTS (hand-ergonomic twins). CLI defects keep surfacing because the ops were authored for the in-pi caller (which reads schemas, retries, infers, tolerates verbose output), the CLI relays them faithfully, and the scripts add ergonomics the ops never had — so every latent op assumption becomes CLI friction on first direct use, invisible to unit tests that always supplied the happy-path input.\n\nThe survey enumerates 8 parity gaps where the CLI sits below script capability: GAP-1 FGAP-019 (array_key declared REQUIRED across a CLASS of 7 block-mutation ops though the script derives it); GAP-2 FGAP-020 (read-schema --path pages an addressed object node down to its incidental `required` array name-list); and 6 newly surveyed — GAP-3 no human-render/--format mode, GAP-4 no --show-schema contract preview, GAP-5 no AJV error translation, GAP-6 no append-time dry-run, GAP-7 no input shorthands (--writer kind:id / --where field:op:value / CSV), GAP-8 coarse exit codes (0/1/2 vs the scripts' 1-5). These were filed as FGAP-021..026 (CLI-side) and FGAP-027 (the in-pi op residual).\n\nCATEGORICAL VERDICT: full script-capability parity is reachable with ZERO in-pi op changes. Each gap maps to a CLI-layer seam under the frozen-ops constraint: GAP-1/GAP-7 = CLI-pre-call (derive/normalize input before the unchanged op — e.g. extend cli.ts's required-filter exemption from {writer} to {writer, arrayKey} and inject the derived key via a helper mirroring injectWriter); GAP-2/3/4/6/8 = CLI-only-shared-helper (a helper in-pi dispatch never touches — for GAP-2, re-run the addressed read with structureForRead({whole:true}) which skips collection paging and returns the full subtree while still applying the 50KB cap); GAP-5 = CLI-post-call (AJV error reshape in the cli.ts catch). No gap is forced into REQUIRES-OP-CHANGE for the CLI surface.\n\nTHE ONE RESIDUAL (filed as FGAP-027): closing FGAP-020 CLI-side corrects ONLY the CLI surface — the in-pi op keeps returning the paged name-list for addressed object-node reads (structureForRead paging at read-element.ts:266-285). Making BOTH surfaces return the full subtree requires passing whole:true at ops-registry.ts:1166, an op change conflicting with the frozen constraint (user-authorized, tracked as FGAP-027).\n\nREGRESSION FINDING: the existing parity-check.ts dual-surface guard keys the script twin by EXACT op-name (parity-check.ts:748), so 29 of 48 use-ops — including the entire block-mutation family where FGAP-019 lives, plus 5 name-mismatched twins — are structurally invisible to it; it also checks only 3 optional params, never a required-but-derivable one, never output shape. A behavioral gate would add, after the static checks: a script↔op name map, fixtures + an execution harness, a required-but-derivable assertion (FAIL if an op's required param is one the script derives), and an output-shape diff (the read-schema --path case FAILs today: op→name-list, script→subtree). This converts the class from found-one-at-a-time-by-dogfooding into caught-at-commit.\n\nScript inventory reconciled: 53 orchestrator scripts = 24 op-twin parity targets + 16 composer + 12 runtime-demo + 1 launch-support; the survey prose's earlier '52' was an arithmetic slip with no impact on the 24 op-twin parity set.",
      "findings_document": "analysis/2026-06-05-cli-ops-scripts-parity-survey.md",
      "grounding": {
        "revisions": [
          "9ef6e3d (branch context-jit-spec-v2)"
        ],
        "dependencies": [
          "structureForRead/discoverArrayKey/resolveCollection collection-paging semantics (read-element.ts) — load-bearing for GAP-2 and its whole:true CLI-side fix"
        ]
      },
      "stale_conditions": [
        "The CLI gains a pre-call input-derivation or post-call output-reshaping seam (cli.ts) — the gap list and CLI-side categorization are realized and the 'below script parity' framing is superseded.",
        "Any of the 8 gaps is closed (FGAP-019/020 especially) — re-derive the residual set.",
        "In-pi op behavior is unfrozen / an op is changed (e.g. whole:true at ops-registry.ts:1166, or a dryRun param added to append-block-item) — the frozen-ops constraint the verdict rests on no longer holds.",
        "parity-check.ts gains a behavioral phase or a script↔op name map (parity-check.ts:738/748) — the regression-gate finding is addressed.",
        "structureForRead's collection-paging heuristic (read-element.ts:266-285) changes — GAP-2's root locus and the whole:true fix shift.",
        "The orchestrator-script inventory changes — the 24 op-twin parity-target enumeration must be re-derived.",
        "cli.ts's reflection machinery (useOps filter :44, required-from-schema :277-282, injectWriter :293-303) is refactored such that the named seams move."
      ],
      "citations": [
        {
          "label": "full survey (deep read)",
          "path": "analysis/2026-06-05-cli-ops-scripts-parity-survey.md"
        },
        {
          "label": "CLI reflects ops 1:1 (useOps filter)",
          "path": "packages/pi-context-cli/src/cli.ts",
          "lines": "44"
        },
        {
          "label": "CLI required-flag check + writer exemption (GAP-1 pre-call seam)",
          "path": "packages/pi-context-cli/src/cli.ts",
          "lines": "277-303"
        },
        {
          "label": "read-schema op wraps addressed node in structureForRead (GAP-2 root locus / the op-change point)",
          "path": "packages/pi-context/src/ops-registry.ts",
          "lines": "1166"
        },
        {
          "label": "structureForRead collection-paging that projects an object node (FGAP-020)",
          "path": "packages/pi-context/src/read-element.ts",
          "lines": "266-285"
        },
        {
          "label": "script derives array_key (FGAP-019 contrast)",
          "path": "scripts/orchestrator/file-block-item.ts",
          "lines": "89-107"
        },
        {
          "label": "script returns full schema subtree directly (FGAP-020 contrast)",
          "path": "scripts/orchestrator/read-schema.ts",
          "lines": "181"
        },
        {
          "label": "parity-check dual-surface guard keys twin by exact op-name (the 29/48 blind spot)",
          "path": "scripts/parity-check.ts",
          "lines": "738-748"
        }
      ],
      "created_by": "human/davidryan@gmail.com",
      "created_at": "2026-06-05T00:00:00.000Z",
      "modified_by": "human/davidryan@gmail.com",
      "modified_at": "2026-06-04T23:35:40.314Z",
      "oid": "3e5f5d547f6e3d1ce9ee1c34924f38f4",
      "content_hash": "d2e94f6d02b8be0ab2828ce152f8290b9238e4d16f1f1128d755d4dd4bfd14df",
      "content_parent": "edab0e7169bd81795ec3f90cbd6b1d8106d889cffcbff81826f592507d145394"
    },
    {
      "id": "R-0004",
      "title": "pi-bound pi-context CLI — porting launch-constrained-pi.sh into a `pi-context --pi-bound` command",
      "status": "complete",
      "layer": "L2",
      "type": "feasibility",
      "question": "Can scripts/launch-constrained-pi.sh be ported into a first-class `pi-context --pi-bound` CLI command, and what does that port entail — surface composition, the static-tools + per-target-composite union, meta-package SKILL.md resolution, and the exit-code/auth contracts?",
      "method": "Spec authored in analysis/2026-06-07-pi-context-pi-bound-cli.md; evaluated twice by independent fresh-context agents that verified each assertion against the codebase (file:line + empirical CLI/`pi -p` runs), then the spec was corrected per the second evaluation and the corrections re-verified.",
      "scope": [
        "pi-project-workflows monorepo, branch context-jit-spec-v2 (2026-06-07)",
        "analysis/2026-06-07-pi-context-pi-bound-cli.md (the spec)",
        "scripts/launch-constrained-pi.sh (the script being ported)",
        "packages/pi-context-cli/src/cli.ts (dispatch / exit-code surface)",
        "docs/reports/2026-06-07-pi-bound-cli-evaluation.md + -v2.md (the two evaluations)"
      ],
      "findings_summary": "The spec proposes replacing scripts/launch-constrained-pi.sh with a first-class `pi-context --pi-bound` CLI command that launches an interactive pi session restricted to a composed tool surface. Two independent code-grounded evaluations found the core architecture SOUND: `--pi-bound` is currently an unknown CLI command (so adding it is non-breaking — verified: it returns exit 2 via cli.ts:444-450); `list-tools` is the lone `surface:\"process\"` op partitioned out of the reflected CLI; and the proposed two-package-root SKILL.md union is byte-identical to the launch script's five-directory glob (69 tools), so the static-tools derivation is preserved.\n\nThe second evaluation (after the spec was edited on the first round's feedback) graded 28 assertions: 26 CONFIRMED, 0 REFUTED, 2 PARTIAL, 2 UNVERIFIABLE (out-of-scope-for-a-spec). The two partials were provenance/precision, not behavioral, and have been CORRECTED in the spec: (1) the `--grant` missing-value exit-2 is a NEW contract the pi-bound CLI introduces, NOT a preserved script behavior — launch-constrained-pi.sh:77 is a bare `shift 2` that merely aborts under `set -euo pipefail` (:61), so the spec now labels it as new; (2) the unknown-command exit-2 cite was corrected to the two-armed conditional cli.ts:444-450 (a process-only-op arm at :445-446 and the unknown-command arm at :447-449 sharing `return 2` at :450). Three off-by-one line-range cites were fixed, and an explicit note was added that the port deliberately collapses the script's two-stage tsx+python3 context pipeline (:104-109) into a single in-process loadContext call — a replacement, not a behavior-preserving port.\n\nThe first evaluation additionally surfaced four architectural omissions the subsequent edit addressed: where `cwd` originates inside runPiBound; the `pi install -l <meta-package>` meta-path resolution strategy (the genuinely novel port piece, previously least-specified); the script's resume invariant of re-deriving `--tools` every launch; and the exec→spawn+exit-relay swap being mislisted under behavior-to-preserve.\n\nNet: the port is feasible and the spec is now code-accurate. Remaining design judgement (not defects): the meta-path resolution strategy ($REPO-relative script vs node-resolution from a published CLI) is the load-bearing open piece for shipping pi-bound from an installed package rather than the in-repo script.",
      "findings_document": "analysis/2026-06-07-pi-context-pi-bound-cli.md",
      "grounding": {
        "revisions": [
          "context-jit-spec-v2 @ 2026-06-07"
        ]
      },
      "stale_conditions": [
        "scripts/launch-constrained-pi.sh changes its tool-union derivation, grant parsing, meta-install, or context pipeline — the port's behavior-preservation claims must be re-checked.",
        "cli.ts dispatch / exit-code structure (the :444-450 unknown-command + process-only conditional) is refactored.",
        "`pi-context --pi-bound` is actually implemented — this feasibility research is superseded by the implementation.",
        "list-tools stops being the lone surface:\"process\" op, or the surface partitioning changes.",
        "The generated SKILL.md glob set or the meta-package skill bundling changes, breaking the 69-tool union equivalence."
      ],
      "citations": [
        {
          "label": "the spec (deep read)",
          "path": "analysis/2026-06-07-pi-context-pi-bound-cli.md"
        },
        {
          "label": "unknown-command exit 2 — two-armed conditional (process-only arm + unknown arm)",
          "path": "packages/pi-context-cli/src/cli.ts",
          "lines": "444-450"
        },
        {
          "label": "--grant parse is a bare shift 2 (no validation; aborts under set -euo pipefail)",
          "path": "scripts/launch-constrained-pi.sh",
          "lines": "61,77"
        },
        {
          "label": "script context pipeline the port collapses in-process (tsx emit + python3 JSON->CSV)",
          "path": "scripts/launch-constrained-pi.sh",
          "lines": "104-109"
        },
        {
          "label": "first evaluation report",
          "path": "docs/reports/2026-06-07-pi-bound-cli-evaluation.md"
        },
        {
          "label": "second evaluation report (post-edit)",
          "path": "docs/reports/2026-06-07-pi-bound-cli-evaluation-v2.md"
        }
      ],
      "created_by": "human/davidryan@gmail.com",
      "created_at": "2026-06-07T00:00:00.000Z",
      "modified_by": "human/davidryan@gmail.com",
      "modified_at": "2026-06-06T22:07:25.817Z",
      "oid": "a6b0683bc10fe3094a3953d7d8e120e1",
      "content_hash": "d288a1b96ba997266a2b7d298769f8259f77808b858b886002fa3bea0afed028"
    },
    {
      "id": "R-0005",
      "title": "Making pi-context a globally installable command, grounded in pi's own bin/publish pattern",
      "status": "complete",
      "layer": "L2",
      "type": "feasibility",
      "question": "Grounded in the canonical pi runtime as exemplar, how is @davidorex/pi-context-cli shipped so the `pi-context` command (including the spec'd `pi-context --pi-bound` mode) resolves on PATH from any directory, not only via in-repo `node packages/pi-context-cli/dist/bin.js`?",
      "method": "Read-only code inspection of the installed pi runtime (resolved via `which pi`) and packages/pi-context-cli + packages/pi-project-workflows + packages/pi-context, cross-checked against the pi-bound spec (R-0004); npm publish state verified via `npm view @davidorex/<pkg> version`.",
      "scope": [
        "@earendil-works/pi-coding-agent at /opt/homebrew/lib/node_modules (the pi exemplar)",
        "packages/pi-context-cli/ (bin, build, deps)",
        "packages/pi-project-workflows/ (the meta-package, --pi-bound's require.resolve target)",
        "analysis/2026-06-07-pi-context-pi-bound-cli.md + research R-0004",
        "npm registry state of all six @davidorex/* packages"
      ],
      "findings_summary": "Pi's global-command recipe (the exemplar): `type:module` + `bin:{\"pi\":\"dist/cli.js\"}`, a `#!/usr/bin/env node` shebang on the BUILT entry, an explicit `chmod +x dist/cli.js` inside the build script (so npm can symlink the command to an executable on global install), `files:[\"dist\",…]` shipping only built output, and all runtime deps declared and shrinkwrapped so they nest under the package's own node_modules and resolve regardless of cwd.\n\npi-context-cli already matches pi on type/bin/shebang/files/publishConfig. The gaps: (1) its build never sets the exec bit on dist/bin.js (it is -rw-r--r--), which blocks `npm link` and weakens `npm i -g` — FGAP-047; (2) for --pi-bound it must declare @davidorex/pi-project-workflows so require.resolve resolves the meta-package from an installed CLI, which the published 0.30.0 does not — FGAP-048.\n\nPublish state (verified via npm view): all six @davidorex/* are published at 0.30.0 (pi-context, pi-context-cli, pi-project-workflows, pi-behavior-monitors, pi-jit-agents, pi-agent-dispatch). So the base `pi-context` command is registry-installable today (`npm i -g @davidorex/pi-context-cli` pulls the published dep tree); an earlier draft's claim that none was published conflated 'not installed on this machine' (checking /opt/homebrew/lib/node_modules) with 'unpublished' and is wrong.\n\nConcrete paths: (a) local dogfooding — add the build chmod, then `npm link` from packages/pi-context-cli puts pi-context on PATH with deps resolving through the existing workspace symlinks; (b) registry — already available for the base command; --pi-bound additionally requires declaring the meta-dep and cutting a new (release-gated) version. Recommended publish unit: CLI-as-own-unit (pi-faithful, lowest delta), recorded as DEC-0013; the meta-package has no dist/bin and folding the CLI there would be a larger restructure.\n\n--pi-bound plugs in with no extra wiring once pi-context is global (it is a CLI process mode at the bin's main(), DEC-0014). The one global-install-specific risk is whether require.resolve('@davidorex/pi-project-workflows/package.json') and the downstream `pi install -l <metaRoot>` resolve correctly from a REGISTRY-installed CLI — npm link only simulates the workspace-symlink path, so this needs a published-package smoke test (TASK-029/030 orchestrator criteria, release-gated).",
      "findings_document": "analysis/2026-06-07-pi-context-global-install-pattern.md",
      "grounding": {
        "dependencies": [
          "@earendil-works/pi-coding-agent — its package.json bin/build/files/shrinkwrap is the global-install exemplar"
        ],
        "revisions": [
          "context-jit-spec-v2 @ 2026-06-07"
        ],
        "external_refs": [
          "npm registry: @davidorex/* all at 0.30.0 (npm view, 2026-06-07)"
        ]
      },
      "stale_conditions": [
        "pi-context-cli's build adds chmod +x dist/bin.js — FGAP-047 closes and the exec-bit gap framing is superseded.",
        "pi-context-cli declares @davidorex/pi-project-workflows — FGAP-048 closes.",
        "A new @davidorex/* release supersedes 0.30.0 — the published-version facts must be re-verified.",
        "pi (@earendil-works/pi-coding-agent) changes its bin/build/files/shrinkwrap pattern — the exemplar recipe shifts.",
        "scripts/launch-constrained-pi.sh or the pi-bound spec changes the meta-path resolution or tool-union derivation."
      ],
      "citations": [
        {
          "label": "pi bin mapping + type:module",
          "path": "@earendil-works/pi-coding-agent/package.json",
          "lines": "5,9-11"
        },
        {
          "label": "pi build sets the exec bit (the step pi-context-cli lacks)",
          "path": "@earendil-works/pi-coding-agent/package.json",
          "lines": "33"
        },
        {
          "label": "pi-context-cli build — no chmod +x (FGAP-047)",
          "path": "packages/pi-context-cli/package.json",
          "lines": "31"
        },
        {
          "label": "pi-context-cli deps lack the meta-package (FGAP-048)",
          "path": "packages/pi-context-cli/package.json",
          "lines": "35-38"
        },
        {
          "label": "full analysis (deep read)",
          "path": "analysis/2026-06-07-pi-context-global-install-pattern.md"
        }
      ],
      "created_by": "human/davidryan@gmail.com",
      "created_at": "2026-06-07T00:00:00.000Z",
      "modified_by": "human/davidryan@gmail.com",
      "modified_at": "2026-06-06T22:29:48.981Z",
      "oid": "e072cd74684d58d1ea83b4c8704491d3",
      "content_hash": "a134a90ee305dd3f7449a0874e42b3840d7ab5273d752f5ae46c8b019178de0c"
    },
    {
      "id": "R-0006",
      "title": "pi CLI command-form convention — bare-subcommand vs flag heuristic, applied to pi-context",
      "status": "complete",
      "layer": "L2",
      "type": "investigative",
      "question": "What is pi's operative convention for making a capability a bare subcommand (pi update, pi config) versus a flag (--model, --export), and what does that heuristic prescribe for pi-context's surface — pi-context update, the reflected ops, and the currently flag-framed --pi-bound?",
      "method": "Read-only audit of the installed pi CLI (pi --help + pi <subcommand> --help + dist/cli.js) enumerating every command form and its kind-of-capability; the heuristic was derived from that enumeration and applied to pi-context's cli.ts dispatch and the planned FEAT-005/FEAT-006 surface.",
      "scope": [
        "@earendil-works/pi-coding-agent installed at /opt/homebrew/lib/node_modules (pi --help command surface)",
        "packages/pi-context-cli/src/cli.ts dispatch + the reflected surface:use op set",
        "FEAT-005 (--pi-bound), FEAT-006 (pi-context update), DEC-0014"
      ],
      "findings_summary": "pi's command surface separates bare verbs from flags by the nature of the capability. Bare verbs: install <source>, remove/uninstall <source>, update [source|self|pi], list, config (config opens a TUI yet is a bare verb, never --config). Flags: modifiers and one-shot mode-selectors of the DEFAULT run — --model, --print/-p, --tools, --export, --list-models, etc. Verbs take positionals for their target (update self|pi); a how-it-runs modifier of a verb is a flag on that verb.\n\nDerived heuristic (first match wins): (1) a distinct self-completing action that is NOT the default run (does its thing and exits) -> bare verb, interactive or not (config is the precedent); (2) the object/target the verb acts on -> positional; (3) a modifier of HOW a verb runs -> flag on that verb (e.g. update --self/--force, the planned update --dry-run); (4) a modifier / mode-selector / one-shot short-circuit of the DEFAULT run -> top-level flag. Litmus: 'its own run-and-exit action, or an option/mode of an existing action?'\n\nApplied to pi-context: the reflected surface:use ops are bare verbs (conformant, rule 1); pi-context update (FEAT-006) is a bare verb mirroring pi update, with --dry-run a verb-flag (conformant, rules 1+3). pi-context --pi-bound (DEC-0014) is MISFRAMED: launching a bounded pi subprocess and exiting is a distinct self-completing action — the exact analogue of pi's bare config verb — so under the heuristic it should be the bare subcommand pi-context pi-bound, not a flag. The recast is pi-faithful (config-as-bare-verb precedent), consistent with update being bare, and easier to type. Routing is unaffected: DEC-0014's load-bearing requirements (route in main() before op resolution; exclude from the reflected surface:use set at cli.ts:44) hold for a bare verb — main() tests first === 'pi-bound' instead of first === '--pi-bound'; the pi-bound module is otherwise unchanged. DEC-0014's decision + consequences are token-agnostic and survive verbatim; only its title/context spelling and the spec md's --pi-bound token need revising. That revision is a separate user decision; this research only establishes the bare-verb form as the heuristic-correct, pi-conformant choice.",
      "findings_document": "analysis/2026-06-07-pi-cli-command-pattern-heuristic.md",
      "grounding": {
        "dependencies": [
          "@earendil-works/pi-coding-agent — its pi --help command surface is the convention exemplar"
        ],
        "revisions": [
          "context-jit-spec-v2 @ 2026-06-07"
        ]
      },
      "stale_conditions": [
        "pi (@earendil-works/pi-coding-agent) changes its command surface — adds/removes bare verbs or restructures flags — so the derived convention must be re-audited.",
        "pi-context adopts the bare pi-context pi-bound form (revising DEC-0014) — the application verdict is realized and the 'misframed' framing is superseded.",
        "pi-context's cli.ts dispatch changes how it routes bare verbs vs flags (the cli.ts:44 reflected-op seam)."
      ],
      "citations": [
        {
          "label": "pi command surface — bare verbs (install/remove/update/list/config) vs top-level flags",
          "path": "pi --help (installed @earendil-works/pi-coding-agent)"
        },
        {
          "label": "full audit + heuristic derivation (deep read)",
          "path": "analysis/2026-06-07-pi-cli-command-pattern-heuristic.md"
        },
        {
          "label": "pi-context reflects surface:use ops as bare verbs; --pi-bound routed before op resolution",
          "path": "packages/pi-context-cli/src/cli.ts",
          "lines": "44"
        }
      ],
      "created_by": "human/davidryan@gmail.com",
      "created_at": "2026-06-07T00:00:00.000Z",
      "modified_by": "human/davidryan@gmail.com",
      "modified_at": "2026-06-06T23:02:11.224Z",
      "oid": "fbb9f0764e97850f64402839bfd97f7a",
      "content_hash": "37f7663e3d71bb6f3e42807900c963c441b629ac8a1efc03e4cd325a6d5d4740"
    },
    {
      "id": "R-0007",
      "title": "CLI write-path UX — actionable AJV validation errors + --show-schema field hint: root cause, fix seams, and existing-filing map",
      "status": "complete",
      "layer": "L3",
      "type": "investigative",
      "question": "What is the root cause of (1) non-actionable pi-context validation errors and (2) the absence of an inline block-field preview on write ops; where do the fixes live; and are they already filed?",
      "method": "Two read-only Explore code investigations tracing the write/validation path (the ValidationError constructor + the validate funnels) and the schema-derivation surface (read-schema/readSchema/resolveBlockItemSchema + the block_kinds array_key mapping), plus substrate read ops (driven through the global pi-context command) enumerating existing gaps/tasks, cross-checked against the live CLI.",
      "scope": [
        "packages/pi-context/src/schema-validator.ts",
        "packages/pi-context-cli/src/cli.ts + bin.ts",
        "packages/pi-context/src/ops-registry.ts (read-schema / read-config)",
        "packages/pi-context/src/schema-write.ts + block-api.ts (resolveBlockItemSchema, validate funnels)",
        "scripts/orchestrator/file-block-item.ts (formatAjvError, contract-preview)",
        ".context FGAP-022 / FGAP-023 / TASK-016 / TASK-017"
      ],
      "findings_summary": "Two write-path frictions in the pi-context CLI, both grounded to root cause and both ALREADY FILED (FGAP-023 -> TASK-016; FGAP-022 -> TASK-017, cli-parity release-group orders 2/5 and 3/5; do not refile).\n\n(1) Non-actionable validation errors. The string 'Validation failed for <block>: <instancePath>: <message>' is built at one shared site — the ValidationError constructor, schema-validator.ts:88 — which maps each AJV ErrorObject to instancePath+message and DISCARDS e.params. AJV runs with allErrors:true (schema-validator.ts:13) and the constructor both receives and stores the full errors[] on this.errors (:92), so the actionable data is present but dropped: params.additionalProperty names the offending field (e.g. 'reproduction'), params.missingProperty the missing required field, params.allowedValues the enum set. Every write op (append/update/upsert/nested block items, write-block, append-relation, amend-config, schema writes) funnels through this one constructor via validate()/validateFromFile()/validateBlockWithMigrationForDir()/validateSchemaAgainstMeta() — so a single change makes all of them actionable. The CLI is a pure err.message passthrough (cli.ts:511-523, bin.ts:8-9) reading neither err.errors nor params. Two fix locations: (a) library-side at schema-validator.ts:88 — fixes every surface incl. the in-pi pi -p tool dispatch, but changes the in-pi op message; (b) CLI-side reading err.errors[].params in the cli.ts catch — keeps in-pi byte-unchanged. TASK-016 chooses (b): lift formatAjvError from scripts/orchestrator/file-block-item.ts:184-205 into the catch + a new render.ts.\n\n(2) No inline block-field preview. The derivation primitive already exists: read-schema (ops-registry.ts:1140-1181) -> readSchema (schema-write.ts:327) addresses any schema node via --path; resolveBlockItemSchema (block-api.ts:2266-2295) returns {arrayKey, itemSchema}, resolving the array property + items $ref. The friction is a 3-step indirection: know schemaName==block; resolve array_key from config.block_kinds (read-config --registry block_kinds, ops-registry.ts:896-942); then read-schema --path properties.<array_key>.items and hand-read required+properties. --show-schema (TASK-017) collapses this into one early-return branch in main() mirroring the parsed.help branch (cli.ts:469-472), before op.run, reusing readConfig+readSchema+resolveBlockItemSchema — confined to pi-context-cli/src/cli.ts.\n\nThe two are sibling/complementary (reactive error-time vs proactive pre-write), both from the 2026-06-05 cli-ops-scripts-parity-survey (GAP-5, GAP-4), both honoring 'in-pi ops byte-unchanged'. Open decision: the in-pi pi -p tool surface keeps raw AJV under the CLI-scoped fix; covering it requires the library-side option (a) at schema-validator.ts:88, which TASK-016 deliberately avoids to keep in-pi output stable.",
      "findings_document": "analysis/2026-06-07-cli-write-path-ux-research.md",
      "grounding": {
        "revisions": [
          "context-jit-spec-v2 @ 2026-06-07"
        ]
      },
      "stale_conditions": [
        "schema-validator.ts ValidationError message builder (:88) is changed to surface AJV params — the root-cause claim is realized.",
        "TASK-016 / TASK-017 land — the 'not yet implemented' framing and the library-vs-CLI fix-location choice are superseded by the shipped behavior.",
        "cli.ts dispatch/catch region is restructured (the :469-472 help-branch seam or :511-523 catch) — the seam file:line references drift.",
        "read-schema / resolveBlockItemSchema / read-config block_kinds derivation surface changes — the --show-schema reuse path shifts."
      ],
      "citations": [
        {
          "label": "validation-error message collapse (root cause) — maps to instancePath+message, discards params",
          "path": "packages/pi-context/src/schema-validator.ts",
          "lines": "88"
        },
        {
          "label": "AJV allErrors:true; full errors[] stored on this.errors",
          "path": "packages/pi-context/src/schema-validator.ts",
          "lines": "13,92"
        },
        {
          "label": "CLI is pure err.message passthrough (reads neither errors nor params)",
          "path": "packages/pi-context-cli/src/cli.ts",
          "lines": "511-523"
        },
        {
          "label": "read-schema op — on-demand schema-node addressing (derivation primitive)",
          "path": "packages/pi-context/src/ops-registry.ts",
          "lines": "1140-1181"
        },
        {
          "label": "resolveBlockItemSchema — array_key + items + $ref resolution",
          "path": "packages/pi-context/src/block-api.ts",
          "lines": "2266-2295"
        },
        {
          "label": "--show-schema seam: parsed.help early-return branch in main()",
          "path": "packages/pi-context-cli/src/cli.ts",
          "lines": "469-472"
        },
        {
          "label": "formatAjvError + contract-preview bodies to lift",
          "path": "scripts/orchestrator/file-block-item.ts",
          "lines": "113-142,184-205"
        },
        {
          "label": "full grounded findings",
          "path": "analysis/2026-06-07-cli-write-path-ux-research.md"
        }
      ],
      "created_by": "human/davidryan@gmail.com",
      "created_at": "2026-06-07T00:00:00.000Z",
      "modified_by": "human/davidryan@gmail.com",
      "modified_at": "2026-06-07T03:40:34.167Z",
      "oid": "e96de2ff2f1387f4eb3a2862455ad7aa",
      "content_hash": "42796d7ce25618cbde2289d5e233dc6073e49228e3933cd29c4ed4db812fa432"
    }
  ]
}