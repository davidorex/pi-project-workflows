dist/
docs/
node_modules/
.claude/
.pi/
.workflows/
CLAUDE.md
# Project block data is runtime state — gitignored
# Project schemas (.project/schemas/) are the type system — tracked in git
.project/*.json
.project/phases/
.project/audits/
# Test artifacts — block-validation tests may create temp dirs in package dirs
packages/*/.workflow/
packages/*/.project/
packages/*/.workflows/
