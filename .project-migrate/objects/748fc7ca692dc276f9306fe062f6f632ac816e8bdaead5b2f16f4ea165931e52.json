{
  "title": "No automatic cross-file value-rename engine for canonical_id changes — schema-migrations handles shape, not canonical_id value renames across relations.json edges + item-id-prefix renumbering",
  "status": "closed",
  "priority": "P2",
  "package": "pi-context",
  "layer": "L1",
  "description": "The framework foresees + carries SCHEMA-SHAPE migration via schema-migrations.ts (MigrationRegistry + runMigrations + validateBlockWithMigration at schema-validator.ts:204): a block file declaring schema_version 1.0.0 migrates forward to current via registered MigrationFn chain on read. This handles field renames / restructures / add-with-default WITHIN a block's item shape. It does NOT handle canonical_id VALUE renames that span files. Two cases: (1) relation_type canonical_id rename — relations.json edges store the relation_type canonical_id (relations.schema.json: edge.relation_type = 'registered relation_type canonical_id'); renaming a relation_type's canonical_id requires rewriting every edge referencing it; no MigrationFn covers cross-file value rewrites; runMigrations operates per-block-file data shape only. (2) item-ID-prefix renumbering (DEC-0026 padding / DEC-0027 casing) — item canonical_id (e.g. DEC-0001, issue-001) EMBEDS the prefix; unlike relation_types (which split stable canonical_id from mutable display_name per config.relation_types[]), item ids ARE the reference with no id/label split, so renaming the prefix requires renumbering every item + every citation across relations.json edges + related_X fields + analysis MDs + commit messages.\n\nCRITICAL MITIGATION ALREADY DESIGNED: the canonical_id <-> display_name decoupling (config.relation_types[].canonical_id vs display_name; config.block_kinds[].canonical_id vs display_name per DEC-0023) means HUMAN-LABEL renames are FREE — edge/reference holds stable canonical_id; label lives in display_name; relabel = display_name edit = live, zero migration. The value-rename gap applies ONLY to canonical_id-itself changes, which are rare by design (primary-key semantics: set once, treat permanent). [2026-05-20 CLOSED — unit 2.3, commit 33b76a7: renameCanonicalId engine landed for 4 canonical_id kinds (item, relation_type, lens, layer) + dual surface (rename-canonical-id Pi tool + scripts/orchestrator/migrate-canonical-id.ts) + writeRelations/writeConfig wrappers + out-of-substrate report (analysis grep + best-effort git log -S, report-only) + dry-run. The two cases this FGAP named — relation_type canonical_id rename + item-id-prefix renumbering — are both delivered, as are lens/layer. block_kind canonical_id rename (the file/data_path/array_key/schema_path cascade) is the one remaining sliver, tracked as FGAP-071.]",
  "evidence": [
    {
      "file": "packages/pi-context/src/schema-migrations.ts",
      "reference": "MigrationRegistry + runMigrations operate per-block-file data shape (currentVersion → targetVersion); no cross-file value-rename surface"
    },
    {
      "file": "packages/pi-context/src/schema-validator.ts",
      "reference": "validateBlockWithMigration:204 — read-time forward migration of one block file's data; does not touch other files' references"
    },
    {
      "file": "packages/pi-context/schemas/relations.schema.json",
      "reference": "edge.relation_type stores registered relation_type canonical_id — renaming the canonical_id orphans existing edges"
    },
    {
      "file": "packages/pi-context/schemas/config.schema.json",
      "reference": "relation_types[].canonical_id + display_name split (lines 70-84) — enables free label rename; canonical_id rename is the migration-cost case"
    },
    {
      "file": ".project/decisions.json",
      "reference": "DEC-0026 + DEC-0027 flag item-id-prefix renumbering churn; DEC-0030 relation_type naming shape"
    }
  ],
  "impact": "When a vocabulary decision is rethought AFTER substrate is populated: human-label revisions are free (display_name edit) but canonical_id revisions have no automatic engine. relation_type canonical_id rename orphans relations.json edges silently (validateRelations may flag dangling refs but doesn't auto-rewrite). item-id-prefix renumbering (DEC-0026/0027) cascades across every reference site with no tooling. The schema-shape migration foresight (FGAP-006, closed) has no value-rename counterpart. Practically: deciding canonical_ids well initially matters because canonical_id is primary-key-permanent; display_names are the cheap-revision surface. Gap is the absence of a migration primitive for the rare-but-real canonical_id-rename case.",
  "canonical_vocabulary": "canonical-id-rename-migration, cross-file-value-rename, relation-type-canonical-id-stability, item-id-prefix-renumbering-engine",
  "proposed_resolution": "Two-part: (a) Document + enforce the canonical_id-is-permanent discipline — canonical_ids (relation_type / block_kind / lens / layer) set once, never renamed; human-facing changes go through display_name. Add to CLAUDE.md substrate canon + schema descriptions. This makes case (1) a non-event by convention. (b) For the genuine canonical_id-rename + item-id-prefix-renumbering cases that remain, author a value-rename migration primitive (e.g. scripts/orchestrator/migrate-canonical-id.ts + library renameCanonicalId(cwd, kind, oldId, newId)) that: rewrites the canonical_id in its home block; rewrites every relations.json edge referencing it (parent + child + relation_type fields); rewrites every related_X field across all blocks that cite it; reports citation sites in analysis MDs + commit messages it cannot auto-rewrite (out-of-substrate). Dry-run mode lists affected sites before write. For item-id-prefix renumbering (DEC-0026/0027), the same primitive batch-renames id prefixes + cascades references. Cross-decision with DEC-0026/0027 (prefix conventions) — settling those AND building this primitive together avoids a second renumbering later.",
  "related_features": [],
  "related_decisions": [
    "DEC-0013",
    "DEC-0023",
    "DEC-0026",
    "DEC-0027",
    "DEC-0030"
  ],
  "related_issues": []
}