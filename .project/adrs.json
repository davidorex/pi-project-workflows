{
	"adrs": [
		{
			"id": "ADR-0001",
			"title": "Agent spec 'model:' field semantics",
			"status": "proposed",
			"context": "Agent spec YAMLs today may declare a model field with or without a provider prefix. packages/pi-behavior-monitors/index.ts:1091-1097 implements parseModelSpec such that bare model ids (e.g. 'claude-sonnet-4-6') default to provider='anthropic'. This structural pin means monitor classify calls attempt Anthropic-direct authentication regardless of the main session's configured provider. The user configured OpenRouter as the sole credential source, and monitors failed with 'Expected either apiKey or authToken' because the Anthropic SDK received undefined credentials. The bundled classifier agent YAMLs (hedge, commit-hygiene, fragility, work-quality, unauthorized-action) all use bare model ids and are all affected. The question is what 'model:' means in an agent spec at the framework level.",
			"decision": "Option (iii): a bare model id in an agent spec is resolved against the current session's configured provider via ExtensionContext. If ExtensionContext cannot supply a current-provider/current-model binding, the dispatch errors with a clear message naming the missing context. Fully-qualified model specs (provider/model) continue to pin the provider regardless of session. Pinning is explicit, not a default.",
			"options_considered": [
				{
					"label": "(i) bare = inherit current session model",
					"description": "Bare model id silently inherits whatever the parent pi session is running. No error if session has no model — monitors use whatever is ambient.",
					"tradeoffs": "Implicit. Agent YAMLs become opaque about what they actually run. Debugging 'why did this monitor use that model' becomes harder. But monitors always follow user's session intent automatically.",
					"rejected_reason": "Silent inheritance violates the spec-loop invariant of explicit provenance; agents should be reproducible from their spec"
				},
				{
					"label": "(ii) bare = parse error",
					"description": "Every agent YAML must declare a fully-qualified provider/model. Bundled classifier YAMLs become unrunnable without user override or package-side commitment to one provider.",
					"tradeoffs": "Maximum explicitness. Forces users to take a stance per spec. But makes bundled YAMLs non-portable across providers and forces pi-behavior-monitors to pick a provider to ship with, which moves the pin instead of removing it.",
					"rejected_reason": "Moves the pin rather than resolving it; forces package authors to prefer one provider over another at ship time"
				},
				{
					"label": "(iii) bare = ExtensionContext lookup with explicit error",
					"description": "Bare model id triggers a runtime lookup against ExtensionContext for the current session's provider and model. If ExtensionContext exposes 'currentModel', that is used. If not, dispatch errors naming the missing binding. Fully-qualified specs continue to pin explicitly.",
					"tradeoffs": "Requires that ExtensionContext expose 'currentModel' to extensions. If pi-agent-core does not expose this yet, the change widens into the pi-agent-core surface. The trade is: framework becomes more honest about what's available at the boundary, and monitors naturally follow the user's session without any bundled-YAML hardcoding."
				}
			],
			"consequences": [
				"pi-behavior-monitors' classifyViaAgent must read the current session model from ExtensionContext at dispatch time, not from the agent YAML",
				"parseModelSpec semantics change — it no longer returns (provider='anthropic') as a default; bare specs route through ExtensionContext lookup",
				"The five bundled classifier YAMLs can remain with bare model ids and will now follow whatever provider the user has configured",
				"If ExtensionContext does not currently expose currentModel, that becomes a prerequisite unblocker — opens a scope question into pi-agent-core that must be verified before implementation",
				"Workflow per-step provider override (currently a known gap in pi-workflows dispatch.ts) becomes the symmetric problem for workflow agents; same resolution applies"
			],
			"supersedes": [],
			"related_findings": [],
			"related_features": ["FEAT-001"],
			"related_gaps": [],
			"references": [
				{
					"label": "parseModelSpec pin",
					"path": "packages/pi-behavior-monitors/index.ts",
					"lines": "1091-1097"
				},
				{
					"label": "classify call site",
					"path": "packages/pi-behavior-monitors/index.ts",
					"lines": "1224-1248"
				},
				{
					"label": "bundled agent YAMLs",
					"path": "packages/pi-behavior-monitors/agents/"
				},
				{
					"label": "operational incident",
					"path": "analysis/openrouter-pi-mono-setup.md"
				}
			],
			"created_by": "agent/claude-opus-4-6",
			"created_at": "2026-04-15T00:00:00Z"
		},
		{
			"id": "ADR-0002",
			"title": "Thinking-seam enforcement for forced-toolChoice dispatch",
			"status": "proposed",
			"context": "Agent spec YAMLs can declare thinking: on, but packages/pi-behavior-monitors/index.ts:1235-1248 does not forward thinking to the complete() call. The comment at line 1231 acknowledges that the Anthropic API rejects thinking + forced toolChoice, so thinking must stay off when the dispatch uses a phantom-tool verdict shape. All five bundled classifier YAMLs (hedge, commit-hygiene, fragility, work-quality, unauthorized-action) declare thinking: on despite the silent drop. The config is currently dead — either the YAMLs are wrong, or the classify path is wrong. The framework does not enforce consistency between spec-declared thinking and dispatch-time behavior.",
			"decision": "executeAgent in pi-jit-agents (once parseModelSpec and classify dispatch move there per ADR-0003) enforces thinking=off whenever toolChoice.type === 'tool' is present in the dispatch options. If the agent spec declares thinking: on in combination with forced toolChoice, the framework emits a warning naming the agent and the incompatibility, and proceeds with thinking disabled. Free-form (non-forced) dispatches continue to forward thinking per the spec. The forced-toolChoice path never silently drops thinking without notification.",
			"options_considered": [
				{
					"label": "(i) forward thinking unconditionally",
					"description": "executeAgent forwards the compiled spec's thinking field to complete() in every path. Anthropic API rejects the combination at the provider boundary, surfacing the error to the caller.",
					"tradeoffs": "Honors the spec literally. But failures happen at the transport layer with cryptic errors; agent authors cannot tell from the spec whether it will work at runtime.",
					"rejected_reason": "Makes the agent spec unreliable — a valid-looking spec fails at dispatch with a transport error"
				},
				{
					"label": "(ii) silently drop thinking when forced toolChoice",
					"description": "Current behavior. Classify path ignores thinking. No warning.",
					"tradeoffs": "Works. But the YAML's thinking field becomes meaningless in the context where it matters most.",
					"rejected_reason": "Silent drops violate explicit-provenance discipline; future authors writing new classifier YAMLs will repeat the same dead-config mistake"
				},
				{
					"label": "(iii) enforce thinking=off with warning when forced toolChoice",
					"description": "executeAgent checks for forced toolChoice; if present and thinking is on, logs a warning naming the agent and disables thinking for this dispatch.",
					"tradeoffs": "Explicit. Agents authors see the warning and can correct the YAML. Does not break working configurations. Slightly more code at the boundary."
				}
			],
			"consequences": [
				"executeAgent gains a pre-dispatch check that inspects toolChoice and the compiled spec's thinking field",
				"The five bundled classifier YAMLs should be corrected to thinking: off for cleanliness, separately from the enforcement change",
				"Any future agent that combines forced toolChoice with thinking: on will be flagged loudly rather than silently mis-executing",
				"Warning channel must exist at the framework boundary — requires either a logger injected via DispatchContext or a structured diagnostic emitted alongside JitAgentResult"
			],
			"supersedes": [],
			"related_findings": [],
			"related_features": ["FEAT-001"],
			"related_gaps": [],
			"references": [
				{
					"label": "classify call site silent drop",
					"path": "packages/pi-behavior-monitors/index.ts",
					"lines": "1231-1248"
				},
				{
					"label": "dead-config YAML",
					"path": "packages/pi-behavior-monitors/agents/hedge-classifier.agent.yaml"
				}
			],
			"created_by": "agent/claude-opus-4-6",
			"created_at": "2026-04-15T00:00:00Z"
		},
		{
			"id": "ADR-0003",
			"title": "parseModelSpec ownership at the execute boundary",
			"status": "proposed",
			"context": "parseModelSpec lives in packages/pi-behavior-monitors/index.ts today. It defaults bare model ids to provider='anthropic', pinning the monitor classify path to Anthropic regardless of session configuration. The same function logic (model-id parsing and provider selection) belongs at the execute boundary, which pi-jit-agents now owns per docs/planning/jit-agents-spec.md §2. Keeping it in pi-behavior-monitors creates duplication (issue-043) and embeds policy in the wrong layer. pi-workflows has the same structural question for its agent-step dispatch but with different currently-hidden behavior.",
			"decision": "Move parseModelSpec into pi-jit-agents as part of the executeAgent boundary. Remove the duplicate from pi-behavior-monitors. Update the consumer's classifyViaAgent to call the pi-jit-agents API. Audit pi-workflows for any implicit model-spec parsing in step dispatch; if found, consolidate there too. The function's new location must accommodate the policy decision from ADR-0001 (ExtensionContext lookup for bare specs) and the enforcement from ADR-0002 (thinking-seam).",
			"options_considered": [
				{
					"label": "(i) keep parseModelSpec in pi-behavior-monitors",
					"description": "Leave the function where it is. Fix only the anthropic-pin default.",
					"tradeoffs": "Minimal change. But the function is structurally in the wrong layer; the next consumer (pi-workflows during migration) would need its own copy.",
					"rejected_reason": "Leaves duplication across consumers and embeds execute-boundary policy in a consumer"
				},
				{
					"label": "(ii) move to pi-jit-agents execute boundary",
					"description": "Export parseModelSpec (or its renamed successor) from pi-jit-agents at the execute surface. Both consumers import it. The function reads ExtensionContext for bare-spec resolution per ADR-0001.",
					"tradeoffs": "Correct boundary. Requires a consumer-migration step to remove the duplicate. Couples with ADR-0001 (ExtensionContext access) and ADR-0002 (thinking-seam)."
				},
				{
					"label": "(iii) move to pi-project SDK",
					"description": "Model resolution becomes a pi-project concern.",
					"tradeoffs": "Wrong package — pi-project is project state, not runtime execution. Rejected on boundary grounds.",
					"rejected_reason": "Violates the layered package model in jit-agents-spec §2"
				}
			],
			"consequences": [
				"pi-jit-agents gains a model-resolution surface at executeAgent — either as a helper function or inlined into the dispatch path",
				"pi-behavior-monitors' classifyViaAgent shrinks — it calls executeAgent with a compiled spec and an ExtensionContext, no longer doing its own provider resolution",
				"pi-workflows' agent-step dispatch audits for equivalent implicit parsing and consolidates there too",
				"Requires ADR-0001 resolved before implementation, because the bare-spec semantics determine what parseModelSpec's new contract is"
			],
			"supersedes": [],
			"related_findings": [],
			"related_features": ["FEAT-001"],
			"related_gaps": [],
			"references": [
				{
					"label": "current location",
					"path": "packages/pi-behavior-monitors/index.ts",
					"lines": "1091-1097"
				},
				{
					"label": "jit-agents-spec §2 boundary contract",
					"path": "docs/planning/jit-agents-spec.md",
					"lines": "66-83"
				}
			],
			"created_by": "agent/claude-opus-4-6",
			"created_at": "2026-04-15T00:00:00Z"
		},
		{
			"id": "ADR-0004",
			"title": "Expression language — keep minimal custom evaluator or adopt CEL",
			"status": "proposed",
			"context": "pi-workflows ships a 339-line bespoke expression evaluator at packages/pi-workflows/src/expression.ts implementing ${{ path.field | filter }} property access with nine built-in filters (duration, currency, json, length, keys, filter, first, last, slugify, shell). The minimalism is deliberate per docs/planning/workflow-extension.md:537-554: 'No arithmetic, no string concatenation, no function calls, no ternary operators. If a workflow needs to transform data between steps, it uses a transform step with explicit logic, not expressions embedded in YAML values.' Comparative analysis against systeminit/swamp (research entry at analysis/2026-04-15-expression-language-comparison.md, grounded against swamp main branch and @marcbachmann/cel-js@7.5.1) surfaces CEL adoption as the industry-standard alternative. CEL provides arithmetic, comparisons, logical operators, ternary, string operations, list/map manipulation, and caller-registered functions with bounded deterministic evaluation. CEL is the expression engine Kubernetes chose for admission-controller policies and is widely adopted across gRPC, Firebase Security Rules, and Envoy filter config. Our evaluator has one documented limitation (workflow-extension.md:253 — adjacent ${{ }}${{ }} misparse) and one compensating complexity (the transform step type exists solely because expressions cannot do arithmetic or logic inline). At v0.14.4 with workflow use still early, either direction remains available.",
			"decision": "Option (iii) — hybrid adoption: keep ${{ }} delimiters and the ExpressionError diagnostic wrapping, replace the property-access evaluator body with CEL (via @marcbachmann/cel-js or equivalent Deno/Node-compatible CEL implementation), retain current filters as CEL-registered functions with type declarations. Retire the transform step type's reason-for-being (inline arithmetic and logic eliminate the class of step that existed only because expressions could not do it). Gate timing on observed frequency of transform steps that exist solely as expression workarounds — the longer transforms accumulate as expression compensation, the more expensive CEL adoption becomes.",
			"options_considered": [
				{
					"label": "(i) Keep current minimal evaluator indefinitely",
					"description": "Ship the existing 339-line evaluator unchanged. Transform step type remains the primary vehicle for any logic beyond property access.",
					"tradeoffs": "Zero disruption, zero new dependency, preserved security posture. But every workflow needing arithmetic, comparison, or inline list/map manipulation spawns a transform subprocess. Workflow authors and LLM agents must learn our custom grammar instead of CEL's training-distribution syntax. Documented parse limitation at workflow-extension.md:253 remains unresolved.",
					"rejected_reason": "Locks in compensating complexity (transform steps as expression workarounds) and project-specific grammar that agents do not already know; rejects the external validation signal that the correct answer already exists as an industry standard"
				},
				{
					"label": "(ii) Adopt CEL wholesale, break backward compatibility",
					"description": "Replace the expression evaluator entirely and migrate all existing workflow YAMLs to pure CEL syntax. Change delimiters if the CEL community convention warrants it.",
					"tradeoffs": "Fastest path to full CEL. But breaks all existing YAMLs, loses the ExpressionError diagnostic shape, and forces a delimiter discussion that would be disruptive with no structural benefit.",
					"rejected_reason": "Disruption is avoidable; ${{ }} delimiter and error-wrapping have zero cost to preserve and their preservation keeps existing workflow YAMLs source-compatible with the hybrid path"
				},
				{
					"label": "(iii) Hybrid: keep ${{ }} delimiters and error wrapping, swap evaluator body to CEL",
					"description": "Preserve the outer syntax (${{ … }}) and the ExpressionError diagnostic class. Replace the internal split-on-dot property-walker with a cel-js call. Register current filters as CEL functions with appropriate type declarations. Existing workflows remain source-compatible because simple property-access CEL produces the same results as our current evaluator.",
					"tradeoffs": "One new dependency on cel-js (supply-chain audit required). Shell filter security boundary must be re-verified against CEL's broader string-operation surface — CEL supports string concatenation which may bypass the single-quote-only escape strategy. Minor migration risk for workflows that exploit specific null-propagation semantics our current evaluator has (our evaluator errors on missing intermediate segments; CEL has different null-propagation rules). But: lifts the arithmetic/logic constraint, retires transform-step-as-expression-workaround, fixes the ${{ }}${{ }} parse limitation, lands in agent training distribution, inherits CEL sandboxing and bounded-evaluation guarantees."
				}
			],
			"consequences": [
				"packages/pi-workflows/src/expression.ts changes substantially — property-walker at lines 78-107 is replaced by a cel-js call, filters at lines 12-31 become CEL function registrations, ExpressionError wrapping at lines 40-53 is preserved",
				"New direct npm/jsr dependency on a CEL implementation; supply-chain audit required before adoption including evaluation-bound guarantees, sandboxing properties, and ongoing maintenance cadence",
				"Shell filter security boundary at expression.ts:24 must be re-reviewed against CEL's string-operation surface — CEL supports string concatenation and method calls which may bypass the current single-quote-only escape strategy for command-step shell contexts",
				"workflow-extension.md:537-554 passage documenting the minimalism rationale becomes historical context; a new section describes the CEL adoption rationale and post-adoption expression capabilities",
				"Transform step type at packages/pi-workflows/src/step-transform.ts remains for genuine cross-step data shaping (format conversion, expensive computation worth a subprocess) but no longer exists solely as an expression workaround",
				"FILTER_NAMES export at expression.ts:33 and expressionRoots/filterNames SDK queries in workflow-sdk.ts continue to work — they describe the registered filters and scope roots regardless of the internal evaluator implementation",
				"Workflow authors gain arithmetic, comparisons, logical operators, ternary, string operations, list/map manipulation, and CEL-registered function calls inline in expressions",
				"Documented limitation at workflow-extension.md:253 (adjacent ${{ }}${{ }} misparse) is resolved by CEL's substring-aware parser",
				"Agent-authored workflow YAMLs benefit from LLM training-distribution familiarity with CEL, reducing the prompt-engineering cost of teaching the grammar"
			],
			"supersedes": [],
			"related_findings": [],
			"related_features": [],
			"related_gaps": [],
			"references": [
				{
					"label": "current evaluator implementation",
					"path": "packages/pi-workflows/src/expression.ts"
				},
				{
					"label": "minimalism rationale in workflow-extension spec",
					"path": "docs/planning/workflow-extension.md",
					"lines": "537-554"
				},
				{
					"label": "phase-2 hedge on adding == / != / > / <",
					"path": "docs/planning/workflow-extension.md",
					"lines": "554"
				},
				{
					"label": "adjacent ${{ }}${{ }} parse limitation",
					"path": "docs/planning/workflow-extension.md",
					"lines": "253"
				},
				{
					"label": "parallel-analysis review confirming no-eval constraint",
					"path": "docs/planning/2026-03-14-parallel-analysis-review.md",
					"lines": "30"
				},
				{
					"label": "comparative research grounding this ADR",
					"path": "analysis/2026-04-15-expression-language-comparison.md"
				},
				{
					"label": "swamp design doc on expressions (source)",
					"path": "/tmp/swamp-analysis/swamp/design/expressions.md"
				},
				{
					"label": "swamp deno.json declaring @marcbachmann/cel-js@7.5.1",
					"path": "/tmp/swamp-analysis/swamp/deno.json"
				}
			],
			"created_by": "agent/claude-opus-4-6",
			"created_at": "2026-04-15T00:00:00Z"
		}
	]
}
