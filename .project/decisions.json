{
  "decisions": [
    {
      "id": "subprocess-dispatch",
      "decision": "Extension owns its own subprocess dispatch via spawning pi --mode json",
      "rationale": "Independence from pi-subagents \u2014 two tools can coexist without shared state or import relationship",
      "phase": 1,
      "status": "decided"
    },
    {
      "id": "control-plane",
      "decision": "Main conversation is the control plane; workflows are subordinate work",
      "rationale": "User never loses their interactive session \u2014 when workflow completes, conversation continues with the result",
      "phase": 1,
      "status": "decided"
    },
    {
      "id": "fail-fast",
      "decision": "Fail-fast on step failure in sequential execution",
      "rationale": "Simplest correct behavior for phase 1 \u2014 no partial results, no recovery complexity",
      "phase": 1,
      "status": "decided"
    },
    {
      "id": "expression-minimal",
      "decision": "Expression evaluator supports property access only \u2014 no arithmetic, no logic, no function calls",
      "rationale": "Push logic into transform steps where it's explicit and testable, keep ${{ }} simple and predictable",
      "phase": 1,
      "status": "decided"
    },
    {
      "id": "expression-filters",
      "decision": "Pipe syntax for formatting: ${{ path | duration }}, ${{ path | currency }}, ${{ path | json }}",
      "rationale": "Formatting is display concern, not logic \u2014 safe to add to expressions without crossing into computation",
      "phase": 1,
      "status": "decided"
    },
    {
      "id": "output-post-hoc",
      "decision": "Output validation is post-hoc AJV after step completes",
      "rationale": "In-loop validation (steering agent toward schema compliance) adds complexity \u2014 defer to later phase",
      "phase": 1,
      "status": "decided"
    },
    {
      "id": "atomic-state",
      "decision": "State persisted via atomic write (tmp file + rename)",
      "rationale": "Crash safety \u2014 partial writes never corrupt state.json",
      "phase": 1,
      "status": "decided"
    },
    {
      "id": "completion-syntax",
      "decision": "Completion field uses ${{ }} syntax consistently, not {{ }} Nunjucks syntax",
      "rationale": "Completion templates resolve against CompletionScope which extends ExpressionScope \u2014 same engine, same syntax",
      "phase": 1,
      "status": "decided"
    },
    {
      "id": "completion-forms",
      "decision": "Two mutually exclusive completion forms: template (inline interpolation) or message + include (instruction text with data attachments)",
      "rationale": "Template form for simple cases, message+include for structured data injection into LLM context",
      "phase": 1,
      "status": "decided"
    },
    {
      "id": "artifact-paths",
      "decision": "Artifacts support latest overwrite pattern and runId-stamped history accumulation",
      "rationale": "latest.md always current without knowing run ID; timestamped files accumulate history for comparison",
      "phase": 2,
      "status": "decided"
    },
    {
      "id": "conservative-plan",
      "decision": "buildConservativePlan() adds implicit sequential deps for steps without explicit ${{ steps.X }} refs",
      "rationale": "Parallelism is opt-in \u2014 safe default prevents unintended concurrent side effects",
      "phase": 3,
      "status": "decided"
    },
    {
      "id": "parallel-structured-only",
      "decision": "Phase 3 parallelism limited to read-only/structured-output workflows",
      "rationale": "Parallel steps writing to working directory share cwd and may interfere \u2014 filesystem isolation deferred",
      "phase": 3,
      "status": "decided"
    },
    {
      "id": "nunjucks-unconditional",
      "decision": "compileAgentSpec() renders both system and task prompts through Nunjucks unconditionally",
      "rationale": "No hasTemplateSyntax gate needed \u2014 plain text renders to itself, template syntax renders correctly",
      "phase": 5,
      "status": "decided"
    },
    {
      "id": "template-protection",
      "decision": "${{ }} workflow expressions protected from Nunjucks interpretation during template rendering",
      "rationale": "Two expression syntaxes coexist \u2014 Nunjucks must not interpret ${{ }} as its own {{ }}",
      "phase": 5,
      "status": "decided"
    },
    {
      "id": "three-tier-resolution",
      "decision": "Three-tier resource resolution: .pi/<dir>/ > ~/.pi/agent/<dir>/ > <package>/<dir>/",
      "rationale": "Project-level overrides user-level overrides package defaults \u2014 standard precedence",
      "phase": 5,
      "status": "decided"
    },
    {
      "id": "checkpoint-resume",
      "decision": "Resume reuses run dir and state, layer loop skips completed steps, re-runs failed steps",
      "rationale": "Partial parallel layers resume correctly \u2014 only pending steps in each layer execute",
      "phase": 4,
      "status": "decided"
    },
    {
      "id": "pause-mechanisms",
      "decision": "Three pause mechanisms: workflow-authored (pause: step), user-initiated (Ctrl+H), crash recovery",
      "rationale": "Deliberate pause points, interactive control, and crash safety are three distinct use cases needing unified resume",
      "phase": 4,
      "status": "decided"
    },
    {
      "id": "output-persistence",
      "decision": "Every step writes output as JSON to disk via persistStepOutput() \u2014 file system is the data bus",
      "rationale": "One workflow's output is another workflow's input. StepResult.outputPath holds the absolute path.",
      "phase": 4,
      "status": "decided"
    },
    {
      "id": "agent-yaml-compilation",
      "decision": "Agent specs are YAML declaring typed contracts; .md is compiled at dispatch time from spec + templates + input",
      "rationale": "The .md exists in memory for milliseconds \u2014 written to temp file, passed to pi, deleted. Never hand-authored.",
      "phase": 5,
      "status": "decided"
    },
    {
      "id": "project-as-data",
      "decision": "Project structure decomposed into typed JSON blocks that templates compose from",
      "rationale": "Master planning doc is a monolith \u2014 no part independently injectable into agent prompts. Blocks enable selective context injection.",
      "phase": 7,
      "status": "decided"
    },
    {
      "id": "budget-defer",
      "decision": "Budget enforcement deferred \u2014 accumulation via addUsage() works but no ceiling gate",
      "rationale": "Needs design for configurable actions (pause, fail, warn) \u2014 accumulation sufficient for now",
      "phase": 4,
      "status": "tentative"
    },
    {
      "id": "foreach-when-global",
      "decision": "forEach when: conditional applies globally before iteration starts, not per-iteration",
      "rationale": "Per-iteration conditions require the as-bound variable in the when expression, but when is evaluated before forEach binding happens. Authors use transform + filter to pre-filter the array instead.",
      "phase": 7,
      "status": "decided"
    },
    {
      "id": "command-fails-nonzero",
      "decision": "command step fails on non-zero exit (unlike gate which always returns completed)",
      "rationale": "Command produces data \u2014 non-zero exit means the data is not available. Gate judges a condition \u2014 both pass and fail are valid outcomes.",
      "phase": 7,
      "status": "decided"
    },
    {
      "id": "output-write-nonfatal",
      "decision": "Output write failures are non-fatal \u2014 step data is in memory for downstream expressions",
      "rationale": "Output files are supplementary persistence. The step already ran. Aligns with artifact write treatment.",
      "phase": 7,
      "status": "decided"
    },
    {
      "id": "state-write-fatal",
      "decision": "State write failures are fatal and halt the workflow",
      "rationale": "state.json is the sole checkpoint for resume. Continuing without it risks silent progress loss.",
      "phase": 7,
      "status": "decided"
    },
    {
      "id": "truncation-not-failure",
      "decision": "Truncated steps remain status:completed with truncated flag and warnings",
      "rationale": "Truncation is orchestrator visibility loss, not agent failure. The subprocess may have succeeded.",
      "phase": 7,
      "status": "decided"
    },
    {
      "id": "missing-agent-explicit-fail",
      "decision": "Missing agents throw AgentNotFoundError instead of returning silent { name } fallback",
      "rationale": "Silent fallback made typos and missing specs indistinguishable from valid agents. Breaking change \u2014 clear error listing all search paths.",
      "phase": 7,
      "status": "decided"
    },
    {
      "id": "d1-propagate-not-catch",
      "decision": "Remove the try/catch entirely rather than re-throwing with a modified message",
      "rationale": "The original catch was adding an input schema hint to the error message, but this is better handled by the framework or the workflow executor itself. Keeping the error unmodified preserves the original stack trace and error context.",
      "status": "decided",
      "phase": 8
    },
    {
      "id": "non-interactive-resume-behavior",
      "decision": "Auto-resume incomplete runs in non-interactive mode instead of failing",
      "rationale": "In handleRun and handleResume, the select() prompts ask whether to resume. In non-interactive mode, auto-resuming is the safer default since the user explicitly invoked run/resume. For handleList, we return early since interactive selection is fundamental to its purpose.",
      "status": "decided",
      "phase": 8
    },
    {
      "id": "d1-context-type-distinction",
      "decision": "Use ExtensionCommandContext for command handlers (handleList, handleRun, handleResume, /workflow handler) and ExtensionContext for tool execute and shortcut handlers",
      "rationale": "ExtensionCommandContext extends ExtensionContext with waitForIdle() and is the type expected by RegisteredCommand.handler. Tool execute and shortcut handlers receive ExtensionContext per the pi SDK type definitions.",
      "status": "decided",
      "phase": 8
    },
    {
      "id": "d2-params-inline-type",
      "decision": "Used inline object type `{ workflow: string; input?: unknown; fresh?: string }` for execute params instead of Static<typeof schema>",
      "rationale": "The parameter schema uses Type.Optional(Type.Unknown()) for input which makes Static inference awkward. Inline type matches the actual shape and is clearer.",
      "status": "decided",
      "phase": 8
    },
    {
      "id": "truncation-point",
      "decision": "Truncate in workflow-executor.ts at the sendMessage call site rather than in formatResult or index.ts",
      "rationale": "The sendMessage path in workflow-executor.ts is the unbounded path identified in the finding. formatToolResult in index.ts is already a compact one-liner summary. Truncating at sendMessage covers both the formatResult and resolveCompletion code paths.",
      "status": "decided",
      "phase": 8
    },
    {
      "id": "truncation-limits",
      "decision": "Use 50KB / 2000 lines matching pi built-in limits",
      "rationale": "P7 conformance reference specifies 50KB / 2000 lines as the built-in limit. Using truncateTail from pi-coding-agent as required by P7.1.",
      "status": "decided",
      "phase": 8
    },
    {
      "id": "cluster-filters-by-code",
      "decision": "Cluster step runs grep checks before clustering \u2014 findings whose verify_pattern no longer matches are already fixed and skipped. Code is source of truth, not resolution metadata fields.",
      "rationale": "Resolution fields are bookkeeping. The code is what matters. A finding with verify_method:grep can be checked right now without depending on prior workflow runs having written resolution fields.",
      "phase": 8,
      "status": "decided"
    },
    {
      "id": "verify-scoring-three-way",
      "decision": "Verify step reports passed/failed/needs_inspect separately. Gate checks failed===0, not passed===total. needs_inspect findings do not block the gate.",
      "rationale": "Findings with verify_method:inspect cannot be automated \u2014 lumping them with failures makes the gate always fail when any inspect findings exist. Separating the counts lets automated checks pass while flagging items for human review.",
      "phase": 8,
      "status": "decided"
    },
    {
      "id": "gate-after-bookkeeping",
      "decision": "Gate steps with onFail:fail must be ordered after bookkeeping steps (update-audit, route-results). Use explicit DAG dependency (when: steps.X.output) to ensure ordering.",
      "rationale": "Gate with onFail:fail kills the pipeline. If bookkeeping steps are siblings of the gate (both depend on verify), DAG may schedule gate first. update-audit never ran in first fix-audit execution because of this.",
      "phase": 8,
      "status": "decided"
    },
    {
      "id": "audit-schema-snippet-required",
      "decision": "snippet is required in audit findings locations[] items. Every finding must carry the actual code at its location.",
      "rationale": "Without required snippets, auditing agents skip them and downstream consumers (audit-fixer template) lack code context. Making it required means the data contract is enforced at schema level \u2014 the auditor can't produce findings without reading the code.",
      "phase": 8,
      "status": "decided"
    },
    {
      "id": "audit-schema-fix-required",
      "decision": "fix is required on every audit finding. Every finding must have suggestion + verify_method.",
      "rationale": "A finding without a fix is not actionable by downstream workflows. The fix-audit workflow needs fix.suggestion for the agent prompt and fix.verify_method for verification. Optional fix means some findings are dead weight.",
      "phase": 8,
      "status": "decided"
    },
    {
      "id": "audit-verify-method-no-human",
      "decision": "verify_method enum is command, grep, inspect. Removed human \u2014 inspect covers it.",
      "rationale": "There is no consumer for human verify_method. The verify step can't do anything different with human vs inspect. Three values with clear semantics: command runs a shell command, grep checks a pattern, inspect means no automated check possible.",
      "phase": 8,
      "status": "decided"
    },
    {
      "id": "add-work-command-scope",
      "decision": "Appending gaps/decisions/rationale to project blocks is a slash command (/workflow add-work), not a workflow. Phase creation and architecture refresh are separate workflows.",
      "rationale": "Array block appends are main-context intelligence work \u2014 the LLM reads the conversation, extracts structured content, validates against schemas. No subprocess dispatch needed. Phase creation needs a dedicated agent (phase-author). Architecture needs codebase analysis. Different processes, different mechanisms.",
      "phase": 8,
      "status": "decided"
    },
    {
      "id": "add-work-sends-message",
      "decision": "The /workflow add-work command injects instruction via pi.sendMessage with triggerTurn:true, not ctx.ui.notify. The LLM receives schemas and acts on them.",
      "rationale": "ctx.ui.notify displays text to the user but doesn't enter the LLM's conversation. pi.sendMessage with triggerTurn:true and deliverAs:followUp waits for the current turn to finish, then the LLM receives the instruction and acts autonomously.",
      "phase": 8,
      "status": "decided"
    },
    {
      "id": "add-work-captures-raw-intent",
      "decision": "/workflow add-work accepts inline text as raw intent capture. '/workflow add-work we need X because Y' passes the text through to the LLM instruction. No args = reads from conversation.",
      "rationale": "Intent capture shouldn't require a separate step. The user types a rough idea after the command, it gets processed into typed blocks. Quick capture, structured later.",
      "phase": 8,
      "status": "decided"
    },
    {
      "id": "monitors-peer-not-dependency",
      "decision": "pi-behavior-monitors is a peer extension, not a dependency. Workflow extension emits structured events into pi's event system. Monitors optionally observe them. No import, no hard coupling.",
      "rationale": "Separation of concerns. The workflow extension knows its domain (steps, blocks, schemas). Monitors know observation patterns (friction detection, gap recording). The contract is the event shape, not the implementation. If monitors aren't installed, the workflow extension still works \u2014 events go nowhere.",
      "phase": 8,
      "status": "decided"
    },
    {
      "id": "deterministic-validation-not-monitors",
      "decision": "Schema validation on block writes is the workflow extension's responsibility, not a monitor's. Monitors observe behavioral patterns over time via LLM classification. Deterministic checks (AJV) happen at write time in the producing code.",
      "rationale": "Monitor architecture requires LLM classification \u2014 overkill for 'run AJV, pass or fail'. More importantly, validation at write time prevents invalid data from being persisted. Monitors observing after the fact can only report the damage. The producing extension is the right place for write-time guards.",
      "phase": 8,
      "status": "decided"
    },
    {
      "id": "workflow-events-additive-not-required",
      "decision": "Workflow extension emitting events (workflow:block-write, workflow:step-complete) is additive for monitors, not required for validation. Events give monitors richer signal without parsing conversation text, but validation doesn't depend on monitors being installed.",
      "rationale": "If validation required monitors, it would be bypassable by not installing the monitor extension. Validation must be in the write path. Events let monitors detect higher-level patterns (agent repeatedly producing invalid output, same finding recurring across runs) \u2014 that's behavioral observation, not validation.",
      "phase": 8,
      "status": "decided"
    },
    {
      "id": "extension-communication-via-events",
      "decision": "Extensions communicate via pi.events.emit/on with agreed event names and payload shapes. No import, no dependency. Event contract is the interface.",
      "rationale": "pi-behavior-monitors already uses pi.events.emit('monitors:abort'). The pattern works for cross-extension communication. Workflow extension emits workflow:* events, monitors optionally consume them. If either extension is absent, nothing breaks.",
      "phase": 8,
      "status": "decided"
    },
    {
      "id": "project-workflow-peer-extensions",
      "decision": "Project and workflow are separate domains that should be peer pi extensions. Project owns typed knowledge structure (.workflow/ blocks, schemas, validation). Workflow owns execution engine (YAML specs, DAG, dispatch, state). Neither imports the other \u2014 they communicate via pi events and shared file conventions.",
      "rationale": "The test: can you use project without workflows? Yes \u2014 manual block management, add-work from conversation, schema validation. Can you use workflows without project? Yes \u2014 YAML specs, step execution, subprocess dispatch. The coupling is incidental \u2014 project infrastructure was built inside workflow because that's where we were working.",
      "phase": 8,
      "status": "decided"
    },
    {
      "id": "project-build-through-use-cases",
      "decision": "Build project extension through use-case modeling alongside workflow, not by premature extraction. Keep project code in this repo, discover the boundary through actual usage, extract when the interface is clear.",
      "rationale": "Extracting now would require guessing the API. Building through use cases \u2014 add-work, block validation, model-config resolution, route-results \u2014 reveals the actual contract between the two domains. The boundary becomes obvious when the use cases stabilize.",
      "phase": 8,
      "status": "decided"
    },
    {
      "id": "investigate-not-plan",
      "decision": "Investigation produces structured findings (understanding), not a plan. Decomposition is a separate step that takes findings and produces specs. Different cognitive tasks, different context needs, different output schemas.",
      "rationale": "An investigator that also plans is doing two things \u2014 plan quality suffers because the context window is full of codebase analysis when it should be focused on decomposition heuristics. Separation gives the decompose agent clean input from both investigation and research.",
      "phase": 8,
      "status": "decided"
    },
    {
      "id": "research-parallel-to-investigate",
      "decision": "Research (external patterns, prior art, heuristics) runs in parallel with investigation (codebase analysis). Both take the gap as input, both produce structured findings, neither depends on the other. DAG planner puts them in the same layer automatically.",
      "rationale": "Decompose agent gets richer input: here's what the codebase looks like (investigate), here's how others have solved this (research). Better decomposition because it works from both the problem and the solution space. Parallel execution is free \u2014 no mutual ${{ steps.X }} references.",
      "phase": 8,
      "status": "decided"
    },
    {
      "id": "do-gap-is-fixed-workflow",
      "decision": "The do-gap workflow has a fixed structure: load \u2192 investigate + research \u2192 decompose \u2192 implement \u2192 verify \u2192 route. The steps don't change per gap. The gap is the input. The investigation shapes what decompose produces, which shapes what implement iterates over.",
      "rationale": "We practiced this pattern manually (investigate agent \u2192 implement agent for graduated-failure). The workflow codifies it. It's not dynamically generated \u2014 the same workflow runs for every gap. The intelligence is in the agents and their composed prompts, not in the workflow structure.",
      "phase": 8,
      "status": "decided"
    },
    {
      "id": "output-schema-injection",
      "decision": "Step executor reads output schema file and injects content into template context as output_schema variable. Templates render {{ output_schema }} instead of duplicating JSON structure.",
      "rationale": "Single source of truth. Schema changes propagate automatically to all agent prompts. Eliminates drift between what the prompt shows and what the validator checks.",
      "phase": 8,
      "status": "decided"
    },
    {
      "id": "agent-prompts-xml-structure",
      "decision": "Agent system prompts use XML tags (objective, workflow, constraints, anti_patterns, success_criteria) instead of markdown headings or freeform prose.",
      "rationale": "XML provides semantic boundaries, better parsing, lower token usage. Based on Claude Code best practices. Required tags ensure every agent has scope boundaries, explicit constraints, and verifiable success criteria.",
      "phase": 8,
      "status": "decided"
    },
    {
      "id": "block-validation-always-rollback",
      "decision": "Block validation rollback runs on every validation failure, not just retry attempts. Invalid data must not persist on disk regardless of whether the step will be retried.",
      "rationale": "Previous behavior: rollback only on attempt < totalAttempts. Final attempt or no-retry steps left corrupted data on disk. Rollback is a data integrity operation, not a retry operation.",
      "phase": 8,
      "status": "decided"
    },
    {
      "id": "tui-single-activity-line",
      "decision": "TUI shows only the most recent tool activity under running steps, not the full ring buffer history.",
      "rationale": "Full buffer caused visual duplication and clutter. Single line gives current status without noise. Ring buffer retained in memory for potential future use but only latest rendered.",
      "phase": 8,
      "status": "decided"
    },
    {
      "id": "prompt-best-practices-as-skill",
      "decision": "Agent prompt authoring best practices are a pi skill (.pi/skills/author-agent-prompts/) not inline documentation. Loaded on demand when creating or editing agent specs.",
      "rationale": "Progressive disclosure \u2014 skill is only loaded when needed. Contains audit checklist, concrete patterns from our agents, and output schema injection flow. Follows pi skill standard with SKILL.md + references/.",
      "phase": 8,
      "status": "decided"
    }
  ]
}
