{
  "issues": [
    {
      "id": "issue-001",
      "title": "Agent input schemas are parsed but never validated at dispatch time",
      "body": "AgentSpec.inputSchema is populated by agent-spec.ts:126 but step-agent.ts never validates the step's resolved input against it. The synthesizer agent requires [structure, quality, patterns] but resumable-analysis.workflow.yaml passes only structure and quality — this silently succeeds. Input schemas are decorative, creating false confidence in typed composition. Either add validation in executeAgentStep() before dispatch, or remove inputSchema from the type to stop pretending.",
      "location": "packages/pi-workflows/src/step-agent.ts:52",
      "status": "resolved",
      "category": "primitive",
      "priority": "critical",
      "package": "pi-workflows",
      "source": "agent",
      "resolved_by": "v0.11.0 commit 92e5427 — inputSchema validation in executeAgentStep(), task-001/ver-001. Criterion 6 (audit all callers) deferred to task-002."
    },
    {
      "id": "issue-002",
      "title": "Template composition (extends/blocks/macros) used in only 1 of 22 template families",
      "body": "The Nunjucks template system supports extends, block, macro, and include — powerful composition primitives. Only the analyzers/ family uses them (structure/quality/patterns extending base-analyzer.md). The other 21 template directories are flat files with no composition. This means the template machinery is over-engineered relative to actual usage, OR the agent library is under-exploiting available composition. Agents like investigator, researcher, gap-identifier, and gap-resolution-assessor share structural patterns (read code → produce structured findings → validate against schema) that could share a base template.",
      "location": "packages/pi-workflows/templates/",
      "status": "open",
      "category": "composition",
      "priority": "high",
      "package": "pi-workflows",
      "source": "agent"
    },
    {
      "id": "issue-003",
      "title": "Agent reuse across workflows is structural repetition, not parametric recombination",
      "body": "code-explorer appears in 6 workflows but always with identical input (path: ${{ input.path }}). structure-analyzer appears in 6 workflows, always with (exploration + path). synthesizer appears in 4 workflows with minor variation. Agents are copy-pasted at the YAML level — the typed input system and Nunjucks templates could enable genuinely different compositions (e.g., explorer with depth parameter, synthesizer with optional analysis channels) but this isn't happening. The agents should be redesigned to accept variable input shapes and adapt their prompts accordingly.",
      "location": "packages/pi-workflows/agents/",
      "status": "open",
      "category": "composition",
      "priority": "medium",
      "package": "pi-workflows",
      "source": "agent"
    },
    {
      "id": "issue-004",
      "title": "No ad-hoc agent invocation path — every use requires a .workflow.yaml spec",
      "body": "pi-subagents allows /run scout 'analyze auth' with zero files written. Our system requires a .workflow.yaml for every agent invocation. This makes the barrier to use much higher for exploration, debugging, and one-off tasks. A /workflow run-agent <name> command or equivalent tool call that dispatches a single agent step with inline input (JSON or key=value) would bridge this gap. The agent's input schema (once enforced) provides validation; the task template provides prompt rendering; the output schema provides structured results — all without a workflow spec.",
      "location": "packages/pi-workflows/src/index.ts",
      "status": "open",
      "category": "capability",
      "priority": "high",
      "package": "pi-workflows",
      "source": "agent"
    },
    {
      "id": "issue-005",
      "title": "Parallel agent steps share working tree — filesystem conflicts in concurrent file-writing workflows",
      "body": "The parallel: step type and DAG-inferred parallelism run agents concurrently in the same working directory. If two parallel agents modify the same file (or even different files that trigger build/lint side effects), results are undefined. pi-subagents solves this with git worktree isolation: temp worktrees per agent, node_modules symlink, diff capture after completion, cleanup in finally block. Our parallel steps need an optional worktree: true flag with equivalent machinery.",
      "location": "packages/pi-workflows/src/step-parallel.ts",
      "status": "open",
      "category": "issue",
      "priority": "high",
      "package": "pi-workflows",
      "source": "agent"
    },
    {
      "id": "issue-006",
      "title": "No recursion depth guard for future nested workflow invocation (Phase 6)",
      "body": "The workflow: field on StepSpec is parsed but rejected at validation time (Phase 6 — not yet implemented). When unblocked, nested workflow invocation will allow unbounded recursive spawning without a depth guard. pi-subagents' pattern: PI_SUBAGENT_MAX_DEPTH env var propagated via PI_SUBAGENT_DEPTH in child process env. Must be in place before workflow: steps are enabled.",
      "location": "packages/pi-workflows/src/workflow-spec.ts",
      "status": "open",
      "category": "capability",
      "priority": "medium",
      "package": "pi-workflows",
      "source": "agent"
    },
    {
      "id": "issue-007",
      "title": "assigned_agent field on tasks is decorative — nothing routes work based on it",
      "body": "The tasks schema has an assigned_agent field but no code reads it for dispatch routing. An agent could be assigned to a task and a different agent dispatched by the workflow. The execute-task workflow dispatches task-worker regardless of assigned_agent. Either the field should drive dispatch (workflow reads it and selects agent) or it should be removed to stop pretending.",
      "location": "packages/pi-project/defaults/schemas/tasks.schema.json",
      "status": "open",
      "category": "primitive",
      "priority": "medium",
      "package": "pi-project",
      "source": "agent"
    },
    {
      "id": "issue-008",
      "title": "No automatic decision recording during agent execution — decisions block task field exists but nothing populates it",
      "body": "The decisions schema has a task field (added v0.11.0) for backlink from decisions to the task during which the decision was made. But no agent, workflow step, or tool automatically records decisions during task execution. The execution-results schema has a decisions[] array in agent output but nothing routes those entries to the decisions block. Decisions made by agents during execute-task are lost.",
      "location": "packages/pi-project/defaults/schemas/decisions.schema.json",
      "status": "open",
      "category": "primitive",
      "priority": "high",
      "package": "pi-workflows",
      "source": "agent"
    },
    {
      "id": "issue-009",
      "title": "No phase-level verification rollup — individual task verification exists but nothing aggregates to phase success_criteria",
      "body": "The verification schema supports target_type: phase but nothing produces phase-level verification entries. Phase specs have success_criteria with verify_method but no workflow connects individual task completion to phase-level verification. A phase could have all tasks completed and verified but no phase-level evidence that success_criteria were met.",
      "location": "packages/pi-project/defaults/schemas/verification.schema.json",
      "status": "open",
      "category": "capability",
      "priority": "high",
      "package": "pi-workflows",
      "source": "agent"
    },
    {
      "id": "issue-010",
      "title": "Issue lifecycle not connected to task completion — resolved_by must be set manually",
      "body": "When task-001 was completed, issue-001 status remained open. No workflow or tool automatically updates issue status when the task that fixes it completes. The issues schema has resolved_by but nothing populates it from task/verification data. Manual intervention required to keep issues block in sync with reality.",
      "location": "packages/pi-project/src/project-sdk.ts",
      "status": "open",
      "category": "primitive",
      "priority": "high",
      "package": "pi-project",
      "source": "agent"
    },
    {
      "id": "issue-011",
      "title": "Agent execution metadata (model, cost, duration) not persisted to project blocks",
      "body": "Workflow step results contain agent name, model, token usage, cost, and duration in run state (.workflows/runs/). But this data is ephemeral — not persisted to any project block. No record of which agent executed a task at what model tier with what quality. The verification block has evidence text but not structured execution metadata.",
      "location": "packages/pi-workflows/src/workflow-executor.ts",
      "status": "open",
      "category": "capability",
      "priority": "medium",
      "package": "pi-workflows",
      "source": "agent"
    },
    {
      "id": "issue-012",
      "title": "Completion message fires on workflow failure — misleading success message",
      "body": "workflow-executor.ts else branch at line 947 handled completed and failed status identically. resolveCompletion() ran unconditionally when spec.completion existed. Static completion.message strings like \"Handoff captured to .project/handoff.json\" fired on failure even though nothing was written. All workflow specs with completion messages assumed success — none checked ${{ status }}. formatResult() already correctly renders failure but was bypassed when spec.completion was present.",
      "location": "packages/pi-workflows/src/workflow-executor.ts:949",
      "status": "resolved",
      "category": "issue",
      "priority": "critical",
      "package": "pi-workflows",
      "source": "agent",
      "resolved_by": "v0.11.3 commit b614be4 — added state.status === \"completed\" guard to completion condition"
    },
    {
      "id": "issue-013",
      "title": "Agent output instructions tell agents to write files they cannot write — fenced JSON rejected",
      "body": "buildPrompt() in step-shared.ts told agents \"Write your result as valid JSON to: <path>\" but 14 of 16 JSON-producing agents have tools: [read] or similar — no write capability. Their only output channel is textOutput (last assistant message). No instruction prohibited markdown fences, so LLMs wrapped JSON in markdown code blocks which JSON.parse() rejected. The prompt instruction was mismatched with agent capabilities.",
      "location": "packages/pi-workflows/src/step-shared.ts:95",
      "status": "resolved",
      "category": "issue",
      "priority": "critical",
      "package": "pi-workflows",
      "source": "agent",
      "resolved_by": "v0.11.3 commit b614be4 — rewritten to \"raw JSON only, no markdown fences\" with file-write as secondary"
    },
    {
      "id": "issue-014",
      "title": "Format-json output path silently swallows JSON parse failures — step completes with output: undefined",
      "body": "step-agent.ts lines 232-238: the format-json path (no schema) caught JSON.parse exceptions and silently left output undefined. Step completed successfully with no output. persistStepOutput then wrote { text: garbage } to the output file. Downstream steps accessing steps.X.output.field got undefined — cascading failures untraceable to the silent swallow. Block writes are fatal on validation failure; this path was not, violating the principle from dec-009 that ungated paths next to gated ones are not enforcement.",
      "location": "packages/pi-workflows/src/step-agent.ts:236",
      "status": "resolved",
      "category": "primitive",
      "priority": "critical",
      "package": "pi-workflows",
      "source": "agent",
      "resolved_by": "v0.11.3 commit b614be4 — parse failure now marks step as failed, same contract as schema-bound path"
    },
    {
      "id": "issue-015",
      "title": "Classify prompt lacks conversation history — monitors produce false positives on multi-turn backreferences",
      "body": "The classify prompt receives user_text and assistant_text from the current turn only. When a user message backreferences prior turns ('reoutput my original task', 'as I said earlier'), the classifier cannot verify whether the response addresses the referenced content. It sees a short user request and a long technical response, pattern-matches to 'deviation', and flags incorrectly. Observed: 3 false positives in a single session from the hedge monitor, all on responses that directly addressed what the user asked. Root cause: no conversation_history collector exists. The classifier reasons in a single-turn vacuum. Fix: add a conversation_history context collector that provides the last N turns, or expand user_text to include referenced prior messages.",
      "location": "packages/pi-behavior-monitors/examples/hedge/classify.md",
      "status": "resolved",
      "category": "issue",
      "priority": "critical",
      "package": "pi-behavior-monitors",
      "source": "human",
      "resolved_by": "conversation_history collector with adaptive window — regex-based referential detection, 1-3 turn summaries, 2000 char budget"
    },
    {
      "id": "issue-016",
      "title": "No TUI visibility into monitor token usage and cost — side-channel LLM calls are invisible",
      "body": "Every monitor classification is an LLM call (classify model, typically claude-sonnet). These calls consume tokens and incur cost but are completely invisible in the TUI. No running tally, no per-monitor breakdown, no session total. In a session with active monitors firing on every turn_end, the accumulated cost can be significant. Users have no way to see what monitors are costing them. Need: running tally in TUI showing monitor token usage (input/output) and cost, updated after each classification call.",
      "location": "packages/pi-behavior-monitors/index.ts",
      "status": "open",
      "category": "capability",
      "priority": "high",
      "package": "pi-behavior-monitors",
      "source": "human"
    },
    {
      "id": "issue-017",
      "title": "No state coherence monitor — project blocks drift from source as development proceeds",
      "body": "Project block data (architecture line counts, domain entry counts, version references, conventions test counts) goes stale as code changes accumulate. No monitor watches commits and compares block assertions against source truth. The block API supports programmatic writes, the monitor system supports write actions on FLAG, and block-to-source mappings are deterministic — a state coherence monitor class could detect staleness on commit and write corrections directly without steering. Requires: project_blocks context collector, block-to-source mapping pattern library, classify template that reasons about state coherence rather than behavioral patterns, write action that updates stale fields.",
      "location": "packages/pi-behavior-monitors/examples/",
      "status": "open",
      "category": "capability",
      "priority": "high",
      "package": "pi-behavior-monitors",
      "source": "human"
    },
    {
      "id": "issue-018",
      "title": "Hedge monitor classifies tool-call-heavy responses as empty — tool_results not in classify context",
      "body": "The hedge monitor's classify context includes tool_calls (names of tools called) but not tool_results (what those tools produced). When the assistant's response is primarily tool work — edits, writes, commits, builds — the text portion is a brief summary. The classifier sees tool names and a terse text response, concludes 'the assistant provided no response', and flags incorrectly. Observed: 'The assistant provided no response at all' on a turn that edited files, ran builds, and committed. The work was in tool results the classifier never saw. Fix: add tool_results to the hedge monitor's classify.context array, or teach the classify prompt that tool_calls represent substantive work even when assistant_text is brief.",
      "location": "packages/pi-behavior-monitors/examples/hedge/hedge.monitor.json",
      "status": "resolved",
      "category": "issue",
      "priority": "critical",
      "package": "pi-behavior-monitors",
      "source": "human",
      "resolved_by": "commit 2745cf9 — tool_results added to hedge classify context, classify template updated to treat tool results as evidence of substantive work"
    },
    {
      "id": "issue-019",
      "title": "Skill generator cannot introspect validator phases or SDK query functions — new capabilities require narrative updates",
      "body": "The skill generator introspects tools, commands, events, shortcuts, and resource directories. It cannot introspect internal function behavior — validator phases and SDK query functions are invisible. When new validation checks or SDK capabilities are added, they only surface in the skill if the narrative (skill-narrative.md) is manually updated. The agent vocabulary extractor (extractAgentVocabulary) proved the pattern: export a registry, generator reads it, produces a vocabulary section. The validator now exports validationChecks() returning 11 check descriptors (commit 6fc1611), and the SDK exports agentContracts() and agentsByBlock(). The registries exist — what remains is generator integration: add extractValidationVocabulary() that calls validationChecks() and produces a <validation_vocabulary> section, parallel to <agent_vocabulary>, <planning_vocabulary>, and <monitor_vocabulary>.",
      "location": "scripts/generate-skills.js",
      "status": "resolved",
      "category": "capability",
      "priority": "high",
      "package": "pi-workflows",
      "source": "human",
      "resolved_by": "commit 4dffeec — extractValidationVocabulary() added to generator, produces <validation_vocabulary> table from validationChecks() registry"
    },
    {
      "id": "issue-020",
      "title": "contextBlocks injection only reads static files — no support for computed blocks derived at dispatch time",
      "body": "compileAgentSpec() reads .project/<name>.json for each contextBlocks entry. Blocks whose content is derivable from code (architecture, domain reference entries) go stale between writes. The injection path should support registered generator functions alongside static files — for derivable blocks, the generator runs at injection time, returning the same shape as the static block. Agent templates and shared macros are unaffected — they render whatever data they receive regardless of source.",
      "location": "packages/pi-workflows/src/step-shared.ts",
      "status": "open",
      "category": "capability",
      "priority": "high",
      "package": "pi-workflows",
      "source": "human"
    },
    {
      "id": "issue-021",
      "title": "Architecture block is manually maintained — modules, dependencies, and patterns are derivable from package.json and source",
      "body": "The architecture block restates what code inspection provides: module names from package.json, entry files, dependencies, responsibility from package descriptions. These go stale on every code change. A projectArchitecture(cwd) function could derive modules and dependencies from package.json files and entry points, producing the same schema shape as the static block. This would make the architecture block a computed block candidate for issue-020.",
      "location": ".project/architecture.json",
      "status": "open",
      "category": "capability",
      "priority": "medium",
      "package": "pi-project",
      "source": "human"
    },
    {
      "id": "issue-022",
      "title": "Domain block reference entries duplicate SDK query results — agent counts, workflow counts, step types go stale",
      "body": "Domain entries like agent-spec ('26 bundled agents'), workflow-spec ('15 bundled workflows'), and step type descriptions restate what availableAgents(), availableWorkflows(), and stepTypes() return dynamically. When agents or workflows are added, the domain entries are wrong. These reference entries should be computed from SDK functions at injection time rather than maintained as static JSON. Human-authored domain entries (design patterns, composition families) remain static.",
      "location": ".project/domain.json",
      "status": "open",
      "category": "capability",
      "priority": "medium",
      "package": "pi-project",
      "source": "human"
    },
    {
      "id": "issue-023",
      "title": "Monitor classify calls produce no debug output — misfires are uninspectable",
      "body": "classifyPrompt() calls complete(), parses the response via parseVerdict(), and discards all intermediate state. When a monitor misfires, there is no way to see: (1) the rendered classify prompt sent to the LLM, (2) the raw LLM response before parsing, (3) the collected context values each collector resolved to, (4) the verdict parsing decision. Subagent workflow steps write full session logs to .workflows/runs/. Monitors write nothing. Debugging requires adding temporary console.error() calls, reproducing, and reading stderr. Need: a session-scoped debug log per monitor that persists the rendered prompt, raw response, and parsed verdict for each classify call. Location could be .pi/monitor-logs/<session-id>/<monitor-name>.jsonl or similar.",
      "location": "packages/pi-behavior-monitors/index.ts:1150",
      "status": "open",
      "category": "capability",
      "priority": "critical",
      "package": "pi-behavior-monitors",
      "source": "human"
    },
    {
      "id": "issue-024",
      "title": "Classify calls with thinking enabled return empty text — model puts verdict in thinking block",
      "body": "classifyPrompt() passes thinkingEnabled: true to complete(). With thinking enabled, the model can place its entire response (including the CLEAN/FLAG/NEW verdict) inside the thinking block and return no text content. extractText() filters for type === 'text' only, so thinking content is invisible. parseVerdict('') hits the unrecognized format path and returns verdict: 'error'. Observed on fragility monitor: 'classify failed: Unrecognized verdict format: \"\"'. Immediate fix: extractText() should fall back to thinking block content when no text blocks are present. This issue is also a symptom of issue-025 — unifying the classify path with agent specs eliminates this bug class entirely since the agent output pipeline already handles thinking extraction.",
      "location": "packages/pi-behavior-monitors/index.ts:1169",
      "status": "resolved",
      "category": "issue",
      "priority": "critical",
      "package": "pi-behavior-monitors",
      "source": "human",
      "resolved_by": "extractResponseText() replaces extractText() in classifyPrompt() — falls back to thinking block content when no text parts present. Agent path uses extractResponseText() natively. Both paths now handle thinking-only responses."
    },
    {
      "id": "issue-025",
      "title": "Monitor classify calls reinvent agent infrastructure — should use agent specs for LLM call contracts",
      "body": "Monitor classify calls hand-roll template rendering, context injection, model resolution, and output parsing that the agent spec system already provides declaratively. Monitors have their own createMonitorTemplateEnv(), collectors parallel contextBlocks, parseVerdict() hand-parses what output.schema validates, and parseModelSpec() reimplements model registry resolution. A monitor's classify call IS an agent call — structured input, prompt template, model, structured output. Direction: monitors declare classify.agent referencing a .agent.yaml that defines the LLM call contract, executed in-process via direct complete() rather than subprocess dispatch. One spec format for all LLM calls, monitor-specific concerns (collectors, patterns, actions, ceiling) stay in .monitor.json, verdict format becomes output.schema with enum validation. Eliminates issue-024 (thinking block extraction) as a side effect since agent output pipeline already handles it. The agent spec parser and template compiler are the only pieces needed from pi-workflows — they could live in pi-project or a small shared package without moving the entire workflow engine. Rejected alternative: extracting shared infrastructure from pi-workflows into pi-project is unbounded refactoring that defers the design question behind a prerequisite wall.",
      "location": "packages/pi-behavior-monitors/index.ts:1150",
      "status": "resolved",
      "category": "capability",
      "priority": "high",
      "package": "pi-behavior-monitors",
      "source": "human",
      "resolved_by": "classifyViaAgent() uses createAgentLoader + compileAgentSpec + in-process complete(). Per-monitor .agent.yaml specs define model, thinking, output schema, task template. Classify templates gain {% if json_output %} conditional. mapVerdictToClassifyResult() replaces text parsing. Legacy path preserved for monitors without classify.agent."
    },
    {
      "id": "issue-026",
      "title": "Structured output via tool-use enforcement for monitor classify",
      "body": "Monitor classify calls relied on prompt instructions ('respond with JSON only') and text parsing (JSON.parse on free-form LLM response). The model could ignore the instruction and produce prose preambles, reasoning text, or malformed output — causing classify failures. Fixed in classifyViaAgent() by defining VERDICT_TOOL with TypeBox parameters, passing it in Context.tools, and setting toolChoice to force the tool call. The model produces ToolCall.arguments matching the schema — no free-form text, no JSON.parse() of LLM output. Scoped to monitors (direct complete() calls). Workflow agent steps use subprocess dispatch via pi --mode json where tool-use enforcement would require pi-coding-agent core changes — that remains a separate concern.",
      "location": "packages/pi-behavior-monitors/index.ts",
      "status": "resolved",
      "category": "issue",
      "priority": "critical",
      "package": "pi-behavior-monitors",
      "source": "human",
      "resolved_by": "VERDICT_TOOL + toolChoice enforcement in classifyViaAgent(), ToolCall extraction replaces extractResponseText() + JSON.parse() + parseVerdict() fallback"
    },
    {
      "id": "issue-027",
      "title": "extractResponseText() uses wrong property name for ThinkingContent — thinking block extraction was broken since implementation",
      "body": "extractResponseText() checked 'text' in part for thinking blocks, but pi-ai ThinkingContent has { type: 'thinking', thinking: string }, not { type: 'thinking', text: string }. The fallback to thinking content never fired. Test fixtures used { type: 'thinking', text: '...' } — a made-up shape that matched the code's assumption, not the real type. Tests passed against wrong fixtures. Fixed in fd02c15 — property corrected to 'thinking', test fixtures corrected. Root cause: implementing agent was not scoped with the exact ThinkingContent type signature.",
      "location": "packages/pi-behavior-monitors/index.ts",
      "status": "resolved",
      "category": "issue",
      "priority": "critical",
      "package": "pi-behavior-monitors",
      "source": "human",
      "resolved_by": "fd02c15 — corrected property access from .text to .thinking, fixed test fixtures to use real ThinkingContent shape"
    }
  ]
}