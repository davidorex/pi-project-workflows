{
  "description": "FGAP-074 C2 — migrate pi-jit-agents consumers (6 files). Repoint old project-* subpath imports + renamed identifiers to the new context-* names across pi-jit-agents (compile.ts, template module + their tests). Green per commit.",
  "status": "completed",
  "files": [
    "packages/pi-jit-agents/src/compile.ts"
  ],
  "acceptance_criteria": [
    "All 6 pi-jit-agents files importing from @davidorex/pi-context/project-* repointed to ./context-* + renamed identifiers",
    "No legacy project-* import string remains in pi-jit-agents",
    "npm run build && check && test green"
  ],
  "depends_on": [
    "TASK-067"
  ],
  "notes": "Decomposition chunk C2. Maps to Claude task #61. FGAP-074. Resolves via aliases until C7 removes them.",
  "verification": "VER-039"
}