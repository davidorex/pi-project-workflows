{
	"number": 6,
	"name": "Composition",
	"intent": "Add workflow inheritance, nested workflows, forEach fan-out, and custom TUI dashboards. Enable workflows to compose from other workflows.",
	"status": "planned",
	"success_criteria": [
		{ "criterion": "Workflow extends: inherits steps from parent workflow", "verify_method": "command" },
		{ "criterion": "Workflow step invokes nested workflow", "verify_method": "command" },
		{ "criterion": "Custom TUI widget and overlay renderers load from workflow spec", "verify_method": "inspect" }
	],
	"specs": [],
	"dependencies": [1, 2, 3, 4, 5],
	"artifacts_produced": []
}
