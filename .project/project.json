{
  "name": "pi-project-workflows",
  "description": "Typed multi-step workflow execution via .workflow.yaml specs. Schema-driven project state in .project/. Behavior monitors that classify agent activity and steer corrections.",
  "core_value": "The runtime is generic; the domain lives in specs, schemas, and templates that users create without touching TypeScript. Add a JSON Schema — get a new block type. Write a .workflow.yaml — get a pipeline. Drop a .monitor.json — get a watchdog. Author an .agent.yaml — get a typed agent contract compiled to prompts at dispatch time.",
  "goals": [
    {
      "id": "workflow-orchestration",
      "description": "Workflow orchestration with DAG-based parallelism forming a typed loop: project state → workflow input → agent output → validated project state → monitor classification → steering"
    },
    {
      "id": "schema-driven-blocks",
      "description": "Schema-driven project blocks with cross-block validation"
    },
    {
      "id": "behavior-monitors",
      "description": "Autonomous behavior monitors with LLM classification"
    }
  ],
  "constraints": [
    {
      "type": "runtime",
      "description": "ESM TypeScript compiled via tsc"
    },
    {
      "type": "versioning",
      "description": "Lockstep versioning across 4 packages"
    },
    {
      "type": "compatibility",
      "description": "pi extension API compatibility"
    }
  ],
  "scope_boundaries": {
    "in": [
      "Monorepo: @davidorex/pi-project, @davidorex/pi-workflows, @davidorex/pi-behavior-monitors, @davidorex/pi-project-workflows"
    ],
    "out": [
      "No in-process agent execution — all agent steps are subprocess-isolated",
      "No pi-subagents dependency — extension owns its own dispatch",
      "No runtime TypeScript — domain logic lives in YAML specs, JSON schemas, Nunjucks templates, and monitor JSON specs"
    ]
  },
  "status": "development"
}