{
	"$schema": "http://json-schema.org/draft-07/schema#",
	"title": "Decisions",
	"description": "Choices made and why. Architectural, technical, and process choices with rationale and status (decided, tentative, revisit). The record of deliberate selection between alternatives. Decisions say what was chosen.",
	"type": "object",
	"required": ["decisions"],
	"properties": {
		"decisions": {
			"type": "array",
			"items": {
				"type": "object",
				"required": ["id", "decision", "rationale", "phase", "status"],
				"properties": {
					"id": { "type": "string" },
					"decision": { "type": "string" },
					"rationale": { "type": "string" },
					"phase": { "type": "integer" },
					"status": { "type": "string", "enum": ["decided", "tentative", "revisit"] }
				}
			}
		}
	}
}
