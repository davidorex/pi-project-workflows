{
  "tasks": [
    {
      "id": "TASK-021",
      "description": "FGAP-026 closure phase 1 (Step 8.7.1; Claude Code Task #21): Resolver foundation + 2 discovery tools (read-config, read-schema) + atomic safety .pi-context.json write",
      "acceptance_criteria": [
        "resolveContextDir(cwd) function in project-dir.ts replacing PROJECT_DIR/SCHEMAS_DIR constants",
        "bootstrap.schema.json with $id pi-context://schemas/bootstrap + version 1.0.0",
        "block-api + project-context cascade through resolver",
        "read-config + read-schema tools registered",
        ".pi-context.json written for this repo containing {contextDir: '.project'}"
      ],
      "status": "completed",
      "notes": "CLOSED at HEAD e88d7ee. FGAP-026 closure phase 1 substantively complete: resolver foundation (Phase 1.1 909a086) + bootstrap.schema.json + per-package atomic resolver cascade (Phase 1.2 sections A-F + Section E husky re-roll 6fb434f) + read-config/read-schema tools (Phase 1.3.A bac1893 + orchestrator scripts 4a24866 / c11ee92) + foundational FGAP-035 fix (f17efa3 + Q4 follow-up e88d7ee). Substrate canon DEC-0014/0015/0017/0018/0019/0020/0021 enacted. Test counts post-cascade: pi-context 401/0/0; pi-jit-agents 150/0/1; pi-workflows 824/0/2; pi-behavior-monitors 157/0/0. Adversarial probe verdict GREEN. See VER-004 for criteria_results. Phase 2 (Task #22 high-impact query tools) unblocked.",
      "verification": "VER-004"
    },
    {
      "id": "TASK-022",
      "description": "FGAP-026 closure phase 2 (Step 8.7.2; Claude Code Task #22): High-impact query tools — filter-block-items, resolve-items-by-id (bulk), walk-ancestors (FGAP-029 partial), walk-by-relation, find-references",
      "acceptance_criteria": [
        "filter-block-items collapses N-call serial probes to 1 call",
        "resolve-items-by-id batch-resolves",
        "walkAncestors JS export + walk-ancestors tool symmetric to walkDescendants",
        "find-references cross-block reverse-index"
      ],
      "status": "completed",
      "verification": "VER-009",
      "notes": "CLOSED at HEAD 680cb1c. Phase 2 (FGAP-026 closure phase 2) complete via 4 atomic sub-phases (2.1 filter-block-items / 2.2 resolve-items-by-id bulk / 2.3 walk-ancestors / 2.4 find-references). walk-by-relation dropped from scope (pre-existing at HEAD bcf2e60 as walkDescendants + project-walk-descendants tool). Test counts: pi-context 401 → 428 (+27 cumulative). All 4 sub-phases shipped DEC-0019 dual-surface (library + pi tool + orchestrator script). See VER-009 + child VER-005/006/007/008."
    },
    {
      "id": "TASK-023",
      "description": "FGAP-026 closure phase 3 (Step 8.7.3; Claude Code Task #23): Work-unit context composition substrate per DEC-0017 — context contract substrate (FGAP-030) + gather-execution-context primitive (FGAP-031)",
      "acceptance_criteria": [
        "context contract substrate in place (block kind OR composition-lens encoding; closure picks)",
        "gather-execution-context(unitId, kind) JS export + tool wrapper",
        "bidirectional traversal per declared relation_types in contract",
        "ContextBundle structure returned as one tool call",
        "FGAP-030 + FGAP-031 closed; FGAP-029 fully closed"
      ],
      "status": "planned"
    },
    {
      "id": "TASK-024",
      "description": "FGAP-026 closure phase 4 (Step 8.7.4; Claude Code Task #24): Workflow-execute + dispatch tools — main LLM's path to bash/read/write under DEC-0014 via JIT-agent dispatch through workflow steps",
      "acceptance_criteria": [
        "workflow-execute(name, params) tool registered",
        "workflow-list + workflow-status tools registered",
        "subcommand-only /workflow surfaces per audit get tool wrappers",
        "main LLM can dispatch a workflow that runs npm run build via JIT-agent"
      ],
      "status": "planned"
    },
    {
      "id": "TASK-025",
      "description": "FGAP-026 closure phase 5 (Step 8.7.5; Claude Code Task #25): Arc-tracking blocks authored as dogfood substrate per FGAP-028 — config + relations + roadmap + phase + (extend tasks.json) + context-contracts for THIS REPO",
      "acceptance_criteria": [
        "config.json declares relation_types[] including DEC-0017 work-unit-relation kinds + lenses",
        "relations.json with phase_depends_on + per-task constraint edges per DEC-0017",
        "roadmap.json with ROADMAP-001 referencing PHASE-006..010",
        "phase.json with PHASE-006..010 entries",
        "tasks.json extended with phase references via update-block-item (TASK-021..030 entries already authored pre-Phase-5; Phase 5 adds phase linkage via relations.json edges per DEC-0013/0017)",
        "context-contract entries per work-unit-kind",
        "gather-execution-context returns valid bundle for representative arc-tracking task"
      ],
      "status": "planned"
    },
    {
      "id": "TASK-026",
      "description": "FGAP-026 closure phase 6 (Step 8.7.6; Claude Code Task #26): Surface rename + context-* tools + context-init/install/migrate",
      "acceptance_criteria": [
        "subcommand /project → /context",
        "pi tool names project-* → context-* (15+ tools)",
        "context-init prompt-required for dirName per DEC-0015",
        "context-install wraps installProject",
        "context-migrate(newName) atomic dir-rename + pointer-update + config.root-update; body authored not stub"
      ],
      "status": "planned"
    },
    {
      "id": "TASK-027",
      "description": "FGAP-026 closure phase 7 (Step 8.7.7; Claude Code Task #27): Cross-package cascade + FGAP-032 item-level contextBlocks selectivity in pi-jit-agents",
      "acceptance_criteria": [
        "pi-jit-agents agent-spec/template/compile cascade through resolver",
        "AgentSpec.contextBlocks gather_for_unit shape; bundle injection as _<name>_bundle template variable",
        "pi-workflows path-construction sites cascade",
        "pi-behavior-monitors block resolution cascade",
        "zero .project/ literals in any production source per grep"
      ],
      "status": "planned"
    },
    {
      "id": "TASK-028",
      "description": "FGAP-026 closure phase 8 (Step 8.7.8; Claude Code Task #28): Test-fixture cascade — all setup helpers write .pi-context.json per-tmpdir",
      "acceptance_criteria": [
        "setupWorkflowDir / setupSchema / makeTmpDir helpers write .pi-context.json",
        "tests cascade through resolver; counts unchanged 390/151/826/157, 0-fail"
      ],
      "status": "planned"
    },
    {
      "id": "TASK-029",
      "description": "FGAP-026 closure phase 9 (Step 8.7.9; Claude Code Task #29): Documentation refresh + skills regen + 4-canon binding statements (DEC-0014/0015/0016/0017)",
      "acceptance_criteria": [
        "CLAUDE.md / READMEs / skill-narratives reflect post-rebuild surface",
        "DEC-0015 9th consequence binding (dual-nature note) in code comments + docs",
        "DEC-0017 surface documented (gather-execution-context + context-contract + per-unit-kind bundle semantics)",
        "DEC-0016 binding statement in CLAUDE.md",
        "npm run skills regenerates SKILL.md outputs"
      ],
      "status": "planned"
    },
    {
      "id": "TASK-030",
      "description": "FGAP-026 closure phase 10 LAST (Step 8.7.10; Claude Code Task #30): Migrate this repo's .project/ → user-chosen dir + final FGAP-028 dogfood-dispatch dry-run",
      "acceptance_criteria": [
        "context-migrate run with prompt for user's chosen dir",
        ".project/ → <user-chosen>/ rename atomic with .pi-context.json + config.root update",
        "arc-tracking blocks move with substrate",
        "all substrate operations work post-migration via gather-execution-context check",
        "final dogfood-dispatch dry-run passes (FGAP-028 gate); failures back-loop into arc as gap closures",
        "FGAP-026 transitions to closed with closed_by = closing commit composition",
        "Task #11 (Step 9 adversarial audit) unblocked; subsequently Task #12 (Step 10 release)"
      ],
      "status": "planned"
    },
    {
      "id": "TASK-031",
      "description": "Close FGAP-035 — eliminate writeBootstrapPointer signature default and initProject .project literal. Make contextDir required at the bootstrap-write surface so DEC-0015 enacts at line 1, not as a Phase 6 promise.",
      "status": "completed",
      "phase": "8.7.1",
      "files": [
        "packages/pi-context/src/project-dir.ts",
        "packages/pi-context/src/index.ts"
      ],
      "acceptance_criteria": [
        "writeBootstrapPointer signature has no parameter default — contextDir: string required",
        "initProject signature changes to initProject(cwd: string, contextDir: string); body replaces .project literal with contextDir parameter",
        "project-dir.ts JSDoc rewritten to remove default-as-feature framing; no \"Default contextDir is .project\" / \"legacy substrate-dir name\" / \"future /context init ceremony will prompt\" wording remains",
        "index.ts initProject body comment block (lines 232-237) rewritten to remove same default-as-feature framing",
        "handleInit subcommand handler signature changes to handleInit(args, ctx): parses positional contextDir via args.trim().split(/\\s+/)[0]; missing arg emits ctx.ui.notify error \"/project init requires a substrate dir name (e.g. '/project init .project' or '/project init .context'). Per DEC-0015, no default.\" and returns; non-empty arg passed to initProject(ctx.cwd, contextDir)",
        "PROJECT_SUBCOMMANDS init dispatch entry changes from handler: (_args, ctx) => handleInit(ctx) to handler: (args, ctx) => handleInit(args, ctx)",
        "project-init pi tool parameters schema gains required contextDir field: Type.Object({ contextDir: Type.String({ description: \"Substrate dir name (e.g. .project). Required per DEC-0015 — no default.\" }) })",
        "project-init pi tool execute body destructures params.contextDir and passes to initProject(ctx.cwd, params.contextDir)",
        "All 168 existing test sites continue to pass without modification (they already pass .project explicitly)",
        "Full test suite green across all 4 packages",
        "Production-source grep returns zero hardcoded substrate-dir literals: grep -rn \"\".project\"\" packages/pi-context/src/ --include=\"*.ts\" | grep -v .test.ts | grep -v project-dir.ts (excluding @deprecated PROJECT_DIR/SCHEMAS_DIR const exports per Phase 7 separation of concerns)",
        "grep -n \"= PROJECT_DIR\" packages/pi-context/src/ returns 0 matches"
      ],
      "depends_on": [
        "TASK-021"
      ],
      "notes": "CLOSED at HEAD e88d7ee. FGAP-035 implementation: 11 cascade rows applied at f17efa3 + Q4 follow-up site at e88d7ee. All 4 packages green (pi-context 401/0/0; pi-jit-agents 150/0/1; pi-workflows 824/0/2; pi-behavior-monitors 157/0/0). Adversarial probe verdict GREEN (8 PASS, 1 PASS-post-fix Q4, 1 PASS-substantively Q10). UX decision (positional arg required; ctx.ui.notify error on missing) preserved verbatim in handleInit. project-init pi tool gains required contextDir Type.Object param. See VER-001 for full criteria_results.",
      "verification": "VER-001"
    },
    {
      "id": "TASK-032",
      "description": "Close FGAP-036 — add scripts/orchestrator/compile-task-context.ts. Canonicalizes implementer-task-block composition from substrate inputs (TASK-NNN + explore-report-path + commit-subject-template). Enables FGAP-035 dispatch via canonical orchestrator-side context-compilation surface rather than hand-authored /tmp/task-template-*.md.",
      "status": "completed",
      "phase": "8.7.1",
      "files": [
        "scripts/orchestrator/compile-task-context.ts"
      ],
      "acceptance_criteria": [
        "scripts/orchestrator/compile-task-context.ts exists and is shebang-tsx executable",
        "Args: --task-id TASK-NNN, --explore-report <path>, --commit-subject <template>, optional --required-reading-extra <comma-separated paths>",
        "Reads task entry from .project/tasks.json via @davidorex/pi-context/block-api readBlock; fails fast if task id absent",
        "Extracts cascade-target table from explore-report via inline extract-markdown-section.ts invocation (or shared helper)",
        "Emits XML-structured task-block to stdout in the shape compile-implementation-context.ts expects via --task-template @file",
        "Composes: read-list (from task.files + required-reading-extra), apply-order (numbered per cascade-table rows), concrete-edits (per-row from explore-report Required-change column), commit-spec (subject from template + forensic body skeleton), STOP triggers boilerplate, report-back format tables-only",
        "FGAP-035 implementation context compiled via this script (validates the surface end-to-end)"
      ],
      "depends_on": [],
      "notes": "CLOSED at HEAD e88d7ee. compile-task-context.ts landed at 7883cea (creation) + 5293cca (console.log normalization) + e3a0247 (build-task-template.ts → compile-task-context.ts rename mirroring pi-jit-agents compileAgent vocabulary). Acceptance criterion 7 (FGAP-035 implementation context compiled via this script) validated end-to-end through the FGAP-035 dispatch chain. See VER-002.",
      "verification": "VER-002"
    },
    {
      "id": "TASK-033",
      "description": "Close FGAP-038 — add scripts/orchestrator/inject-context-items.ts. Item-level substrate-item projection into subagent briefs. Selector + projection + format args. Doubles as test surface for block-schema shape discovery: every friction point encountered while writing the script gets filed as a follow-up FGAP, NOT silently worked-around.",
      "status": "completed",
      "phase": "8.7.1",
      "files": [
        "scripts/orchestrator/inject-context-items.ts"
      ],
      "acceptance_criteria": [
        "scripts/orchestrator/inject-context-items.ts exists, shebang-tsx executable",
        "Args: --items <block:itemId[,block:itemId...]> + --format markdown|json|xml + optional --fields <comma-list> projection",
        "Reads each (block, itemId) via @davidorex/pi-context/block-api readBlock + filter by id",
        "json format emits items as JSON array; xml format wraps each item in <item block=\"X\" id=\"Y\">...</item>; markdown format uses per-block-kind render macros (FGAP-037 dependency — until macros land, markdown emits a minimal fallback projection)",
        "Fails fast if any block:itemId selector is unresolvable; reports the unresolvable selector verbatim",
        "compile-explore-context.ts + compile-implementation-context.ts gain a --context-items arg that splices this script output into the context at a <context_items> XML block position",
        "FGAP-035 Explore re-dispatch uses --context-items framework-gaps:FGAP-035,tasks:TASK-031,decisions:DEC-0015 validating the surface end-to-end",
        "Every friction point encountered while writing the script that surfaces a schema-shape question gets filed as a follow-up FGAP — substrate-canon discipline, not silently worked-around"
      ],
      "depends_on": [
        "TASK-032"
      ],
      "notes": "CLOSED at HEAD e88d7ee. inject-context-items.ts landed at 2c3794a; --context-items arg integrated into compile-{explore,implementation}-context.ts at ad02726. Acceptance criterion 7 (FGAP-035 Explore re-dispatch via canonical surface with item-level injection) validated: rendered context embeds <context_items> verbatim; Explore agent produced 11-row cascade table without OR-hedging because TASK-031 UX decision was injected. Friction points (array_key heuristic / id-field assumption / singleton-block shape / colon-escape) filed inline in JSDoc + commit body per dogfood discipline. See VER-003.",
      "verification": "VER-003"
    },
    {
      "id": "TASK-034",
      "description": "Phase 2 sub-phase 2.1: ship filter-block-items — library predicate-filter primitive + pi tool + scripts/orchestrator/filter-block-items.ts. Wraps existing readBlock + caller-side filter into a standalone tool. No closure-table dependency.",
      "status": "completed",
      "phase": "8.7.2",
      "files": [
        "packages/pi-context/src/project-sdk.ts",
        "packages/pi-context/src/index.ts",
        "scripts/orchestrator/filter-block-items.ts"
      ],
      "acceptance_criteria": [
        "filterBlockItems(cwd, blockName, predicate) library function exported from project-sdk.ts; signature: (cwd: string, blockName: string, predicate: {field: string, op: 'eq'|'neq'|'in'|'matches', value: unknown}) => unknown[]",
        "filter-block-items pi tool registered in packages/pi-context/src/index.ts with Type.Object schema {block, field, op, value}",
        "scripts/orchestrator/filter-block-items.ts shebang-tsx executable; args --block --field --op --value [--format json|table]",
        "Tests cover: filter by id / by enum field / by partial-string match / empty-result / missing-field; mkdtempSync + writeBootstrapPointer per test",
        "Husky pre-commit gate (npm run check && npm test) green at commit time"
      ],
      "depends_on": [
        "TASK-022"
      ],
      "notes": "CLOSED at HEAD 15cd881. filterBlockItems library + filter-block-items pi tool + scripts/orchestrator/filter-block-items.ts shipped. Tests: pi-context 401 → 407 (+6). Live-substrate runtime demos green. See VER-005.",
      "verification": "VER-005"
    },
    {
      "id": "TASK-035",
      "description": "Phase 2 sub-phase 2.2: ship resolve-items-by-id (bulk variant) — library bulk-resolve wrapping buildIdIndex + pi tool + scripts/orchestrator/resolve-items-by-id.ts. Singular resolve-item-by-id tool already exists. No closure-table dependency.",
      "status": "completed",
      "phase": "8.7.2",
      "files": [
        "packages/pi-context/src/project-sdk.ts",
        "packages/pi-context/src/index.ts",
        "scripts/orchestrator/resolve-items-by-id.ts"
      ],
      "acceptance_criteria": [
        "resolveItemsByIds(cwd, ids) library function exported from project-sdk.ts; signature: (cwd: string, ids: string[]) => Map<string, ItemLocation | null>; wraps existing buildIdIndex (project-sdk.ts:616)",
        "resolve-items-by-id pi tool registered in packages/pi-context/src/index.ts with Type.Object({ids: Type.Array(Type.String())}); coexists with existing singular resolve-item-by-id tool",
        "scripts/orchestrator/resolve-items-by-id.ts shebang-tsx executable; args --ids <csv> [--format json|table]",
        "Tests cover: all-found / partial-found / none-found / cross-block resolution / prefix-mismatch fail mode",
        "Husky pre-commit gate green at commit time"
      ],
      "depends_on": [
        "TASK-022"
      ],
      "notes": "CLOSED at HEAD 2ddf85f. resolveItemsByIds library + resolve-items-by-id pi tool (coexists with singular resolve-item-by-id) + scripts/orchestrator/resolve-items-by-id.ts shipped. Tests: pi-context 407 → 413 (+6). Live-substrate runtime demo green. See VER-006.",
      "verification": "VER-006"
    },
    {
      "id": "TASK-036",
      "description": "Phase 2 sub-phase 2.3: ship walk-ancestors — library parent-direction closure-table traversal + pi tool + scripts/orchestrator/walk-ancestors.ts. Mirrors walkDescendants (project-context.ts:325) inverted. FGAP-029 partial closure.",
      "status": "completed",
      "phase": "8.7.2",
      "files": [
        "packages/pi-context/src/project-context.ts",
        "packages/pi-context/src/lens-view.ts",
        "packages/pi-context/src/index.ts",
        "scripts/orchestrator/walk-ancestors.ts"
      ],
      "acceptance_criteria": [
        "walkAncestors(itemId, relationType, edges) library function exported from project-context.ts; signature: (itemId: string, relationType: string, edges: Edge[]) => string[]; visited-set cycle protection; iterates edges.filter(e => e.child === id) recursing on e.parent",
        "walkAncestorsByLens(cwd, itemId, relationType) wrapper exported from lens-view.ts mirroring walkLensDescendants pattern",
        "walk-ancestors pi tool registered in packages/pi-context/src/index.ts with Type.Object({itemId, relationType})",
        "scripts/orchestrator/walk-ancestors.ts shebang-tsx executable; args --item-id --relation-type [--format json|chain]",
        "Tests cover: linear DAG ancestor chain / branching DAG / cycle protection / no-ancestors / relation_type filter; per-test fixtures create mkdtempSync + writeBootstrapPointer + .project/relations.json synthetic edges",
        "Husky pre-commit gate green at commit time"
      ],
      "depends_on": [
        "TASK-035"
      ],
      "notes": "CLOSED at HEAD 7cb17f0. walkAncestors + walkAncestorsByLens + walk-ancestors pi tool + scripts/orchestrator/walk-ancestors.ts shipped. tsconfig.json gained @davidorex/pi-context/lens-view path mapping. Tests: pi-context 413 → 420 (+7). Closure-table runtime demo deferred to Phase 5 (per-test fixtures own synthetic relations.json). See VER-007.",
      "verification": "VER-007"
    },
    {
      "id": "TASK-037",
      "description": "Phase 2 sub-phase 2.4: ship find-references — library reverse-edge lookup + pi tool + scripts/orchestrator/find-references.ts. Specialization of walkDescendants/walkAncestors patterns returning Edge[] not string[].",
      "status": "completed",
      "phase": "8.7.2",
      "files": [
        "packages/pi-context/src/project-context.ts",
        "packages/pi-context/src/lens-view.ts",
        "packages/pi-context/src/index.ts",
        "scripts/orchestrator/find-references.ts"
      ],
      "acceptance_criteria": [
        "findReferences(itemId, edges, direction?) library function exported from project-context.ts; returns Edge[] where edge.child === itemId (inbound) OR edge.parent === itemId (outbound); direction param defaults to 'both'",
        "findReferencesInRepo(cwd, itemId, direction?) wrapper exported from lens-view.ts using existing loadRelations path",
        "find-references pi tool registered in packages/pi-context/src/index.ts with Type.Object({itemId, direction: Type.Optional(Type.Union([...]))})",
        "scripts/orchestrator/find-references.ts shebang-tsx executable; args --item-id [--direction inbound|outbound|both] [--format json|table]",
        "Tests cover: inbound only / outbound only / both / no-references / multiple relation_types between same pair; per-test fixtures",
        "Husky pre-commit gate green at commit time"
      ],
      "depends_on": [
        "TASK-036"
      ],
      "notes": "CLOSED at HEAD 680cb1c. Final atomic unit under TASK-022. findReferences + findReferencesInRepo + find-references pi tool + scripts/orchestrator/find-references.ts shipped. Tests: pi-context 420 → 428 (+8). Edge[] return type intentionally distinct from walkDescendants/walkAncestors string[] semantic. See VER-008.",
      "verification": "VER-008"
    },
    {
      "id": "TASK-038",
      "description": "Phase 3 sub-phase 3.1: ship context-contracts block kind — schema + registry default (3 starter contracts: task/decision/verification) + block-kind registration. Closes FGAP-030 substrate side. No closure-table dependency.",
      "status": "completed",
      "phase": "8.7.3",
      "files": [
        "packages/pi-context/registry/schemas/context-contracts.schema.json",
        "packages/pi-context/registry/blocks/context-contracts.json",
        "packages/pi-context/src/index.ts"
      ],
      "acceptance_criteria": [
        "packages/pi-context/registry/schemas/context-contracts.schema.json exists with $id pi-context://schemas/context-contracts + version 1.0.0",
        "Schema declares per-item: id (pattern ^CTX-\\d{3,}$), unit_kind (string), bundle_relation_types[] (array of {relation_type, direction in/out/both, max_depth, applicability_predicate?}), created_by, created_at; optional modified_by/at/description/notes",
        "packages/pi-context/registry/blocks/context-contracts.json seeded with 3 default contracts (CTX-001 task / CTX-002 decision / CTX-003 verification; each with empty bundle_relation_types[])",
        "Block kind registered in packages/pi-context/src/index.ts following existing pattern",
        "Schema-validation tests cover: AJV round-trip / required-field rejection / id pattern rejection / bundle_relation_types[] shape rejection",
        "Husky pre-commit gate green"
      ],
      "depends_on": [
        "TASK-023"
      ],
      "notes": "CLOSED at HEAD 6ddb1ba. context-contracts schema + registry default + auto-discovery registration shipped. Tests: pi-context 428 → 447 (+19). See VER-010.",
      "verification": "VER-010"
    },
    {
      "id": "TASK-039",
      "description": "Phase 3 sub-phase 3.2: ship gatherExecutionContext primitive — library + pi tool + orchestrator script + 7 tests. Closes FGAP-031. Closure-table dependency: per-test fixtures own synthetic relations.json.",
      "status": "completed",
      "phase": "8.7.3",
      "files": [
        "packages/pi-context/src/execution-context.ts",
        "packages/pi-context/src/index.ts",
        "scripts/orchestrator/gather-execution-context.ts",
        "tsconfig.json"
      ],
      "acceptance_criteria": [
        "gatherExecutionContext(cwd, args) library function exported from new packages/pi-context/src/execution-context.ts; returns ContextBundle | {error: string}",
        "ContextBundle / BundleRelationTypeSpec / GatherExecutionContextArgs types exported",
        "Library body: reads unit via resolveItemById; reads context-contract via filterBlockItems(cwd, 'context-contracts', {field: 'unit_kind', op: 'eq', value: kind}); iterates bundle_relation_types[] walking edges per direction (walkAncestors / walkDescendants / findReferences) within max_depth; resolves reached ids via resolveItemsByIds; returns ContextBundle with unit + perRelationType + traversal_depth + scoped_at",
        "gather-execution-context pi tool registered in index.ts with Type.Object({unitId, kind, maxDepth?})",
        "scripts/orchestrator/gather-execution-context.ts shebang-tsx executable; --unit-id --kind [--max-depth N] [--format json|summary]",
        "tsconfig.json gains @davidorex/pi-context/execution-context path mapping",
        "7 tests per per-test fixture pattern: happy path / multi-relation / direction semantic / max_depth bound / missing unit / missing contract / empty bundle_relation_types",
        "Husky pre-commit gate green"
      ],
      "depends_on": [
        "TASK-038"
      ],
      "notes": "CLOSED at HEAD 6bea7e1. gatherExecutionContext library + ContextBundle types + gather-execution-context pi tool + scripts/orchestrator/gather-execution-context.ts + tsconfig path mapping shipped. Tests: pi-context 447 → 454 (+7). FGAP-031 closure pending sub-phase 3.3 (will transition FGAP-031 status to closed in VER-013 cascade). See VER-011.",
      "verification": "VER-011"
    },
    {
      "id": "TASK-040",
      "description": "Phase 3 sub-phase 3.3: integration test composing 3.1 + 3.2 end-to-end + FGAP cluster closure (FGAP-029 fully / FGAP-030 / FGAP-031). Closure-table dependency: integration fixture authors full synthetic substrate (config.json + relations.json + context-contracts.json + per-block items).",
      "status": "planned",
      "phase": "8.7.3",
      "files": [
        "packages/pi-context/src/execution-context.integration.test.ts",
        ".project/framework-gaps.json"
      ],
      "acceptance_criteria": [
        "Integration test composes full synthetic substrate per mkdtempSync + writeBootstrapPointer + .project/config.json + .project/relations.json + .project/context-contracts.json + per-block items (tasks/decisions/requirements)",
        "Test exercises gather-execution-context against a realistic task unit end-to-end",
        "Asserts ContextBundle shape matches interface + perRelationType buckets contain expected items + traversal_depth honored",
        "Verifies substrate-canon: unit via filterBlockItems; relations via loadRelations; ids via resolveItemsByIds; bundle per context-contract",
        "FGAP-029 transitioned to status: closed in .project/framework-gaps.json",
        "FGAP-030 transitioned to status: closed",
        "FGAP-031 transitioned to status: closed",
        "Husky pre-commit gate green"
      ],
      "depends_on": [
        "TASK-039"
      ],
      "notes": "Phase 3 sub-phase 3.3. Final atomic unit under TASK-023. Completes FGAP-029 full closure + FGAP-030 + FGAP-031. After landing: TASK-023 (parent) ready to close; Phase 4 (TASK-024 / workflow-execute + dispatch tools) unblocked."
    }
  ]
}