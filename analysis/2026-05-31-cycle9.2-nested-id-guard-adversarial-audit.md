# Cycle 9.2 — nested id-bearing array guard: adversarial audit

Date: 2026-05-31
Auditor: fresh-context adversarial agent (not the implementer)
Repo: `/Users/david/Projects/workflowsPiExtension`  ·  branch `context-jit-spec-v2`  ·  base `cabb8c3` (uncommitted Cycle-9.2 tree)
Mode: READ-ONLY (Read / Grep / `npx tsx` / `npx tsc --noEmit` / `git diff`). No edits, no npm, no commit.
Default-to-FLAG-when-uncertain applied.

Verdict: **CLEAN across all 7 probes.** 0 FLAGs (high/med/low). Three low-severity *predicate-boundary observations* documented (theoretical bypass avenues; none live in shipped or active schemas).

---

## Probe 1 — Predicate exactness + $ref handling — CLEAN

`findNestedIdBearingArrays` at `packages/pi-context/src/schema-write.ts:61`. Reproduced via tsx against the live source:

| case | result | expect | verdict |
|---|---|---|---|
| depth-0 top-level id array | `[]` | `[]` | OK |
| depth-1 nested id array | `["plans.layers"]` | `["plans.layers"]` | OK (correct dotted path) |
| depth-1 nested NON-id array | `[]` | `[]` | OK |
| depth-2 id array | `["a.b.c"]` | `["a.b.c"]` | OK |
| depth-3 id array | `["a.b.c.d"]` | `["a.b.c.d"]` | OK |
| object wrapper (no array) around id array | `[]` | `[]` | OK (object recursion holds depth at 0) |
| real `.context-jit-spec-v2/schemas/layer-plans` | `["plans.layers","plans.migration_phases"]` | same | OK |
| real `samples/schemas/layer-plans` | `["plans.layers","plans.migration_phases"]` | same | OK |

**$ref handling (load-bearing) — CLEAN, no false-negative hole:**

| case | result | verdict |
|---|---|---|
| nested items `$ref` → `#/definitions/Layer` (has `id`) | `["plans.layers"]` | resolved + detected |
| nested items `$ref` → `#/$defs/Layer` (has `id`) | `["plans.layers"]` | resolved + detected |
| enclosing-array items `$ref` → `#/$defs/Plan` (nested id-array inside) | `["plans.layers"]` | resolved + detected |
| unresolvable external `$ref` (`https://…`) | `[]` (no throw) | degrades cleanly |
| unresolvable local `$ref` (missing def) | `[]` (no throw) | degrades cleanly |

`deref` (schema-write.ts:79-87) resolves one-level `#/definitions/*` and `#/$defs/*` exactly as `resolveBlockItemSchema` does; unsupported/external refs return the node as-is, the walk finds no `properties`, and the pass never throws (it is a lint, not a load gate). **(a) The $ref-indirected nested-id case IS caught; no $ref-blind false-negative hole.**

Low-severity boundary observations (match the LOCKED predicate spec — `items.properties.id` presence + one-level local `$ref` — not regressions):
- `id` declared only in item `required` (not `properties`) → NOT detected (`[]`).
- nested item schema behind `anyOf`/`oneOf`/`allOf` composition → NOT walked (`[]`).
- tuple-form `items` (array of schemas) → NOT walked (`[]`).
These are theoretical hand-crafted bypasses. Verified NOT live: probe 6 + the work-orders composition check below.

## Probe 2 — Throw on every authoring surface + file never lands — CLEAN

Placement confirmed by diff: `assertNoNestedIdBearingArray` in `writeSchema` (schema-write.ts:190) AFTER `validateSchemaAgainstMeta` (:185) and BEFORE `fs.mkdirSync`/write (:193+); and in the `writeSchemaChecked` dry-run branch (:289) alongside its inline meta-validate.

tsx reproductions (real source, scratch substrate):

| surface | result | file landed? |
|---|---|---|
| `writeSchema` nested-id | THREW | NO (`badsc.schema.json` absent) |
| `writeSchema` top-level-id (good) | no-throw | YES (lands) |
| `writeSchemaChecked` create nested-id | THREW | NO |
| `writeSchemaChecked` `{dryRun:true}` nested-id | THREW (label `(dry-run)`) | NO |
| `writeSchemaChecked` replace nested-id (over existing good) | THREW | prior good file UNCHANGED (`$id==="good"`) |

Surface inheritance (grep-confirmed, no bypass):
- Pi tool `write-schema` → `writeSchemaChecked` (index.ts:1849).
- CLI `scripts/orchestrator/write-schema.ts` → `writeSchemaChecked` for both dry-run (:162) and commit (:190).
- `updateSchema` → delegates the atomic write to `writeSchema` (schema-write.ts:333) → inherits the guard.
- `writeSchemaChecked` commit branch (:295) delegates to `writeSchema` → inherits.

No surface writes a schema without routing through `writeSchema`/`writeSchemaChecked`.

## Probe 3 — Report non-fatal + correct set in the real substrate; deviation correct — CLEAN

`validateContext(".")` reproduced: **total 58 / errors 53 / warnings 5 / status `invalid`**. The 2 added issues:
```
{block:"layer-plans", field:"plans.layers",          severity:"warning", code:"nested_id_bearing_array"}
{block:"layer-plans", field:"plans.migration_phases", severity:"warning", code:"nested_id_bearing_array"}
```
`edge_endpoint_unregistered` = 30 (unchanged), dangling = 0 (unchanged), errors stay 53. Severity `warning` confirmed; status stays `invalid` from the pre-existing 53 errors, not from the new warnings.

**Deviation scrutiny — CLEAN.** The pass resolves the substrate dir via `tryResolveContextDir` (context-sdk.ts diff) rather than throwing `schemasDir`/`resolveContextDir`:
- `tryResolveContextDir` (context-dir.ts:159-166) returns the IDENTICAL resolved dir as `resolveContextDir` when a pointer is present (it only catches `BootstrapNotFoundError` by name; re-throws all other errors). So the warning is NOT skipped in the normal pointer-present case — proven by the golden 58/53/5 above (warnings present).
- Pointer-less scratch dir reproduced: `validateContext(tmp)` → NO THROW, 0 issues, no `nested_id_bearing_array` warning. Degrades cleanly as the regression test requires.
- The deviation matches existing read-pass convention (`listSchemas`, `discoverSchemas`, `discoverArrayKeyMappings` all use `tryResolveContextDir`). The `ctxRoot === null → null` branch only fires when there is genuinely no pointer; it does not accidentally null-out a present pointer.

**(b) The warning is non-fatal (severity `warning`, errors untouched, status unchanged-by-it) AND present in the real pointer case (2 warnings on layer-plans).**

## Probe 4 — Install carve-out holds — CLEAN

`installContext` (index.ts:373; copy loop :411-431) copies via `fs.copyFileSync` — verbatim. Grep confirms `assertNoNestedIdBearingArray`/`findNestedIdBearingArrays` appear NOWHERE in index.ts: the guard is NOT wired into the install copy loop.

tsx repro (real `installContext`, config = shipped conception with `installed_schemas:["layer-plans"]`):
- install did NOT throw; `installed: ["schemas/layer-plans.schema.json"]`, `notFound: []`.
- `layer-plans.schema.json` present in installed substrate.
- post-install `validateContext` → 2 `nested_id_bearing_array` issues, ALL severity `warning`, ZERO as error.

## Probe 5 — Catalog pin bites — CLEAN

Pin at `samples-catalog.test.ts:172-182`: iterates `samplesCatalog().kinds`, loads each via `k.schema_path`, asserts `findNestedIdBearingArrays(schema).length>0` carriers `== ["layer-plans"]`. Loads via `block_kinds[].schema_path` → a NEW nested-id *block kind* is covered (not just hand-listed names).

tsx repro (synthetic second carrier added in-memory + a scratch schema file; real catalog UNTOUCHED): carriers became `["layer-plans","synth-carrier"]`; `assert.deepStrictEqual(result, ["layer-plans"])` FAILS. **(c) The catalog pin bites on a second carrier.**

## Probe 6 — No false positives on the shipped catalog — CLEAN

Ran `findNestedIdBearingArrays` over EVERY `samples/schemas/*.schema.json`. Single hit: `layer-plans.schema.json → ["plans.layers","plans.migration_phases"]`. No other shipped schema flagged.

Live-hole cross-check: `work-orders.schema.json` (the only shipped schema using `oneOf`) places its composition at `work_orders[].context_blocks[].items.oneOf[0|1]` (depth 2) but NEITHER branch carries `id` in `properties` — so the composition limitation from probe 1 is NOT a live false-negative anywhere shipped. No tuple-form `items` in any shipped/active schema.

## Probe 7 — Scope + parity + layering — CLEAN

- `git diff cabb8c3 --name-only`: source changes confined to `packages/pi-context/src/context-sdk.ts` + `schema-write.ts`; tests `context-sdk.test.ts`, `install-subcommand.test.ts`, `samples-catalog.test.ts`, `schema-write-tool.test.ts`, `schema-write.test.ts`; one untracked demo `scripts/orchestrator/runtime-demo-nested-id-guard.ts`. NO `.project`/`.context*` data or schema, NO `.pi/`, NO `docs/`, NO pi-jit-agents/pi-workflows changes.
- `npx tsc --noEmit` (root) exit 0. (pi-context tsconfig is `tsconfig.build.json`; root project references it; root noEmit covers it.)
- `npx biome check src/schema-write.ts src/context-sdk.ts` → 0 issues: no unused imports, no dead code, format pass touched only formatting.
- Import direction: `context-sdk` imports `findNestedIdBearingArrays` from `./schema-write.js`; `schema-write` has NO import of `context-sdk` → acyclic.
- Predicate has ONE definition (`schema-write.ts:61`). All consumers import it: `samples-catalog.test.ts`, `schema-write.test.ts`, `context-sdk.ts`, and the demo (via public `@davidorex/pi-context/schema-write`). No re-implementation (no local `walk`/predicate in tests or demo).

## Independent re-verification of orchestrator claims

| claim | re-verified | result |
|---|---|---|
| build/check/test green | biome + root tsc + pi-context tests | clean / 910 pass / 0 fail |
| pi-context 910/910 fail-0 | `npx tsx --test src/*.test.ts` | 910 pass, 0 fail |
| golden 58/53/5, 2 new warnings = layer-plans:plans.layers + plans.migration_phases | `validateContext(".")` | exact match; 30 unregistered + 0 dangling unchanged |
| predicate → `["plans.layers","plans.migration_phases"]` for layer-plans, `[]` for decisions | tsx | layer-plans matches; decisions (`options_considered` nested non-id array) → `[]` |
| new demo + prior demos exit 0 | ran all 4 | nested-id-guard / write-ordering / promote-item / resolve-ref all exit 0 |

(Decisions confirmed `[]` separately: its only nested array `options_considered` items carry no `id`.)

---

## Summary

All seven probes CLEAN; 0 FLAGs. The guard is exact (depth-≥1 + `items.properties.id`), resolves one-level local `$ref` indirection on both enclosing and nested items (no $ref-blind hole), fires on every write surface (lib/tool/CLI/dryRun/replace/update) before any disk write, reports the existing `layer-plans` carrier as a non-fatal warning in the real pointer-present substrate, leaves install verbatim, and the catalog pin (loaded via `block_kinds[].schema_path`) bites on a second carrier. The `tryResolveContextDir` deviation is correct: identical to `resolveContextDir` when a pointer is present (warning surfaces), null only on the absent-pointer branch (degrades, no throw).

Single most important finding: none requiring action. The only nuance worth recording is the predicate's documented boundary — it keys on `items.properties.id` + one-level local `$ref`, so `required`-only `id`, `anyOf`/`oneOf`/`allOf` composition, and tuple-form `items` are not walked. These are theoretical hand-crafted bypasses, NOT live in any shipped or active schema (work-orders' `oneOf` carries no nested `id`). They match the locked predicate spec; flagging them as scope-completeness notes for Phase H awareness, not as defects in this cycle.
