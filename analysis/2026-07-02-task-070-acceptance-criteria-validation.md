# TASK-070 acceptance-criteria strict validation — 2026-07-02

Validation of TASK-070's filed text (read back verbatim from the substrate via `pi-context read-block-item --block tasks --id TASK-070 --json`; the filed text matches the brief's quoted text byte-for-byte) against the CURRENT code state on branch `operations-constraint`. Validates the criteria only; nothing implemented, nothing written to the substrate.

## A. Reference accuracy (observed today)

| Anchor cited | Current fact | Verdict |
|---|---|---|
| `loadConfigForDir` at `context.ts:564-581` | Function spans **565-582** today (`565: export function loadConfigForDir(substrateDir: string): ConfigBlock \| null {` … `582: }`). At filing-era commit `9125385` (2026-06-17) it started at **564** (`git show 9125385:packages/pi-context/src/context.ts` → grep hit `564:export function loadConfigForDir…`), so the cite was exact at filing; post-filing commits `2c3df7e`/`e1cf07f` shifted it by one line. | STALE by 1 line; symbol-relocatable |
| `loadConfigForDir` behavior premise ("no migration branch") | Confirmed: body is read → `JSON.parse` → `validateFromFile(bundledSchemaPath("config"), data, …)` at 580 → return. No `schema_version` read, no registry consult. | TRUE |
| `validateBlockWithMigrationForDir` at `schema-validator.ts:205-242` | **Exact today**: `205: export function validateBlockWithMigrationForDir(` … `242: }`. Implements the claimed pattern: reads `schema.version` (225), reads `data.schema_version` (226-229), on mismatch runs `runMigrations(registry, schemaName, blockVersion, schemaVersion, data)` (238), then `validate` (241); throws at 233-237 when versions differ and no registry supplied. | EXACT |
| `TransformOp` union at `migrations-store.ts:64-68` | **Exact today**: `64: export type TransformOp =` with members `rename`(65) / `set`(66) / `delete`(67) / `coerce`(68). No `map_each`. | EXACT |
| `applyOp` in `migration-registry-loader.ts` | Exists at **132-182**; switch covers exactly `rename`/`set`/`delete`/`coerce`. `walkPath` (80-124) rejects `[`/`]` segments (97-101) and requires `$`-prefix (85-87). | EXACT |
| `migrations.schema.json` `definitions/TransformOp` `oneOf` | Exists: `71: "TransformOp": { 72: "oneOf": [` with four variants (`rename`:78, `set`:91, `delete`:103, `coerce`:112). No `map_each`. | EXACT |
| `map_each` pre-existence | `grep -rn map_each` over `packages/pi-context/{src,schemas,samples}` returned **nothing** — the primitive is genuinely absent, as the task presumes. | TRUE |

## B. Mechanism-assumption truth (observed)

- **(i) Schema-name-keyed registry keyed by `config`**: TRUE. `schema-migrations.ts` `resolve(schemaName, fromVersion, toVersion)` (96) walks a `Map` keyed by `schemaName`; any string including `"config"` is a legal key. `buildRegistryFromSubstrateForDir` (migration-registry-loader.ts:231-245) builds it from `loadMigrationsFileForDir` (migrations.json); `getProjectMigrationRegistryForDir` (313-320) is the cached dir-keyed accessor the design doc wires in.
- **(ii) `write-schema-migration` op**: EXISTS at `ops-registry.ts:1404` (create/replace/remove; kinds identity/declarative-transform), writes to the substrate's migrations.json via `writeSchemaMigrationExecute` (`write-schema-migration-tool.ts`). It is **`authGated: true`** (ops-registry.ts:1440) — criterion 2's filing step requires interactive operator confirmation at the auth gate; implementable, but not agent-autonomous.
- **(iii) Version gap is factually current**: bundled `packages/pi-context/schemas/config.schema.json:4` = `"version": "1.7.0"`; live on-disk config `schema_version` read via `pi-context read-config --registry schema_version --json` → `{"ok":true,…"data":"1.0.0"…}`. **The "real on-disk gap (1.0.0 → current)" in criterion 3 is 1.0.0 → 1.7.0 and is real today.** Registry is currently **empty for `config`**: `pi-context read-block --block migrations --json` returned 17 declarations, none with `schemaName:"config"` (criterion 2's premise holds). `samples/migrations.json` likewise has no `config` entry; `samples/conception.json:2` config `schema_version` is `"1.0.0"`.
- **(iv) Exit-5 brick mechanics**: the brick is **shape-conditional, not version-conditional**. Runtime reproduction on a scratch copy (samples conception copied to scratchpad, never the live substrate), `npx tsx -e` importing `loadConfigForDir` from `src/context.ts`:
  - `A: lagging-version load ok, schema_version = 1.0.0` — a schema_version-1.0.0 config **loads valid TODAY** against the 1.7.0 bundled schema with no migration machinery (also independently confirmed: the live `.context` config at 1.0.0 loaded fine through `read-config`, which calls `loadConfig → loadConfigForDir → validateFromFile`).
  - `B: threw, name = ValidationError` — the same config with one unknown top-level key throws AJV `ValidationError` (config schema is `additionalProperties: false`, schema:9), which `pi-context-cli/src/cli.ts:1216` maps to **exit 5** (`if (isValidationError(err)) code = 5;`).
  - Historical grounding: FGAP-095's `impact` records the reproduced brick ("the TASK-020 state_derivation.blocked_by shape change bricked the live .context until the change was reverted").
  - Corollary the criteria do not name: `runMigrations`/`resolve` throw a **plain Error** on an unresolvable chain (`schema-migrations.ts:100-104, 120-125`) → CLI **exit 1**, not 5. "Zero exit-5 brick" does not guard the new brick mode the change itself introduces (below).

## C. Internal consistency

- No criterion contradicts another or the description. Criterion 1's mismatch-branch + criterion 5's match-no-consult are the two halves of one behavior; criterion 4's fixture-verified `map_each` is deliberately decoupled from the real-gap chain of criteria 2/3 (the real 1.0.0→1.7.0 chain needs no transform — observed fact A above shows the 1.0.0 shape already satisfies 1.7.0, so an identity edge suffices; `map_each` ships inert for the future 1.7.0→1.8.0 `state_derivation` change per the design doc). Each criterion is independently testable.
- **The unmatched-fallback shape in criterion 4** (`{relation_type: element, item_endpoint: fallback}`) maps the future `state_derivation.blocked_by.relation_types: string[]` → `relations: [{relation_type, item_endpoint}]` delta (design doc Step 6; feasibility doc Part B demonstrated it). It is NOT needed for the 1.0.0→1.7.0 gap — consistent, since criterion 4 only demands a fixture.
- **Underspecified points found**:
  1. **Criterion 1 does not specify behavior on mismatch with an empty/unresolvable chain.** As designed (corrected-steps Step 4: no try/catch, fail-fast), `runMigrations` throws → every config-loading op fails (exit 1) for any substrate whose config lags AND whose migrations.json lacks a `config` edge. Today such substrates load fine (observed fact A). Affected class: the repo's four non-active substrates (`listSubstrates` → `.context-archived-2026-06-30`, `.context-jit-spec-v2`, `.project`, `.project-archived`), any `.pi/` test substrate, and **every fresh install** — `/context accept-all` adopts `conception.json` with `schema_version: "1.0.0"` (observed), `migrations` is not among conception's installed blocks (grep for `"migrations"` in conception.json: no hit), and `loadMigrationsFileForDir` returns null when the file is absent → empty registry → throw. All six criteria can pass on the scratch demo while fresh installs brick at first config read. This is exactly the design doc's **Step 7 "DECISION"** (coverage from 1.0.0; explicitly "not resolved in this document") — the task inherits it unresolved and the criteria do not state its resolution.
  2. **Criterion 4 leaves the table-mode no-fallback case undefined** (design: `op.fallback ?? "parent"` defaults to `"parent"`; schema constrains `fallback` to enum `parent|child`). Criterion text says only `[+ fallback]`.
  3. **Criterion 3's "zero exit-5 brick"** is precise about the FGAP-095 brick mode but silent on the exit-1 unresolvable-chain mode introduced by criterion 1 (see B(iv) corollary).
  4. **Criterion 5 "loads byte-unchanged"** — `loadConfigForDir` never writes the file, so read it as: returned data deep-equals the parsed on-disk data and the registry is not consulted. Testable, mildly loose.
- **files[] ↔ criteria inconsistency**: `files` lists `packages/pi-context/samples/migrations.json`, but no criterion states what ships there (the catalog `config` edge? conception re-stamp?). Criterion 2 pins only the active substrate's migrations.json via `write-schema-migration`. What lands in samples is undetermined by the criteria despite being in the declared file set.

## D. Design-doc consistency

All three docs exist with the exact filenames: `analysis/2026-06-18-config-migration-corrected-implementation-steps.md`, `analysis/2026-06-18-config-migration-design-feasibility.md`, `analysis/2026-06-18-config-migration-proposal-validation.md`.

- Criteria 1, 4, 5 faithfully reflect the corrected-steps doc (Steps 1-4: `map_each` union/schema/applyOp shapes match one-for-one, including the table-XOR-(field+value) modes and the fallback object shape; Step 4's load-path code consults the registry only inside the mismatch branch — matching criteria 1 and 5).
- **Scope divergence (deliberate, but unstated)**: the design doc's Steps 5-6 (config.schema.json bump to 1.8.0 + the declarative 1.7.0→1.8.0 catalog decl) are NOT in TASK-070 — `schemas/config.schema.json` is absent from `files[]`, and criteria 2/3 target the 1.0.0→1.7.0 gap instead. TASK-070 is therefore the hook+primitive+real-gap-coverage subset; that subset is exactly what an implementation of FGAP-095's `proposed_resolution` needs, so this is legitimate scoping — but it leaves Step 7's coverage DECISION unresolved (finding C.1).
- The feasibility doc's C5 claims `map_each` needs a FOURTH lockstep edit ("the write-schema-migration Pi tool's presence/absence guard"). **Verified not required**: `write-schema-migration-tool.ts` enumerates no transform op names (grep for `rename|coerce|map_each` over the tool source: no op-vocabulary hits — only the kind-based presence/absence guard, which is op-agnostic; the TransformSpec body is AJV-validated downstream against migrations.schema.json). The corrected-steps doc (the settled extraction) uses three places; criterion 4's three places match the settled doc. Non-criterion note: the `write-schema-migration` op description string (`ops-registry.ts:1407`) enumerates "rename/set/delete/coerce" and will need the docs-surface-sync refresh when `map_each` lands — pipeline step 7, not a criterion defect.
- Criterion 3's "carried forward in memory" matches the settled no-write-back scope (mirrors `validateBlockWithMigrationForDir`'s documented "Out of scope: writing the migrated form back to disk").

## E. Feature-binding (criterion 6)

- Edge exists: `pi-context find-references --id TASK-070` → `task_addresses_feature` TASK-070 → FEAT-010 (plus `task_addresses_gap` → FGAP-095 and `item_governed_by_convention` → feature-decomposition).
- FEAT-010 criterion 1 (read back verbatim): "A config whose schema_version lags the bundled config schema is migrated forward on load and loads valid: loadConfigForDir migrates-then-validates via the schema-name-keyed config migration chain." TASK-070's criteria 1+3 implement exactly this; the additional specifics (no-consult-on-match, `map_each`, real-gap demo, non-empty registry) are derivable verbatim-near from FGAP-095's `proposed_resolution` ("Add a load-time config-migration path mirroring validateBlockWithMigrationForDir… Requires a declarative-transform primitive able to map array elements (map_each) and a config migration registry entry") and the cited design docs. **No delta that adds a requirement absent from the feature/gap/design chain.** Criterion 6's scope statement ("config instance, not the block-data class") is consistent with FEAT-010 criterion 4 carrying the block-data class separately.

## Per-criterion verdicts

| # | Verdict | Basis |
|---|---|---|
| description | **STALE (trivially)** | `context.ts:564-581` was exact at filing; function now spans 565-582 (1-line drift from post-filing commits). `schema-validator.ts:205-242` still exact. All mechanism claims true. |
| 1 | **AMBIGUOUS** | Correct and satisfiable as written for the mismatch+chain and match paths; but silent on mismatch-with-no-resolvable-chain, where the designed fail-fast throw converts today's loads-fine state into an exit-1 failure for every substrate/fresh-install lacking a `config` edge (design doc Step 7 DECISION, inherited unresolved). Needs a user determination: fail-fast (and then ship coverage — samples edge + conception handling) vs. fall-back-to-raw-validate on empty chain. |
| 2 | **VALID** | `write-schema-migration` exists (ops-registry.ts:1404), writes the substrate migrations.json; registry observedly empty for `config` today; gap 1.0.0→1.7.0 real; a single identity edge satisfies it (the 1.0.0 config already validates against 1.7.0 — observed). Note: op is authGated (interactive confirmation required). |
| 3 | **VALID** (premise current; one precision note) | Gap "(1.0.0 → current)" verified real today (1.0.0 live vs 1.7.0 bundled). Demonstrable on a scratch substrate exactly as written. Note: the migrated-path demo proves chain execution + no regression, not rescue-from-brick (the real 1.0.0 shape already passes 1.7.0 validation); "zero exit-5 brick" does not guard the exit-1 unresolvable-chain mode. |
| 4 | **VALID** (two minor underspecifications) | All three anchors exact today; `map_each` absent everywhere (greenfield as presumed); modes/shapes match the settled design doc one-for-one; feasibility doc's "fourth place" verified not required. Unstated: fallback default when absent (design: `"parent"`) and fallback's `parent\|child` enum. |
| 5 | **VALID** | Testable as returned-data-unchanged + registry-not-consulted; matches design (registry accessed only inside the mismatch branch). "Byte-unchanged" is loose phrasing for a function that never writes the file. |
| 6 | **VALID** | `task_addresses_feature` edge to FEAT-010 already filed (observed); TASK-070's criteria are a faithful specialization of FEAT-010 criterion 1 + FGAP-095's proposed_resolution; no augmentation delta found. |

## Overall verdict

**TASK-070's criteria are executable as filed**, with one determination required before implementation and three textual corrections advisable:

1. **Requires determination (criterion 1)**: the on-mismatch-with-no-resolvable-chain behavior and its coverage consequence — the design's fail-fast throw bricks (exit 1) any lagging-config substrate without a `config` migration edge, including every fresh install (`conception.json` ships `schema_version: "1.0.0"` and installs no migrations.json). This is the design doc's unresolved Step 7 DECISION; the criteria must state its resolution (e.g., ship the identity `config 1.0.0→1.7.0` edge in `samples/migrations.json` + seed it at install, or re-stamp conception, or specify a raw-validate fallback on empty chain). Relatedly, add the missing criterion for what `samples/migrations.json` (in `files[]`) ships.
2. **Correct (description)**: refresh `context.ts:564-581` → current 565-582 (or drop the line cite for the symbol name).
3. **Correct (criterion 4)**: state the table-mode fallback default (`"parent"` when `fallback` absent) and its `parent|child` domain.
4. **Correct (criterion 3, optional tightening)**: extend "zero exit-5 brick" to "zero config-load failure of any exit class" so the demo also guards the exit-1 unresolvable-chain mode the new branch introduces.

Evidence provenance: all substrate facts via `pi-context` CLI read ops (read-block-item, read-block, read-config, find-references); all code facts via direct Read/grep of `packages/pi-context*/src` + `schemas` + `samples`; runtime facts via one `npx tsx -e` eval against a scratch copy under the session scratchpad (live substrate untouched); filing-era line check via `git show 9125385:…`.
