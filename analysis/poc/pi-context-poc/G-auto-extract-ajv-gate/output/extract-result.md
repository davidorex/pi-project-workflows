# POC G — auto-extract AJV-gate result

Synthetic 5-turn session → stub classifier → AJV-gate per candidate.
Each candidate is validated against its target block's JSON Schema.

**Total candidates:** 3
**Accepted:** 2
**Rejected:** 1

## Per-candidate verdict

| # | target_block | source_turn | candidate id | verdict |
|---|--------------|-------------|--------------|---------|
| 1 | decisions | 2 | `DEC-0099` | ACCEPTED |
| 2 | issues | 4 | `issue-091` | ACCEPTED |
| 3 | issues | 5 | `issue-XYZ` | REJECTED |

## Accepted — payload detail

### `DEC-0099` → `decisions`

```json
{
  "id": "DEC-0099",
  "title": "Extend LensSpec with kind + members for lens-of-lenses composition",
  "status": "enacted",
  "body": "LensSpec gains `kind: atomic | composite` and `members: string[]` for composite lenses. Atomic lenses retain current shape; composite lenses reference member lens ids.",
  "phase": "substrate-arc"
}
```

### `issue-091` → `issues`

```json
{
  "id": "issue-091",
  "title": "AJV schema cache returns stale validation after schema-file mtime change",
  "status": "open",
  "body": "Reproduce: edit schemas/foo.schema.json, then re-run validation in the same process — AJV's compiled-schema cache returns errors against the prior schema version. Cache invalidation should key on schema-file mtime or content hash, not just $id.",
  "priority": "medium"
}
```

## Rejected — diagnostics

### `issue-XYZ` → `issues`

Payload:

```json
{
  "id": "issue-XYZ",
  "title": "Post-step snapshot diff occasionally empty under write contention",
  "status": "open"
}
```

Diagnostics (2):

- `(root)` — must have required property 'body' (keyword: `required`, params: `{"missingProperty":"body"}`)
- `/id` — must match pattern "^issue-\d{3}$" (keyword: `pattern`, params: `{"pattern":"^issue-\\d{3}$"}`)

---

Generated by `npx tsx render.ts` from `data/session.jsonl` via stub `classifier.ts`.