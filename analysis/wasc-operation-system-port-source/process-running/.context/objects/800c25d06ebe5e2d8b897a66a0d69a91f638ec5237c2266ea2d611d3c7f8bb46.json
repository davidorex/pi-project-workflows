{
  "description": "Prefer creating a new commit over `--amend`. After a pre-commit hook failure, the commit did NOT happen; `--amend` would modify the PREVIOUS commit and risk destroying prior work.",
  "enforcement": "manual",
  "severity": "error"
}