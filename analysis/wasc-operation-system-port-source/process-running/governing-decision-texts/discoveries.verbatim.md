# Governing discoveries (DISC-*)

Source (byte-verbatim JSON also persisted): `context-migration/decomposed/discoveries.json`

Each entry below is the exact `raw` (or `body`) string from the source JSON, copied verbatim — no rewording, no augmentation.

---

DISC-01-0519-A | 01 | 2026-05-19T08:37:45+0800 | verification-failure | `docker compose run --rm web ...` fails at image build with `failed to fetch anonymous token: Get "https://ghcr.io/token..." : net/http: TLS handshake timeout` on the `COPY --from=ghcr.io/astral-sh/uv:0.9.11 /uv /uvx /bin/` line | Future phases that follow the docker-first convention may stall identically; running steps via the host venv is a workaround, not a workflow fix; the failure mode looks transient (network) but recurred across retries within this session | logged; ran the Phase 1 `manage.py` commands via the host venv (`uv 0.9.11` on PATH) against the running compose postgres on `localhost:5433`; continued per phase content as-is | 02; 03; 04; 05; 06; 07; 08; 09; 10; 11; 12; 13; 14; 15 | 0d54ffe — ghcr.io confirmed structurally unreachable from this network (`docker pull ghcr.io/astral-sh/uv:0.9.11` → same TLS handshake timeout today); Dockerfile builder stage switched from `COPY --from=ghcr.io/astral-sh/uv:0.9.11 /uv /uvx /bin/` to `RUN pip install --no-cache-dir 'uv==0.9.11'` (PyPI wheel ships the same Astral binary); verified `docker build` exits 0, image runs `manage.py check` clean against compose postgres |

---

DISC-02-0519-A | 02 | 2026-05-19T13:06:57+0800 | architectural-debt | `mypy + django-stubs 6.0.4` emits 17 `django-manager-missing` errors against `school/models/*.py` once a Django app uses the `models/` package layout with cross-model FKs; both `.objects` resolution and reverse-related-manager resolution (`school.cycles`, `school.policies`, etc.) fail; the project's Phase 1 baseline was clean only because `users/models/` held a single model with no inter-model FKs | The `models/`-package + cross-FK layout is universal across the remaining phases (accreditation in 03; plans-app aggregate in 05+); each new phase will add a comparable bolus of mypy errors against this same plugin diagnostic; an explicit per-model `objects: Manager[Self] = Manager()` declaration did not silence even the reverse-related-manager errors so the dodge used on `users.User` does not generalise; possible paths the human can choose between are (a) upgrade django-stubs and re-test, (b) configure the plugin with explicit `apps` / model path overrides, (c) add per-file `# type: ignore[django-manager-missing]` blanks at each model file, (d) widen the `pyproject.toml` mypy override pattern to cover `*.models.*` for the diagnostic, or (e) treat the diagnostic as advisory and accept a non-zero mypy exit on app code until a fix lands upstream | logged; Phase 2 mypy run shows the issue but no Phase-2 step in the phase content requires mypy to pass as a gate; continued per phase content as-is, leaving the diagnostic visible for human adjudication | 03; 04; 05; 06; 07; 08; 09; 10; 11; 12; 13; 14; 15 | a68ba12 + f0d5c67 — django-stubs removed from the project per user direction (root-cause path); mypy now runs without the plugin and treats Django bits as `Any`, eliminating the diagnostic for all phases |

---

DISC-02-0519-B | 02 | 2026-05-19T18:00:00+0800 | artifact-conflict | Retroactive per-model tests for the `school/` app surfaced six modeltranslation registrations that the shipped Phase 2 implementation does not provide but `phases/phase-02-school-app.md` declares: (1) `School.name` translated (line 68); (2) `Cycle.label` translated (line 69) — `Cycle` is not registered in `school/translation.py` at all; (3) `Role.label` translated (line 70) — `Role` is not registered in `school/translation.py` at all; (4) `LearnerOutcome.description` translated (line 72) — only `("label",)` is registered; (5) `MissionArea.description` translated (line 72) — only `("label",)` is registered; (6) `AreaForImprovement.description` translated (line 72) — only `("label",)` is registered | Two of the six (School.name, the missing `description` registrations) appear deliberate per the `school/translation.py` module docstring, which explicitly states `School.name is treated as a school proper-noun identifier and is not listed as translated`; the other four (Cycle.label, Role.label, LearnerOutcome.description, MissionArea.description, AreaForImprovement.description) have no docstring rationale and may be implementation oversights; the per-language admin form-field rendering and `i18n_patterns`-driven URL handling will silently omit translated input affordances for these fields until reconciled; the schema-only Phase-2 admin-create verification did not exercise this so the gap went unnoticed at Phase-2 sign-off; the retroactive tests now assert the implementation as-shipped (so the suite passes) with comments naming the spec line and pointing at this row | logged; per-model tests written to assert the as-shipped translation shape with explicit comments naming the phase-MD line numbers and pointing at this discovery row; no code (models, admin, migrations, translation.py) modified — that's a human-direction decision | 03 (accreditation app uses the same translation-registration pattern); 04 (plans app declares many translated fields and must not inherit the same omissions); 11 (export/format work for translated artifacts); 12 (admin UX polish) | {Phase 2 follow-up commit} — user direction: honor Phase 2 MD as written; all 6 registrations now in school/translation.py (School.name, Cycle.label, Role.label, LearnerOutcome.description, MissionArea.description, AreaForImprovement.description); migration school/0003 adds per-language columns; school tests updated to assert correct shape; all 45 pytest cases pass |

---

DISC-03-0519-A | 03 | 2026-05-19T19:25:00+0800 | architectural-debt | The permission-group seed/extend pattern used in `school/migrations/0002_seed_permission_groups.py` (and now in `accreditation/migrations/0002_extend_permission_groups.py`) reads `auth.Permission` rows for the app's own ContentTypes from inside `RunPython`. Django's `create_permissions` is connected to `post_migrate`, which `core/management/commands/migrate.py` calls EXACTLY ONCE at the end of a `migrate` invocation (`emit_post_migrate_signal` in `django/core/management/sql.py:42-59`). On a single-shot `migrate` that applies both `0001_initial` (creates models + ContentTypes) and `0002_*permission_groups` (reads Permissions), the Permission rows for the new app's models do not yet exist when `0002` runs — so the grant set is silently empty. School 0002 only "worked" because its initial apply on 2026-05-19 happened in two separate `manage.py migrate` invocations 3 minutes apart (per `django_migrations.applied` column), so `post_migrate` fired between them. A clean `migrate` on a fresh DB that applies school 0001 and 0002 together would seed school_curator with 0 perms, not 40 | accreditation 0002 hit this bug on first apply: school_curator/plan_author/read_only all received 0 accreditation perms. school/migrations/0002_seed_permission_groups.py carries the same latent defect — any fresh-DB `migrate` (e.g. CI, deploy bootstrap, post-Phase-15 re-derivation) will silently produce empty permission groups for school models; every subsequent app-introducing phase (4, 5, 6+) that follows the same data-migration pattern is fragile in the same way; admin curators may appear to have correct group membership while lacking any actual access | logged; root-caused inside this phase rather than deferred (per mandate-004); the accreditation 0002 migration calls `django.contrib.auth.management.create_permissions(accreditation_config, using=alias, verbosity=0, interactive=False)` at the top of its RunPython before reading Permission rows, which materialises the rows that `post_migrate` would otherwise create later; verified: rolled accreditation back to 0001, re-applied 0002, all three groups now hold the expected accreditation grants; the school 0002 file was NOT modified (out of phase 3 scope — that's a phase 2 retrofit decision for the human); the equivalent fix in school 0002 would only surface on a fresh-DB migrate, so the live DB state is correct today | 04; 05; 06; 07; 08; 09; 10; 11; 12; 13; 14; 15 (every app-introducing phase needs the same `create_permissions` call in its group-extension migration); 02 (school 0002 needs the same fix retroactively for fresh-DB safety) | 5be0231 — school/0002 retrofitted with `_ensure_all_permissions(schema_editor)` helper that iterates every app_config and calls `create_permissions(app_config, using=alias, verbosity=0, interactive=False)` before any Permission read; verified on fresh `test_disc_03_fix` DB with single-invocation migrate: school_curator=48, read_only=24, plan_author=2 (non-zero); pre-fix would have been 0/0/0 |

---

DISC-09-0520-A | 09 | 2026-05-20T10:10:00+0800 | architectural-debt | `ActionStepDependency.depends_on` is `ForeignKey(... on_delete=SET_NULL)` AND `Meta.constraints` includes an exactly-one-of-target `CheckConstraint` requiring exactly one of `depends_on` / `cross_plan_ref` to be non-null. When a referenced `ActionStep` is deleted, Postgres attempts to set `depends_on = NULL`; on within-plan-dependency rows where `cross_plan_ref` is also NULL, the resulting state violates the exactly-one constraint, and the delete propagation raises `IntegrityError`. Net effect: deleting an `ActionStep` that is the target of a within-plan `ActionStepDependency` is blocked at the DB layer by the constraint rather than cleanly nulled by the SET_NULL — opposite of typical SET_NULL semantics | Phase 9 MD specifies BOTH the SET_NULL cascade rule AND the exactly-one CheckConstraint literally; they contradict at delete-time. Phase 9 IMPL (commit `f5a02ce`) implemented both literally and pinned the contradiction via test `test_actionstepdependency_depends_on_set_null_on_delete_then_exactly_one_violated`, which encodes the IntegrityError-on-cascade as the intended behavior. User decision pending: (a) keep current behavior (delete of target-step is blocked, author must hand-edit or delete the dependency row first); (b) change `on_delete` to `CASCADE` so dependency rows vanish with their target; (c) relax the constraint to allow both target fields NULL on transient cascade. The current state may be intentional (block accidental loss of dependency information), but Phase 9 MD does not say so | logged retroactively by orchestrator after IMPL commit `f5a02ce`; not blocking; Phase 9 IMPL test pins behavior as-shipped | 15 (revisit during post-Phase-15 batched verification when end-to-end author workflows expose whether the IntegrityError-on-delete is the desired UX) | 5daabef — `ActionStepDependency.depends_on` changed from `on_delete=SET_NULL` to `on_delete=PROTECT` per user direction. Root-cause path: the observable "delete blocked while target depended upon" behavior is now produced by the FK rule that names what it does, not by an IntegrityError emerging from a constraint-cascade race. Mirrors the project's PROTECT convention already established at `Plan.author` (Phase 6), `FeedbackChannel.owner_department` / `owner_role` (Phase 7), and `Assignment.department` / `role` / `stakeholder_group` (Phase 9). Migration `plans/0009_alter_actionstepdependency_depends_on.py` applied. Test renamed to `test_actionstepdependency_depends_on_protect_blocks_target_delete`; assertion now expects `ProtectedError` rather than `IntegrityError`. Phase 9 MD line 46 updated from `on_delete=SET_NULL` to `on_delete=PROTECT` so MD agrees with as-shipped code. Verification: 208 tests pass, all gates clean. |

---

DISC-05-0520-A | 05 | 2026-05-20T06:22:38+0800 | artifact-conflict | Phase 5 verification clause reads `admin shows empty Plans section`, but Django's stock `AdminSite.get_app_list` (`django/contrib/admin/sites.py`) iterates `self._registry.items()` and only emits an app section for apps that have at least one model registered with the admin site; Phase 5 step 1 declares `plans/models/__init__.py` as an "empty re-export list at this phase" and the phase does not authorize creating a placeholder model or overriding `AdminSite.get_app_list`. With zero registered plans models, the admin index will not render a "Plans" heading at all under the strict reading | Two paths exist to literally render an empty heading: (a) declare a placeholder model in `plans/models/` and register it with the admin, or (b) subclass `AdminSite` to emit headings for apps without registered models. Both add code beyond what step 1 lists. The charitable reading treats "empty Plans section" as describing the app being admin-discoverable (verbose_name registered, no models to enumerate); under this reading the verification is met as built. Without explicit user direction the implementation followed the charitable reading | logged; continued per phase content as-is — built the scaffold exactly as steps 1–3 describe (empty models package, admin package aggregator imports nothing), did not add a placeholder model or override `AdminSite`; surfaced the literal-vs-charitable reading here for human adjudication during Part B | 06 (first phase that actually adds a plans model — at that point the admin section will appear naturally and this discovery becomes moot) | 35a2653 — Phase 6 IMPL registers `Plan` with PlanAdmin plus `PlanAccreditationStandard` and `PlanPredecessor` inlines; stock `AdminSite.get_app_list` now emits the Plans app heading naturally. Literal-vs-charitable reading is moot from Phase 6 forward. |

---

DISC-15-0521-A | 15 | 2026-05-21T18:30:00+0800 | architectural-debt | No `Milestone` tier, and timing cannot anchor to a declared completion. The prototype's `cadence: "milestone"` (`fc-stakeholder-feedback`, "Stakeholder feedback on draft SLOs") fires when a declared level of work is reached, not on a period. **Sharpened (user):** a milestone is *an aspiration declaring a particular level of work complete* — a checkpoint that GROUPS one or more `Phase`s (e.g. 3 milestones, each with n phases), carrying a target (aspired completion) and an achieved state. The model has no such tier (hierarchy is `Plan → Phase → ActionStep`), and the timing carriers anchor only to a recurrence or fixed date: `FeedbackChannel.frequency` (FK, period), `Communication.frequency` (FK, period), `ReviewEvent.scheduled_date` (DateField, a fixed calendar date — NOT a frequency; corrects the original entry). So "do X when milestone N is reached" cannot be expressed, and the model cannot declare a level of work complete (`ActionStep` has no status — §B). Surfaced during the Phase-15 data round classifying the source `cadence` strings | A milestone is a tier ABOVE `Phase`, not a kind of cadence and not a single Phase reference. It belongs on the plan's goal/aspiration axis (kin to `SuccessCriterion`/`desired_state`), with the timeline merely referencing it. Modeling it subsumes three needs at once: milestone-anchored timing, a top-level plan progress spine, and the missing "declare work complete" capability. Distinct from the `Frequency` axis (D3: periods only; `ongoing` redundant, `one-off` an occurrence-mode) | RESOLUTION DIRECTION (user-confirmed, not yet implemented — separate plan→impl): introduce a `Milestone` tier — `Plan → Milestone → Phase → ActionStep`; `Phase.milestone` optional FK (SET_NULL, mirroring `ActionStep.phase`); `Milestone(plan, label, order, target [aspired], achieved-state audited à la PlanStatusTransition)`; anchor the timing carriers (`FeedbackChannel`/`Communication`/`ReviewEvent`) to a `Milestone` as a distinct timing axis (e.g. a `TimingKind` mirroring `TimelineKind`). Catalogued in `data/source-model-gaps.md` §H; concept recorded in `docs/domain-model.md` | 7 (FeedbackChannel/US-19); 8 (Phase/US-17); 10 (Communication/US-18); 11 (ReviewEvent/US-20); also the §B ActionStep-status gap | e824283 — Milestone tier implemented (IMPL subagent): new `Milestone` model (plan CASCADE; label translated; order; target_date [aspiration]; status/reached_on/reached_by service-stamped via `mark_milestone_reached`, per-status CheckConstraint); `Phase.milestone` optional FK SET_NULL + same-plan `clean()`; the three timing carriers gained `timing_kind` (`TimingKind`) + `milestone` FK (PROTECT) with `frequency`/`scheduled_date` now nullable and per-kind CheckConstraints (periodic/one-off/scheduled/milestone). Migrations `plans/0018` (schema) + `plans/0019` (Layer-A perms: plan_author CRUD, curator/read_only view). Gate green (check 0, makemigrations clean, ruff/mypy clean, pytest 408); applied to dev DB; schema + grants verified. Hierarchy is now `Plan → Milestone → Phase → ActionStep`. |

---

DISC-16-0521-A | 15 | 2026-05-21T22:30:00+0800 | architectural-debt | The org model is too flat for real planning: `Department` has no nesting (no `parent`), `Role` is an abstract seat detached from people, and there is no `Person`/teacher concept — so an organization with hierarchy down to individuals (PO / C&T → subject depts → teachers; SAO → YGLs + HRTs; AAO; Recruitment & Admissions), with heads who see their unit's work and can sub-assign to a person, cannot be represented. Surfaced when the user gave the authoritative department list and noted the missing hierarchy/people concept. (Supersedes the provisional plan-data `Department` placeholders incl. "PTA"/"Heads of Department (collective)".) | Determined design (user rulings, this session): one nested **Unit** tree via `Department.parent`; each unit has a single **head** (`Person`, optional until staffed); **Person** is the optional last level; **membership** = Person ∈ Unit with an optional position title (HOD = head; YGL/HRT/teacher = members; SAO members YGL/HRT are leaf, subject depts under C&T are sub-units with HOD head + teacher members). **Responsibility on an action step = a Unit (sufficient) + optional Person.** **Visibility:** a head sees their unit's assigned work AND all descendant units' (roll-up, top sees all); a named person sees their own; ordinary members see only what's explicitly assigned (no inheritance). App admins (Django superusers) see/do all. The existing **`Role`** model + the `*_role` owner FKs fold into Unit+Person; **`StakeholderGroup`** is retained as the distinct engagement/audience axis (impact / communication audience / feedback source), allowed as a *participant* but not as the *responsible* party. | RESOLUTION DIRECTION (design determined; NOT built — next is formal user stories then a plan→impl): add `Department.parent` (self-FK nesting) + `Person` + `Membership(person, unit, optional title)` + `Unit.head`; change action-step responsibility to Unit + optional Person; refactor the owner polymorphism (`Assignment`, `Policy.evaluation_responsible_*`, `FeedbackChannel.owner_*`, `ReviewEvent.responsible_*`) from dept/role/stakeholder to Unit(+Person), keeping StakeholderGroup on the participant/audience side; re-seed Departments to the authoritative tree (PO/C&T/AAO/SAO/Recruitment & Admissions + subject depts), removing the "PTA"/"collective" placeholders; this subsumes D2 (Role→Department becomes concrete). User stories listed (not yet written) in conversation. | 2 (school: Department/Role models + Department seed `school/0016` re-do); 5–14 (plans: owner-FK refactor across Assignment/Policy/FeedbackChannel/ReviewEvent); D2 (subsumed); D7 (User↔Person link) | 048cf71 — **structural slice done** (IMPL): `Person` + `Membership` added; `Department.parent` + `head` + same-school `clean()`; responsibility refactored to Unit(required dept)+optional `Person` across FeedbackChannel/Communication/Evidence/ReviewEvent/Policy and per-kind on `Assignment`; `*_role`→`*_person`, exactly-one constraints swapped; **`Role` retired** (model + perms + stale contenttype removed); Layer-A perms for Person/Membership; admin/translation updated. Migrations school/0021–0023 + plans/0020–0021. Gate green (check 0, makemigrations clean, ruff/mypy clean, pytest 448); applied to dev DB; schema verified (`*_department` NOT NULL on the 5 owners, `Assignment.department` nullable gated by the per-kind constraint). Delivers US-ORG-1..11 at model/admin level. **Remainder (sequenced, in scope):** authoritative Department re-seed + Person/Membership/head data; execution tracking (US-ORG-18/19 — §B status + §H evidence); front-end visibility (US-ORG-12–17,19). D2 dissolved (no Role to map). |

---

DISC-14-0521-A | 14 | 2026-05-21T16:40:00+0800 | architectural-debt | Phase 14 placed `subject_kind` and `planning_method` as single FKs on `Plan`, so a plan could carry only one subject kind and one planning method and the kind→method promotion gate was plan-wide. Real schoolwide improvement plans are bundles of differently-kinded work (the prototype's lead plan spans policy + curriculum + PD), so one plan-level kind is too coarse — it forces a single classification onto heterogeneous steps and cannot express which unit of work is which kind | The model already treats `ActionStep` as the unit of work (assignments, timelines, required resources, evidence live on the step; `_check_promotion_gates` iterates steps), so a plan-level kind is inconsistent with that granularity; a richer subject-kind vocabulary (`data/subject-kind-candidates.md`) makes "pick one kind for the whole plan" harder, not easier. User identified the single FK as brittle for real-world planning and chose to relocate the classification to the step | user-directed remodel: `subject_kind` + `planning_method` moved off `Plan` onto `ActionStep` (FK, null/blank, PROTECT); the kind→method gate predicate relocated into the per-step loop in `_check_promotion_gates` (a step whose `subject_kind.requires_planning_method` is true must have `planning_method`); `Plan` retains no kind column or property; `PlanningMethod.applicable_subject_kinds` stays advisory | 15 (post-Phase-15 walkthrough exercises per-step kind/method) | 057f5ed — IMPL subagent moved both FKs to `ActionStep`, relocated the gate predicate per-step, dropped them from `PlanAdmin` and surfaced them on `ActionStepAdmin`; migration `plans/0017_*` (RemoveField on Plan + nullable AddField on ActionStep; verified 0 Plan rows carried values → lossless); tests relocated to `test_actionstep.py` and the three gate tests rewritten per-step. Gate green (check 0, makemigrations clean, ruff/mypy clean, pytest 344); migration applied to dev DB; columns confirmed off `plans_plan` and on `plans_actionstep`. phase-14 MD carries a superseded-in-part pointer; domain-model + US-ext + source-model-gaps + subject-kind-candidates updated. **Altitude later refined (DEC-14/DISC-17): `subject_kind`+`planning_method` move `ActionStep`→`Milestone` [pending plan→impl]; `SubjectKind`→`ImprovementType`.** |

---

DISC-17-0522-A | 15 | 2026-05-22T08:30:00+0800 | architectural-debt | Naming collisions surfaced once subject departments + academic subjects entered the picture: (1) **"department"** names two things — the org unit (`Department` model: PO/AAO/SAO/C&T/Recruitment & Admissions) AND a subject department (Math/Humanities, nested under C&T); (2) **"subject"** names two things — `SubjectKind` (the *kind of improvement work* a plan/ActionStep is about) AND an academic subject (Math/Humanities, the taught discipline). No current model collision (subject depts are `Department` rows; academic subject isn't modeled; both `Department` and `SubjectKind` are single models) — it is a clarity + future-collision risk (if an academic `Subject` is ever modeled it clashes with `SubjectKind`). | Two of the four concepts share two words. The org-unit concept and the academic-unit concept are structurally the same kind of thing (nested unit with a head + positions) but conceptually distinct in natural language. `SubjectKind` is genuinely mis-named: it is the kind of *work/initiative*, never a school subject. | DECISION (user, this turn — DEC-13): rename the **org unit** concept to **School Division** (the `Department` model holds School Divisions: PO/AAO/SAO/C&T/Recruitment & Admissions); the academic units (Math/Humanities, under a division) are **Subject Departments**. STILL OPEN (surfaced previous turn, not yet user-ruled): the **"subject" word** — `SubjectKind` should rename away from "subject" (recommended `WorkKind`) to free "Subject" for the academic discipline. Persisted now; implementation (model rename `Department`→`Division`, a Subject-Department concept, and the `SubjectKind` rename) is a separate plan→impl. | 2 (school `Department`→`Division` rename: model/seeds `school/0006`–`0028`/admin/translation/perms); 5–14 (plans owner FKs reference the renamed unit); SubjectKind rename (model/`ActionStep.subject_kind`/32 seeds/candidate doc/US/tests); domain-model + US-ORG wording | PARTIAL. **DEC-13 IMPLEMENTED `d5a2ad2` (Pass A):** model `Department`→`Division` ("School Division") + every targeting FK field `*_department`→`*_division` across school+plans, via a non-destructive `RenameModel`/`RenameField` split (school/0029 hand renames; 0030 generated AlterField/AlterModelOptions; 0031 deletes 4 orphan `*_department` perms; plans/0022 hand renames with `RemoveConstraint(assignment_target_matches_kind)` ordered ahead of the rename for the SQLite test backend; 0023 generated re-adds the constraint with the `division` predicate + AlterField residuals). Seeded rows + PKs preserved; gate green (check 0, makemigrations clean, ruff baseline-only, mypy clean, **pytest 460**); applied to dev DB + verified (5 divisions intact ids 2/3/7/8/9, FK columns renamed, `school_department` gone, content type → `division`, orphan perms 0, `/admin/school/division/` 500 cleared). Subject Department confirmed as a nested `Division` (no new model). domain-model + seed-round DEC-13 updated. **DEC-14 IMPLEMENTED `19cc102` (Pass B):** non-destructive `RenameModel`(`SubjectKind`→`ImprovementType`) + `RenameField`(`PlanningMethod.applicable_subject_kinds`→`applicable_improvement_types`); `subject_kind`/`planning_method` removed from `ActionStep`, added to `Milestone` as `improvement_type`/`planning_method` (FK PROTECT null/blank); promotion gate relocated to a per-`Milestone` loop in `services.py` (service-level, no DB constraint — parity with the prior ActionStep rule); the four granted `*_subjectkind` permission codenames renamed in place → `*_improvementtype` (group grants preserved, since `school/0005` had granted them — distinct from Pass A's department orphan-delete); migrations school/0032 (hand RenameModel/RenameField), 0033 (generated AlterModelOptions + constraint rename + AlterField), 0034 (hand perm rename-in-place), plans/0024 (generated AddField milestone + RemoveField actionstep). Gate green (check 0, makemigrations --check clean, ruff baseline-only, mypy 179, **pytest 460**); applied to dev DB + verified (32 improvement types preserved, `school_subjectkind` gone, M2M through-table renamed with 5 wiring rows intact, `Milestone` carries `improvement_type_id`+`planning_method_id`, `plans_actionstep` carries neither, content type → `improvementtype`, perms add/change/delete×1 + view×3 grants preserved, 0 stale `*_subjectkind`, unique constraint renamed). **DISC-17-0522-A FULLY RESOLVED** (DEC-13 `d5a2ad2` + DEC-14 `19cc102`). |

---

DISC-18-0522-A | 15 | 2026-05-22T11:35:00+0800 | architectural-debt | Authoring the US-ONB onboarding stories surfaced a model gap (user-flagged): there is no **per-school admin status**. The admin actor of US-ONB-3 (assign positions), US-ONB-4 (bulk CSV create), and US-ONB-5 (deactivate a user) is "a user with admin status for a single school", but `users.User` carries only Django's global `is_staff`/`is_superuser`, the three permission groups (curator / plan_author / read_only), and a nullable `school` FK — none expresses "admin **of** this one school". | Multi-tenant scoping: a school admin must act only on their own school's users/data, distinct from `is_superuser` (whole-site). The existing "site admin" wording in US-ONB-3/4 and this "school admin" need one coherent home (global/site admin vs per-school admin). US-ONB-5's effects map cleanly to existing fields (`User.is_active` default-true gates login; PROTECT / `Person.user` SET_NULL preserve history; "not presented going forward" is an active-only selection/assignment queryset filter) — only the *actor's* authority is the gap. | Recorded as a noted gap to build (not optional). Story set US-ONB-1..5 authored against it (`00-preamble.md` + `US-STATUS.md`). A per-school admin status is needed to support the admin actor; the mechanism (candidate shapes: a per-school admin group/role keyed on `User.school`; a flag + school scope; or a membership table) is to be settled in plan mode before the onboarding/permission build. | onboarding + permission/visibility build (US-ONB-3/4/5; relates to US-ORG-12..16 visibility scoping) | RESOLVED (Slices 1–6 of the per-school-admin/onboarding build). Mechanism = a `school_admin` permission Group (authority) + `User.school` (scope) + admin enforcement; superuser-only minting. Slice 2 `school/0036` creates the group + cross-app grant (user add/change/view no-delete; school full-CRUD except view-only School; plans full-CRUD except view-only PlanStatusTransition; accreditation view-only); Slice 4 `users/admin/mixins.py` `SchoolScopedAdminMixin`/`SchoolScopedInlineMixin`/`SchoolOwnAdminMixin` scopes non-superusers to `request.user.school` across the school-app admins (get_queryset fail-closed, FK/M2M choice scoping w/ current-value OR-in, save stamp/validate, object-level has_*_permission); Slice 5 extends it to all plans-app admins (deep `plan__school` chains; global accreditation left unscoped); Slice 6 `UserAdmin(SchoolScopedAdminMixin, DjangoUserAdmin)` adds the full privilege-escalation guard set (is_superuser/user_permissions stripped+reset; groups `{plan_author, read_only}` allow-set; school readonly+stamped; no delete; can't touch other-school users or any superuser; coherence school_admin⇒is_staff+school). Superuser unchanged throughout. Two-school scope + escalation tests (pytest 555). Applied school/0036+0037 to dev DB (school_admin = 151 codenames). Enables US-ONB-3 + US-ONB-5 (enabled, `391b893`). [`01693bc`, `13910cf`, `f3c0268`, `391b893`; + Slice-1 `10a9dc4` chinese_name, Slice-3 `b009502` deactivation; cleanup `6c83c1b`] |

---

DISC-19-0523-A | 15 | 2026-05-23T06:00:00+0800 | architectural-debt | `school/0036_seed_school_admin_group` grants the `school_admin` group by calling `create_permissions` against the **live** `plans` app_config (the DISC-03 idiom) and a **hardcoded** view-only carve-out (only `planstatustransition`). On a FRESH build, `create_permissions` materializes the Permission rows for EVERY plans model present in code at build time — including view-only **audit** models added in later migrations (`ActionStepStatusTransition`, `EvidenceStatusTransition`) — and 0036's grant logic then sweeps them into `school_admin`'s **full-CRUD** branch (they aren't in the carve-out). Surfaced when the Slice-B audit-model perm test showed `school_admin` holding `add/change/delete_actionstepstatustransition`. | A new view-only/audit plans model is over-granted mutation perms to `school_admin` on fresh builds until a later migration strips them; the admin's `has_*_permission → False` blocks the UI but the grant DATA is wrong (defeats the append-only-audit intent at the data layer). `0036` is frozen (applied), so its hardcoded carve-out can't be edited. | Each new audit model's group-extension migration both GRANTS `view_<model>` view-only to the four groups AND **strips** `add/change/delete_<model>` from `school_admin` (mirroring 0036's `planstatustransition` carve-out): `plans/0027` (ActionStepStatusTransition), `plans/0029` (EvidenceStatusTransition). | any future view-only/audit plans model | RESOLVED-by-pattern for the two current audit models (`plans/0027`, `plans/0029`); the underlying `0036` carve-out is hardcoded — every future view-only plans model MUST repeat the strip, or a future refactor generalizes 0036's carve-out (e.g. exclude `*statustransition`/audit models). Recorded so the next audit-model build does the strip. **REFINED (DEC-17 `ActionStepImpediment`, `3694fad`):** the rule is two-sided and the deeper cause is that `school/0036` is a ONE-TIME grant. Its `create_permissions`-on-live-registry side-effect grants `school_admin` only on a FRESH build (where 0036 runs after the model is in code); on a DB where 0036 already ran **before** a model was added (the dev DB), 0036 never grants it. So EVERY new plans model after `school/0036` must set `school_admin`'s grant **explicitly in its own migration** — **view-only + strip mutation** for an append-only audit log (`plans/0027`/`0029`), or **full CRUD granted explicitly** for an editable child (`plans/0032`, after `plans/0031` wrongly relied on 0036 and under-granted `school_admin` on the dev DB). Never rely on 0036 for already-migrated DBs. (Fresh-build perm tests don't catch the divergence — the test DB is always fresh.) **Verified `3694fad`+sweep:** a dev-DB reconciliation of `school_admin`'s per-plans-model grants confirmed full consistency (every editable plans model full CRUD; the 3 audit logs view-only; none zero) — no remaining divergence. The convention is recorded in `phases/PHASE-LAUNCH-CHECKLIST.md` item 2a (explicit per-model `school_admin` grant). Guards: this convention + a periodic reconciliation sweep (fresh-build tests can't); optional future structural cure = an idempotent `post_migrate`/mgmt-command reconciliation from a declared posture map. Theoretical root: a migration whose effect reads the LIVE registry is not truly replayable (its result depends on build order); grant by frozen codename in each model's own migration. |

---

DISC-20-0525-A | 15 | 2026-05-25T14:50:00+0800 | phase-content-gap | The `planner` create wizard cannot link to SAME-SESSION milestones/phases: `Phase.milestone`, `ActionStep.phase`, and the `milestone` FK on FeedbackChannel/Communication/ReviewEvent are plain FK `ModelChoiceField`s scoped to the plan's EXISTING rows, which are empty on a single-page create; `save_nested` index-resolves ONLY the FeedbackChannel/ReviewEvent references (`channel_index`/`trigger_index`), never the hierarchy or milestone-timing links. Surfaced by the create-page pulldown audit. | The `Plan→Milestone→Phase→ActionStep` hierarchy (US-17, US-ext-milestone) and milestone-anchored timing (`timing_kind=milestone`) are NOT capturable in the create UI; the FKs are optional so flat creation still works, but choosing `timing_kind=milestone` is a dead-end (empty milestone select → `_clean_timing` unsatisfiable). NOT the empty-cycle issue (that was the logged-in user's `school=None`) and NOT the person-picker gap (D7 roster unseeded). | Recorded as **US-UI-5** (create-UI realization of same-session milestone/phase linking, extending the existing `channel_index`/`trigger_index` create-order-index mechanism, or dynamic select population); not built; awaiting user sequencing into plan→impl. | planner / US-UI | `3aa780b` — US-UI-5 BUILT: the five FKs converted to form-only `*_index` fields resolved in `save_nested` against `milestones_by_index`/`phases_by_index`; new `milestone-phasing.js` populates the index selects from same-session row labels; `_clean_timing`/`ReviewEventForm.clean` read `*_index`; a milestone-timed carrier with a bad/missing index → form error (HTTP 200, no IntegrityError). No model change/migration. Gate green (mypy 251, pytest 791). |

---

DISC-21-0525-A | 15 | 2026-05-25T20:30:00+0800 | architectural-debt | Surfaced by the new jsdom apply-layer test harness (DEC-31, `d30cd59`): `planner/static/wizard/js/formsets.js::reindexRow` reindexes a cloned formset row's `name` but NOT its `id_`-prefixed `id`/`for`. The regex is `^(${prefix})-(\d+)-` and the function DOES list `id`/`for` in its attribute loop, but Django's default `auto_id` renders `id="id_{prefix}-{n}-{field}"` / `<label for="id_{prefix}-{n}-...">`, and the `^`-anchored regex cannot match past the `id_` prefix — so on every cloned/added row the `id`/`for` keep the source row's index. Confirmed in code (formsets.js:26) + empirically by the IMPL (the addRows test pins current behavior). | Added formset rows carry DUPLICATE element `id`s (e.g. two `id_steps-0-label`) and labels whose `for=` targets the FIRST row's input → invalid HTML; clicking a cloned row's `<label>` focuses the wrong input; any id-based logic resolves to the wrong element; a11y degraded. Affects EVERY multi-row formset — both the in-page "+ Add" control and the F4/F5 multi-row assists (which call `addRows`). The assist PREFILL itself is unaffected (the apply functions select by `[name$="-field"]`, and `name` IS reindexed), so it escaped notice until the behavioral harness. Pre-existing (not introduced by DEC-31). | RECORDED as a work item (mandate-007), NOT fixed and NOT out-of-scope: the DEC-31 increment built the test harness (user-directed) and its `addRows` test pins the current (buggy) behavior; fixing `reindexRow` is a separate, small plan→impl. RESOLUTION DIRECTION: widen the regex to also rewrite the `id_`-prefixed form — e.g. `^(id_)?(${prefix})-(\d+)-` with replacement `$1$2-${newIndex}-` (handles both bare `name` and `id_`-prefixed `id`/`for`) — and flip the harness assertion to expect `id`/`for` reindexed; the new jsdom suite is exactly the tool to lock the fix. User decides whether/when to build it. | planner (formsets.js; every multi-row formset; the F4/F5 multi-row assists; the in-page "+ Add" control) | `7217045` — RESOLVED. User directed the fix as the next step (canonical plan→explore→plan→impl). `reindexRow` regex widened `^(${prefix})-(\d+)-` → `^(id_)?(${prefix})-(\d+)-` (replacement `$1-` → `$1$2-`): `$1` captures the optional `id_` (empty for `name`), `$2` the prefix → cloned rows now reindex `id`/`for` alongside `name`; only the leading index segment rewrites (a nested row's own deeper index stays 0). JS-only (no Python/template/migration); apply functions untouched (they select by `[name$=]`, never read `id`). The DEC-31 jsdom harness now ASSERTS the fix: the prior add-rows id assertion flipped (`id_feedback-0-label`→`id_feedback-1-label`) + a new `for`-reindex test (fixture `field({label:true})`) + a new nested-row id-reindex test (`steps` parent clone → `id_steps-1-resource-0-note`, nested own `-0-` intact); `node --test` 25→27 all green; a local revert-probe to the old regex made the 3 id/for/nested assertions FAIL, then restored. Gate: make test-js 27/27; mypy 260; pytest 1079 (unchanged — JS-only). |

---

DISC-22-0526-A | 15 | 2026-05-26T10:45:00+0800 | architectural-debt | Surfaced by the user during the DEC-32 build (the SAO org question): **responsibility is modeled at the DIVISION grain only, but responsibilities are role-borne.** (1) `Assignment` (the responsibility carrier on an ActionStep) targets a `division` (required for `responsible`) + optional `person` + participant-only `stakeholder_group` — there is **no `position` target**, so a step's accountable party can only be a unit or an individual, never a role; SAO has no sub-divisions and YGL/HRT are `Position`s, so SAO-pastoral steps collapse to "SAO". (2) DEC-32's `DivisionResponsibility` atoms hang off `Division` ONLY — a `Position` (e.g. a future **dorm supervisor** vs a **year group leader**) cannot carry its own responsibility atoms, so role-specific responsibilities within a division are not articulable. | The responsibility/role vocabulary the plan-decomposition (US-LLM-24 orchestration + US-LLM-27 responsibility-fulfillment assist) reasons over is too coarse: distinct roles within one division (dorm-supervisor vs YGL within SAO) cannot be expressed as distinct, attributable responsibility sets, so "the elaboration of possible responsibilities and roles for carrying out a school-wide plan can't be exactly articulated" (user). Accreditation evidence + execution accountability care which ROLE owns a step, not just the office. The DEC-32 atom layer (Layer 1 `DivisionResponsibility`, just built) is division-grained; the just-shipped grain is wrong for role-borne work (division-level atoms remain valid for office-wide purview — they EXTEND, not waste). | RECORDED as a real gap (mandate-007), shape OPEN. RESOLUTION DIRECTION (make responsibility position-grained as well as division-grained): (a) responsibility atoms attachable to a `Position` — either a new `PositionResponsibility` (FK position, statement, order) parallel to `DivisionResponsibility`, OR a generalized `Responsibility` with a division-OR-position owner; (b) add `Assignment.position` (the missing accountable rung: unit / role / individual); (c) extend the DEC-32 `ActionStepResponsibility` linkage to reference a position's responsibility atom; (d) emit positions + their atoms in grounding so the LLM reasons at the role grain. **SEQUENCING: settle this BEFORE US-LLM-27** (the responsibility-fulfillment assist must reason/link at the right grain). The model shape (PositionResponsibility-parallel vs generalized-Responsibility; keeping division-level atoms for office-wide purview) is the user's open decision. Faithful to DISC-16 (YGL/HRT deliberately positions, not units) — does NOT require modeling year-groups/homerooms as sub-divisions. | school (Division/Position/`DivisionResponsibility`); plans (Assignment, the DEC-32 `ActionStepResponsibility` linkage); ai (grounding); US-LLM-24/27 (the orchestration + responsibility-fulfillment assist reason over the grain) | `a38c561` — RESOLVED (dedicated parallel models, per the user's polymorphic-owner rejection). `PositionResponsibility` (school/0041; mirrors `DivisionResponsibility`; no own school FK — school via `position__division__school`) + `Assignment.position` (plans/0038; `assignment_target_matches_kind` rewritten — responsible = exactly one of {division, position}, person optional, stakeholder null; participant = ≥1; existing division-set rows stay valid) + `ActionStepPositionResponsibility` (plans/0038; a SECOND dedicated through-model + `ActionStep.position_responsibilities` M2M + soft coherence clean(); the 2a `ActionStepResponsibility` UNTOUCHED) + capture via `ACTIONSTEP_CHILD_FORMSETS["position_responsibility"]` (flows through the generic per-step render/save — no wiring added) + grounding `_divisions` nests positions+their atoms (the division+position responsibility INVENTORY) + `school_admin` full-CRUD grants (school/0042, plans/0039) + `AssignmentForm` clean mirrors the constraint. Gate predicate UNCHANGED (grain-indifferent); F2 UNCHANGED; no JS. Verified on dev DB: both tables; 8 grants; responsible-position-only OK; division+position rejected (IntegrityError); grounding nests positions+atoms; cross-school position rejected by form scoping. Gate: mypy 281, pytest 1383 (+ new tests), make test-js 37 (unchanged). **US-LLM-27 now unblocked** (the responsibility-fulfillment assist can reason/link at the position grain). |

---

DISC-23-0528-A | 15 | 2026-05-28T17:30:00+0800 | architectural-debt | The `planner` create-plan flow does NOT capture `Plan.guiding_clauses` (the M2M → `school.StatementClause` through `plans.PlanGuidingClause` carrying a translated `rationale` per linkage; DEC-32 Layer 2a, shipped). The Plan model holds the field; the planner has no form, formset, or `extra_formsets` entry for it (verified: no `guiding_clause` / `PlanGuidingClause` references anywhere under `planner/`). | A plan author cannot select which vision/mission clauses the plan advances or attach per-link rationale during create-plan. For the canonical-render-with-real-data constraint specifically: canonical's plan-scoped vision/mission rendering depends on this linkage; without it, every plan authored via the planner has an empty `guiding_clauses` set and canonical renders without that section. The DEC-32 rationale-on-every-linkage pattern is partially absent at the planner level (the planner does carry `PlanAccreditationStandard` rationale capture — the precedent for the missing piece). | RECORDED as a real gap (mandate-007); resolution shape OPEN. User direction pending — not prescribing the formset structure here. | planner (forms.py / extra_formsets / templates); canonical-render-with-real-data constraint | — |

---

DISC-24-0528-A | 15 | 2026-05-28T17:30:00+0800 | scope-question | After DEC-38 typed `PlanPredecessor` with `continuation_type` + translated `rationale` (`77fce3b`), the `planner` create-plan flow still does NOT capture `Plan.predecessors` (no `PlanPredecessor` formset; verified: no `predecessor` references anywhere under `planner/`). Predecessors are admin-only today. Whether the planner SHOULD capture predecessor declarations at create-time (vs. predecessors being added post-creation via admin as a separate step) is an OPEN design question DEC-38 surfaced in its out-of-scope clause. | If the planner is meant to be the single authoring surface for plan content, this is a real gap — a plan author cannot declare typed continuation at the moment of authoring. If the admin is the authoritative predecessor-management surface with the planner focused on within-plan content, no gap exists. The two postures imply different where-does-this-belong choices; the choice has not been made. | RECORDED as a scope question (mandate-007); resolution shape OPEN. User direction pending — not prescribing which posture is correct. | planner (forms.py / extra_formsets / templates); DEC-38 follow-on chain | — |

---

DISC-25-0528-A | 15 | 2026-05-28T17:30:00+0800 | verification-failure | The `Person` picker resolution against the post-CSV-import roster (57 Persons from the `import_staff_roster` real run, `0b475cc`) has NOT been browser-verified on `/en/planner/create/`. `Person.__str__` has a 4-path fallback chain (display → given+family → name_native → pk-defensive) post-`51ff5cd`; pytest covers the fallback semantics but no browser pass has been done against the populated dev DB. | The picker may resolve correctly (showing labels like "David" or "Ryan, David" via display_name or family+given fallback for `davidryan@chiway-repton.com`) OR it may degrade to `"Person #N"` if a path is unexpectedly blank for some imported rows. Until browser-verified, the Assignment.person picker's behavior against real Chiway data is assumed-not-verified — relevant when an author tries to assign a real staff member to an ActionStep. | RECORDED as a verification gap (mandate-007); shape OPEN. Single browser pass at `/en/planner/create/` would close it — not in scope of any current plan→IMPL. | planner (Assignment.person picker); post-Phase-15 verification batch | — |

---

DISC-27-0529-A | 15 | 2026-05-29T10:15:00+0800 / partial-resolve 2026-05-29T15:45:00+0800 / FULL-RESOLVE 2026-05-29T16:30:00+0800 | architectural-debt | **No-fabrication invariant is absent from the prompt corpus at landing time.** Surfaced empirically by prompt-workshop experiment 05 (F1 decompose-action-steps, capture `prompt-workshop/outputs/2026-05-29-10-02-27/05-decompose-action-steps.json`): the dispatched opus sub-agent invented seven non-existent leadership Position titles ("Vice Principal for Academics", "Vice Principal for Bilingual Programme", "Heads of Early Years, Primary, and Secondary", "Heads of Chinese and English Departments", "Head of Co-Curricular Programme", "Head of Student Affairs", "Head of Admissions and Communications") plus invented role-labels ("gradebook configurator", "PLC-minutes template owner", "classroom-walkthrough form owner") to comply with the strengthened-preamble directive to "name real Chiway actors and units from the grounding data — never generic placeholders". Root cause is two-sided: (a) F1's `grounding_include` tuple (`planner/specs.py:544-552`) carries `("school", "cycle", "improvement_types", "planning_methods", "areas_for_improvement", "learner_outcomes", "draft_state")` and does NOT carry `divisions` (which nests positions+responsibility-atoms via the `_divisions` emitter at `ai/services/grounding.py:195-230`) or `stakeholder_groups`; (b) the shared preamble's "name real actors" directive was positive-only (told the LLM to be specific) without the inverse constraint (that anything not in the grounding is not in the school). The actual dev-DB roster is 15 Divisions (AAO / Art / Business and Economics / College Counseling / Curriculum and Teaching / English / Future Scholar Innovation Center / Humanities / Library / Math / Music and Sports / Principal's Office / Recruitment and Admissions / SAO / Sciences) + ~46 Positions including IHS Principal / Director of AAO / Director of SAO / Director of Teaching and Curriculum Center / Director of Future Scholar Innovation Center / College Counseling Department Coordinator / Head of Art-Business-English-Humanities-Math-Sciences / IG1-IG2-AS-A2 Year Leaders + per-subject teacher Positions; there is no Vice-Principal layer, no EY/Primary/Secondary band heads (the school is subject-organized, not band-organized; year-leader Positions are IG/AS/A-Level Cambridge-style), no Bilingual Programme division, no Co-Curricular Programme leader Position. **Per-spec grounding-tuple inventory** (at this turn): A1 missing `divisions`; B1 missing `divisions, stakeholder_groups`; D1 missing `divisions, stakeholder_groups`; F1 missing `divisions, stakeholder_groups` (the exemplar); A3 missing `divisions`; B2 carries only `school, draft_state`; F3/F4 minimal grounding; F2/F5 carry `divisions` but possibly missing other vocabularies; RESPONSIBILITIES (US-LLM-27) carries `divisions` (correct); C1/G1 carry `divisions + stakeholder_groups + frequencies` (likely correct for their purviews); A2 carries `accreditation_standards` but may invoke divisions/positions in body. | The 14-spec corpus is structurally set up to invite fabrication in any spec whose body invokes actors/units/roles that its grounding tuple does not enumerate. The F1 exemplar is a clear demonstration: the LLM produced a coherent-looking, specific-named, evidence-organized 6-step decomposition that referenced organizational roles the school does not have. A WASC reviewer reading the output would see a plausible-sounding plan with non-existent owners; a school admin would have to rewrite every step's named actors. This makes the corpus's current production output un-promote-ready by the success-criterion-2 standard ("an admin would accept it without rewriting"). The defect is structural (grounding tuples + preamble language), not per-prompt-body — corpus-wide remediation is required. The Exp 04 type-method-invariant finding (parse_propose_milestones permissive on the type-method M2M) is the SAME class of finding at a different decision joint: in both cases the LLM is producing valid-shape output whose content substitutes its general training for the school's admin-curated structure. The project pattern (admin-curated structure is source of truth; LLM chooses from, never overrides) inverts at the prompt-corpus level: prompts must NOT leave gaps the LLM can fill from training. | RECORDED + invariant FORMALIZED as DEC-39 (this turn). LANDED this turn: shared preamble at `prompt-workshop/shared/preamble.md` carries the no-fabrication constitutive constraint (between the solution-architect framing and the success criteria — applies to all 14 workshop snippets via `{% include %}`). OPEN for follow-on plan→IMPL (Mode B): per-spec `grounding_include` audit + remediation across the 14 specs at `school-improvement-plans/planner/specs.py` (no migration required — grounding tuples are pure Python config; the workshop dispatch picks up the change automatically via `_workshop.py`-side grounding builder reading production sections; the eventual production landing carries via the same Python edit + corresponding `ai/0018+` prompt-template-evolution migration packaging the validated bodies). Per-spec body strengthening where a body references categories the expanded grounding now provides is part of the same plan→IMPL slice. F1 experiment 05's capture stays as the forensic exemplar of the pre-fix state; no rerun planned until grounding remediation lands. | ai (services/grounding.py — emitters already correct); planner (specs.py grounding-include tuples for all 14 specs); prompt-workshop (snippets + dispatch; shared preamble LANDED); the eventual `ai/0018+` PromptTemplate-evolution migration | **FULL-RESOLVE** — full DEC-39 enforcement chain landed across `488906e` (workshop preamble carries the no-fabrication invariant) + `9d7f07f` (planner/specs.py `grounding_include` expansion across 9 specs) + `e81b7b6` (`ai/0018_install_no_fabrication_preamble` — corpus-wide PromptTemplate body update injecting preamble + per-spec free-text-tightening clauses) + `39fc96e` (workshop snippet preamble + clause sync) + `b12e3dd` (workshop snippet body render blocks — 28 per-section block insertions across 9 snippets closing the body↔grounding rendering gap) + `ef1ce18` (`ai/0019_render_no_fabrication_catalogues` — mirrors the 9 body render blocks to production PromptTemplate rows via guarded byte-match) + `0b018fa` (structural-guard test `test_prompt_body_iteration_coverage.py` promoting the body-iteration-coverage audit dimension to a permanent gate-time machine test). Goal-met test executed at `prompt-workshop/outputs/2026-05-29-15-23-28/` via F1 re-dispatch on cleared draft.action_steps: ZERO fabrication; every named actor (26 distinct Positions + 8 Divisions + 6 Stakeholder groups) is a verbatim match to the enumerated catalogue; all 10 fabrication categories from the experiment 05 exemplar absent. **Chain mechanic**: grounding tuple includes section → context dict carries section data → body iterates section → rendered prompt text contains catalogue → LLM has both the directive and the enumeration the directive references → LLM names only enumerated actors → zero fabrication. Workshop and production prompt bodies are byte-identical post-this-chain (modulo the workshop's `{% include "shared/preamble.md" %}` which preamble-substitutes at render time vs production's inline preamble from `ai/0018`). The structural-guard test prevents future drift: any spec edit adding a section to `*_GROUNDING_INCLUDE` without a corresponding body render block fails the gate at IMPL time. Tracking persistence at `852aa3f` (mid-chain) + this turn's tracking commit (chain close). | _Prior:_ **PARTIAL-RESOLVE** — `9d7f07f` + `e81b7b6` + `39fc96e` land the enforcement INFRASTRUCTURE (grounding-include expansion across 9 specs; corpus-wide no-fabrication preamble injected into all 14 production PromptTemplate bodies via guarded `ai/0018`; per-spec free-text-tightening clauses inserted; workshop snippets synced); per-slug canary verified on dev DB; gate green at each commit. **GAP REMAINING (sub-agent surfaced per mandate-007)**: the per-spec free-text-tightening clauses reference catalogues "above" that the bodies do NOT render — F1's body now says "name from the divisions catalogue above" but has no `{% if divisions %}{% for d in divisions %}- {{ d.label }}{% endfor %}{% endif %}` block; the grounding context dict carries the data, Django Template renders only what `{% if %}` blocks invoke. Fabrication risk persists at the same joints, possibly worse (the LLM has a stricter directive with no enumeration to comply against). Canonical resolution path: a 4th IMPL slice adding `{% if <section> %}{% for %}{% endfor %}{% endif %}` rendering blocks to each affected snippet body + a follow-on `ai/0019_render_no_fabrication_catalogues.py` migration mirroring the additions to the 9 affected production PromptTemplate bodies (`ai/0016`/`0018` guarded byte-match pattern). NOT a rollback (the three commits are valid infrastructure); the only mandate-compliant path is the follow-on slice. **LANDED-INVARIANT + ENFORCEMENT-INFRASTRUCTURE landed (`9d7f07f` + `e81b7b6` + `39fc96e`); ENFORCEMENT-CONTENT-RENDERING open (user decides next slice scope).** |

---

DISC-26-0528-A | 15 | 2026-05-28T20:30:00+0800 | architectural-debt | The 14-spec LLM prompt corpus at `school-improvement-plans/ai/migrations/0003`–`0017` (seeded prompt bodies for the 14 `AssistSpec` registrations at `planner/specs.py:2791::register_specs()`) is not aligned with US-DRAFT-1's "the LLM uses ALL data from the DB as its universe of context, renders a draft plan in all its elements ready to be evaluated and fine tuned" vision. Full audit at `docs/wizard-v2-coverage-and-prompt-quality-audit-2026-05-28.md` enumerates 11 specific gaps: (1) no whole-plan composer prompt; (2) sequencing dependencies encoded only in `draft_state`, no orchestration framing; (3) per-element prompts don't optimize for downstream consumability; (4) "fine-tune by user" affordance not modeled in prompts; (5) WASC-evidence orientation uneven (only A3-post-0016 + US-LLM-27 are explicit; F2/F3/F4/D1/G1 lack it); (6) no prompt is told it operates in whole-plan-draft mode; (7) meta fields (framing_focus/priority_tier/target_cycle/gap_statements/outcomes) uncovered by any spec; (8) no prompt carries general LLM success criteria for itself (defaults to "valid-shape" not "substantive"); (9) no prompt explicitly forbids LLM punting/hedging/mandates-style antipatterns (no "consider consulting X", no "as an AI I cannot", no options-instead-of-decisions, no half-fills/TBDs when data is in grounding); (10) no prompt encodes operational policies of the planning context (e.g., "all divisions must be involved in bringing about the specific improvements through the lens of their particular purviews" — an organizational principle that should bias F2/F5/US-LLM-27/C1/G1 choices); (11) no prompt frames the LLM's role as mapping a solution path from current state to desired state — the META-FRAMING for evaluating and devising the LLM's role: given its general training + all the framing data (vocabularies, divisions, responsibilities), the LLM is a solution-architect synthesizing the school's organizational reality into a coherent current→desired improvement trajectory, not an isolated per-element generator. | The 14 prompts produce valid-shape per-element output that passes per-element parsers; they do NOT produce content that reads as a coherent, accreditation-evidence-ready, school-policy-honoring whole plan without significant per-element user fine-tuning. US-DRAFT-1's production landing requires prompt-corpus uplift across all 10 gaps; per-spec quality scoring across 6 axes (output discipline / constraint articulation / context awareness / WASC-evidence orientation / possibility-space consideration / sibling coherence) in the audit identifies which prompts are strongest (A3-post-0016, US-LLM-27, F1, B1) and weakest (F2, F3, B2, D1). Aggregate findings: output discipline uniformly strong; WASC-evidence orientation is the WEAKEST axis in aggregate (2 D + 1 borderline-D out of 14); only ONE explicit inter-spec coherence directive across the entire corpus (US-LLM-27 → F2). | RECORDED as a real architectural-debt finding (mandate-007); resolution shape OPEN. User direction pending — not prescribing the prompt-rewrite shape here. The audit doc carries the per-spec ratings + per-gap detail; resolution would naturally be a multi-phase prompt-corpus rewrite plan→IMPL (per-element prompt rewrites + a new orchestrator/whole-plan composer + shared prompt-preamble with success criteria + zero-hedging language + operational-policy directives + WASC-evidence framing across all prompts). The user's gap-8/9/10 additions (general success criteria + zero-room-for-hedging + operational policies) are user-flagged additions to the audit agent's original 7-gap list — preserved here so the architectural condition is complete. | ai (the 14 prompt templates); planner (the 14 `AssistSpec` definitions + the orchestration trio US-LLM-23/24/25 + the per-element assists US-LLM-6..27); US-DRAFT-1 production landing; the canonical-render-with-real-data constraint (a coherent whole-draft is the content that constraint will render) | — |

---

DISC-28-0529-A | 15 | 2026-05-29T20:00:00+0800 | architectural-debt | **Parser-layer fabrication closure — structural impossibility of fabrication entering the system, complementary to DEC-39's prompt-language probabilistic suppression.** Surfaced as the still-open carry-forward from DEC-39 Slice 1 + Slice 2 (which I unilaterally out-of-scoped in those plans). The chain: every `parse_*` function in `planner/specs.py` now (a) receives `school + grounding` per Commit 1's signature widening (`ec31656`); (b) validates every structured named-entity field (improvement_type code, planning_method name, division label, position label, stakeholder label, frequency code, accreditation standard code, mission-area/AFI/learner-outcome label, responsibility-atom statement) against the corresponding grounding catalogue at parse-time per Commit 2 (`77712d3`), raising `ValueError` with spec/field/offending-value/available-members on miss; (c) cross-validates type-method invariant at D1 (chosen `planning_method.applicable_improvement_types` must contain the chosen `improvement_type` or be empty) + F1 (chosen method's applicable_improvement_types must include at least one of draft milestones' improvement_types or be empty) per Commit 3 (`d173679`); (d) scans every free-text prose field via `planner/_freetext_audit.py`'s `extract_proper_name_spans` + `CatalogueUnion` + `audit_freetext`, normalizing leading articles ("The ") + possessives, with a tuned `KNOWN_GENERIC_TERMS` denylist that excludes entity-category nouns ("Board", "Council", "Office", "Department") so they survive to be catalogue-checked — closing the experiment-05 fabrication class (10 invented Position titles all rejected by regression test) per Commit 4 (`7e450a9`); (e) `test_parser_catalogue_enforcement_coverage.py` is the future-drift guard — parametrized over all 14 registered specs, asserts every parser rejects fabricated input per Commit 5 (`6d43a2a`); (f) voice-constraint reinforcement landed in `prompt-workshop/shared/preamble.md` + `ai/migrations/0020` per Commit 6 (`997d633`) — concrete antipattern examples + positive school-report exemplar inlined; voice canary verified in all 14 PromptTemplate rows on dev DB. **All 8 `resolve_*` hooks (D1/C1/F2/F5/A2/A3/US-LLM-27/G1) tightened from lenient-drop to raise-on-miss** as part of Commit 2; catalogue presence is now validated at parse so resolve's PK lookup should never miss — if it does, raise (means a parse/resolve cache desync, never should happen). The DEC-39 chain (prompt-language layer) + DEC-40 chain (parser layer) together = structural fabrication closure end-to-end. **Chain mechanic**: prompt LLM faces enumerated catalogues in rendered prompt + directive against fabrication (DEC-39); LLM cannot land fabricated names in draft because parser rejects them at the LLM→DB boundary (DEC-40); admin-curated catalogues are the only universe for both directive (prompt) and enforcement (parser). | The parser layer was the load-bearing carry-forward gap from DEC-39: probabilistic suppression at the LLM cannot guarantee structural closure because a future dispatch with different sampling can still emit a fabricated name; the parser must be the gate. Without this layer the user's directive "no opportunity for fabrication" was met only probabilistically (one F1 re-dispatch was clean; subsequent dispatches were not guaranteed). With it, fabrication cannot enter the system regardless of LLM sampling. | RESOLVED across `ec31656` + `77712d3` + `d173679` + `7e450a9` + `6d43a2a` + `997d633`. Gate: pytest 1762 → 1880 (+118 tests across the slice); mypy clean throughout (301 → 306 source files); ruff baseline; test-js unchanged. Dev DB: `migrate ai 0020` applied; voice canary present in all 14 PromptTemplate rows. **Plan source**: `/Users/david/.claude/plans/gleaming-skipping-emerson.md` (parser-level fabrication closure — full completion of every out-of-scoped item). | planner; ai (the corpus); workshop dispatch; future per-spec experiments (06+); tenant onboarding (any future tenant inherits the same parser-layer closure for its enumerated roster) | RESOLVED — DEC-40; parser-layer closure complete modulo DISC-29 (single-token gap). |

---

DISC-29-0529-A | 15 | 2026-05-29T20:00:00+0800 / closure-path-settled-via-DEC-42 2026-05-30T09:45:00+0800 / closure-path-revised-to-DEC-43 2026-05-30T11:00:00+0800 / RESOLVED 2026-05-30T12:00:00+0800 | architectural-debt | **Single-token invented organizational nouns in free-text prose are NOT caught by `planner/_freetext_audit.py`'s current MULTI-token-only policy.** Discovered + surfaced by the IMPL sub-agent during Commit 4 (`7e450a9`): the audit was originally written per the plan to flag both single-token and multi-token capitalized spans against the catalogue, but empirical testing against the existing endpoint test suite revealed that single-token capitalized words in narrative prose ("Attendance", "Dashboard", "Intervention", "Stronger", "Mid-cycle") produce too many false positives to be a viable check — they are sentence-initial nouns or ordinary domain capitalizables, not org-name fabrications. The IMPL refined the audit to flag ONLY multi-token capitalized spans (`planner/_freetext_audit.py:629-647`); the 10 experiment-05 fabricated names are all multi-word ("Vice Principal for Academics", etc.) and remain rejected via parametrized regression test. **What is NOT caught**: a single-token invented organizational noun in free-text prose (e.g. LLM writes "the Strigil reviewed the report"; "Strigil" is invented, not in the catalogue, but single-token → skipped by the audit; the structured-field validation in Commit 2 catches single-token vocab fields like `improvement_type` codes and `division` labels but not free-text prose). Real org names are almost always multi-word, so the fabrication-surface area of this gap is small in practice but real. | The DEC-40 parser-layer closure covers the multi-word org-name fabrication class (experiment 05's pattern) end-to-end. A single-token fabrication in free-text prose remains as an unaddressed surface — not caught by Commit 4's audit, not caught by Commit 2's structured-field check (which only applies to typed catalogue fields, not free-text), not caught by Commit 3's type-method invariant (which only applies to D1+F1's planning_method/improvement_type cross-check). The user's "no opportunity for fabrication" directive is met for the multi-word case; the single-token case is an open surface. | RECORDED as a known fragility (mandate-007). Inline TODO at `planner/_freetext_audit.py:634-642` references this DISC ID. Closure direction: extend `audit_freetext` to ALSO flag single-token capitalized spans that (a) don't match a catalogue entry AND (b) don't match an English-dictionary word AND (c) aren't in `KNOWN_GENERIC_TERMS` — the English-dictionary check is the discriminator that suppresses sentence-initial-noun false positives while still catching invented role tokens. Implementation: a small dictionary file under `school-improvement-plans/planner/data/english_word_list.txt` (or use Python's standard `enchant` package if dev-DB tooling permits) + an `is_english_word()` helper + a refined single-token policy in `audit_freetext`. Alternative mechanism: require all single-token capitalized words in free-text to appear in EITHER the catalogue OR a curated school-glossary table (admin-curated) — would extend admin-curated-structure-is-source-of-truth pattern to the lexical level. **NOT in scope of DEC-40 itself**; recorded here per mandate-007 so it survives session-end and is available as a discrete work item the user can sequence into a future slice. | planner._freetext_audit; specs.py free-text scan policy; any future fabrication-class extension | RESOLVED — `394e561` (DEC-43 IMPL). The single-token gap closes structurally: (a) every single-token catalogue label (Math, English, IG1, IG2, AS, A2, GROUNDED, ROUNDED, UNBOUNDED, and similar) admits because the spaCy PhraseMatcher emits it as an exact-match span regardless of token count; (b) single-token fabrications that spaCy `ner` tags as ORG/PERSON/GPE/NORP/FAC are flagged if not in the gazetteer AND not in `KNOWN_GENERIC_TERMS`. The inline TODO at the prior `_freetext_audit.py:634-642` is removed in this commit (the function it referenced no longer exists). DEC-43 architecture does NOT require an English-dictionary check or admin-curated school-glossary table — the multi-token-only policy that motivated DISC-29 is replaced by the PhraseMatcher/ner two-layer design where single-token discrimination happens at the NER-tag level rather than at a token-count gate. Coverage gap surfaced empirically (DISC-35): spaCy ner misses some edu-domain organizational titles entirely; that gap is the Option H escalation candidate per DEC-43's fallback path. |

---

DISC-30-0529-A | 15 | 2026-05-29T22:30:00+0800 / RESOLVED 2026-05-30T08:00:00+0800 | architectural-debt | **Chiway tenant `PlanningMethod.applicable_improvement_types` M2M is INCOMPLETE for the SLO-improvement plan shape.** Surfaced empirically by the post-DEC-40 F1 re-dispatch (`3c5576e`): the LLM picked "Policy revision (6-step consultative)" — the closest process-fit of the 4 admin-curated methods — and the parser correctly rejected at the type-method invariant because that method's `applicable_improvement_types` = `{policy-establishment, policy-revision}` and the draft milestones' improvement types are `{schoolwide-learner-outcomes, curriculum-development, communications, compliance-accountability}`. Hardening the F1 snippet prompt to explicitly direct the LLM to enforce the invariant (commit pending — `prompt-workshop/snippets/05-decompose-action-steps.md`) surfaced the deeper issue: **NONE of the 4 Chiway-tenant planning methods has an `applicable_improvement_types` covering any of these milestones' types, and NONE is an "any-type" method (empty `applicable_improvement_types`)**. Per-method coverage at Chiway: "Data quality improvement" → `data-collection-improvement`; "Event design (recurring)" → `event-design`; "Policy revision (6-step consultative)" → `policy-establishment, policy-revision`; "Tool / platform customization" → `tool-or-platform`. None covers SLO / curriculum-development / communications / compliance-accountability. | The parser's correct behavior under DEC-40 turns an LLM-emitted F1 output into a structural rejection for ANY method the LLM picks — there is no admin-curated method that satisfies the invariant for this plan's shape. The plan IS a real SLO-improvement plan the user has been working on; this is exactly the shape the platform must support. The system is correctly saying "no admin-curated process applies"; the closure path is admin curation extension, not parser loosening (per the project pattern: admin curation is source of truth; LLM chooses from, never overrides). This is the tenant-data layer analog of the DEC-39/DEC-40 corpus-layer closure pattern: where the structural enforcement reveals an incomplete admin-curated reference set, the closure is to extend the admin-curated set, not to relax the enforcement. | RECORDED as a tenant-data gap (mandate-007). Four closure directions, all admin-curation decisions the user owns: (a) extend the existing 4 methods' `applicable_improvement_types` M2M to cover the additional types appropriate to each method's process (e.g. "Policy revision" naturally extends to `schoolwide-learner-outcomes` for SLO-descriptor authoring + consultation; "Tool / platform customization" naturally extends to `curriculum-development` for unit-plan template + gradebook config work; etc.); (b) seed additional Chiway planning methods that cover SLO / curriculum / communications / compliance work — possibly drawing on `dev-planning-knowledge-source/` or `docs/` for canonical recipes; (c) change the milestones in this plan to use improvement types that the existing methods cover (constrains plan content to fit the curated process; weak option since milestones drive plan substance); (d) add an "any-type" method (one with empty `applicable_improvement_types`) as a safety net for plans that don't fit a curated process — would be the user's explicit decision that some plans get unconstrained methods. The prompt-side hardening (DEC-40 follow-on in `prompt-workshop/snippets/05-decompose-action-steps.md` + would-be `ai/0021` mirror to production) IS still correct — the directive to honor the invariant is right regardless of data coverage; it just makes the data gap visible upstream of the parser rejection. **Closure of this DISC blocks F1 re-dispatch until admin curation triage is complete**: any further F1 dispatch against the current dev DB state will produce a guaranteed parser rejection regardless of LLM behavior. | planner (parser correctness — unchanged); ai/0021 production-prompt mirror of the snippet directive (still desirable for production parity once the directive itself is validated); Chiway tenant seeds (school/migrations/*); the orchestration trio (US-LLM-24/25/23) — when sequencing reaches F1 in production, this gap blocks unless closed; future tenant onboarding (the same gap class will surface for any tenant whose admin-curated M2M doesn't cover its milestone scope) | RESOLVED — `ec2b187` (school/0057_extend_planning_method_applicable_improvement_types) extends all 4 Chiway methods' M2M per closure path (a): Policy revision +schoolwide-learner-outcomes +communications; Tool/platform customization +curriculum-development; Data quality improvement +assessment-and-data; Event design +professional-learning. Dev DB applied + per-method M2M verified post-migration. F1 re-dispatch unblocked. |

---

DISC-31-0530-A | 15 | 2026-05-30T07:00:44+0800 / RESOLVED 2026-05-30T08:00:00+0800 | architectural-debt | **`planner/_freetext_audit.py::extract_proper_name_spans` has multiple noun-phrase extraction defects that produce false-positive rejections on prose containing real catalogue-named entities.** Surfaced empirically by the post-DEC-40 A1 re-dispatch (response captured at `prompt-workshop/outputs/2026-05-30-07-00-44/01-narrative-draft.response.json`; full evaluation at sibling `.evaluation.md`). The LLM (opus) produced clean fabrication-free A1 narrative output naming only real Chiway catalogue entities (all 7 SLOs by name, all 15 Divisions, 4 real Director/Principal Positions, 4 Year Groups IG1/IG2/AS/A2, 3 mission areas, 3 mission clauses GROUNDED/ROUNDED/UNBOUNDED, 6 WASC standards, 7 AFIs, 5 stakeholder groups). Parser rejected with 10 false-positive spans. Defect classes (each closure-actionable): (1) **comma-list collapse** — a span like "Math, English, Humanities, Sciences, Business and Economics, Art Department, and Music and Sports Department" is grouped as ONE span rather than split into 7 individual catalogue candidates; same defect on "Bilingual Communicators, Compassionate, Engaged, ..." (6-SLO list) and "Students in IG1, IG2, AS, and A2" (Stakeholder + 4 Year Groups); (2) **sentence-boundary cross** — extractor grouped "SLO-tagged. PLC" across a period+capital boundary; (3) **paren-unbalance** — extractor stripped opening parens before lookup, producing "SAO - Student Affairs Office (Pastoral and Well-Being" (missing close paren) which fails catalogue lookup against the real "SAO - Student Affairs Office (Pastoral and Well-Being)" entry; same on "Principal's Office (PO"; (4) **sentence-initial article/adverb merge** — extractor grouped "Only Confident" (sentence-start adverb + real SLO Confident); (5) **class-noun-not-entity** — "Schoolwide Learner Outcomes" is a class noun (the category of which Confident/Bilingual Communicators/etc. are members), not an entity name; needs `KNOWN_GENERIC_TERMS` extension to include "Schoolwide Learner Outcomes" / "Schoolwide Learner Outcome" / "SLO" / "SLOs" + similar class-noun-not-entity tokens. | The parser over-rejects on prose-heavy specs (A1 has 5 free-text fields; B1/D1/F1/A3/US-LLM-27/A2/G1 all have rationale or description prose). The DEC-40 closure intent (no fabrication) is correct; the implementation produces false negatives on real LLM output. This blocks workshop re-dispatch of every prose-heavy spec until the extractor is hardened. The DEC-40 structural-guard test suite (Commit 5, `6d43a2a`) does not surface these defects because the synthetic fixtures used short, simple inputs without comma-lists / parens / cross-sentence content. False-positive rejection class: the LLM produces clean output → parser rejects → looks like fabrication catch → isn't. Mistakes the discovery surface. | RECORDED as parser-side defect (mandate-007). Closure direction (each defect class is independently actionable; sequence-flexible): (a) split spans on commas + Oxford "and" + semicolons before catalogue lookup; check each list-element separately; (b) extend `_PROPER_NAME_SPAN_RE` (or equivalent) to NOT cross sentence-terminating punctuation; (c) balance parentheses — either include matched close-paren in the span or skip span-start at unmatched open-paren; ensure catalogue entries with parens match prose spans with the same parens; (d) extend `KNOWN_GENERIC_TERMS` with class-noun-not-entity tokens (Schoolwide Learner Outcomes, SLO, SLOs, plan-related class nouns); (e) extend leading-article strip to also drop sentence-initial qualifiers ("Only", "Even", "Just", "Indeed") OR ensure span starts at first content noun. Each fix needs unit-test coverage in `planner/tests/test_freetext_audit.py` derived from this dispatch's actual LLM output (the rejected spans become the test fixtures). The closure should also add a regression-test class to `test_parser_catalogue_enforcement_coverage.py` that feeds realistic prose (not just minimal synthetic fixtures) and asserts the parser correctly admits real catalogue-named entities in comma-list / paren-bearing / cross-sentence shapes. **A1 re-dispatch is BLOCKED on DISC-31 closure**; same blocker applies to any other spec with similar prose shapes (B1's `text`, D1's `label`, A3's `rationale`, A2's `rationale`, US-LLM-27's `rationale`, G1's `condition`/`action`/`scheduled_note`/`channel`, F5's `label`/`location`). DISC-31 closure unblocks experiments 01-04 (and the prose-bearing parts of US-LLM-27/A2/G1/F5); DISC-30 separately blocks F1/F2. Both DISCs need closure before workshop iteration resumes cleanly. | planner._freetext_audit (extract_proper_name_spans + KNOWN_GENERIC_TERMS + audit_freetext); planner/tests/test_freetext_audit.py (new regression fixtures derived from A1 dispatch); planner/tests/test_parser_catalogue_enforcement_coverage.py (extend to realistic-prose coverage); workshop re-dispatch of all prose-heavy specs (01/02/03/04 + 07/13/14/05[free-text-only-once-DISC-30-resolves]) | RESOLVED — `141f8f4` (planner/_freetext_audit.py extractor hardening across all 5 defect classes — comma-list splitting, sentence-boundary respect, paren balance, sentence-initial qualifier strip, KNOWN_GENERIC_TERMS extended with class-noun tokens; A1-derived regression fixtures in test_freetext_audit.py; realistic-prose coverage class added to test_parser_catalogue_enforcement_coverage.py). Pytest 1880 → 1899 (+19 tests); mypy clean (306 files); ruff baseline; test-js unchanged. Three IMPL deviations surfaced (all design refinements, no silent scope expansion): (i) single-token `Strigil` substituted with multi-token `Strigil Outcome` in the negative-test fixture since the audit's multi-token gate intentionally skips single-token spans per DISC-29 carry-forward; (ii) pre-existing `test_wire_planning_method_improvement_types.py` updated to subset semantics (anticipated by directive); (iii) realistic-prose payload for narrative-draft adjusted to comma-list form to match A1 dispatch's actual prose shape. Prose-heavy spec re-dispatch unblocked. |

---

DISC-33-0530-A | 15 | 2026-05-30T10:30:00+0800 / closure-path-settled-via-DEC-43 2026-05-30T11:00:00+0800 / RESOLVED 2026-05-30T12:00:00+0800 | architectural-debt | **spaCy `doc.noun_chunks` is empirically incompatible with the catalogue-label shape this audit's contract requires.** Surfaced by the DEC-42 IMPL slice (Commit 1 `a384c67` landed the spacy>=3.8 + en_core_web_sm 3.8.0 dependency; Commit 2 was written per the plan — lazy `_get_nlp()` singleton + `extract_proper_name_spans` rewrite delegating to `doc.noun_chunks` + deletion of the 5 obsolete helpers — then halted before committing when 23 tests failed in the existing `test_freetext_audit.py` + `test_parser_catalogue_enforcement_coverage.py` suites). Concrete chunk behavior observed via `uv run python -c "import spacy; ..."` on regression-fixture prose: (a) **PP-attachment exclusion** — `Director of Studies` → `['Director', 'Studies']`; `Head of Department` → `['Head', 'Department']`; `Director of Student Welfare` → `['Director', 'Student Welfare']`. Every catalogue Position with `of`-PP is split. spaCy's docs are explicit: *"A base noun phrase, or 'NP chunk', is a noun phrase that does not permit other NPs to be nested within it — so no NP-level coordination, no prepositional phrases, and no relative clauses."* (b) **Coordination-head collapse** — `Bilingual Communicators, Compassionate, Engaged, Innovative, Reflective, and Responsible` → `['Bilingual Communicators']` only; the other 5 SLOs (real catalogue entries) drop. (c) **Paren contents split off** — `SAO - Student Affairs Office (Pastoral and Well-Being)` → `['SAO - Student Affairs Office', 'Pastoral', 'Well-Being']`; the full catalogue label cannot reassemble. (d) **Stopword-coincident codes lost** — `Students in IG1, IG2, AS, and A2` → `['Students', 'IG1', 'IG2', 'A2', 'SLO vocabulary']`; year-group code `AS` is dropped because spaCy tokenizes it as the stopword conjunction "as". (e) **Spurious multi-token chunks** — `PLC cycles`, `SLO vocabulary`, `the SLOs`, `Unit plans`, `an SLO frame`, `Year 4`, `Term 1 unit plans` all emit as multi-token chunks where the prior regex extractor emitted single-token spans the DISC-29 multi-token-only gate intentionally skipped. | The DEC-42 chosen primitive (`doc.noun_chunks`) does not emit catalogue-label-shaped spans. Patching around the PP-attachment / coordination / paren-emission behavior — gluing `Director` + `of` + `Studies` back into one span; gluing comma-list pieces back together; gluing paren content back to the preceding head — would mean reintroducing exactly the kind of post-extraction regex/walking-logic that DEC-42 set out to eliminate (the asymptotic incremental-patching pattern). At that point the extractor is "spaCy as a tokenizer plus a hand-rolled post-process," which is the asymptote DEC-42 declared unacceptable. The empirical finding is that **`doc.noun_chunks` is the wrong primitive for this audit's contract**; not that NLP-grade extraction is wrong, just that this specific API is wrong. **Commit 1 (a384c67) — spacy dependency + model install — landed and remains valid regardless of which extraction API is chosen next.** Commit 2 not landed; working tree clean. | RECORDED + user-decision-required on extraction-API direction. Six options surfaced by the IMPL agent (not in priority order; each independently actionable): (1) **spaCy dependency-parse walk** — use `doc` for tokenization + POS + parse, then walk the dep tree to assemble proper-name spans WITH their PP-attached children + coordinated siblings + paren contents; structural NLP use rather than the chunker API; (2) **NLTK `RegexpParser` over averaged-perceptron tagger** — was rejected in the plan on glue cost; worth re-evaluating on output shape since the rejection ground may not hold; (3) **Stanza** (StanfordNLP) — heavier dep; offers full constituency parsing where catalogue-label shape would land as a noun-phrase subtree; (4) **`en_core_web_lg`/`trf` model swap** — documented `noun_chunks` behavior (no PP attachments) is consistent across spaCy models; unlikely to help on the structural exclusion but may help on smaller defects; (5) **spaCy + benepar** — benepar adds constituency parsing to the spaCy pipeline; the `_.constituents` API DOES include PP-attached NPs; single dep add, same install path; (6) **Hold DEC-42, keep the regex extractor, add the colon-boundary patch for DISC-32 only** — accepts the asymptote DEC-42 rejected but unblocks A1 re-dispatch immediately. Per the plan's "IF YOU GET STUCK" clause (verbatim: "the whole point of DEC-42 is to trust the library; if the library is wrong for our use, that itself is a finding"), the IMPL halted and reported rather than silently patching around it. **Commit 1 not rolled back** — the spaCy dependency is install-side, useful regardless of which extraction API the next slice chooses (options 1, 4, 5 all use spaCy; only options 2/3/6 would make the dep unused, and even then `uv remove` is one command). | planner._freetext_audit (extract_proper_name_spans extraction-API choice); school-improvement-plans/pyproject.toml (the spacy + en_core_web_sm + typer<0.16 deps from Commit 1, kept pending direction); DEC-42 (the slice that surfaced this; the architectural direction is still right at the principle level — NLP-grade extraction — only the specific API was wrong); A1 re-dispatch + cascade 02/03/04/05/06 (all blocked pending direction) | RESOLVED — `394e561` (DEC-43 IMPL). `doc.noun_chunks` is no longer called from `_freetext_audit.py`; the extraction stage now uses spaCy `PhraseMatcher` (gazetteer admission, attr="LOWER" for case-insensitive token-based matching) + spaCy `ner` (fabrication detection on ORG/PERSON/GPE/NORP/FAC tags). All five chunk-behavior incompatibilities documented in the original DISC-33 finding (PP-attachment exclusion, coordination-head collapse, paren contents split off, stopword-coincident codes lost, spurious multi-token chunks) become non-issues because the new architecture does not depend on `noun_chunks` at all. `_get_nlp()` loads `en_core_web_sm` with `disable=["parser", "lemmatizer"]` keeping only the `tok2vec`/`tagger`/`ner` components needed for the fabrication-detection layer. _Prior:_ **CLOSURE-PATH-SETTLED via DEC-43 (2026-05-30T11:00)** — user accepted the Explore agent's reframing: closed-set entity linking + outlier detection, not open noun-phrase extraction. |

---

DISC-32-0530-A | 15 | 2026-05-30T09:22:00+0800 / closure-path-settled-via-DEC-42 2026-05-30T09:45:00+0800 / closure-path-revised-to-DEC-43 2026-05-30T11:00:00+0800 / RESOLVED 2026-05-30T12:00:00+0800 | architectural-debt | **`planner/_freetext_audit.py::extract_proper_name_spans` has a 6th extraction defect class — colon `:` does not terminate spans, causing class-noun-followed-by-list false positives.** Sibling to DISC-31's 5 closed defect classes. Surfaced empirically by the post-ai/0021 A1 re-dispatch (`outputs/2026-05-30-09-22-00/01-narrative-draft.response.json`). The LLM wrote "The school operates with seven Schoolwide Learner Outcomes: Bilingual Communicators, Compassionate, Confident, Engaged, Innovative, Reflective, and Responsible." The extractor's behavior: "Schoolwide Learner Outcomes" IS in `KNOWN_GENERIC_TERMS` (DISC-31 closure added it as a class-noun). But the colon `:` was NOT treated as a span-terminator. "Bilingual Communicators" (a real SLO) attached to the class-noun via the colon → joined span `"Schoolwide Learner Outcomes: Bilingual Communicators"`. All-tokens-generic shortcut failed (Bilingual Communicators isn't generic). Catalogue admit failed (no entry matches the joined span). → ValueError raised. **Only 1 rejected span vs DISC-31's 10**; substantial narrowing of the false-positive surface from this dispatch vs the prior A1, but the long-tail of punctuation boundary classes the prior fixtures didn't cover continues to surface one-by-one. | The DEC-40 + DISC-31 + ai/0021 closure chain has reduced the false-positive surface from 10 spans across 5 defect classes (prior A1 at `outputs/2026-05-30-07-00-44/`) to 1 span / 1 defect class (this A1 dispatch). The remaining defect class is in the same family — span-extraction not terminating at a punctuation boundary. The LLM's prose is otherwise clean (0 fabricated names; 12 catalogue-verbatim Divisions including full hyphenated parens forms like "AAO - Academic Affairs Office" and "SAO - Student Affairs Office (Pastoral and Well-Being)"; all 7 SLOs by name; 4 Year Groups; 3 mission clauses; 6 WASC standards; 7 AFIs; 5 stakeholder groups; VERBATIM directive worked — LLM used `Math` not "Mathematics"). The single failure mode is the colon-boundary extractor defect. **Blocks A1 re-dispatch until closed** + applies to any spec whose prose introduces a list with a colon (`"Year groups: IG1, IG2, AS, A2"`, `"WASC standards: A1, A4, B1, ..."`, `"Stakeholder groups: Students, Parents, ..."` etc. — common natural prose pattern). | RECORDED as parser-side defect, sibling to DISC-31 (mandate-007). Closure direction: add `:` (and likely `;` if not already covered, en-dash `–`, em-dash `—`, slash `/`, ellipsis `…`) to the span-terminator punctuation set in `planner/_freetext_audit.py::extract_proper_name_spans`. The simplest closure tightens span-extraction to ONLY consume capitalized-token-runs separated by spaces; any other punctuation (excluding intra-name characters like `-` in "AAO - Academic Affairs Office" and `(...)` in paren-bearing labels) terminates the span. Regression fixture: the A1 response captured at `outputs/2026-05-30-09-22-00/01-narrative-draft.response.json` — the rejected span `"Schoolwide Learner Outcomes: Bilingual Communicators"` becomes the positive-case test (post-fix, parser should admit because "Schoolwide Learner Outcomes" is in KNOWN_GENERIC_TERMS and "Bilingual Communicators" is a real SLO; the colon terminates the span). Also add fixtures for `;` `–` `—` `/` `…` cases. **Meta-finding**: the single-token (DISC-29) + colon-boundary (DISC-32) + DISC-31's 5-class closure together imply the audit module needs a more comprehensive tokenization+boundary pass than the current incremental patches. Future hardening direction (NOT in DISC-32's immediate scope; surfaced for the user's awareness): replace `_WORD_TOKEN_RE` + ad-hoc punctuation handling with a small grammar (or NLP-library noun-phrase extraction) that handles boundary cases systematically; the current implementation patches each surfacing class but the surface area is the punctuation taxonomy. | planner._freetext_audit (extract_proper_name_spans punctuation-boundary handling); planner/tests/test_freetext_audit.py (regression fixtures from this A1 response); planner/tests/test_parser_catalogue_enforcement_coverage.py (extend realistic-prose coverage to include colon-bearing class-noun-introduces-list patterns); workshop re-dispatch of A1 + every spec whose prose may introduce lists with colons | RESOLVED — `394e561` (DEC-43 IMPL). The colon-boundary case `"Schoolwide Learner Outcomes: Bilingual Communicators"` admits empirically: the spaCy PhraseMatcher matches `Schoolwide Learner Outcomes` (denylisted via `KNOWN_GENERIC_TERMS`) AND matches `Bilingual Communicators` (in catalogue.learner_outcomes) AS TWO SEPARATE token-based hits because the colon is a separate token. Inline integration test in `planner/tests/test_freetext_audit.py` uses the actual A1 dispatch's `current_state` prose (carrying this exact phrase) and asserts `audit_freetext(prose, catalogue)` returns `[]`. The future-tail of punctuation-boundary defect classes (en-dash, em-dash, slash, ellipsis, semicolon-in-prose) all close together at this slice because PhraseMatcher is token-based — any punctuation that spaCy tokenizes as a separate token is automatically a boundary. _Prior:_ **CLOSURE-PATH-SETTLED via DEC-42 (2026-05-30T09:45)** — incremental punctuation-class patching superseded by NLP noun-phrase chunker; DEC-42 IMPL halted at `noun_chunks` mismatch (DISC-33); DEC-43 superseded DEC-42's API choice with PhraseMatcher + ner. |

---

DISC-34-0530-A | 15 | 2026-05-30T12:00:00+0800 / RESOLVED-INLINE 2026-05-30T12:00:00+0800 | architectural-debt | **spaCy `en_core_web_sm` 3.8.0 NER over-triggers + emits sub-spans / super-spans + tags ordinary edu/admin English as ORG/PERSON/GPE.** Surfaced empirically during DEC-43 IMPL (`394e561`) by running the existing test fixtures through the new PhraseMatcher + ner architecture. Three observed NER-characteristic classes — each handled inline in the same IMPL slice rather than escalated: (a) **NER over-triggers on common edu/admin words**: `Dashboard` tagged PERSON; `Attendance` tagged ORG; `Approx` (in "Two hours; Approx") tagged GPE; `fidelity` tagged PERSON; `Convene the review committee` tagged ORG (super-span over a verb-led clause); (b) **NER tags numeric-suffix multi-word forms**: `Term 2` (in "every department's Term 2 unit plans") tagged ORG where `Term` is denylisted and `2` is pure-numeric — similar shape `WASC A1` where `WASC` is denylisted + `A1` is in accreditation_standards; (c) **NER emits SUB-spans inside catalogue labels**: `Department` inside `Head of Department`; `Repton Xiamen` inside `Chiway Repton Xiamen`; `Student Welfare` inside `Director of Student Welfare`. NER also emits SUPER-spans grouping multiple catalogue labels: `Humanities, Sciences, Business and Economics` as one ORG. | Each class would cause false-positive fabrication flags if not handled. The audit's role per DEC-43 is to admit catalogue-named entities and flag only genuine fabrications; ordinary nouns and partial sub-spans of admitted catalogue labels are not fabrications. The class is characteristic of spaCy `en_core_web_sm` (trained on general English news/Wikipedia, not on edu-admin prose). Larger spaCy models (`_lg`/`_trf`) may behave differently but the API contract is the same. | RESOLVED INLINE in `394e561`: (a) closure for over-triggering — `KNOWN_GENERIC_TERMS` denylist extended with `Dashboard`/`Dashboards`/`Attendance`/`Approx`/`Approx.`/`Convene`/`Fidelity`/`fidelity` plus prior DISC-31-populated entries; future workshop dispatches may surface additional NER-over-triggers requiring further denylist extension; (b) closure for numeric-suffix multi-word forms — audit's coverage rule extended to allow pure-numeric tokens (`is_alpha=False`, `like_num=True`) as filler between denylisted/admitted tokens; (c) closure for sub-spans / super-spans — audit's containment-admission rule (sub-span of an admitted catalogue match → admit) + word-token-coverage rule (super-span whose every alphabetic token is covered by admitted matches or denylist → admit). All three closures are local to `_freetext_audit.py`'s fabrication-layer logic; do not require model retraining or library swap. **Future tenant-onboarding pattern**: each tenant's prose may surface new NER over-triggers / sub-span / super-span shapes; the closure pattern is `KNOWN_GENERIC_TERMS` extension and/or coverage-rule refinement, same as for Chiway. | planner._freetext_audit (KNOWN_GENERIC_TERMS + audit_freetext coverage rules); future tenant-onboarding (same NER-characteristic class will surface per tenant) | RESOLVED INLINE — `394e561` (DEC-43 IMPL). |

---

DISC-35-0530-A | 15 | 2026-05-30T12:00:00+0800 / closure-path-settled-via-DEC-44 2026-05-30T14:00:00+0800 / RESOLVED 2026-05-30T14:30:00+0800 | architectural-debt | **spaCy `en_core_web_sm` NER misses edu-domain organizational titles entirely — Option H escalation candidate per DEC-43's fallback path.** Surfaced empirically during DEC-43 IMPL (`394e561`) by the fabrication-positive integration test. Test prose contained 7 invented Position titles from experiment 05 plus 2 additional synthetic fabrications (`Assistant Head for Teaching`, `Real Programme`). spaCy ner caught some via sub-spans (`Bilingual Programme` inside `Vice Principal for Bilingual Programme`; `English Departments` inside `Heads of Chinese and English Departments`; `The Head of Co-Curricular Programme`; `The Head of Student Affairs`; `The Head of Admissions and Communications`) AND missed the following ENTIRELY (no NER tag at all): `Vice Principal for Academics`, `Vice Principal for Bilingual Programme` (full form), `Assistant Head for Teaching`, `Heads of Chinese and English Departments` (the full coordinated form is broken into two partial NER spans), `Head of Co-Curricular Programme` (the full leading `Head of` form — NER tags only when "The" precedes it), `Head of Admissions and Communications` (similar — surfaces only with leading "The"), `Real Programme` (in "The Real Programme 2 begins."). Captured in 4 `@pytest.mark.xfail(strict=True)` decorators in `test_freetext_audit.py` + the `EXP_05_FABRICATIONS_NER_MISSES` list. | The DEC-43 architecture's admission layer (PhraseMatcher) is empirically watertight — every catalogue label in the A1 forensic-capture prose admits. The fabrication-detection layer (spaCy ner) is empirically partial — a meaningful fraction of invented `Vice Principal for X` / `Head of Y` / `Assistant Head for Z` titles are not tagged at all by `en_core_web_sm`'s NER. The class is structural to general-English NER training data (Wikipedia/news rarely names "Vice Principal for X" as an organization). Larger spaCy models (`_lg`/`_trf`) may improve coverage but the gap will persist. The xfail-strict pattern empirically locks current behavior so future model upgrades that close the gap surface as PASSING xfail (which strict=True flags as a test-design event for the orchestrator). | RECORDED + Option H escalation candidate per DEC-43 (data/seed-round-plan.md, 2026-05-30). DEC-43's fallback path was anticipated in the original architectural decision: "LLM-based fabrication detection (Explore agent's Option H) kept as fallback if spaCy `ner` proves too noisy empirically." This DISC documents the empirical evidence that `ner` IS empirically partial — half the test fabrications are missed entirely. **User decision**: whether to escalate to Option H (LLM validator pass on detected-but-unadmitted spans + on org-shaped capitalized sequences NER misses) OR accept the current partial-coverage and rely on the prompt-layer DEC-39/DEC-40 fabrication suppression as the primary defense (parser is then a complement, not a structural-impossibility gate, for the prose-layer fabrication class). The PhraseMatcher admission layer is structurally complete regardless; this is a question about how strong the fabrication-side gate should be. **Closure direction if Option H selected**: add a second pass in `audit_freetext` that scans for capitalized multi-word sequences NOT matched by PhraseMatcher AND NOT tagged by ner; send those candidates to an LLM with the catalogue inlined for a final yes-this-is-an-org-name / no-this-is-ordinary-prose verdict. Cost: ~one extra LLM call per dispatch with ~3-5KB of prompt context (catalogue + prose snippet) + ~few hundred output tokens. Latency: a few seconds added to the Django POST handler flow. Reuses the existing `provider.stream()` infrastructure; no new dependency. **Closure direction if NOT escalated**: accept partial fabrication detection at the parser layer; rely on DEC-39 (prompt-language layer) + DEC-40 (structured-field parser layer) + admin-curated VERBATIM directive to keep edu-domain titles in-catalogue. The xfail-strict tests stay as forensic markers. | planner._freetext_audit (fabrication-detection layer); ai/services/* (Option H would add an LLM-validator call); user decision (which closure direction to pursue); future tenant-onboarding (same NER coverage gap will manifest per tenant's title-prose patterns) | RESOLVED — `d1258a6` (DEC-44 IMPL). Third stage appended to `audit_freetext` per the DEC-44 architecture: capitalized-multi-word leftover scan (new helper `_find_capitalized_multiword_leftovers`) → batched LLM call via new module `planner/_llm_fabrication_validator.py::validate_candidates` (Haiku 4.5; temperature=0.0; max_tokens=1500; structured-JSON output classifying each candidate as `fabricated`/`catalogue-variant`/`ordinary-prose`; only `fabricated` verdicts append to survivors). Trigger fires only when leftover candidates exist; failure-falls-back-without-blocking (provider error / malformed JSON / timeout → logger.warning + return []). All 4 DISC-35 `pytest.mark.xfail(strict=True)` markers flipped to passing positive assertions. Gate: pytest 1948 passed (prior 1938 + 4 xfail flipped + 6 new validator tests); mypy clean (310 source files); ruff baseline 6; manage.py check 0; make test-js 42 unchanged. IMPL surfaced two in-scope refinements (mandate-007): prompt builder uses prose-described JSON shape instead of literal example (avoids mock-regex phantom-candidate detection); walker trims leading sentence-initial stopwords (`In Term 2` does not glue `In Term` as a candidate). Both refinements local to new IMPL scope; not pre-existing fragility. _Prior:_ **CLOSURE-PATH-SETTLED via DEC-44 (2026-05-30T14:00)** — user direction: "let's go ahead and do option h." |

---

DISC-36-0531-A | DEC-45 framing | 2026-05-31 | architectural-debt | _llm_fabrication_validator.py _build_prompt enumerates 13 of 14 CatalogueUnion sub-fields; guiding_clauses omitted | a real clause anchor is invisible to the DEC-44 LLM-validator, risking mis-classification; pre-existing, not DEC-45-caused | recorded during DEC-45 agent verification (mandate-007); confirmed _llm_fabrication_validator.py:163-178 | DEC-45 renames guiding_clauses->mvv_clauses and touches this file; the rename slice fixes the omission | 

---

DISC-37-0531-A | DEC-45 Slice 1 | 2026-05-31 | phase-content-gap | Slice-1 plan made makemigrations' destructive rename-autodetect a STOP condition; the STOP fired on settled-convention behavior | self-inflicted halt; prior renames 0029/0032 hand-author RenameModel for exactly this reason (per their docstrings) | recorded as planning lesson; resolve Slice 1 by hand-authoring 0058 (mirror 0032) + 0059 (mirror 0034); general rule: rename-slice plans instruct hand-authoring not generate-then-STOP | Slices 2/3 also rename/delete models — their plans must instruct hand-authored migrations | 

---

DISC-38-0531-A | DEC-45 Slice 2 | 2026-05-31 | process-gap | mission seed 0060 committed on a scoped check, silently reddened the seed tests; full pytest not re-run | report green only after full gate post-migration | resolved 4f558a5

---

DISC-39-0531-A | DEC-45 process | 2026-05-31 | process-gap | 3 context-currency drifts (frozen-LOG framing, in-progress status broke the focus query, false-green) from narrating state instead of maintaining+verifying the JSON | fix: status contract open/done + priority:next, verify-don't-narrate (re-run session-start queries), full-gate after data migrations | 88e0e60 + CLAUDE.md

---

DISC-40-0602-A | DEC-45 Slice 5 | 2026-06-02 | architectural-debt | reference-data-spec.md asserts pre-DEC-45 names Department/SubjectKind + deleted MissionArea as live seed-vocab (lines 17/24 + section 7 142-144); unmaintained through school/0029+0032 renames | misdescribes current seed vocab on 3 counts; broader than DEC-45 | recorded Slice-5 scoping (mandate-007) | not in Slice-5 scope (which fixes domain-model.md + model-surface-audit.md); needs full sweep if live or archive-marking if frozen | 

---

DISC-41-0603-A | DEC-45 Slice 5 | 2026-06-03 | architectural-debt | model-surface-audit.md inventory header (lines ~9-25) is a pre-DEC-45 stale snapshot: counts 48/17/24 wrong vs current ~20 school/~31 plans; pre-existing omissions independent of DEC-45 | the designated field-authority misstates the model census | recorded during Slice 5 (subagent-surfaced; mandate-007/008); DEC-45 tokens fixed, header not rebuilt | needs an inventory-refresh; subsumed if model-surface-audit is demoted/derived per DEC-0001, else manual recount | 

---

DISC-42-0603-A | Mode-C workshop mvv-consistency | 2026-06-03 | architectural-debt | post-DEC-45/46, 3 production PromptTemplate bodies still carry deleted mission_areas: plan-narrative-draft (full dead block + prose; never migrated), propose-accreditation-standards + propose-responsibilities (residual prose ai/0024 missed) | prompt corpus not fully mvv-consistent; A2/US-LLM-27 name a vocabulary the grounding no longer emits | surfaced by the workshop pass (f2f6a9f); corrected snippets encode the clean versions | future ai/00NN migration mirrors the corrected snippets into production (DEC-41 bridge) | 

---
