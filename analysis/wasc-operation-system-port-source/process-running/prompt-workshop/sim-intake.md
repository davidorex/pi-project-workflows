# Simulation intake — what I need from you

Fill the `>>>` lines. Frame detail is in `sim-revised-frame.md`; process in `SIMULATION-PROCESS-ARC.md`.

## 1. Seed — the one-line intent that starts the plan
```
>>>
```

## 2. Year focus skeleton
Pre-filled from the revised frame (the 9-strand spiral; full table in `sim-revised-frame.md`). Confirm or edit.
```
>>> CONFIRM  /  edits:
```

## 3. SLOs
Set by the revised frame (Grounded · Rounded · Unbounded + sub-traits + the retained Bilingual Communicators). Refine a sub-trait gloss only if you want.
```
>>> (leave blank to use the frame as-is)
```

## 4. Baselines — where divisions start on the focus strands
Provide any you have (`Division | strand | current state`), or write `PLACEHOLDER OK` to let me simulate marked placeholders.
```
>>>
```

## 5. Scope (defaults set — keep or change)
```
Divisions:   >>> default 7-division subset  /  all 16  /  list
Granularity: >>> default action-grain  /  cell matrix
Layer:       >>> default within-skeleton  /  pure per-division
```

*Minimum to run: §1 + §2. Add §4 baselines (or PLACEHOLDER OK) for the arcs. §3 is already set.*
