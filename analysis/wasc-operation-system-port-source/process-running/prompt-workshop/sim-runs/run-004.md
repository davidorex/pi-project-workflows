# Sim run 004 — +College Counseling + DECISION REQUESTS (articulate the need, don't skip)

## 1. Metadata
- **Date:** 2026-06-09
- **Route:** per-department fan-out → synthesis, within the spiral; **4 divisions** (PO, SAO, C&T, **College Counseling Department** — new).
- **Mode:** sim-land (4 parallel sub-agents).
- **Status:** complete; readout compares to run-003.

## 2. Inputs (vs run-003: +College Counseling; + the decision-request element)
- Same seed, revised 9-strand frame, spiral skeleton, dual tracks, stakeholder comms, corrected bilingual, front-load + options posture (runs 002–003).
- **NEW (D) — Decision requests / "articulate the need":** wherever a plan element needs a decision/input the division doesn't own (growth-target increment, precursor leadership/PO decision, data system, policy, another division's output), the division emits an explicit DECISION REQUEST (what · from whom · why · blocks/unblocks · recommendation) instead of a silent placeholder. PO consolidates these upward.
- **NEW division — College Counseling Department** (College Counseling Department Coordinator + College Counselor; IG1→A2 phased programme): natural owner of Unbounded·Seeking-opportunity + Unbounded·Intellectually-curious/internationally-minded; strong contributor to Bilingual (application English) + Reflective (portfolio).
- Baselines placeholder; action-grain; within-skeleton.

## 3. Process
- 4 `general-purpose` sub-agents IN PARALLEL. IDs: C&T `a729cafb82fd86ed3` · SAO `a58848c41f8c1c8de` · College Counseling `a56ef075a81ce9272` · PO `a08f73e467ed4486c`.
- Orchestrator synthesis + readout.

## 4. Results (condensed; full front-load + options as runs 002-003, plus the new Decision-Requests sections)

### 4a. C&T (a729cafb82fd86ed3)
Front-load: [STUDENT] lesson-artifact scan + exit-ticket strand-naming; [STAFF behavior] observation rubric SLO-embedding criterion (primary) vs template-completion (compliance, secondary); bilingual = in-lesson-adapted English-usage survey. Consolidated comms = one "SLO-in-Practice" termly brief (bilingual, simultaneous). Options across 4 focuses (lesson-design embedding / teaching-eval surfacing / PD / student-facing visibility), each 2-3 "OPTIONS — DM decides".
**Decision Requests:** DR-1 growth increments; DR-2 authorize SLO criterion on the teaching-eval rubric; DR-3 canonical 9-strand list + values-mapping rendered in the template; DR-4 cross-division portfolio platform owner; DR-5 PD time allocation; DR-6 ratify the spiral as binding curriculum spine.

### 4b. SAO (a58848c41f8c1c8de) — out-of-lesson
Front-load: [STUDENT] Year-Leader-administered baseline pulse (stage-worded IG1/IG2/AS/A2) + HRT/Dorm observation tally; [STAFF] compliance self-report (secondary) vs peer/leader observed-enactment (primary) + the verbatim English-usage survey (bilingual primary). Consolidated comms = one monthly SAO pastoral bulletin + single feedback intake. Options across 4 out-of-lesson focuses (spiral-strand pastoral embedding / dorm Grounded ownership / bilingual always-on out-of-lesson / values-ethos + SLO-moment portfolio), each 2-3 "DM decides".
**Decision Requests:** DR-1 growth increment; DR-2 SAO pastoral data/log system; DR-3 Year-Leader stage-wording authorization; DR-4 English-dept coordination; DR-5 pastoral-policy authorization for dorm routines; DR-6 baseline values set; DR-7 consolidated-channel authorization.

### 4c. College Counseling Department (a56ef075a81ce9272) — NEW
Front-load: [STUDENT] year-group-phased pathway-articulation instrument (IG1 reflective intake → A2 destination + SLO/English-competency mapping); [STAFF/counselor behavior] SLO-framed touchpoint tally + self-efficacy + counseling-adapted English-usage survey (bilingual primary). Consolidated comms = one termly "College Counseling SLO & Pathways brief". Options across 4 focuses: **May Seeking-opportunity + portfolios** (pathways-portfolio entry / opportunity-board + conferencing / peer achievement-story showcase); **Apr Intellectually-curious/internationally-minded** (global-pathways exploration unit / alumni international sessions); **Bilingual application-English** (English-conducted counseling touchpoints / personal-statement+interview English workshops); **Jun synthesis** (phased SLO-pathways synthesis conference / coordinator synthesis brief). Each 2-3 "DM decides".
**Decision Requests:** DR-1 growth increment; DR-2 single data home for counseling SLO data; DR-3 master bilingual English-usage survey instrument (adapt not rebuild); DR-4 counselor SLO→counseling crosswalk sign-off; DR-5 May portfolio interface/ownership; DR-6 A2 end-of-cycle calendar accommodation.
*College Counseling cleanly OWNS the Unbounded strands run-003 left only retrieved — resolves the coverage gap for those strands; its IG1→A2 phasing maps to the spiral over-time progression.*

### 4d. PO (a08f73e467ed4486c) — governance + the consolidation (the headline)
- **Cycle-start GATES (sequential G1→G2→G3):** G1 baseline parity-locked (9 strands × 2 tracks = **18-cell coverage matrix**; a strand not "measured" until both cells exist; <18 → return naming empty cells). G2 measurement statements (instrument·cadence·success per cell; every staff measure labelled COMPLIANCE/BEHAVIOR-CHANGE; every target = baseline+[DM-INPUT] or returned — PO never invents numbers). G3 precursor sweep (each prerequisite owner+resource+ready-by; unresourced precursor HOLDS its strand).
- **Two-level anti-overload (resolves run-003 finding #1):** Level-0 within a point (consolidate to 2-4 combinable options + 1 recommendation); **Level-1 across points** (sequence the points by what they block; mark cross-division combinable decisions, present once).
- **CONSOLIDATED DECISION-REQUEST BACKLOG (the standout):** de-duped + grouped + sequenced by what each unblocks, with decider + unblocks + recommendation + cross-division combination:
  - **Tier 0 Foundation:** D1 canonical SLO list + values-mapping · D2 values baseline set (bundle w/ D1) · D3 spiral as binding curriculum spine.
  - **Tier 1 Systems:** D4 portfolio platform owner (**COMBINED C&T+CC+SAO — one platform, one owner, divisions are tenants**) · D5 SAO pastoral data/log system (shares data layer w/ D4) · D6 PD time allocation (funds staff behavior-change across all strands).
  - **Tier 2 Policy:** D7 teaching-eval rubric authorization (C&T; needs D1) · D8 pastoral-policy + Year-Leader stage-wording (SAO; needs D5) · D9 English-dept coordination (**COMBINED SAO+CC**).
  - **Tier 3 Targets:** D10 growth-target increments **all 4 divisions, one sitting, AFTER G1 baselines land** · D11 bilingual all-staff governance (needs D6, D9).
  - **Critical path: D1 → D6 → D10.** D1 is highest-leverage (blocks all baselines); D10 undraftable until baselines exist.
- Governance arcs: Launch Charter (sign D1-D3 + run gates) → Accountability+Resourcing (monthly vs G3 ledger; hold strands on slipped precursors) → Dec/Mar/May synthesis beats → Bilingual all-staff governance (Feb spotlight, always-on) → Jun re-measure-all-9-both-tracks → report → revise (next cycle's charter revised FROM this).

## 5. Readout — run-003 → run-004
**The decision-request element landed strongly — this is the most mature run:**
- **Divisions articulate needs instead of skipping.** Every division surfaced an explicit Decision-Requests section (6-7 DRs each) — growth increments, data systems, policy authorizations, cross-division coordination — rather than silent `[DM-INPUT]`/`[SIM-PLACEHOLDER]`. The human-input boundary (run-003 finding #2) is now a first-class, actionable surface.
- **PO consolidation is the emergent gold:** a **tiered, dependency-sequenced, cross-division-combined decision backlog** with deciders, unblock-mapping, recommendations, and a **critical path (D1→D6→D10)**. This is precisely the "what leadership must decide, in what order, without overload" artifact — the upward flow complementing the downward options.
- **Two-level anti-overload (run-003 finding #1) resolved** — PO's Level-0 (within point) + Level-1 (sequence points + combine cross-division).
- **Coverage improved (run-003 finding #4):** College Counseling cleanly OWNS the Unbounded strands previously only retrieved (Seeking-opportunity, Intellectually-curious/internationally-minded) + the application-English bilingual share + the Reflective portfolio. Its IG1→A2 phasing fits the spiral.
- **Values strand (run-003 finding #3)** now handled correctly — surfaced as an explicit decision (D2 values baseline set) rather than a silent gap.

**New findings (candidates for filing):**
1. **The human inputs have a DEPENDENCY STRUCTURE, not a flat list.** PO's backlog tiers them (foundation→systems→policy→targets) with a critical path. So the act-1/act-3 inputs must be presented to leadership as a **critical-path-ordered decision queue**, not a flat ask — and some inputs (baselines) gate others (targets) by construction.
2. **A class of SHARED-INFRASTRUCTURE precursors recurs across divisions** — a data home/log system + a master measurement-instrument (English-usage survey) + a portfolio platform were independently requested by C&T, SAO, AND College Counseling. These are schoolwide prerequisites decided ONCE (PO combined them: D4/D5 data layer, D3-instrument), distinct from division-specific precursors. The synthesis layer must detect + combine cross-division precursors.
3. **Cross-division decisions need an above-division owner** — portfolio platform, English-dept interface, growth increments are decisions no single division can make; PO/leadership is their home. Confirms the PO/synthesis layer as the cross-division decision consolidator.
4. **Coverage still not all-9-owned** — Politically-informed (wants Humanities), Technologically-fluent (wants IT/Future Scholar) remain thin; widening works, full coverage needs the full division set.
5. **FEAT-006 (the synthesis layer) is now well-specified by accretion:** it must run (a) the 3 gates incl. 18-cell parity, (b) two-level option consolidation, (c) the tiered/critical-path decision-request backlog, (d) cross-division precursor + decision combination, (e) comms-dedup, (f) target reconciliation. The sim has effectively written FEAT-006's acceptance criteria.

### 5a. Post-run correction (2026-06-09, human) — strand-ownership was a foreclosure
Runs 003/004 told divisions which strands they "own" (e.g. College Counseling = "natural owner of Unbounded").
That FORECLOSES and re-introduces concentration at the strand level. Corrected by the **fractal/holographic
principle** (`../sim-revised-frame.md`): each shard contains and IS the whole, in its dimension — every division
refracts ALL 9 strands (+ vision/mission/values) through its own purview, owning none exclusively. The next run's
briefs must NOT assign strands to divisions; they ask each division to refract the WHOLE through its lens (and say
where its refraction of a strand is faint rather than ignore it). This also reframes the coverage finding (#4):
"all-9-owned" is the wrong target — every strand is engaged by every division in its dimension; coverage = the
whole present everywhere, refracted.

## 6. Seeds-next
- The framework is mature (front-load gates + downward options + upward decision-requests + PO consolidation with critical path). Strongest next moves: (a) supply REAL baselines + the D10 increments + precursor confirmations to run a SUBSTANTIVE cycle past the gates into concrete targets; (b) widen to the full division set for all-9 ownership; (c) begin converting the framework to FGAP/DEC/R-0009 filings (the front-load gates, the options/decision-request duality, the critical-path backlog, the FEAT-006 synthesis spec are strong governing candidates) to drive the workshopping revisions.
