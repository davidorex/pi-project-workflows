# Orchestrator state — session handoff

Living state of the orchestrator's view of the project. Updated by the main-context orchestrator after every commit, every subagent invocation, and every user direction. A fresh Claude Code session reads this file first to pick up exactly where the prior session left off — zero context loss.

**Three-layer persistence** (zero-loss): (1) `git log` — the commit substrate, never lags. (2) `ORCHESTRATOR-LOG.md` — the **append-only event spine**: one immutable line per event (commit, user direction, decision, established definition, recorded gap, correction), in order, never rewritten. (3) **this file** — the derived synthesis (current position, pending, conventions), reproducible from (1)+(2). The synthesis can trail; the log and git cannot. A fresh session reads this synthesis for orientation, then the `ORCHESTRATOR-LOG.md` tail + `git log` for exact catch-up.

**Last updated:** 2026-05-31T00:00+0800 (Asia/Shanghai)

**Session status (2026-05-31):** DEC-45 ESTABLISHED (decision + field survey retained at `88164d3` + `8856df9`) but UNIMPLEMENTED — its IMPL was attempted, produced 9 failing tests, and all three IMPL commits were excised via `git rebase --onto 528c649 ff5c1ec`. Full suite is back to 1948 passed; the latest `.context/session-notes` SESSION-004 is the authoritative current-state record. NEXT: re-plan DEC-45 IMPL with smaller commit boundaries and the mechanical rename done deterministically, not via a single sprawling agent dispatch.

**Prior session status preserved for continuity:** A1 WORKSHOP DISPATCH PASSED LIVE END-TO-END POST-DEC-44 (`3780bac`). First live workshop dispatch where all 4 fabrication-closure layers compose successfully against fresh LLM-assist output. 4 OpenRouter LLM-validator HTTP-200 calls fired across the accepted dispatch. Empirical iteration loop produced 4 architectural-completeness fixes in `planner/_freetext_audit.py` + 1 validator config fix: (1) `CatalogueUnion.admits` plural-form fallback (handles `Homeroom Teachers` vs catalogue's `Homeroom Teacher`); (2) `_is_admitted_by_matcher` Rule (b) lowercase-content-word filler (handles NER super-spans like `School Culture carries Compassionate, Responsible` where lowercase verb bridges capitalized catalogue tokens); (3) `build_catalogue_union` `guiding_statements` extraction (mission anchors `GROUNDED`/`ROUNDED`/`UNBOUNDED` + full clause text); (4) `KNOWN_GENERIC_TERMS` extension with `Stakeholder`/`Stakeholders` class-nouns; (5) validator `_DEFAULT_MODEL` changed from Anthropic-native `claude-haiku-4-5` to OpenRouter routing format `anthropic/claude-haiku-4.5`. System-default `LLMProviderConfig` added via manage.py shell (DB write, not version-controlled): user=NULL, provider=openrouter, api_key copied from user-3, default_model=anthropic/claude-opus-4.7. **Gate**: pytest 1948 passed (unchanged baseline — no regression); mypy clean 310; ruff baseline 6; make test-js 42. Two forensic captures at `prompt-workshop/outputs/2026-05-30-15-08-00/` (initial rejected) + `prompt-workshop/outputs/2026-05-30-17-38-27/` (post-fixes accepted; apply.py merged into draft.plan). **Workshop iteration cascade**: A1 ✓; 02 (A3 propose-domain-alignment) / 03 (D1 propose-milestones) / 04 (B1 draft-success-criteria) / 05 (F1 decompose-action-steps) / 06 (F2 propose-assignments) ready to resume on user direction. DEC-44 + the 4 architectural fixes apply uniformly to every spec's prose-bearing fields.

**Architectural insight from this iteration arc**: the empirical-iteration loop the workshop dispatch implements is the canonical way new false-positive classes get surfaced and closed. Each dispatch through novel LLM output reveals catalogue/denylist/Rule-completeness gaps that synthetic tests don't cover. The 4 architectural fixes are cumulative completeness improvements to the DEC-43/44 architecture; not new architectural-debt items. Persistence-in-same-turn observed: fixes landed in the same commit (3780bac) as the forensic captures.

**Standing-open refresh**: DISC-35 STRUCK (RESOLVED at d1258a6 per prior status block). NEW: 4 operational completeness findings recorded in 3780bac commit message + the 17:40 LOG event (not promoted to DISC class — they're fixes-already-landed not architectural-debt-pending). Carry-forward standing-opens unchanged (DISC-23/24/25/26, Plan.continuation_narrative, US-LLM-23/24/25 orchestration trio, carry-forward 4+5, US-DRAFT-1/2/3, planner UX decision). pi-context substrate adoption arc unchanged (Tier-3 + schema widens + 3 custom block kinds + DEC/DISC migration all standing).

**Prior session status preserved for continuity:** DEC-44 IMPL LANDED (`d1258a6`) — Option H LLM-validator third stage deployed; DISC-35 RESOLVED; fabrication closure structurally complete across all 4 layers. All 4 prior `pytest.mark.xfail(strict=True)` markers flipped to passing positive assertions. Gate: pytest 1948 passed (prior 1938 + 4 xfail flipped + 6 new validator tests); mypy clean (310 source files); ruff baseline 6; make test-js 42 unchanged. Empirical state: Layer 1 (DEC-39 prompt-language) + Layer 2 (DEC-40 structured-field parser) + Layer 3a (DEC-43 PhraseMatcher admission) + Layer 3b (DEC-43 spaCy ner) + Layer 3c (DEC-44 LLM-validator second pass) compose the complete fabrication closure end-to-end. Workshop iteration cascade fully unblocked. **Slice A (Option H IMPL) complete.** Slice B (A1 re-dispatch via Mode C) follows per the user's two-part direction.

**Mandate-007 IMPL surfaces** (recorded for next-baseline calibration; not DISC-class): (1) plan's expected "3 pre-existing axes-backend failures in `users/tests/`" did not reproduce — full suite 1948/1948 green; baseline-state change. (2) IMPL's two in-scope refinements (prose-described JSON output shape in `_build_prompt`; leading-stopword trim in `_find_capitalized_multiword_leftovers`) are local to new IMPL scope, not pre-existing fragility classes.

**Prior session status preserved for continuity:** DEC-44 ESTABLISHED — Option H landed as DEC-43's fabrication-detection-layer empirical-gap closure (DISC-35 resolution); IMPL slice next. User direction: implement Option H now + then proceed with the next workshopping step (A1 re-dispatch presumed). Architecture: third stage appended to `audit_freetext` after PhraseMatcher + spaCy ner — scans for capitalized-multi-word leftover spans → batched LLM call (claude-haiku-4-5) to new module `planner/_llm_fabrication_validator.py` → structured-JSON classification per candidate → `fabricated` verdicts append to survivors. Triggers only when leftover spans exist; failure-falls-back-without-blocking; reuses `provider.stream()`. Plan-mode for IMPL begins this turn after this tracking commit. Two-slice arc: (A) Option H IMPL via canonical Mode B pipeline; (B) A1 re-dispatch via Mode C iteration after Option H lands.

**Prior session status preserved for continuity:** pi-context substrate adoption in flight — `.context/` baseline-committed, operator how-to doc landed, 44 conventions entries populated, SESSION-003 records this session's full arc. No code or workshop changes since the 12:00 status block — all activity since has been pi-context-adoption tracking work (substrate + scripts baseline + operator doc + conventions populate + Mode B dedupe + doc exactness pass). Substrate is the orientation surface from this point; MD spine remains the detail surface until DECs/DISCs/stories migrate. 

**Workshop iteration (unchanged from 12:00):** ARCHITECTURALLY UNBLOCKED. DEC-43 IMPL landed (`394e561`); admission layer (PhraseMatcher) empirically watertight on captured A1 prose; fabrication-detection layer (spaCy ner) empirically partial — DISC-35 user-decision pending on Option H escalation. A1 re-dispatch + cascade 02/03/04/05/06 ready to resume on user direction. Live re-dispatch deferred (the inline integration test exercises the same parser code path).

**pi-context adoption arc this session** (9 commits between 12:15 and 13:15):
- `d925061` — baseline-commit `.context/` substrate + `scripts/orchestrator/` + `.pi/` + `.workflows/` + `.pi-context.json` (97 files / 10316 insertions)
- `27ea49d` — operator how-to doc landed at `docs/2026-05-30-pi-context-claude-code-howto.md` (400 lines, canonical operator-side reference)
- `92e5f3f` — SESSION-003 appended to `.context/session-notes.json` (first orchestrator-script substrate write; validates the operational mode end-to-end)
- `ef175db` — how-to doc Environment-prerequisites section + NODE_PATH workaround
- `b7532d0` — conventions block Tier-1 populate (42 entries via writeBlock from `/tmp/load-conventions.mjs`; absolute file:// import workaround discovered for ESM mode)
- `52dca7b` — how-to doc ESM-vs-CJS divergence note
- `3b6adbe` — conventions block Tier-2 append (3 working-mode entries via file-block-item.ts loop; 42 → 45)
- `28a9ec6` — Mode B convention dedupe (45 → 44; updated `mode-B-canonical-pipeline-shape` with full body; removed `mode-B-architecture-refinement-slice`)
- `f83114c` — how-to doc exactness pass (Block-api function reference + Upsert + Remove + Bulk-write workflows + Common-errors row for ERR_MODULE_NOT_FOUND)

**Conventions block (44 entries) is now the canonical queryable project-discipline source.** Categories: 9 mandates + 10 orchestrator-discipline + 3 sub-agent-posture + 10 commit + git-safety + 2 newly-load-bearing (NODE_PATH + no-out-of-scope) + 3 working-mode (mode-A/B/C; mode-B is the deduped long-canonical version) + 7 promoted-from-feedback. Distribution: 38 enforcement=manual + 6 enforcement=review; 40 severity=error + 4 severity=warning. Query via `NODE_PATH=/Users/david/Projects/workflowsPiExtension/node_modules npx tsx scripts/orchestrator/filter-block-items.ts --block conventions --field <f> --op <op> --value <v>` or `read-block-item.ts --block conventions --id <id>`. The prose-summary versions of orchestrator-discipline + sub-agent-posture + Mode A/B/C still live in CLAUDE.md (IDE-loadable surface); the substrate is the queryable mirror.

**Substrate operational invariants** (now landed as conventions):
- Orchestrator-script invocations require `NODE_PATH=/Users/david/Projects/workflowsPiExtension/node_modules` prefix (CJS scripts)
- ESM loaders (.mjs / npx tsx -e) require absolute `file://` URL imports (NODE_PATH doesn't apply to ESM bare-specifier resolution)
- Writer attestation explicit per write: `--writer human:davidryan@gmail.com` (CLI flag) or `{kind: "human", user: "davidryan@gmail.com"}` (library ctx)
- block-api remove function is `removeFromBlock` (not `removeItemFromBlock`); append is `appendToBlock` (not `appendItemToBlock`)
- Auth-gate bypassed from orchestrator-script side per the dual-surface convention; only Pi-REPL tool dispatch fires `ctx.ui.confirm`

**Standing-open items refresh** (updated since 12:00):
- DISC-35 (Option H escalation candidate; user-decision pending on whether to add LLM-validator second pass on capitalized multi-word sequences NER misses) — unchanged
- pi-context schema widens (decisions `^DEC-\d{4}$` → `^DEC-\d{1,4}$` + x-prompt-budget relaxation; framework-gaps id pattern accepts DISC-N-DATE-LETTER + status enum extension + new kind/timestamp_chain/resolved_by_commit fields; story id widen + status enum `pending|enabled|vivified` + vivifying_commit field) — prerequisite to DEC/DISC/story migration
- 3 custom block kinds pending (workshop-experiment / prompt-spec / migration) — design surface for the project's distinctive entity classes
- Tier-3 project-info reference data (dev DB DSN, ports, directory layout, key Make targets, NODE_PATH value) — design decision pending (custom `project-info` block OR remain in CLAUDE.md)
- Carry-forward from prior sessions: DISC-23 (planner Plan.guiding_clauses capture) / DISC-24 (planner Plan.predecessors capture) / DISC-25 (Person picker browser verification) / DISC-26 (11-gap prompt-corpus audit; closure path = Mode C iteration which is now unblocked) / Plan.continuation_narrative / US-LLM-23/24/25 orchestration trio (DEC-29 north-star) / carry-forward 4+5 / US-DRAFT-1/2/3 / planner UX decision

**Session-start ritual for next session:**
1. Read `MANDATES.md` (binding)
2. Read this `ORCHESTRATOR-STATE.md` (synthesis layer)
3. Read `.context/session-notes.json` SESSION-003 (highest-density single-file orientation; carries this session's full arc)
4. Read `ORCHESTRATOR-LOG.md` tail (the 9 events appended at 12:15-13:15 cover the pi-context adoption arc)
5. `git log --oneline -20` + `git status`
6. Read `docs/2026-05-30-pi-context-claude-code-howto.md` if doing any substrate write or running any orchestrator script

Local 11 commits ahead of origin/main; push direction is user-decision.

**Prior session status preserved for continuity:** DEC-43 IMPL LANDED (`394e561`) — gazetteer admission layer is **DEC-43 IMPL LANDED (`394e561`) — gazetteer admission layer is empirically watertight; fabrication-detection layer is empirically partial (DISC-35, Option H escalation candidate pending user decision).** One-commit IMPL slice rewrote `planner/_freetext_audit.py` per the DEC-43 architecture: spaCy `PhraseMatcher` (gazetteer admission via `attr="LOWER"` token-based exact-match over every catalogue label) + spaCy `ner` (fabrication detection on ORG/PERSON/GPE/NORP/FAC tags). External API preserved — all 12 `audit_prefill` call sites in `planner/specs.py` continue to work unchanged. Gate: pytest 1938 passed + 4 xfailed (xfail count rose surfacing the NER coverage gap per DISC-35); mypy clean (308 source files); ruff baseline-only; manage.py check 0; make test-js 42 unchanged. **A1 forensic-capture admission integration test PASSED** — 0 fabrications across all 5 A1 prose fields against the Chiway-fixture catalogue, including the DISC-32 colon-boundary case. **DISC-29 / DISC-32 / DISC-33 / DISC-31-residual RESOLVED at `394e561`**. **Fabrication-positive test surfaced DISC-35** — spaCy `en_core_web_sm`'s NER misses several edu-domain fabrications entirely (`Vice Principal for Academics`, `Assistant Head for Teaching`, `Head of Co-Curricular Programme` leading-form, etc.); captured in 4 `xfail(strict=True)` decorators as forensic markers. **User decision pending on DISC-35**: escalate to Option H (LLM-validator second pass on capitalized multi-word sequences NER misses) OR accept current partial-coverage at fabrication layer (admission layer is structurally complete regardless; rely on DEC-39 prompt-language layer + DEC-40 structured-field layer + VERBATIM directive for prose-layer fabrication suppression). **IMPL agent surfaced DISC-34 inline-resolved**: 3 spaCy NER characteristic classes (over-triggers on common edu/admin words; numeric-suffix multi-word forms; sub-spans/super-spans of catalogue labels) handled via `KNOWN_GENERIC_TERMS` denylist extension + pure-numeric-token coverage rule + containment/word-coverage rules. **Standing-open refresh**: A1 re-dispatch UNBLOCKED; workshop iteration cascade 02/03/04/05/06 UNBLOCKED. New standing-open: DISC-35 user-decision. Local 24 commits ahead of origin/main; push direction is user decision. Carry-forward standing-opens unchanged (DISC-23/24/25/26, Plan.continuation_narrative, US-LLM-23/24/25 orchestration trio, carry-forward 4+5, US-DRAFT-1/2/3, planner UX decision).

**Prior session status preserved for continuity:** DEC-43 ESTABLISHED — DEC-42's specific extraction-API direction superseded; the principle stands. User accepted the Explore agent's reframing of DISC-33: the prior framing was open noun-phrase extraction; the correct framing is closed-set entity linking + outlier detection. The catalogue per tenant is a closed admin-curated set (~200 labels) known at parse time; any noun-phrase parser fights the catalogue-label grammar (PP-attachments like `Director of Studies`, hyphen bridges like `SAO - Student Affairs Office`, paren suffixes, coordinated labels like `Business and Economics`). **DEC-43 architecture**: spaCy `PhraseMatcher` (gazetteer admission — deterministic exact-match against every catalogue label; guaranteed correct on hard cases including comma-and-Oxford-and lists, colon-boundary, paren-bearing labels, hyphen bridges) + spaCy `ner` (fabrication detection — flags ORG/PERSON/GPE-tagged spans not in gazetteer; `KNOWN_GENERIC_TERMS` denylist from DISC-31 filters class-noun false positives). LLM-based fabrication detection (Explore agent's Option H) kept as fallback if `ner` proves empirically noisy. **Resolution scope of DEC-43 closure**: DISC-29 + DISC-31 long-tail + DISC-32 + DISC-33 all close together; entire future-tail of punctuation-boundary defect classes eliminated structurally because PhraseMatcher is token-based and the catalogue IS the matcher config (data-driven). **What's retained from DEC-42 IMPL**: Commit 1 `a384c67` (spaCy 3.8 + en_core_web_sm 3.8.0 + typer<0.16) — both DEC-43 primitives use the existing install. **NEXT-SESSION FOCUS**: implement DEC-43 — rewrite `planner/_freetext_audit.py::extract_proper_name_spans` to use PhraseMatcher + ner; migrate test fixtures in `planner/tests/test_freetext_audit.py` + `planner/tests/test_parser_catalogue_enforcement_coverage.py`; gate green; tracking commit. Per DEC-41 the fix lands once for workshop + production. Plan-mode underway this turn; canonical Mode B pipeline will dispatch IMPL after ExitPlanMode user approval. **Standing-open list**: DISC-29 / DISC-31-residual / DISC-32 / DISC-33 all close at DEC-43 IMPL land; A1 re-dispatch + cascade 02/03/04/05/06 unblock at that point; carry-forward standing-opens from prior sessions (DISC-23/24/25/26, Plan.continuation_narrative, US-LLM-23/24/25 orchestration trio, carry-forward 4+5, US-DRAFT-1/2/3, planner UX decision) all unchanged. Local 22 commits ahead of origin/main; push direction is user-decision (no autopilot push).

**Prior session status preserved for continuity:** DEC-42 IMPL IN FLIGHT — Commit 1 LANDED (`a384c67`: `spacy>=3.8` + `en_core_web_sm 3.8.0` + `typer<0.16` deps), Commit 2 HALTED PER MANDATE-008. DISC-33 recorded: spaCy `doc.noun_chunks` is empirically incompatible with the catalogue-label shape this audit's contract requires. Concrete chunk evidence on regression fixtures: (a) **PP-attachment exclusion** — `Director of Studies` → `['Director','Studies']`; `Head of Department` → `['Head','Department']`. spaCy's docs are explicit that noun_chunks deliberately excludes PPs, NP-coordination, and relative clauses. (b) **Coordination-head collapse** — `Bilingual Communicators, Compassionate, Engaged, ..., Responsible` emits only `['Bilingual Communicators']`; the 5 remaining SLOs drop. (c) **Paren contents split off** — `SAO - Student Affairs Office (Pastoral and Well-Being)` → `['SAO - Student Affairs Office','Pastoral','Well-Being']`; full catalogue label cannot reassemble. (d) **Stopword-coincident codes lost** — year-group `AS` dropped because spaCy classifies it as the conjunction "as". (e) **Spurious multi-token chunks** — `PLC cycles`, `SLO vocabulary`, `Year 4`, `Term 1 unit plans` emit as multi-token chunks where the prior regex emitted single-token spans the DISC-29 multi-token-only gate intentionally skipped. 23 tests fail in `planner/tests/test_freetext_audit.py` + `planner/tests/test_parser_catalogue_enforcement_coverage.py` against the chosen primitive — failures are evidence, not fixture migration matters. Per the plan's "IF YOU GET STUCK" clause ("the whole point of DEC-42 is to trust the library; if the library is wrong for our use, that itself is a finding"), IMPL halted rather than patching around the structural behavior (which would reintroduce exactly the regex-walking-logic DEC-42 set out to eliminate — gluing `Director`+`of`+`Studies` back into one span; gluing comma-list pieces; gluing paren content back to head). Working tree clean. **Commit 1 NOT rolled back** — spaCy dep is install-side, useful regardless of which next-API the user picks. DEC-42 architectural direction (NLP-grade extraction) is right at the principle level; only the specific API (`doc.noun_chunks`) was wrong. **USER DECISION REQUIRED on extraction-API direction.** Six options surfaced in DISC-33 (not in priority order): (1) **spaCy dependency-parse walk** — use `doc` for tokenization/POS/parse, walk dep tree to assemble spans with PP-attached children + coordinated siblings + paren contents; structural NLP use rather than the chunker API; (2) **NLTK `RegexpParser` over averaged-perceptron tagger** — rejected in the plan on glue cost; worth re-evaluating since the rejection ground may not hold; (3) **Stanza** (StanfordNLP) — heavier dep; offers full constituency parsing where catalogue-label shape lands as an NP subtree; (4) **`en_core_web_lg`/`trf` model swap** — documented `noun_chunks` behavior (no PP attachments) is consistent across spaCy models; unlikely to help on the structural exclusion; (5) **spaCy + benepar constituency parser** — benepar adds constituency parsing to the spaCy pipeline; the `_.constituents` API DOES include PP-attached NPs; single dep add, same install path; (6) **Hold DEC-42, keep the regex extractor, add the colon-boundary patch for DISC-32 only** — accepts the asymptote DEC-42 rejected but unblocks A1 re-dispatch immediately. Local is 21 commits ahead of origin/main; A1 re-dispatch + cascade 02/03/04/05/06 remain blocked pending direction.

**Last updated:** 2026-05-30T09:45+0800 (PRIOR — preserved for continuity)
**Last updater:** main-context orchestrator (**DEC-42 ESTABLISHED — NEXT-SESSION FOCUS: REPLACE `_freetext_audit.py::extract_proper_name_spans`'s AD-HOC REGEX WITH NLP NOUN-PHRASE CHUNKER (spaCy / NLTK / similar)**. User directive: "update all our updatable context so next session knows exactly the focus." This is THE single concrete next action for the next session. **Why DEC-42** (the reasoning that drove the decision): the empirical re-dispatch loop documented DISC-29 + DISC-31's 5 closed classes + DISC-32 colon-boundary as an asymptotically-converging incremental-patching pattern; each dispatch surfaces a new punctuation-boundary defect class; the LLM-prose-pattern space is too large to enumerate boundary-by-boundary via regex. NLP noun-phrase chunking is the structural alternative: long-solved by mature libraries; punctuation boundaries (commas, colons, semicolons, parens, em-dashes, en-dashes, slashes, ellipses, sentence terminators, possessives) all come for free; eliminates the entire long-tail of defect classes in ONE landing. **Engineering shape for DEC-42 IMPL**: (1) add dependency to `school-improvement-plans/pyproject.toml` (recommended: spaCy with `en_core_web_sm` model; alternatively NLTK + averaged_perceptron_tagger); (2) rewrite `planner/_freetext_audit.py::extract_proper_name_spans` to call the chunker — return noun-phrase spans; catalogue lookup logic (`CatalogueUnion::admits`, `KNOWN_GENERIC_TERMS`) stays unchanged; (3) migrate existing regression fixtures in `planner/tests/test_freetext_audit.py` + `planner/tests/test_parser_catalogue_enforcement_coverage.py` to assert against the new extractor's outputs; the existing DISC-31 + DISC-32 + DISC-29 fixture cases must all pass post-rewrite; (4) gate green; commit. **Resolves on landing**: DISC-29 (single-token gap), DISC-32 (colon-boundary), and the entire future-tail of punctuation-boundary classes; supersedes DISC-31's incremental-patching closure direction (the 5 closed classes remain valid but the underlying approach evolves). **Project pattern alignment**: admin-curated catalogue stays as source of truth; parser stays as structural enforcement gate; only the noun-phrase extraction stage swaps from regex to library. Per DEC-41, lands once for both workshop iteration AND Django service dispatch. NOT a model change; NOT a migration; pure Python module + dependency. **After DEC-42 lands**: A1 re-dispatch resumes (the colon-boundary span that DISC-32 documents should now admit correctly); 02/03/04/05/06 cascade unblocks; workshop iteration loop converges end-to-end on the post-DEC-40 + DEC-42 corpus. **Pre-this-DEC standing-open items**: still standing; the workshop iteration loop's unblock depends on DEC-42 landing first. DISC-23/24/25/26 (planner alignment gaps), Plan.continuation_narrative, Plan.is_continuation derived property, _plan_predecessors grounding emitter, canonical.html "Continuation of:" section, responsibility atoms (admin-authored, owed), Division.head FK assignments, orchestration trio US-LLM-23/24/25 (DEC-41-framed as Django-service realization of the Mode C loop the workshop iterates), carry-forward 4+5 (tenant_chiway app + AccreditationBody parent), US-DRAFT-1/2/3 production landing, Option 1 vs Option B vs synthesis decision for planner UX. Origin sync: local 20 commits ahead of origin/main after this tracking commit; push direction is user-decision. **Workshop-iteration narrowing trend across A1 dispatches (the empirical signal that drove DEC-42)**: pre-DISC-31 produced 10 false-positives / 5 defect classes; post-DISC-31 produced 1 / 1; the narrowing is real but the asymptote isn't zero with incremental patches; DEC-42 is the structural fix that closes the asymptote to zero. _Prior:_ **POST-ai/0021 A1 RE-DISPATCH — VERBATIM + NUMERIC-TOKEN VECTORS HELD; 6TH EXTRACTOR DEFECT CLASS SURFACED AS DISC-32 (colon-boundary)**. First A1 re-dispatch after the de9f205 pre-flight slice. LLM produced fabrication-free output with 0 fabricated names + 12 catalogue-verbatim Divisions (including hyphenated paren forms like `AAO - Academic Affairs Office` and `SAO - Student Affairs Office (Pastoral and Well-Being)`) + 4 leader Positions + all 7 SLOs by name + 4 Year Groups + 3 mission clauses + 6 WASC standards + 7 AFIs + 5 stakeholder groups. **VERBATIM directive worked**: LLM used `Math` not "Mathematics", `Music and Sports Department` catalogue form, full hyphenated paren Division labels — variant-form vector closed empirically. **Numeric-token vector held** per action-1 confirmation in de9f205. Parser rejected exactly ONE span: `'Schoolwide Learner Outcomes: Bilingual Communicators'` — the extractor crossed a colon between class-noun (in `KNOWN_GENERIC_TERMS`) and the first SLO. **DISC-32 recorded** — 6th defect class in the same family as DISC-31's 5 classes; colon `:` (and likely `;`/en-dash/em-dash/slash/ellipsis) not in the span-terminator set. **Narrowing trend across A1 dispatches**: pre-DISC-31 produced 10 false-positives / 5 defect classes; this dispatch produced 1 / 1. The empirical re-dispatch loop is correctly narrowing the surface one boundary-class at a time. **Meta-finding in DISC-32**: cumulative DISC-29 + DISC-31 + DISC-32 closure pattern suggests `_freetext_audit.py` needs a more comprehensive tokenization+boundary pass than incremental punctuation patches (future hardening direction: replace `_WORD_TOKEN_RE` + ad-hoc handling with a small grammar or NLP noun-phrase extractor; NOT in DISC-32 immediate scope). Per DEC-41: workshop-time finding surfaces production parser bug; fix lands once, benefits both modes. **Standing-open items updated**: (0) **NEW BLOCKER** — DISC-32 closure (colon-boundary + sibling punctuation; same fix surface as DISC-31 — `_freetext_audit.py` module + regression fixtures derived from this A1 response); blocks A1 re-dispatch + every spec whose prose may introduce lists with colons (common natural prose pattern); (1) A1 re-dispatch — BLOCKED on DISC-32; cascade through 02/03/04/05/06 functionally blocked on A1 dispatch chain; (2) DISC-29 single-token gap (separate slice, unchanged); (3) Audit-module comprehensive-tokenization hardening — RECORDED in DISC-32 as future direction; not yet a dedicated DISC; (4) Origin sync (local 19 commits ahead of origin/main after this tracking commit); plus pre-this-session standing-opens (DISC-23/24/25/26, Plan.continuation_narrative, Plan.is_continuation derived property, _plan_predecessors grounding emitter, canonical.html Continuation section, responsibility atoms, Division.head FK assignments, orchestration trio US-LLM-23/24/25 — DEC-41-framed, carry-forward 4+5, US-DRAFT-1/2/3 production landing, Option 1 vs Option B vs synthesis decision — planner UX). _Prior:_ **A1 PRE-FLIGHT SLICE COMPLETE — three predicted failure vectors closed before re-dispatching workshop experiment 01** — IMPL landed `de9f205` covering: (1) `_freetext_audit.py` numeric-token policy AUDITED, confirmed current behavior correct (`_WORD_TOKEN_RE` is letter-anchored; numerals never tokenize; "Term 2"/"Year 4" yield single-token spans the DISC-29 multi-token gate skips); 6 regression tests added to `test_freetext_audit.py` locking the correct behavior against future drift; no audit-module change needed; (2) `prompt-workshop/shared/preamble.md` strengthened with VERBATIM-label directive extending the no-fabrication invariant from "don't invent" to "don't paraphrase" — addresses the variant-form risk (LLM expanding compact `Math` to "Mathematics Department" or contracting `"AAO - Academic Affairs Office"` to "AAO"); parser layer already catches paraphrases as fabrications via non-byte-match, but the directive pre-empts at LLM prompt level; (3) `ai/migrations/0021_extend_no_fabrication_directives.py` mirrors both changes to production — strengthened VERBATIM preamble across ALL 14 PromptTemplate bodies + F1 type-method invariant directive (sitting in workshop snippet since `c7292a1` but never mirrored) into the `decompose-action-steps` body specifically. Single guarded byte-match RunPython migration following ai/0016/0018/0019/0020 pattern. Per-slug test `test_extend_no_fabrication_directives_migration.py` (30 parametrized cases). Gate: pytest 1880 → 1935 (+35 = 5 freetext-audit + 30 ai/0021); mypy clean on touched files; ruff baseline-only; test-js unchanged. Pre-existing 3 axes-backend auth-test failures in `users/tests/` baseline-confirmed via `git stash` (not introduced by this slice). Dev DB: `migrate ai 0021` applied; per-slug canary verification confirmed VERBATIM present in all 14 PromptTemplate rows + TYPE_METHOD present in `decompose-action-steps` only (no leak). **DEC-41 framing in action**: snippet edit → mirrored to production via guarded byte-match migration; the F1 type-method directive from `c7292a1` now also lives in production. **A1 re-dispatch is now ready** — three predicted failure vectors (variant-form, numeric-token, paraphrase) all closed at parser + prompt layers. **Standing-open items updated**: (1) RE-DISPATCH EXPERIMENT 01 (A1 narrative-draft) — pre-flight complete, ready to dispatch; (2) cascade through 02 → 04 → 03 → 05 → 06 once each lands cleanly; (3) DISC-29 single-token gap (separate slice, unchanged); (4) origin sync (local 17 commits ahead of origin/main after this tracking commit); (5) ai/0021 mirror of the F1 prompt-side hardening — STRUCK (landed in `de9f205`); plus pre-this-session standing-opens unchanged (DISC-23/24/25/26, Plan.continuation_narrative, Plan.is_continuation, _plan_predecessors grounding emitter, canonical.html Continuation section, responsibility atoms, Division.head FK assignments, orchestration trio US-LLM-23/24/25 — DEC-41-framed as production realization of Mode C loop, carry-forward 4+5, US-DRAFT-1/2/3, planner UX decision). _Prior:_ **DEC-41 ESTABLISHED — WORKSHOP IS THE DEVELOPMENT-TIME MODE OF THE DJANGO PRODUCTION SYSTEM** — user directive: "ensure that that framing is wholly explicit to all sessions' context." Establishes the workshop ↔ production relationship as project-level architectural policy. The framing is load-bearing for every Mode C dispatch + every US-LLM-23/24/25 design choice. Persisted in three durable surfaces: `CLAUDE.md` gains a new "Workshop ↔ production: one system, two modes" section (read at every session start; carries the 5 operational consequences + what the framing forbids + what it enables); `data/seed-round-plan.md` gains DEC-41 (policy formalization with cross-references to DEC-29 / DEC-39 / DEC-40 / DISC-26 / DISC-30 / DISC-31); this LOG event + STATE refresh in the same tracking commit per persistence-in-same-turn discipline. **Five operational consequences** the framing makes explicit: (1) workshop and production share code — `prompt-workshop/dispatch/{render,apply}.py` import `build_grounding` + `parse_*` from production modules; bugs and fixes cross the boundary in both directions; (2) "output schema" framing reframes as Django-validation framing — parsers ARE the schema validator, imperative-style; declarative JSON Schema would land inside `parse_*`, not as a workshop-side layer; (3) snippet body settled in workshop → guarded byte-match migration mirrors to production PromptTemplate row (precedent: `ai/0016`, `ai/0018`, `ai/0019`, `ai/0020`); (4) the orchestration trio US-LLM-23/24/25 (DEC-29 north-star) IS the Django-service-driven realization of the orchestrator-driven Mode C loop — building it = lifting the iteration loop from session-time to service-time over the same code substrate; (5) workshop findings on Chiway are tenant-onboarding findings for every future tenant (DISC-30 is the gap-class admin curation will surface; DISC-31 is the gap-class realistic prose will surface). **What the framing forbids**: treating workshop edits as throwaway iteration; treating workshop bugs as workshop-only; building a parallel workshop-only validation layer; designing the orchestration trio as a separate new system. **What it enables**: 11-gap DISC-26 audit closes when the workshop's whole-plan sequence produces a promotion-ready draft against Chiway dev DB; each new tenant inherits parser + grounding + prompt-corpus closure (DEC-39 + DEC-40); orchestration trio's UX must surface parser-rejection signals to school admins as actionable curation gaps (where DISC-30 / DISC-31 close at the UX layer). **Standing-open items unchanged** by this DEC (it's policy establishment, not implementation): (1) re-dispatch experiments 01-04 against post-DISC-31 corpus; (2) F1 re-dispatch against post-DISC-30 + post-DISC-31 corpus; (3) F2 (experiment 06) resume once F1 settles; (4) DISC-29 single-token fabrication gap; (5) origin sync (local 15 commits ahead after this tracking commit); plus pre-this-session standing-opens (DISC-23/24/25/26, Plan.continuation_narrative, Plan.is_continuation, _plan_predecessors grounding emitter, canonical.html Continuation section, responsibility atoms, Division.head FK assignments, orchestration trio US-LLM-23/24/25 — now framed by DEC-41 as production-realization of Mode C loop, carry-forward 4+5 — tenant_chiway app + AccreditationBody parent, US-DRAFT-1/2/3 production landing, Option 1 vs Option B vs synthesis decision — planner UX). _Prior:_ **DISC-30 + DISC-31 BOTH RESOLVED — WORKSHOP ITERATION UNBLOCKED** — IMPL sub-agent landed both closures sequentially after a prior socket-drop required a fresh self-contained dispatch carrying the dirty-tree state. `141f8f4` closes DISC-31 (parser noun-phrase extractor hardening across all 5 defect classes: comma-list splitting, sentence-boundary respect, paren balance, sentence-initial qualifier strip, `KNOWN_GENERIC_TERMS` extended with class-noun tokens; A1-derived regression fixtures added to `test_freetext_audit.py`; realistic-prose coverage class added to `test_parser_catalogue_enforcement_coverage.py`; gate green pytest 1880 → 1899, mypy clean). `ec2b187` closes DISC-30 (`school/0057_extend_planning_method_applicable_improvement_types.py` extends all 4 Chiway PlanningMethods' M2M per user-chosen path (a) — extend existing methods based on process fit; Policy revision +schoolwide-learner-outcomes +communications; Tool/platform customization +curriculum-development; Data quality improvement +assessment-and-data; Event design +professional-learning; gate green pytest 1899 → 1903, mypy 307, makemigrations No changes; dev DB applied + per-method M2M verified post-migration). Three IMPL deviations honestly surfaced (all design refinements, no silent scope expansion): negative-test fabrication substituted to multi-token form per DISC-29 carry-forward; pre-existing wiring-test updated to subset semantics; realistic-prose payload adjusted to comma-list form to match A1 dispatch's actual shape. **The fabrication-closure project pattern (DEC-32 → DEC-39 → DEC-40 → DISC-30/31 closures) now stands complete at the prompt-language + parser + tenant-data layers**: parser-extractor implementation no longer over-rejects on realistic prose; Chiway tenant's M2M covers the actual plan shapes the platform must support; admin-curated structure remains source of truth across all three layers. **Standing-open items updated**: DISC-30 STRUCK (resolved by `ec2b187`); DISC-31 STRUCK (resolved by `141f8f4`). **Now unblocked + ready**: (1) re-dispatch experiments 01–04 against the post-DISC-31 corpus — expected clean admission given the LLM produced fabrication-free output even before the extractor fix; (2) F1 re-dispatch against the post-DISC-30 + post-DISC-31 corpus — type-method invariant should now be satisfiable since each milestone improvement_type has at least one applicable method post-`ec2b187`; (3) F2 (experiment 06) resume once F1 settles. **Carry-forward (unchanged)**: DISC-29 (single-token fabrication gap, separate slice); pre-this-session standing-opens (DISC-23/24/25/26, Plan.continuation_narrative, Plan.is_continuation derived property, _plan_predecessors grounding emitter, canonical.html "Continuation of:" section, responsibility atoms — admin-authored owed, Division.head FK assignments, orchestration trio US-LLM-23/24/25 — DEC-29 north-star, carry-forward 4+5 — tenant_chiway app + AccreditationBody parent, US-DRAFT-1/2/3 production landing, Option 1 vs Option B vs synthesis decision — planner UX); origin sync (local 14 commits ahead of origin/main after this tracking commit). **Persistence discipline note**: the prior session-end-vs-DISC-30/DISC-31-discovery timing produced two separate DISC entries with their own LOG events + STATE refreshes IN THE SAME TURN per the reinforced discipline; the resolutions land here in the same tracking commit with both `resolved_by` cells filled + LOG events + STATE refresh. _Prior:_ **POST-DEC-40 A1 RE-DISPATCH SURFACED DISC-31 — `_freetext_audit.py` NOUN-PHRASE EXTRACTOR PRODUCES FALSE-POSITIVE REJECTIONS ON PROSE-HEAVY SPECS**. The first re-dispatch of A1 after DEC-40 landed. LLM (opus) produced clean fabrication-free output naming only real Chiway catalogue entities across all 5 prose fields: 7 SLOs, 15 Divisions, 4 leader Positions (IHS Principal / Director of Teaching and Curriculum Center / Director of Student Affairs Office / Director of AAO), 4 Year Groups (IG1/IG2/AS/A2), 3 mission areas, 3 mission clauses (GROUNDED/ROUNDED/UNBOUNDED), 6 WASC standards (A1/A4/B1/B2/B3/C1), 7 AFIs, 5 stakeholder groups. Voice constraint held (no em-dash chains, no rhetorical tetrads, no "across all four X" sweeps, no hedged transitions; short declarative sentences in school-report register). Parser rejected with 10 false-positive spans — extraction defects in `_freetext_audit.py::extract_proper_name_spans`, NOT LLM fabrications. Five distinct defect classes recorded in DISC-31: (1) comma-list collapse (multi-entity lists grouped as one span); (2) sentence-boundary cross (tokens across `. CAP` joined); (3) paren-unbalance (close-paren stripped → real Divisions like "SAO - Student Affairs Office (Pastoral and Well-Being)" fail lookup); (4) sentence-initial article/adverb merge ("Only Confident"); (5) class-noun-not-entity ("Schoolwide Learner Outcomes" — needs `KNOWN_GENERIC_TERMS` extension). Forensic artifacts at `prompt-workshop/outputs/2026-05-30-07-00-44/`. The DEC-40 closure intent (no fabrication) is correct; the IMPLEMENTATION over-rejects on prose-heavy specs because the structural-guard test (Commit 5 of DEC-40, `6d43a2a`) used short synthetic fixtures lacking comma-lists / parens / cross-sentence content. **Standing-open items updated** — TWO ACTIVE BLOCKERS on workshop re-dispatch: (0a) **DISC-31** — `_freetext_audit.py` noun-phrase extractor false-positive class; blocks A1 + every prose-heavy spec (02 A3 / 03 D1 / 04 B1 / 07 US-LLM-27 / 13 A2 / 14 G1 / 10 F5). Five closure directions enumerated in DISC-31 entry; all independently actionable; fix surface is local (single module `_freetext_audit.py` + unit-test fixtures derived from this dispatch's actual rejected spans + regression-test class added to `test_parser_catalogue_enforcement_coverage.py` covering realistic prose shapes). Decision-owner: code-side IMPL slice. (0b) **DISC-30** — Chiway tenant `PlanningMethod ↔ ImprovementType` M2M incomplete for SLO/curriculum/communications/compliance work; blocks F1 + cascades to F2 (which reads F1's action_steps). Four closure paths in DISC-30: extend existing methods' M2M / seed additional methods / change milestones / add any-type method. Decision-owner: admin-curation triage. (1) Re-dispatch experiments 01-04 — UNBLOCKED ONLY AFTER DISC-31 closes (the parser must stop false-positively rejecting before any prose-heavy spec can produce a clean re-dispatch). (2) Resume Mode C at experiment 06 (F2 propose-assignments) — UNBLOCKED ONLY AFTER DISC-30 + F1 re-dispatch complete. (3) DISC-29 closure (single-token-gap heuristic) — separate slice, unchanged. (4) Origin not yet synced — local has 11 commits ahead of origin/main (DEC-40 chain + tracking + F1 capture + DISC-30 surfacing + STATE refresh + A1 capture + DISC-31 surfacing + this update); push direction is user-decision. (5) Production mirror of F1 prompt-side hardening — would-be `ai/0021` migration; sequenced after DISC-30 + DISC-31 closure + F1 re-dispatch validation. Plus the pre-this-session standing-open items unchanged (DISC-23/24/25/26, Plan.continuation_narrative, Plan.is_continuation derived property, _plan_predecessors grounding emitter, canonical.html "Continuation of:" section, responsibility atoms — admin-authored owed, Division.head FK assignments, orchestration trio US-LLM-23/24/25 — DEC-29 north-star, carry-forward 4+5 — tenant_chiway app + AccreditationBody parent, US-DRAFT-1/2/3 production landing, Option 1 vs Option B vs synthesis decision — planner UX). **Two distinct gap-classes the DEC-40 verification surfaced**: DISC-30 is a tenant-data gap (admin curation incomplete; closure is admin-side); DISC-31 is a parser-code gap (extractor implementation has 5 enumerable bug classes; closure is code-side). Different fix surfaces, different decision-owners, but both must close before workshop iteration resumes cleanly. _Prior:_ **POST-DEC-40 F1 RE-DISPATCH GOAL-MET TEST PASSED + DISC-30 SURFACED — TENANT-DATA GAP BLOCKS FURTHER F1 WORK; F1 PROMPT-SIDE HARDENING LANDED** — the first live LLM dispatch since DEC-40's 6-commit chain landed (commits `3c5576e` capture + `c7292a1` prompt fix + DISC-30). Result: parser-layer enforcement works empirically. Opus produced a fabrication-free F1 output (every Position / Division / Stakeholder named is verbatim from Chiway's catalogue — 26 distinct Positions + 8 Divisions + all 6 Stakeholder groups) AND chose method "Policy revision (6-step consultative)" whose admin-curated `applicable_improvement_types` = `{policy-establishment, policy-revision}` does not intersect the draft milestones' improvement_types `{schoolwide-learner-outcomes, curriculum-development, communications, compliance-accountability}`. Parser raised `ValueError` at the type-method invariant check (Commit 3 of DEC-40, `d173679`) with the exact mismatch named; the row did NOT land in the draft. DEC-39 prompt-language layer continued to suppress name fabrications; DEC-40 parser layer caught the cross-field invariant the prompt didn't deter. Together = structural fabrication closure end-to-end verified live. Forensic artifacts at `prompt-workshop/outputs/2026-05-29-22-10-21/` (response.json + evaluation.md). **F1 PROMPT-SIDE HARDENING LANDED IN c7292a1**: `prompt-workshop/snippets/05-decompose-action-steps.md` now carries explicit type-method invariant directive ("The chosen method's `applies to:` list MUST include at least one of the draft milestones' improvement_type codes, or be empty") + rationale + per-method/per-milestone cross-check instruction. Re-rendering with the directive surfaced **DISC-30 — Chiway tenant `PlanningMethod.applicable_improvement_types` M2M is INCOMPLETE for the SLO-improvement plan shape**: none of the 4 admin-curated methods (Data quality improvement / Event design (recurring) / Policy revision / Tool / platform customization) covers any of the milestones' types, and none is an any-type method. The type-method invariant is structurally unsatisfiable against current tenant data for this plan; re-dispatching F1 would burn tokens producing the same guaranteed rejection regardless of LLM behavior. Per project pattern (admin curation is source of truth; LLM chooses from, never overrides; DEC-32 + DEC-39 + DEC-40), the closure path is admin curation extension, NOT parser/prompt loosening. Production-side mirror (would-be `ai/0021` migration mirroring the F1 prompt directive to the production PromptTemplate) is desirable once admin triage on DISC-30 resolves. **Standing-open items updated**: (0) **NEW BLOCKER** — DISC-30 closure (admin curation triage on Chiway tenant's PlanningMethod ↔ ImprovementType M2M): four enumerated paths in the DISC entry: (a) extend existing 4 methods' M2M coverage; (b) seed additional methods covering SLO/curriculum/communications/compliance; (c) change current plan's milestones to use covered types (weak — milestones drive substance); (d) add an "any-type" safety-net method. **F1 RE-DISPATCH BLOCKED on DISC-30 closure**; any further F1 dispatch against current dev DB produces guaranteed parser rejection. (1) Re-dispatch experiments 01-04 against post-DEC-40 corpus — UNAFFECTED by DISC-30 (those specs don't hit the M2M invariant); still open. (2) Resume Mode C at experiment 06 (F2 propose-assignments) — FUNCTIONALLY BLOCKED on F1 closure (F2 reads F1's action_steps which are currently empty due to parser rejection); becomes available once DISC-30 + F1 re-dispatch completes. (3) DISC-29 closure (single-token-gap heuristic) — separate slice, unchanged. (4) Origin not yet synced — local has 9 commits ahead of origin/main (DEC-40 chain + tracking + F1 capture + DISC-30 surfacing); push direction is user-decision. (5) Production mirror of F1 prompt-side hardening — would-be `ai/0021` migration; sequenced after DISC-30 resolution + F1 re-dispatch validation under the resolved data set. Plus the pre-this-session standing-open items unchanged (DISC-23/24/25/26, Plan.continuation_narrative, Plan.is_continuation derived property, _plan_predecessors grounding emitter, canonical.html "Continuation of:" section, responsibility atoms — admin-authored owed, Division.head FK assignments, orchestration trio US-LLM-23/24/25 — DEC-29 north-star, carry-forward 4+5 — tenant_chiway app + AccreditationBody parent, US-DRAFT-1/2/3 production landing, Option 1 vs Option B vs synthesis decision — planner UX). **The DISC-30 class of finding is the tenant-data layer analog of the DEC-39/DEC-40 corpus-layer closure pattern**: where structural enforcement reveals an incomplete admin-curated reference set, the closure is to extend the admin-curated set, not relax the enforcement. The orchestration trio US-LLM-24/25/23 (DEC-29 north-star) must surface this class of gap to school-admin actors at create-time when reached in production — this same gap class may surface at other M2M joints in F2-F5/G1/A2/US-LLM-27 once those experiments resume. _Prior:_ **DEC-40 PARSER-LAYER FABRICATION CLOSURE LANDED — STRUCTURAL CLOSURE END-TO-END; DISC-28 RESOLVED; DISC-29 RECORDED** — 6-commit chain `ec31656` → `77712d3` → `d173679` → `7e450a9` → `6d43a2a` → `997d633` lands parser-layer enforcement that complements DEC-39's prompt-language probabilistic suppression with structural rejection at the LLM→DB boundary. Together DEC-39 + DEC-40 = fabrication structurally cannot enter the system; admin-curated catalogues are the only universe for both directive (prompt) and enforcement (parser). **What landed**: (1) parse signature widened to `parse(text, *, school, grounding)` across all 14 `parse_*` functions + `AssistStreamView` dispatch + workshop `apply.py` dispatch; (2) structured-field catalogue lookups across 11 parsers — every label/code field validates against grounding catalogue at parse-time with ValueError on miss; resolve-hook hardening from lenient-drop to raise-on-miss across all 8 resolve hooks; (3) type-method invariant cross-validation at D1 + F1 closes the Exp 04 finding at parse; (4) free-text noun-phrase scan via new `planner/_freetext_audit.py` — `extract_proper_name_spans` + `CatalogueUnion` + `audit_freetext`; tuned `KNOWN_GENERIC_TERMS` denylist excludes entity-category nouns so fabricated multi-word org names don't all-tokens-pass; the workflow's prior "The IHS Principal Office" bug closed via leading-article normalization; all 10 experiment-05 fabricated Position titles rejected via parametrized regression; (5) `test_parser_catalogue_enforcement_coverage.py` future-drift structural guard parametrized over all 14 specs; (6) voice-constraint reinforcement at preamble + `ai/0020` production migration; voice canary verified in all 14 PromptTemplate rows on dev DB. **Gate**: pytest 1762 → 1880 (+118 tests); mypy clean throughout (301 → 306 source files); ruff baseline; test-js unchanged. **DISC-29 single-token gap recorded** (mandate-007): single-token invented organizational nouns in free-text prose are NOT caught by Commit 4's multi-token-only policy (refined during IMPL after single-token policy produced unmanageable false positives on ordinary domain capitalizables). Multi-word fabrication class fully closed; single-token surface remains as enumerated standing-open item. Inline TODO at `planner/_freetext_audit.py:634-642` references DISC-29; closure direction documented (English-dictionary-check + catalogue-miss heuristic, or admin-curated school-glossary). **Standing-open items** (post-DEC-40 landing): (0) **STRUCK** — parser-level fabrication closure (DEC-40), type-method invariant parser tightening (Commit 3), voice-constraint reinforcement (Commit 6); (1) workshop re-dispatch experiments 01-05 against the post-DEC-40 corpus — verify the closed pipeline against prior captures (Commit 7 of the approved plan, orchestrator-driven, ~5 specs × render→opus→apply→sonnet→evaluate); (2) resume Mode C at experiment 06 (F2 propose-assignments) — the substantive workshopping work the DEC-39+40 chain was meant to unblock (Commit 8 of the approved plan); (3) DISC-29 closure — single-token gap heuristic (separate slice); (4) origin not yet synced — local has 6 commits ahead of origin/main; user-direction needed for push (force-push earlier this turn aligned origin to 4a0bc56; the 6 new commits are local-only). Plus the pre-this-session standing-open items unchanged: DISC-23/24/25/26, Plan.continuation_narrative, Plan.is_continuation derived property, _plan_predecessors grounding emitter, canonical.html "Continuation of:" section, responsibility atoms (admin-authored, owed), Division.head FK assignments, orchestration trio US-LLM-23/24/25 (DEC-29 north-star), carry-forward items 4+5 (tenant_chiway app, AccreditationBody parent), US-DRAFT-1/2/3 production landing, Option 1 vs Option B vs synthesis decision (planner UX). **Plan source**: `/Users/david/.claude/plans/gleaming-skipping-emerson.md` — "parser-level fabrication closure — full completion of every out-of-scoped item." Workshop dispatches (Commit 7+8) + tracking (Commit 9; this commit) close the slice modulo Commit-7+8 sub-agent costs the user must authorize separately. _Prior:_ **DEC-39 ENFORCEMENT FULLY LANDED — GOAL MET; DISC-27 FULL-RESOLVE** — chain closed across `488906e` + `9d7f07f` + `e81b7b6` + `39fc96e` + `852aa3f` + `b12e3dd` + `ef1ce18` + `0b018fa` + this turn's tracking commit. Slice 2 added: (Commit 1/3 `b12e3dd`) 28 per-section catalogue render-block insertions across 9 workshop snippets — `divisions` shape uses the DISC-22 nested positions + responsibility-atoms pattern; simpler sections (stakeholder_groups, policies, accreditation_standards, learner_outcomes, mission_areas, areas_for_improvement, guiding_statements, frequencies, year_groups, cycle) match the corresponding emitter outputs at `ai/services/grounding.py:97-269`; (Commit 2/3 `ef1ce18`) `ai/migrations/0019_render_no_fabrication_catalogues.py` mirrors the 9 body render blocks to production PromptTemplate rows via the same guarded byte-match pattern from `ai/0016`/`0018`; gate green; dev DB applied + per-slug canary verified across all 9 affected slugs; (Commit 3/3 `0b018fa`) structural-guard `test_prompt_body_iteration_coverage.py` promotes the body-iteration-coverage audit dimension to a permanent gate-time machine test (sibling to `test_grounding_completeness.py`; parametrized over 14 specs from `planner.specs.SPECS`; future spec edit that adds a section to `*_GROUNDING_INCLUDE` without a corresponding body render block fails the gate at IMPL time, not as runtime fabrication). **CHAIN MECHANIC**: grounding tuple includes section → context dict carries section data → body iterates section → rendered prompt text contains catalogue → LLM has both the directive AND the enumeration the directive references → LLM names only enumerated actors → zero fabrication. **GOAL-MET TEST PASSED**: F1 re-dispatch at `prompt-workshop/outputs/2026-05-29-15-23-28/` on full-rendering context with cleared `draft.action_steps` (avoid contamination from experiment-05's prior fabricated steps). Result: ZERO FABRICATION. 26 distinct Positions + 8 Divisions + 6 of 6 Stakeholder groups named, all verbatim matches to enumerated catalogues for tenant `chiway-repton-xiamen`. All 10 fabrication categories from experiment 05 exemplar (Vice Principal for Academics, Vice Principal for Bilingual Programme, Heads of EY/Primary/Secondary, Heads of Chinese and English Departments, Head of Co-Curricular Programme, Head of Student Affairs, Head of Admissions and Communications, gradebook configurator, PLC-minutes template owner, classroom-walkthrough form owner) ABSENT in the new output. Forensic prior capture at `outputs/2026-05-29-10-02-27/` stays as the pre-enforcement exemplar. Three artifacts persisted at `outputs/2026-05-29-15-23-28/` (capture + sonnet rendering + orchestrator evaluation with per-name cross-check table). Voice-constraint compliance noticeably improved over experiment 05 (no "across all four X" antipatterns; no rhetorical tetrads). **Standing-open items updated**: STRUCK — "DEC-39 enforcement-content-rendering 4th IMPL slice" (closed by Slice 2); "F1 rerun on expanded grounding" (executed, passed); "per-spec grounding_include remediation" (closed by Slice 1's `9d7f07f` + Slice 2's `b12e3dd`+`ef1ce18`). REMAINING: (1) resume per-spec workshop iteration (next: 06 = F2 propose-assignments per dependency-respecting strongest-first); (2) voice-constraint reinforcement (separate Mode C iteration; not gated); (3) type-method-invariant parser tightening (separate parser-level slice; not gated); plus the prior standing items (DISC-23/24/25/26, Plan.continuation_narrative, Plan.is_continuation, _plan_predecessors emitter, canonical.html Continuation section, responsibility atoms, Division.head FK assignments, orchestration trio US-LLM-23/24/25, carry-forward items 4+5, US-DRAFT-1/2/3 production landing, Option 1 vs Option B vs synthesis decision). **DEC-39 invariant is now structurally enforced end-to-end**; project-goal-met disposition: confirmed. PyRight diagnostics on the new test/migration files all match documented false-positive patterns (`_`-prefixed unused unpacks + pytest-django `transactional_db` fixture injection); mypy clean per IMPL gate. Pre-existing 3 axes-backend test failures in users/tests are not introduced by this slice (reproducible on pre-Slice tip per IMPL `git stash` check). _Prior:_ **DEC-39 ENFORCEMENT INFRASTRUCTURE LANDED across three commits + LOAD-BEARING GAP SURFACED (mandate-007/008)** — `9d7f07f` (Commit 1/3: `planner/specs.py` grounding-include expansion across 9 specs; new completeness test; gate green; mypy 298 unchanged; pytest 1500 → 1514) + `e81b7b6` (Commit 2/3: `ai/migrations/0018_install_no_fabrication_preamble.py` — corpus-wide PromptTemplate body update across all 14 specs via guarded byte-match pattern mirrored from `ai/0016`; SHARED_PREAMBLE constant carries the workshop preamble verbatim into production; per-spec free-text-tightening clauses inserted; new per-slug parametrized migration test; gate green; mypy 298 → 299; pytest 1514 → 1543; dev DB applied + per-slug canary verified — all 14 PromptTemplate rows now contain "The grounding IS the universe" canary) + `39fc96e` (Commit 3/3: workshop snippet body + frontmatter sync across 9 affected snippets; `{% include shared/preamble.md %}` unchanged; F1 re-render verification — preamble + per-spec clause + expanded frontmatter all confirmed present). 5 specs required NO change per audit (A3, D1, F2, B2-bind-measurement-channels, C1-suggest-feedback-channels). 9 specs got both preamble + per-spec free-text-tightening clause (A1, B1, F1, US-LLM-27, F3, F4, F5, A2, G1). Slug correction surfaced + handled in Commit 2 (A1's production slug is `plan-narrative-draft` not `narrative-draft`). **LOAD-BEARING GAP SURFACED by IMPL sub-agent (mandate-007/008)**: the per-spec free-text-tightening clauses reference catalogues "above" but the snippet/production bodies do NOT render those catalogues — F1's body now says "name from the divisions catalogue above" but has no `{% if divisions %}{% for d in divisions %}- {{ d.label }}{% endfor %}{% endif %}` block; the grounding context dict carries the data, Django Template renders only what `{% if %}` blocks invoke; nothing iterates the newly-grounded sections in any of the 9 affected bodies. Fabrication risk persists at the same joints, possibly worse (LLM faces a stricter directive with no enumeration to satisfy it). The user's directive ("such that we never run into invention again") is NOT met by the three commits alone; the enforcement is structurally incomplete. **Canonical resolution path** (the only mandate-compliant fix): a 4th IMPL slice adding `{% if <section> %}{% for x in <section> %}- {{ x.label }} ...{% endfor %}{% endif %}` rendering blocks to each affected snippet body (9 snippets) + a follow-on `ai/0019_render_no_fabrication_catalogues.py` migration mirroring the additions to the 9 affected production PromptTemplate bodies (`ai/0016`/`0018` guarded byte-match pattern). NO ROLLBACK of the three landed commits (they are valid enforcement-infrastructure; rollback would discard real work without closing the gap). **Standing-open list updated**: (0) **NEW LOAD-BEARING** — DEC-39 enforcement-content-rendering 4th IMPL slice (snippet body `{% if %}` blocks + ai/0019 migration mirroring); the prior "per-spec grounding_include remediation" + "voice-constraint reinforcement decision" closed by Commits 1+2 are STRUCK; (1) F1 rerun on expanded-AND-rendered grounding once the 4th slice lands; (2) voice-constraint reinforcement (separate Mode C iteration; not gated by enforcement); plus the prior 14 standing items (DISC-23/24/25/26, Plan.continuation_narrative, Plan.is_continuation, _plan_predecessors emitter, canonical.html Continuation section, responsibility atoms, Division.head FK assignments, orchestration trio US-LLM-23/24/25, carry-forward items 4+5, US-DRAFT-1/2/3 production landing, Option 1 vs Option B vs synthesis decision). **DEC-39 body** (`data/seed-round-plan.md`) annotated with the three landed commit SHAs + the gap note + the canonical resolution path. **DISC-27** (`phases/discoveries.md`) `resolved_by` cell filled with PARTIAL-RESOLVE recording infrastructure landed + content-rendering open. PyRight diagnostics on specs.py + the migration test file — all match documented false-positive patterns (`_`-prefixed unused unpacks + pytest-django `transactional_db` fixture injection); mypy is the type gate per project convention; mypy clean per IMPL gate. _Prior:_ **DEC-39 ESTABLISHED + EXPERIMENT 05 PERSISTED + SHARED PREAMBLE STRENGTHENED with no-fabrication invariant** — user directive: "the presumption must be that there is no opportunity for fabrication because all context for all prompts is constrained by db content, and all db content is given as and when necessary in prompt." Surfaced empirically by prompt-workshop experiment 05 (F1 decompose-action-steps; capture `prompt-workshop/outputs/2026-05-29-10-02-27/`): the dispatched opus sub-agent chose "Policy revision (6-step consultative)" + produced 6 well-shaped steps + all 6 assessments populated + sonnet rendering held (datapoint #3) + production parser accepted + draft merged — BUT the step descriptions named SEVEN non-existent leadership Position titles ("Vice Principal for Academics" / "Vice Principal for Bilingual Programme" / "Heads of Early Years, Primary, and Secondary" / "Heads of Chinese and English Departments" / "Head of Co-Curricular Programme" / "Head of Student Affairs" / "Head of Admissions and Communications") plus invented role-labels ("gradebook configurator", "PLC-minutes template owner", etc.). None exist in dev DB; actual roster = 15 Divisions + ~46 Positions including IHS Principal / Director of AAO / Director of SAO / Director of Teaching and Curriculum Center / Director of Future Scholar Innovation Center / College Counseling Department Coordinator / Head of Art-Business-English-Humanities-Math-Sciences / IG1-IG2-AS-A2 Year Leaders. **Cause** (two-sided): (a) F1's `grounding_include` tuple at `planner/specs.py:544-552` does not carry `divisions` (the section that nests positions+responsibility-atoms via `ai/services/grounding.py:_divisions`); (b) the strengthened preamble's "name real actors" directive was positive-only without the inverse constraint that anything not in the grounding is not in the school. **Landed this turn**: DEC-39 in `data/seed-round-plan.md` (formalizes the no-fabrication invariant as project policy with per-spec gap inventory + corpus-side and language-side corollaries); DISC-27 in `phases/discoveries.md` (records the F1 evidence + per-spec grounding-tuple gap inventory + corpus-wide remediation scope); `prompt-workshop/shared/preamble.md` carries the no-fabrication constitutive constraint as a new bolded paragraph between the solution-architect framing and the success criteria (applies to all 14 workshop snippets via `{% include %}`); F1 experiment capture + sonnet rendering + orchestrator evaluation persisted at `prompt-workshop/outputs/2026-05-29-10-02-27/`; two LOG events appended (the experiment landing + the DEC-39 establishment). **Per-spec grounding-tuple gap inventory** (the corpus-side scope DEC-39 + DISC-27 record): A1 missing `divisions`; B1 missing `divisions, stakeholder_groups`; D1 missing `divisions, stakeholder_groups`; F1 missing `divisions, stakeholder_groups` (the exemplar); A3 missing `divisions`; B2 carries only `school, draft_state` (broad gap); F3/F4 minimal grounding; F2/F5 carry `divisions` but possibly missing other vocabularies; RESPONSIBILITIES (US-LLM-27) carries `divisions` (correct); C1/G1 carry `divisions + stakeholder_groups + frequencies` (likely correct for their purviews); A2 carries `accreditation_standards` but may invoke divisions/positions in body. **Voice-constraint check** (first experiment under preamble antipattern #9, post-`8239df5`): mixed — assessments held tight (factual, short, artifact-or-event); step descriptions tripped the "across all four X" antipattern twice (Step 2 "across Chinese-medium and English-medium settings, across EY, Primary, and Secondary, and across academic and co-curricular contexts"; Step 6 "across all four framing audiences and across every division") + ran long compound sentences. Voice constraint partially translated; reinforcement needed. **Type-method-invariant gap propagates from D1 to F1** (same canonical resolution as Exp 04 — parser tightening at Step 6 production landing). **Standing-open items updated**: (1) **NEW** — per-spec `grounding_include` remediation across the 14 specs at `planner/specs.py` (follow-on plan→IMPL Mode B; no migration required — pure Python config edit; workshop dispatch picks it up automatically; production landing is the same Python edit + the eventual `ai/0018+` PromptTemplate-evolution migration packaging validated bodies); (2) **NEW** — F1 rerun on expanded grounding once remediation lands (confirm LLM names enumerated actors, not fabricated); (3) **NEW** — voice-constraint reinforcement decision (preamble strengthening on the description-side specifically, or per-spec body explicit-call); plus the prior 14 items (DISC-23/24/25/26, Plan.continuation_narrative, Plan.is_continuation derived property, _plan_predecessors grounding emitter, canonical.html "Continuation of:" section, responsibility atoms — admin-authored, Division.head FK assignments, orchestration trio US-LLM-23/24/25, carry-forward items 4+5, US-DRAFT-1/2/3 production landing, Option 1 vs Option B vs synthesis decision). **NEXT in workshop** (user's call): either (a) close the corpus-side grounding-tuple remediation slice and re-run F1 on the expanded grounding to confirm the fix, OR (b) continue per-spec experiments against current grounding state to surface more per-joint defects before remediating. _Prior:_ **PROJECT-CONTEXT REFRESH** — `CLAUDE.md` updated with the post-Phase-15 working modes (Mode A phased / Mode B architecture-refinement slices / Mode C prompt-workshop iteration) + expanded authoritative-artifacts list (decision+reference docs section naming `data/seed-round-plan.md` + `docs/domain-model.md` + the 5 design/audit docs that landed this session: planner UI/UX audit, Option 1 tree-navigator, Option B single-scroll-qcards, wizard-v2 coverage+prompt-quality audit, hod-default-responsibilities, tenant-vs-generic-cleavage) + operational manage-the-work surfaces section naming `prompt-workshop/` + `web/wizard-v2.html`. Directory conventions expanded to include `docs/`, `data/`, `prompt-workshop/`, `web/`, `mockups/`, the `ai/`+`planner/`+`devmocks/` Django apps + this STATE + LOG at repo root. Preview section names wizard-v2 as reachable by direct URL `/mocks/web/wizard-v2.html` (launcher card hand-maintained, not auto). STATE itself refreshed (this update). **Current synthesis position (load-bearing recent landings, in reverse chronological order)**: `b7a0693` + `86c316c` — 14 prompt-workshop snippets bootstrapped verbatim from production `ai/migrations/0003`–`0017` (initial-state baseline; iteration substrate ready); `a0b4cca` — `prompt-workshop/` directory landed with working-model README + shared preamble (audit-gap #8/#9/#10/#11 corrections populated upfront: general LLM success criteria + zero-hedging language + operational policy "all divisions involved through their particular purviews" + current→desired solution-path meta-framing) + per-subdir READMEs; `c089b62` — wizard-v2 spec-coverage + prompt-quality audit persisted at `docs/wizard-v2-coverage-and-prompt-quality-audit-2026-05-28.md` (11 gaps for US-DRAFT-1 production); DISC-26 in `phases/discoveries.md` umbrella for the 11 gaps; `9094883` — audit + DISC-26 amended to add gap #11 (LLM-role meta-framing) per user-direction; `dbbcb2d` — Option B doc revised to remove cost-framing language per user-direction (mandate antipattern correction); `065a572` — Option B LLM-assist machinery section added; `1944154` — Option B (`docs/planner-ux-option-b-single-scroll-qcards-with-preview.md`) persisted as sibling to Option 1; `638adfd` — Option 1 (`docs/planner-ux-option-1-tree-navigator.md`) persisted as first-pass UX design idea; `4a288f6` — planner UI/UX audit (`docs/planner-ui-ux-audit-2026-05-28.md`) persisted; `2767be0` — DISC-23/24/25 added for planner alignment gaps (Plan.guiding_clauses not captured; predecessor capture-time posture; Person picker browser-verification pending); `916f9bf` + `471722f` + `b5b7ecc` — wizard-v2 prototype landed + revised: 12-file static prototype at `web/wizard-v2.html` with Option B + US-DRAFT capabilities + real Chiway dev-DB content + audit-gaps #8-11 applied + Django parser-shape fidelity across all 14 stubs; `6d40e79` — US-DRAFT segment (3 stories) added to `phases/00-preamble.md` + `phases/US-STATUS.md`; `bd68f9a` + `77fce3b` — DEC-38 + IMPL: `PlanPredecessor` typed with `ContinuationKind` (EXTENSION / REVISION / RENEWAL / SUPERSESSION, default EXTENSION) + translated `rationale` TextField via `plans/0041`; tenant-rounds preceding (Position.scope_summary `bc6bf8a` → CharField(500) Division parity; Innovation Center seed `06f9ba2` + importer extension `0b475cc` → Axel landed → D7 roster fully closed 56/56; phantom Division + mission clause cleanup `7846aa6`; staff roster importer `9ec2a97` US-ONB-4 enabled; 25 CSV-implied Positions pre-seed `e10ddc5`; rename Dorm Supervisor → Dormitory Teacher `470b320`; 3 additional Positions `f2218af`; 4 additional Divisions `71e5fff`; DEC-36 Director-of-X series complete `b0ff1fd` + year-leader split `463054a` + DEC-35 HOD `6468ed1`; DEC-37 Position.kind `dbe1685`; YearGroup model `ea0b5a6`; Person locale-neutral name `51ff5cd` + `363454f`). **Dev DB tenant `chiway-repton-xiamen` content** (post-importer + post-cleanup): 15 Divisions / 16 leader + 31 staff = 47 Positions (16 leader includes the Innovation Center Director) / 57 Persons + 57 Users / 75 Holdings / 4 YearGroups / 3 division-owned Policies / 12 WASC standards (3 categories) / 4 framings / 6 stakeholder groups / 7 AFIs / 3 priority tiers / 3 mission areas / 4 planning methods (20 steps) / 32 ImprovementTypes / 7 LearnerOutcomes (post-0039 reseed per audit finding) / 2 Cycles / 1 vision GuidingStatement (6 clauses 0-5) + 1 mission GuidingStatement (3 clauses 0/1/2). Plans: 0 (admin-authored or wizard-v2-stub-demonstrated; production whole-plan-draft pipeline pending DISC-26 resolution). DivisionResponsibility / PositionResponsibility atoms: 0 each (admin-authored, owed). **Standing-open items the user owns**: (1) DISC-23 — `Plan.guiding_clauses` capture in planner; (2) DISC-24 — predecessor-at-create-time posture; (3) DISC-25 — Person picker browser-verification; (4) DISC-26 — prompt-corpus rewrite (11 gaps; named resolution path = `prompt-workshop/`); (5) `Plan.continuation_narrative` field (DEC-38 surfaced soft-continuation gap); (6) `Plan.is_continuation` derived property; (7) `_plan_predecessors` grounding emitter; (8) `web/canonical.html` + `web/data/plan-data.js` "Continuation of:" render-section update; (9) Innovation Center responsibility atoms (16 leader + 31 staff Position scope_summaries + responsibility atoms — admin-authored, owed); (10) `Division.head` FK assignments (Persons exist now); (11) orchestration trio (US-LLM-23/24/25 + US-UI-6); (12) carry-forward items 4 + 5 (`tenant_chiway` app, AccreditationBody parent); (13) US-DRAFT-1/2/3 production landing (requires `PlanDraftVersion` modeling + prompt-corpus rewrite + the orchestration trio); (14) Option 1 vs Option B vs synthesis decision for planner UX redesign. _Prior:_ **DEC-38 + IMPL `77fce3b` — `PlanPredecessor` typed with `ContinuationKind` (EXTENSION / REVISION / RENEWAL / SUPERSESSION, default EXTENSION) + translated `rationale` TextField.** Plan continuation is now typed-not-boolean: every Plan↔predecessor edge declares one of 4 kinds (each with distinct WASC evidence semantics — sustained focus / reflection-driven improvement / standing practice / adaptive learning from failure) and carries school-authored rationale prose mirroring the DEC-32 uniform pattern. `plans/choices.py::ContinuationKind` new TextChoices (4 members exactly; no fallback). `plans/models/plan.py::PlanPredecessor` adds 2 fields. `plans/translation.py` registers `PlanPredecessorT(fields=("rationale",))`. `plans/admin/plan.py::PlanPredecessorInline` base promoted from `admin.TabularInline` to `TranslationTabularInline` (precedent: 8 sibling rationale-bearing inlines). `plans/0041_planpredecessor_continuation_type_and_more` auto-generated additive AddFields; reversible; dep `0040_year_group_fks`. `plans/tests/test_planpredecessor.py` extended with 4 new tests (default; parametrized round-trip ×4; blank rationale; translation registration). `docs/domain-model.md` new `PlanPredecessor` entry. **Process note**: IMPL stopped per mandate-008 on interactive makemigrations prompt for the non-nullable continuation_type default (Django couldn't prove dev DB row count statically; 0 rows in actuality); orchestrator-direct continuation per DEC-38's explicit fallback authorization added `default=ContinuationKind.EXTENSION` (both migration-safety and the natural semantic — "continuing unfinished prior work" is the most common reading). Pre-stop work preserved + folded into the one IMPL commit. Gate independent: check 0, makemigrations No changes, ruff baseline-only (6 pre-existing), format clean (423 files), **mypy 297** unchanged, **pytest 1500** (+7 from 1493 baseline), **make test-js 42** unchanged. PyRight 2 `ContinuationKind` "not accessed" FPs (used at class scope + parametrize decorator); documented. **Dev DB**: `plans/0041` applied + verified via shell round-trip on real Postgres (REVISION-typed PlanPredecessor with non-blank rationale round-trips correctly including `get_continuation_type_display()='Revision'`; second probe edge without explicit continuation_type lands as `extension` default; probes cleaned up; PlanPredecessor count returned to 0). **OUT OF SCOPE per DEC-38** (deliberate, mandate-007-surfaced for separate user direction): (1) `Plan.is_continuation` derived @property; (2) `Plan.continuation_narrative` TextField for the soft-continuation case (consultant-doc `continuation: true` proposals — `p-pd-eval-align`, `p-plc-build` — that don't have predecessor Plan records); (3) `_plan_predecessors` grounding emitter; (4) `web/canonical.html` + `web/data/plan-data.js` "Continuation of:" render-section update; (5) grant migration (model-scoped perms cover new fields). **NO US-STATUS move** (typing an existing edge is model refinement, not user-story-enabling). **Standing-open list** unchanged + adds the soft-continuation gap as a discrete tracked item per DEC-38's own out-of-scope clause: (1) responsibility atoms (admin-authored, owed — also includes seat-level scope_summary prose for each of the 46 Positions per the prior commit pair); (2) `Division.head` FK assignments — Persons fully present now; (3) orchestration trio (US-LLM-24/25/23 + stored stage substate + US-UI-6); (4) carry-forward items 4 + 5 (`tenant_chiway` app, AccreditationBody parent); (5) **NEW** — soft-continuation `Plan.continuation_narrative` field decision; (6) **NEW** — `canonical.html` "Continuation of:" section addition decision. _Prior:_ **POSITION.SCOPE_SUMMARY ADDED (`fa3a1f0` + correction `bc6bf8a`) — the two responsibility-atom containers (Division + Position) now have identical container shape: prose overview (translated CharField(500)) + ordered atoms.** Canonical pipeline (plan → IMPL `fa3a1f0` per plan's explicit `TextField` declaration); discrepancy surfaced post-IMPL (my plan's rhetoric said "Mirrors Division.scope_summary shape exactly" but Division's is actually CharField(max_length=500), not TextField); user-directed re-cut to strict CharField(500) mirror landed forward as orchestrator-direct correction `bc6bf8a` (1 model line swap + migration regen; tests/translation/admin/grounding from fa3a1f0 unchanged — modeltranslation is shape-agnostic; tests use short strings that fit within 500-char cap). fa3a1f0's TextField migration was never applied to dev DB (orchestrator paused per the surfaced discrepancy), so no schema rollback. **Final state**: `Position.scope_summary = CharField(max_length=500, blank=True, default="")`, translated via modeltranslation (3 columns: scope_summary + _en + _zh_hans, all CharField(500)); `school/0056_position_scope_summary` (additive AddFields, reversible, depends on 0055); `PositionT.fields = ("label", "scope_summary")`; `PositionAdmin.list_display`+`search_fields` extended to mirror DivisionAdmin precedent; grounding `_divisions` positions-nest emits `scope_summary` per position. 4 new tests (round-trip + translation-fields-exist + fallback + grounding-shape). Gate independent (post-correction tree): check 0, makemigrations No changes, ruff baseline-only (6 pre-existing), format clean, **mypy 297** unchanged, **pytest 1493** (+4 over 1489 baseline), **make test-js 42** unchanged. PyRight: numerous `_`-prefixed unused-unpack diagnostics + pytest-django `db` fixture-injection — all documented false positives. **Dev DB**: `school/0056` applied + verified — 46 existing Positions all have `scope_summary = ""` blank default; round-trip via shell-edit on "IHS Principal" Position confirmed CharField+translation wire-through works. **NO US-STATUS move** (structural-consistency field addition, not a user-story-enabling change). **Process note**: my plan introduced a self-contradiction (rhetoric vs explicit declaration); IMPL correctly followed the explicit spec per mandate-008; orchestrator surfaced honestly per mandate-007 rather than silently keeping the divergent landing; user-directed correction landed forward (no destructive revert) as a new commit pair. Forensic trail intact in both commit bodies. **NEXT-FOCUS pointers unchanged**: (1) responsibility atoms (admin-authored, owed — now also includes seat-level `scope_summary` prose for each of the 46 Positions); (2) `Division.head` FK assignments; (3) orchestration trio (US-LLM-24 → 25 → 23 + stored stage substate + US-UI-6); (4) carry-forward items 4 + 5 (`tenant_chiway` app, AccreditationBody parent). _Prior:_ **IMPORTER MAPPING EXTENDED + AXEL CHEN'S ROW LANDED (`0b475cc`) — D7 roster owed-item now FULLY closed.** Pairs with `06f9ba2` (the `school/0055_seed_innovation_center` seed). `users/management/commands/import_staff_roster.py`: `TITLE_TO_POSITION` += `"Director of Future Scholar Innovation Center"` → `("Future Scholar Innovation Center", "Director of Future Scholar Innovation Center")` (labels CSV-canonical per 470b320); module docstring + in-file comment updated to the now-complete coverage (described as "15 Divisions + 46 Positions across school/migrations/0006…0055"). `test_real_csv_dry_run` flipped from "exactly one FAILED row" to "FAILED count == 0 + tally contains `errors=0`"; dry-run rollback assertions preserved. Gate independent: check 0, makemigrations No changes (no model touches), ruff baseline-only (6 pre-existing), format clean (421 files), **mypy 297 source files** unchanged, **pytest 1489** unchanged (assertions replaced, not added), **make test-js 42** unchanged. PyRight diagnostics — same documented false positives (`*args` / `row_number` / pytest-django `db` fixture); mypy clean. **Dev DB (orchestrator post-edit pass):** importer real run reported `persons_created=1, persons_reused=55, holdings_created=2, holdings_deduped=73, errors=0`. **Axel Chen landed**: 1 new Person (Ge Chen / Axel, email `chenge@chiway-repton.com`) + 1 new User + **TWO** new Holdings (Position cell `"Biology Teacher & Director of Future Scholar Innovation Center"` → Holding(Biology Teacher / Sciences) + Holding(Director of Future Scholar Innovation Center / Future Scholar Innovation Center)). **`holdings_created=2` corrects the prior tracking projection in 21d7975 which said "73→74"** — Axel carries TWO atoms not one, so the actual importer-created Holding move is 73→75. Dev DB totals post-run: `Person.objects.filter(school=tenant).count() = 57`; `User.objects.filter(school=tenant).count() = 57`; `Holding.objects.filter(person__school=tenant).count() = 75` (the +1 over importer-created Person count = 1 pre-existing non-importer Person/User pair, present before importer ran — likely admin-added). **D7 roster owed-item is now FULLY closed** (56/56 logical CSV rows imported; 0 unmapped errors; tenant Person/Holding inventory fully reflects the CSV staff roster). US-ONB-4 remains `enabled` (no US-STATUS row moved — this closes an importer-side gap, not enabling new behavior). **NEXT-FOCUS pointers**: (1) responsibility atoms for the 16 leader + 31 staff Positions (admin-authored, owed per `docs/hod-default-responsibilities.md`); (2) `Division.head` FK assignments — Persons fully present now; (3) orchestration trio (US-LLM-24 → 25 → 23 + stored stage substate + US-UI-6) — content fully populated on both leadership + roster sides; (4) carry-forward items 4 + 5 (`tenant_chiway` app, AccreditationBody parent). _Prior:_ **FUTURE SCHOLAR INNOVATION CENTER DIVISION + DIRECTOR POSITION SEEDED (`06f9ba2`) — structural side of Axel Chen's importer-row gap closed.** Orchestrator-direct seed per CLAUDE.md convention (small enough — 1 Division + 1 Position in one migration). `school/0055_seed_innovation_center` (idempotent reversible RunPython; single migration combining both rows so the Division precondition for the Position is satisfied in one operation): Division "Future Scholar Innovation Center" top-level (parent NULL; non-blank `scope_summary` "Future Scholar Innovation Center programs and initiatives." per the `test_seed_departments.py::test_divisions_have_scope_summary` invariant); Position "Director of Future Scholar Innovation Center" under the new Division, `kind="leader"` (DEC-37 discriminator), `year_group=NULL` (unit-wide, not year-bound), `label_zh_hans=""` (DEC-4). Both labels CSV-verbatim per the canonical-CSV-label convention (470b320). `get_or_create((school,label))` for Division, `get_or_create((division,label))` for Position. Reversible: unseed deletes Position first (avoids FK constraint), then Division. New `test_seed_innovation_center.py` (4 tests). `test_seed_departments.py::EXPECTED_LABELS` extended with `"Future Scholar Innovation Center"` (strict-set-equality top-level assertion would otherwise reject the addition — dynamic-keeping-pace pattern from `b0ff1fd`/`71e5fff`). **Deliberate narrow-directive parse** (user said "seed innovation center division and director position"): the `import_staff_roster` `TITLE_TO_POSITION` mapping was NOT extended here; that is a separate code change + re-run decision the user owns. The importer's `test_real_csv_dry_run` therefore still asserts the same 1 unmapped-atom error (structural side satisfied; mapping side not). No Holding for Axel; no `Division.head`; no responsibility atoms; no admin/grant changes (school/0036 + school/0042 Division+Position grants cover the new rows generically). Gate independent: check 0, makemigrations No changes, ruff baseline-only (6 pre-existing), format clean (421 files), **mypy 297 source files** (+1 test module), **pytest 1489** (+4), **make test-js 42** unchanged. Dev DB: `school/0055` applied + verified — Division top-level with the non-blank scope; Position with `kind=leader`, `year_group=NULL`, `label_zh_hans` blank; **tenant top-level Divisions 7 → 8**, **tenant Positions 45 → 46**. **Next steps to land Axel's row (user-decided):** extend `TITLE_TO_POSITION` with `"Director of Future Scholar Innovation Center"` → `("Future Scholar Innovation Center", "Director of Future Scholar Innovation Center")` + update `test_real_csv_dry_run` to assert 0 unmapped / 56 clean + re-run importer (would create 1 new Person/User/Holding for Axel, tenant counts 55→56/55→56/73→74). Other open: (2) responsibility atoms (admin-authored, owed); (3) `Division.head` FK assignments — feasible now; (4) orchestration trio (US-LLM-24 → 25 → 23 + stored stage substate + US-UI-6); (5) carry-forward items 4 + 5 (`tenant_chiway` app, AccreditationBody parent). _Prior:_ **STAFF ROSTER IMPORTER LANDED (`9ec2a97`) — US-ONB-4 → enabled.** `users/management/commands/import_staff_roster.py` (first management command in the project) + 8 tests + 2 package-init files. Args: `csv_path` positional + `--school-slug` (default `chiway-repton-xiamen`) + `--dry-run`. Per-row atomic (`_RowError` propagates rollback per row; whole import is NOT one big transaction → per-row isolation lets failures co-exist with successes). Two module-level mapping tables: **`DIVISION_MAP`** (13 entries — CSV Division cell after `_normalize_cell` → seeded `Division.label`; absorbs typos "Mathmatics Subject Group" / "Student AffairsvOffice" / curly-apostrophe variant) and **`TITLE_TO_POSITION`** (44 entries — CSV atom → `(Division.label, Position.label)`; absorbs CSV typos "Drector of AAO" / "Assistant the Student affairs" / "I G2 Year Leader", case variants "English teacher", "Head of <X> Subject Group" → seeded "Head of <X>"). Strict-fail per mandate-004 on unmapped Division / unmapped title atom / empty email / email-collision-with-unlinked-User (each raises `_RowError` → that row rolls back, other rows continue). Forward-fill Division across rows where the cell is blank (CSV uses Excel-style merged-cell pattern). Idempotency: User by normalized email → reuse if Person-linked, fail-fast if unlinked collision, else create_user; Person by user-link fallback to `(school, family, given, display)` 4-tuple, else create + link; Holding via `get_or_create((person, position))`. Person field map: family ← "Last Name", given ← "First Name", display ← "English Name", name_native blank (CSV has no 汉字 column). User field map: email normalized, school ← tenant, password=None (unusable — onboarding flow's email-code first-login sets it per signup_user precedent); is_active default True; first_name/last_name/chinese_name blank (Person is the authoritative name surface). Per-row stdout via BaseCommand styles ("row N: ok — Person(display) [email] with K Holding(s)" / "row N: FAILED — reason") + final tally (persons_created/persons_reused/holdings_created/holdings_deduped/errors). 8 tests: dry-run-no-writes; creates-persons-and-users-and-holdings; idempotent-rerun; unmapped-atom-fails-row; unmapped-division-fails-row; email-normalization; email-collision-with-unlinked-user-fails; real-csv-dry-run (loads actual CSV, asserts exactly 1 unmapped error = "Director of Future Scholar Innovation Center"). **IMPL deviations (2 minor, both surfaced by `test_real_csv_dry_run` against the actual CSV, neither covered by the directive's mapping table; resolved surgically rather than stopping):** (1) curly apostrophe `’` (U+2019) in row 3's `"IHS Principal’s Office"` → `_normalize_cell` extended to map it to ASCII `'`; (2) `&` overloaded in row 29 as both separator AND compound-name glue (`"Business Teacher & Head of Economics & Business Subject Group"`) → `_split_title_atoms` pre-rewrites `"Economics & Business"` → `"Economics and Business"` before splitting; `TITLE_TO_POSITION` key updated to the rewritten form. Both documented inline + in commit body. Gate independent: check 0, makemigrations No changes, ruff baseline-only (6 pre-existing), format clean (419 files), **mypy 296 source files** (+1 source — the new command module; counted by file rather than the +1 delta since the test-file added to source count too), **pytest 1485 passed** (+8 new), **make test-js 42** unchanged. PyRight 3 diagnostics — all documented false positives per project convention (`test_import_staff_roster.py:43` `db` not accessed = pytest-django fixture injection; `import_staff_roster.py:267` `*args` not accessed = Django `BaseCommand.handle` required signature; `import_staff_roster.py:357` `row_number` not accessed inside `_process_row` = passed for caller-side stdout context, harmless). Mypy is the type gate; clean. **Dev DB (orchestrator post-IMPL pass):** dry-run + real + idempotent-rerun against `postgres://postgres:postgres@localhost:5433/school_improvement_plans`. **CSV row count clarification:** plan said "61 staff rows" but `csv.DictReader` produces **56 logical rows** (embedded quoted newlines in 5 Division cells + Caroline Yang's Position-cell-starts-with-newline collapse 62 file lines into 56 logical rows); importer correctly swept all 56. **Dry-run result:** 55 ok / 1 fail (row 18 Axel Chen — "Director of Future Scholar Innovation Center", the expected Innovation Center precondition gap) / 73 Holdings would be created / 0 DB rows written. **Real run result:** **55 Persons + 55 Users + 73 Holdings created** + 1 row failed (Axel Chen, same gap). Spot-checks pass (Steve Lu → 3 Holdings: Physics Teacher + Head of Sciences + Director of Teaching and Curriculum Center; David Ryan → 2: English Teacher + Senior Teaching Coordinator; James Tang → 3: Math Teacher + AP Coordinator + AS Year Leader). **Idempotent re-run result:** 0 persons created / 55 reused / 0 holdings created / 73 deduped / 1 failed (same gap) — **idempotency confirmed**. **D7 roster owed-item now substantially closed (55/56 logical rows imported).** Tenant Person/Holding tables: 0 → 55 + 73 on dev DB. 55 new Users carry unusable password (will set via US-ONB-1/2 email-code first-login flow). **NEXT-FOCUS pointers:** (1) Innovation Center Division + Director Position seed (closes Axel Chen's row's gap, brings to 56/56); (2) responsibility atoms for the 15 leader Positions + 30 staff Positions (admin-authored, owed per `docs/hod-default-responsibilities.md`); (3) `Division.head` FK assignments — now feasible (Persons exist); (4) orchestration trio (US-LLM-24 → 25 → 23 + stored stage substate + US-UI-6) — the broader north-star, now content-rich on both leadership and roster sides; (5) carry-forward items 4 + 5 (`tenant_chiway` app, optional AccreditationBody parent) remain pending user direction. _Prior:_ **25 CSV-IMPLIED POSITIONS PRE-SEEDED (`e10ddc5`)** — user-directed pre-seed before roster importer US-ONB-4. `school/0054_seed_subject_teachers_and_specialty_roles`: 16 subject teachers (Math/Sciences/English/Humanities/Business and Economics/Art/Music+Sports — with their subject-specific labels) + 9 specialty roles (IHS Assistant under PO; Academic Affairs Officer + Edexcel Exam Officer + CIE Exam Officer + AP Coordinator under AAO; Senior Teaching Coordinator under C&T; Assistant to Student Affairs under SAO; College Counselor under College Counseling Department; Librarian under Library). All `kind="staff"`. Idempotent + reversible. `test_seed_positions.py` EXPECTED dict extended from 6 → 13 keys; new 4-test file. Gate independent: check 0, makemigrations No changes, ruff baseline-only, format clean, **mypy 292** (+1), **pytest 1477** (+4), **make test-js 42** unchanged. Dev DB: `0054` applied + verified — **tenant Position inventory now 45 total (15 leader + 30 staff)** across 13 of 14 Divisions (Recruitment & Admissions has none — no CSV-identified role). **All CSV staff-row title strings now have a seeded Position to map to**; roster importer (US-ONB-4) fully Position-side unblocked — becomes a pure Person + Holding creation task with a (CSV-title → seeded-Position-label) mapping table. **NEXT-FOCUS pointers**: (1) roster importer US-ONB-4 — major next build, all preconditions met; (2) responsibility atoms (admin-authored, owed); (3) `Division.head` FK assignments — wait on roster; (4) orchestration trio. _Prior:_ **CSV-CONTROLLING RENAME (`470b320`): `(SAO, "Dorm Supervisor")` Position → `"Dormitory Teacher"`.** User-directed: CSV is canonical for Position labels. `school/0053_rename_dorm_supervisor_to_dormitory_teacher` (in-place UPDATE preserving pk + future-safe for any incoming FKs; reversible). Immutable `school/0052` left untouched; the rename runs after it (cumulative state on any DB is "Dormitory Teacher"). Tests updated post-`0053`. Gate independent: check 0, makemigrations No changes, ruff baseline-only, format clean, **mypy 291** unchanged, **pytest 1473** unchanged (pure rename), **make test-js 42** unchanged. Dev DB: `0053` applied + verified — old absent, new present, **pk preserved (=6)** confirming the in-place UPDATE pattern. Convention established: **CSV is canonical for Position labels going forward.** Subject-teacher Positions (Math Teacher / English Teacher / etc. — 27 CSV rows across 7 subject depts) NOT seeded — flagged per mandate-007 as the next likely importer precondition. **NEXT-FOCUS pointers**: (1) subject-teacher Position seeds (or importer creates them on-the-fly — design choice the importer plan will settle); (2) roster importer US-ONB-4 — major next build; (3) responsibility atoms; (4) orchestration trio. _Prior:_ **3 ADDITIONAL POSITIONS SEEDED (`f2218af`)** — Art HOD + College Counseling Coordinator (the 2 leader Positions unblocked by the 4-Division seed) + Dorm Supervisor (user's admin-added row now reproducible). `school/0052_seed_additional_positions` (idempotent + reversible). `kind` explicit on all 3. `get_or_create` no-op against the user's existing Dorm Supervisor row on the dev DB (confirmed). `test_seed_positions.py` EXPECTED extended (SAO +Dorm Supervisor; 2 new top-level entries). New 4-test file. Gate independent: check 0, makemigrations No changes, ruff baseline-only, format clean, **mypy 291** (+1), **pytest 1473** (+4), **make test-js 42** unchanged. Dev DB: `0052` applied + verified. **Tenant Position inventory now 20 total (15 leader + 5 staff)**: leader = 6 HOD + 4 Director + 4 Year Leader + 1 Coordinator; staff = PD Leader + Exam Officer + Class Schedule Officer + Homeroom Teacher + Dorm Supervisor. Phantom College Counseling Division was deleted earlier this session via admin per user direction (dev DB now 14 Divisions, matches seed-derived count). **Mandate-007 surfaced**: CSV's 4 "Dormitory Teacher" rows under SAO have no matching Position yet; roster importer (US-ONB-4) will need it OR those rows fail; deferred to user direction. **NEXT-FOCUS pointers**: (1) Dormitory Teacher Position seed; (2) roster importer US-ONB-4 — major next build, all preconditions met (modulo Dormitory Teacher); (3) responsibility atoms; (4) orchestration trio. _Prior:_ **4 ADDITIONAL DIVISIONS SEEDED (`71e5fff`)** — structural precondition for the roster importer US-ONB-4. `school/0051_seed_additional_divisions`: Art + Music and Sports under C&T (subject-dept pattern, blank scope); College Counseling Department + Library top-level (service-unit pattern, non-blank scope). Idempotent + reversible. 5 new tests in `test_seed_additional_divisions.py`; 2 existing EXPECTED sets extended (`test_seed_departments.py`, `test_seed_subject_departments.py`) — same dynamic-keeping-pace-with-seeds pattern from the `b0ff1fd` precedent. Process note: IMPL stopped per mandate-008 on a directive deficiency (my "no test EXPECTED breakage" claim was scoped to `test_seed_positions.py` only — missed the two analogous EXPECTED sets); resolved via fresh self-contained agent re-spawn. Gate independent: check 0, makemigrations No changes, ruff baseline-only, format clean, **mypy 290** (+1), **pytest 1469** (+5), **make test-js 42** unchanged. Dev DB: `0051` applied + verified — 4 new Divisions present with correct parent/scope fields. **Discrepancy surfaced (mandate-007 honest):** dev DB has **15 Divisions, not 14** — there's a phantom row `"College Counseling"` (no "Department" suffix, blank scope, parent=NULL) admin-added at some prior point, parallel to the Dorm Supervisor case (school/0028 also owed a seed for an admin-added row). Canonical "College Counseling Department" exists (from 0051) alongside the phantom; user-direction needed to delete the phantom via admin (it's user-owned data). **NEXT-FOCUS pointers:** (1) 2 leader-Position seeds for the new Divisions (Head of Art Subject Group; College Counseling Department Coordinator) — DEC-36 pattern; (2) roster importer US-ONB-4 — major next build, all structural preconditions now met; (3) phantom-Division cleanup + Dorm-Supervisor seed at user direction; (4) responsibility atoms for the 13+ leader Positions (admin-authored, owed); (5) the orchestration trio (US-LLM-24 → 25 → 23). _Prior:_ **DEC-36 DIRECTOR-OF-X SEED LANDED (`b0ff1fd`) — SERIES COMPLETE.** Third and last of three downstream seeds DEC-37 unblocked. `school/0050_seed_director_positions` (idempotent reversible RunPython): 4 Director Positions under leadership Divisions — `(AAO, "Director of AAO")` + `(SAO, "Director of Student Affairs Office")` + `(C&T, "Director of Teaching and Curriculum Center")` + `(PO, "IHS Principal")`, all `kind=leader`, `year_group=NULL`, per-tenant labels CSV-verbatim (typo "Drector" cleaned). Tests: new `test_seed_director_positions.py` (2 tests); `test_seed_positions.py` EXPECTED updated + AAO blank-zh_hans test refactored to dynamic `len(EXPECTED[...])` (future-proof). Recruitment & Admissions skipped (no CSV leader); Innovation Center / College Counseling / Library / Art / Music & Sports out of scope (Divisions not seeded). Process note: IMPL stopped per mandate-008 on a directive ambiguity (test-file structure misidentified); resolved via fresh self-contained agent re-dispatch (SendMessage to quiescent agents hook-blocked; full-context-fresh-agent is the canonical resume). Gate independent: check 0, makemigrations No changes, ruff baseline-only, format clean, **mypy 289** (+1), **pytest 1464** (+2), **make test-js 42** unchanged. Dev DB: `0050` applied + verified — 4 Directors present; **full tenant leadership inventory now 13 leader Positions**: 5 HOD + 4 Director + 4 Year Leader. **SERIES COMPLETE: DEC-35 ✓ `6468ed1`, year-leader split ✓ `463054a`, DEC-36 ✓ `b0ff1fd`.** Structural leadership representation matches the CSV's identified roles for all currently-seeded Divisions. **NEXT-FOCUS POINTERS**: (1) responsibility atoms for any of the 13 leader Positions (admin-authored, owed); (2) Director seats for unseeded Divisions need their Divisions seeded first (Innovation Center / College Counseling / Library / Art / Music & Sports); (3) D7 roster + Holdings linking actual humans to these 13 seats; (4) the orchestration trio (US-LLM-24 → 25 → 23 + stored stage substate + US-UI-6) is the broader north-star, content-richer now that the leadership inventory is populated. _Prior:_ **YEAR-LEADER POSITION SPLIT LANDED (`463054a`)** — second of three downstream seeds DEC-37 unblocked. `school/0049_seed_year_leader_positions` retires the phantom `(SAO, "Year Group Leader")` Position from school/0028 (no real-world referent per CSV) and seeds 4 year-scoped `kind=leader` Positions under SAO: "AS Year Leader" / "A2 Year Leader" / "IG1 Year Leader" / "IG2 Year Leader", each FK'd to its YearGroup. Reversible (unseed deletes the 4 + recreates the original generic with 0028 defaults). NO Holdings existed; cascade empty. NO atoms; NO admin/grant changes. Gate independent: check 0, makemigrations No changes, ruff baseline-only, format clean, **mypy 288** (+1 test file), **pytest 1462** (+3), **make test-js 42** unchanged. Dev DB: `0049` applied + verified — generic absent; 4 year-leader rows present with correct kind=leader and year_group codes (as/a2/ig1/ig2); test_seed_positions.py EXPECTED dict SAO entry updated to the new 5-label set. **Series status: 2 of 3 downstream seeds done** (DEC-35 HOD ✓; year-leader split ✓). Remaining: DEC-36 Director-of-X (AAO/SAO/T&C/PO/Innovation Center/College Counseling), not blocked. _Prior:_ **DEC-35 HOD SEED LANDED (`6468ed1`)** — first of three downstream seeds DEC-37 unblocked. `school/0048_seed_hod_positions` creates 5 HOD `Position` rows under the subject depts ("Head of Business and Economics" / "Head of Humanities" / "Head of English" / "Head of Math" / "Head of Sciences"), per-subject labels, `kind="leader"` explicit, `year_group=NULL`, `label_zh_hans=""`. Idempotent reversible data migration mirroring `school/0028`. NO responsibility atoms (admin-authored, owed); NO Holdings (D7 roster owed); NO admin/grant changes. Gate independent: check 0, makemigrations No changes, ruff baseline-only, format clean, **mypy 287** (+1), **pytest 1459** (+2), **make test-js 42** unchanged. Dev DB: `0048` applied + verified — 5 HODs present with correct fields. **REMAINING in series**: DEC-36 Director-of-X seed (AAO/SAO/T&C/PO/Innovation Center/College Counseling); year-leader Position split (needs a one-line design choice on the existing "Year Group Leader" Position's fate before its IMPL). _Prior:_ **DEC-37 + COMMIT `dbe1685`: HOD/leader-recognition fork settled by `Position.kind` (TextChoices: STAFF default / LEADER).** Resolves fork 1 of `docs/hod-default-responsibilities.md`. Path (c) — explicit role-type marker on `Position` — chosen; paths (a) label-substring / (b) Division.head structural ruled out per mandate-004. `school.choices.PositionKind` mirrors existing `AssignmentKind`/`MilestoneStatus`/`ActionStepStatus` idiom. `school/0047_position_kind` AddField with default STAFF (all 6 existing positions remain correct); no CheckConstraint (choices alone bind). PositionAdmin lists+filters by kind; grounding `_divisions` emits `"kind"` per position. DEC-35's "no model change" framing superseded — small structural cost for label-matching robustness. Gate independent: check 0, makemigrations No changes, ruff baseline-only, format clean, **mypy 286**, **pytest 1457** (+4), **make test-js 42** unchanged. Dev DB: `school/0047` applied + verified — 6 existing positions default `staff`; PD Leader round-trips; grounding emits `kind` (both `staff` and `leader` surfaceable). **Three downstream seeds now unblocked** (each its own plan→IMPL): DEC-35 HOD positions per 5 subject depts; DEC-36 Director-of-X positions per leadership unit (AAO/SAO/T&C/PO/Innovation Center/College Counseling); year-leader Position splits per YearGroup (AS/A2/IG1/IG2). All three can write `Position(..., kind=PositionKind.LEADER)` cleanly. _Prior:_ **TENANT-VS-GENERIC CARRY-FORWARD ITEM 3 BUILT + VERIFIED (`ea0b5a6`): `YearGroup` model + per-element year-group scoping.** Realizes the user's "yeargroup model because plans' enactment and steps and criteria etc are different for different year groups" directive. Shape: `YearGroup` school-scoped vocabulary (`code` + translated `label` + `order`; `UniqueConstraint(school, code)`; `Meta.ordering=["order"]`; evergreen — not cycle-bound) mirroring `PriorityTier`. Four FK use-sites (all optional, PROTECT): `Position.year_group` (year-leader seats), `ActionStep.year_group`, `SuccessCriterion.year_group`, `Milestone.year_group` (per-element scoping when enactment differs by cohort; NULL = school-wide). Gate UNCHANGED. Migrations: school/0044 (model + Position.year_group FK + UniqueConstraint) + school/0045 (`school_admin` grant per DISC-19) + school/0046 (tenant seed: `ig1/IG1, ig2/IG2, as/AS, a2/A2`) + plans/0040 (3 plan-element FKs). Grounding extended (`_year_groups` emitter + SECTION_KEYS). Three planner forms + admins updated. Existing single "Year Group Leader" Position (school/0028) unchanged — splitting into 4 year-specific Positions gated by DEC-36's open recognition fork. Plan-level `Plan.year_groups` M2M not added (per-element coverage suffices). Tenant seed lands in `school/` for now; `tenant_chiway` app boundary (item 4) deferred. Gate independent: check 0, makemigrations No changes, ruff baseline-only, format clean, **mypy 286** (+3), **pytest 1453** (+15), **make test-js 42** unchanged. Dev DB: all 4 migrations applied + verified — 4 YearGroups seeded for tenant in order; school_admin holds 4 perms on yeargroup; all four FK round-trips work with reverse accessors (`yg.positions` / `.actionsteps` / `.successcriteria` / `.milestones`); temp probe rolled back cleanly. Three benign IMPL deviations documented (migration-graph leaf resolution; school-side dep tightening; test-isolation code change). **Items 1 + 2 + 3 → built/addressed. Items 4 (`tenant_chiway` app) + 5 (optional AccreditationBody parent) remain pending user direction.** _Prior:_ **DECISION (user) — DEC-36: generalize DEC-35's "leader = `Position` + `Division.head`" pattern to ANY `Division` whose leadership has allocatable-in-plans responsibilities, not only HOD/subject-dept.** Addresses cleavage carry-forward item 2. Discovered shape: every Chiway leadership unit (Directors of AAO/SAO/T&C/Innovation Center/PO/College Counseling, IHS Principal) is structurally identical to the HOD case — DEC-35 was narrowly stated; DEC-36 promotes the pattern. NO model change (DISC-22 machinery already supports any Division); per-Division seeding decisions remain per-tenant; DEC-35 stays valid as first specific instance. Open question inherited (unresolved): how the system recognizes a leader Position vs. other Positions — `docs/hod-default-responsibilities.md` fork 1; same question covers all leader-Position seeds, settling once unblocks all. No new US row, no code, no migration. Persisted: DEC-36 (seed-round-plan.md) + cleavage doc item 2 → addressed + cleavage-bleed point 3 updated + domain-model.md `Division.head` and `Position` entries cross-ref both DEC-35 and DEC-36 + LOG. **Cleavage carry-forward items 1 + 2 → BUILT/ADDRESSED. Items 3 (YearGroup model), 4 (`tenant_chiway` app), 5 (optional AccreditationBody parent) remain pending user direction.** _Prior:_ **CLEAVAGE CARRY-FORWARD ITEM 1 BUILT + VERIFIED (`51ff5cd` + `363454f`): locale-neutral `Person` name structure + project-wide modeltranslation fallback fix.** Two code commits: (1) `51ff5cd` replaced `Person.name` (single translated CharField) with four locale-neutral fields — `family_name`/`given_name`/`display_name` (Latin, untranslated) + `name_native` (translated, `_en`+`_zh_hans`); new `__str__` fallback chain (display → given+family → name_native → pk-defensive); `Meta.ordering = ["family_name", "given_name"]`; migration `school/0043` (6 AddField + 3 RemoveField + 1 AlterModelOptions; 0 rows → safe); ~30 test fixtures + new positive coverage. User auth model UNTOUCHED. (2) `363454f` added `MODELTRANSLATION_FALLBACK_LANGUAGES = ("en", "zh-hans")` to `config/settings/base.py` after dev-DB verify of (1) exposed a defect — modeltranslation's active-language proxy returns empty when only `_zh_hans` has content under EN-active context. **Project-wide audit found the same bug pattern in 32 of 47 `__str__` methods** (Position, Division, Plan, Phase, Milestone, ActionStep, Communication, Evidence, FeedbackChannel, ReviewEvent, RevisionRule, SuccessCriterion, every reference-vocab label-bearing model, School.name itself); ONE config line fixes them all (read-only fallback cascade; no save/filter change). New regression tests in `school/tests/test_translation_fallback.py` (Position + DivisionResponsibility) + chinese-active reverse-direction test in `test_person.py`. Gate independent: check 0, makemigrations No changes, ruff baseline-only, format clean, **mypy 283**, **pytest 1438**, **make test-js 42**. Dev DB: `school/0043` applied + verified (six new cols + three dropped); 4-path Person `__str__` round-trip all correct including `path 3 zh_hans-only → "方苑苑"` (previously `"Person #2"`); Position `label_zh_hans`-only → `"数学教师 (...)"`; DivisionResponsibility `statement_zh_hans`-only → `"按时检查作业"`. **Cleavage carry-forward item 1 → BUILT + VERIFIED.** Items 2–5 (DEC-35 generalization, YearGroup model, `tenant_chiway` app, optional AccreditationBody parent) remain pending user validation. Process notes: IMPL of `51ff5cd` applied `0043` to dev DB during its run (orchestrator territory, idempotent, re-verified); a transient AskUserQuestion proliferated options when path was settled → recovered to single-path plan + saved memory `feedback-options-proliferation-noise`. Working-tree: intake doc + roster CSV remain user-owned working surfaces. _Prior:_ **FUTURE-FOCUS DESIGN DOC → `docs/tenant-vs-generic-cleavage.md`** (user-directed 2026-05-26): the tenant-Chiway-focused-app vs generalized-multi-tenant cleavage analysis triggered by reading `data/may-27-2026-staff-names-crs.csv` (61-row staff roster). Substance verbatim, not user-validated. CORE FINDING: the model layer is already ~90% generic (every school-scoped row carries a `school` FK; tenant content is in seed migrations scoped to `chiway-repton-xiamen`); THREE places where tenant opinion bleeds past the data layer: (1) **`Person.name` is undermodeled** — single translated field but the CSV exposes a three-part Latin name (Pinyin family / Pinyin given / English chosen) + missing native-script field; locale-neutral remedy = `family_name`/`given_name`/`display_name`/`name_native[+ translated variants]`; (2) **year groups unmodeled** but tenant data implies them (AS/A2/IG1/IG2 vs 9/10/11/12 vs DP1/DP2); remedy = generic `YearGroup` model + tenant labels; (3) **DEC-35's HOD-as-Position pattern is stated narrowly** (subject-dept only) but the CSV shows the same shape at every leadership unit (Directors of AAO/SAO/T&C/Innovation Center/IHS Principal); generic remedy = extend DEC-35 to "any leadership unit." CLEAVAGE RECOMMENDATION: two layers — Layer A (current apps: generic model + global reference; **no new tenant seeds going forward**) + Layer B (a NEW `tenant_chiway` app, zero models, data migrations only; second tenant = parallel `tenant_xxx` app via INSTALLED_APPS); existing `school/0006–0042` historical migrations stay in place. CARRY-FORWARD (5 items, ranked): (1) locale-neutral `Person` name structure; (2) DEC-35 generalization; (3) `YearGroup` model; (4) create `tenant_chiway` app — first migration = the owed Dorm Supervisor seed; (5) optional — `AccreditationBody` parent / promote planning recipes to a global curated library. **NEXT FOCUS (user-directed): item 1 — the Person name shape.** _Prior:_ **SYNTHESIS REFRESH** capturing post-US-LLM-27 micro-events + dev-DB observations through 2026-05-26T16:10. **HEAD `083df81`** (`data: school-content-intake §1 (mission) — mark satisfied …`). Sequence since the prior STATE: `ea8a1df` US-UI-13 row added → `53fce6b` `data/school-content-intake-2026-05-26.md` created (date-stamped working intake doc covering mission, SLO descriptions, division/position responsibility atoms, HOD seats + the DEC-35 default mapping concise restatement, roster D7, holdings + heads, additional positions/policies, accounts) → user authored a mission `GuidingStatement` via admin (prose + 3 clauses) and hand-added a Dorm Supervisor `Position` under SAO → `083df81` marked intake §1 satisfied. **Dev-DB observations recorded:** mission now present in the dev DB (prose + 3 clauses) but **(a) all 3 mission clauses carry `order=0`** (admin form didn't auto-sequence; likely intended 0/1/2 for GROUNDED/ROUNDED/UNBOUNDED — admin-editable, not blocking) and **(b)** the prose carries Windows CRLF line endings (cosmetic). Vision still seeded (prose + 6 clauses, properly ordered 0–5). Dorm Supervisor exists in the dev DB only — **not in any migration**, so a fresh-build environment would not carry it; a small additive `school/0043` (idempotent `get_or_create((SAO, "Dorm Supervisor"))`) is the reproducibility path, no-op on the dev DB. Otherwise unchanged: 0 Persons/Holdings/heads; 0 `DivisionResponsibility`/`PositionResponsibility` atoms (admin-authored, owed); 6 of 7 SLO descriptions still empty (only Confident seeded). **Working-tree note:** `data/school-content-intake-2026-05-26.md` carries uncommitted user edits on top of `083df81` (working surface — orchestrator does not auto-commit it). **NEXT FOCUS unchanged:** the orchestration trio (US-LLM-24 → 25 → 23) + stored drafting-stage substate + US-UI-6 (the per-element evidence assist layer is complete after US-LLM-26 + 27). **Standing open items the user owns:** the three `docs/hod-default-responsibilities.md` forks (fork 1 — HOD-recognition — is load-bearing, bears on DEC-35's "no model change"); the Dorm-Supervisor seed migration call; the US-UI-13 design/scope; the content authoring against the intake doc (clauses ordering, SLO descriptions, responsibility atoms, mission seed-vs-admin choice, roster D7). _Prior:_ **STORY ADDED (user-directed) — US-UI-13: "A school admin uses an intuitively designed interface to populate school-related content"** (status pending; persisted faithfully — no augmentation; scope intentionally general; the only adjacent stories are US-UI-11/12 [narrow, Django-admin-mediated authoring of `DivisionResponsibility` atoms and vision/mission clauses] + US-ONB-3/4/5 [user/account population], confirming this as a real gap — the rest of school reference data is populated via orchestrator-direct seed migrations + the generic `school_admin` Django admin CRUD, neither story-backed as a designed interface). Persisted to `phases/00-preamble.md` § "User-story milestones (inlined)" + `phases/US-STATUS.md`. No DEC (design pending). _Prior:_ **US-LLM-27 BUILT → enabled (`622aa35`): the responsibility-fulfillment assist** — per action step, which responsibility atom(s) it fulfills + `rationale`, in BOTH DISC-22 grains (`ActionStepResponsibility`→`DivisionResponsibility`; `ActionStepPositionResponsibility`→`PositionResponsibility`). Realizes DISC-22's purpose (the division+position inventory feeding the orchestration). One assist `propose-responsibilities` (target_step="steps", preview="responsibilities", grounding `("school","divisions","draft_state")`); per-step two-grain object contract (F5 doubled); both grain lists optional (NOT gate-required); resolve maps statement+owner→school-scoped pk (drop-unresolvable; LLM never sees pks); client `applyResponsibilitiesPrefill` fills both nested formsets per step (FK + EN `-rationale`); seed `ai/0017` (new template via get_or_create). Soft coherence by prompt + through-model clean(), not hard-coupled to F2. NO model/schema/capture/render/grounding/gate change; NO plans/school migration. Gate independent: check 0, makemigrations No changes, ruff baseline-only, format clean, mypy 282, pytest 1431, make test-js 42. ai/0017 applied + verified on dev DB (two-grain contract; temp atoms parse+resolve to `{pk,rationale}`, unknown dropped, school-scoped, rolled back). **The per-element evidence assist layer is now COMPLETE (US-LLM-26 + 27).** NEXT FOCUS = the orchestration trio (US-LLM-24 → 25 → 23) + stored drafting-stage substate + US-UI-6. _Prior:_ **US-LLM-26 BUILT → enabled (`dda9291`): the A3 domain-alignment assist now proposes a per-relation `rationale` (the WASC accreditation evidence)** — closes the DEC-32 L2b half-build (the six alignment through-models' rationale columns were filled by nothing). OUTPUT CONTRACT → object-per-row `{label,rationale}` per relation (selection rules unchanged; rationale lenient-optional; EN-only); `parse`→`{label,rationale}` / `resolve`→`{pk,rationale}` (drop-unresolvable preserved); client `applyAlignmentFormsetsPrefill` fills the EN `-rationale` field; **`ai/0016` = a GUARDED REVERSIBLE template-body UPDATE migration, establishing the reusable prompt-template-evolution pattern** (get_or_create won't update; the precedent for the HOD-doc fork-2). NO model/schema change; AssistSpec/forms/formsets/grounding/gate untouched. Gate independent: check 0, makemigrations No changes, ruff baseline-only, format clean, mypy 281, pytest 1394, make test-js 38. ai/0016 applied + verified on dev DB (body now requires objects; parse captures rationale; resolve emits `{pk,rationale}` + drops unresolvable). NEXT = US-LLM-27 (responsibility-fulfillment assist over the DISC-22 inventory). _Prior:_ **DEC-35 (user): HOD = a `Position` under the subject-dept `Division`, complementary to `Division.head`** — so HOD responsibilities enter the DISC-22 allocatable inventory (`PositionResponsibility` atoms = the HOD's job description) and the HOD is `Assignment.position`-assignable for US-LLM-24/27; `Division.head` retained as the org-leader rollup pointer; a dept leader is linked both ways. **NO model change** (the DISC-22 Position/PositionResponsibility/Assignment.position/grounding machinery exists); follow-on build = an orchestrator-direct seed creating an HOD `Position` under each of the 5 subject depts + admin-authored responsibility atoms (people-linking D7-blocked). Supersedes the domain-model "an HOD is not a position" prior art (corrected). Persisted DEC-35 + domain-model + LOG. _Prior:_ **DISC-22 RESOLVED + BUILT (`a38c561`) — position grain of responsibility.** `PositionResponsibility` (school/0041; mirrors `DivisionResponsibility`, no own school FK — school via `position__division__school`; doubles as the position's job description) + `Assignment.position` (plans/0038; `assignment_target_matches_kind` rewritten to option (i): responsible = exactly one of {division, position}, person optional, stakeholder null; participant ≥1; existing division-set rows valid → no data fix) + `ActionStepPositionResponsibility` (plans/0038; a SECOND dedicated through-model + `ActionStep.position_responsibilities` M2M + soft coherence clean() — per the user's dedicated-not-polymorphic ruling; the 2a `ActionStepResponsibility` UNTOUCHED) + capture via `ACTIONSTEP_CHILD_FORMSETS["position_responsibility"]` (rides the generic per-step machinery, no wiring/JS added) + grounding `_divisions` nests positions + their atoms = the division+position responsibility INVENTORY + `school_admin` grants (school/0042, plans/0039). Gate predicate UNCHANGED (grain-indifferent); F2 UNCHANGED. Gate re-run independent: check 0, makemigrations No changes, ruff baseline-only, format clean, **mypy 281**, **pytest 1383**, **make test-js 37**. school/0041–0042 + plans/0038–0039 applied + verified on dev DB (both tables; 8 grants; responsible-position-only OK; division+position rejected; grounding nests positions+atoms). DISC-22 `resolved_by` filled. **US-LLM-27 now UNBLOCKED**; US-LLM-26 (A3 rationale) independent — next either order. _Prior:_ **DEC-17 / US-ORG-20 BUILT (`cc79349` + perm fix `3694fad`):** stored "blocked" = `plans.ActionStepImpediment` (per-`ActionStep` editable child — translated reason; server-stamped raised/resolved who/when; paired-resolution CheckConstraint) + `raise_impediment`/`resolve_impediment` services + school-scoped admin (resolve action, readonly stamps, open/resolved "list all blocked" filter) + `ImpedimentInline`; migrations plans/0030–0032. Blocked = open impediment, orthogonal to status. US-ORG-20 → enabled; gate pytest 632, mypy 199; applied + verified (all four group grants). **DISC-19 refined** (the perm fix): `school/0036` is a one-time grant → every new plans model after it must set `school_admin`'s grant EXPLICITLY in its own migration (view+strip for audit logs; full for editable children) — never rely on 0036 for already-migrated DBs (the dev DB exposed this; fresh-build tests don't). **Earlier this session — DEC-16 (3 slices):** per-step execution + global status auditability. Evaluated the model vs `web/canonical.html` → 2-of-6 WASC action-step columns had no model home; user chose the **audited** form + **global auditability**. Built: (A) `ActionStep.assessment` (translated) + `ActionStep.communications` M2M (`deca4d5`, plans/0025); (B) audited `ActionStep.status` — reusable `StatusTransition` base + `ActionStepStatusTransition` + `set_action_step_status` + readonly status + bulk actions + view-only scoped transition admin (`cba276f`, plans/0026–0027); (C) audited `Evidence.status` — `EvidenceStatusTransition` + `set_evidence_status`, closing `source-model-gaps §H` audit half (`c54f8046`, plans/0028–0029). Status auditability now global (Plan/Milestone/ActionStep/Evidence). **US-ORG-18 → enabled** (`cba276f`). New gotcha **DISC-19**: `school/0036`'s create-permissions-on-live-registry over-grants `school_admin` on new view-only plans models → each audit migration strips it. Gate pytest 607, mypy 198; plans/0025–0029 applied + verified on dev DB. **Earlier this session:** mockups `5de0381` (5 UX directions — front-end direction still unchosen by user); onboarding flow `9d637ae`; per-school-admin foundation `391b893` (DISC-18); subject depts `school/0035`; DISC-17 (`d5a2ad2`,`19cc102`). **Process note:** an earlier over-step (invented front-end blocker / mis-read directive) was corrected; memory `feedback-user-decision-is-a-directive-to-act` saved.)

## Current working mode (post-Phase-15)

The phased Plan(0–15) workflow (render phase prompt → IMPL/AUDIT) is complete. Post-Phase-15 work is **architecture refinement in discovery-driven slices**, following this loop (proven across DISC-14/15/16, DEC-6..13):
1. **Design is settled in conversation** with the user (a "think-out"), then **persisted** the moment it settles — a `DISC-NN`/`DEC-N` + a `docs/domain-model.md` entry + an `ORCHESTRATOR-LOG` line — never left only in chat.
2. **User stories** (e.g. `US-ORG-*`) are written into `phases/00-preamble.md` + `phases/US-STATUS.md` as the acceptance surface before building.
3. **plan mode** → write the plan to the plan file → **ExitPlanMode** for approval.
4. **Code** is built by a **focused IMPL subagent** (general-purpose, given a complete self-contained directive; STOPs on ambiguity), which runs the full static gate and commits. **Reference-data seeds are orchestrator-direct** data migrations (no subagent).
5. **Orchestrator then**: verifies the gate independently (`check`, `makemigrations --check`, `ruff`, `mypy`, `pytest`), applies migrations to the dev DB via the **postgres superuser DSN** (`postgres://postgres:postgres@localhost:5433/school_improvement_plans`), verifies schema, then writes the **docs/tracking** (discoveries `resolved_by`, US-STATUS flips, domain-model, source-model-gaps, seed-round DEC, LOG, STATE) and a tracking commit. **Tree always left clean.**
6. PyRight editor-LSP `.objects`/import errors are **false positives** (the harness LSP ignores the repo `pyrightconfig.json typeCheckingMode:off`); **mypy is the type gate**.

Two-part changes (model via IMPL, then data seed via orchestrator) are normal. Big changes are built in **forward-compatible slices** after the whole is designed — not deferred. The dev DB holds only the tenant `chiway-repton-xiamen`'s seeded reference data; plan-owned tables are empty.

## Current position

- **Active phase:** Phase 15 — final validation (IN PROGRESS), now extended by the post-Phase-15 architecture refinements (DISC-14/15/16/17), the `ai` LLM-service slice (DEC-21), and the **`planner` plan-creation front-end** (DEC-25/26/27; US-UI-1 + US-UI-2 enabled), the **`devmocks` DEBUG-only mock-launcher** (`4f68cb1`), the **US-LLM foundation** (US-LLM-1..5 enabled, `7553a06`), the **A1 narrative-draft assist** (US-LLM-6 enabled, `92657ae` — the FIRST real `AssistSpec`), **US-UI-3 evidence capture + `Evidence.location`** (enabled, `edf4840`), **US-UI-5 same-session milestone/phase linking** (enabled, `3aa780b`), **US-LLM-16 (F1) decompose-recipe→action-steps assist** (enabled, `e83e437` — the 2nd real `AssistSpec`), **US-LLM-11 (B1) draft-typed-success-criteria assist** (enabled, `0fb3001` — the 3rd real `AssistSpec`), **US-LLM-14 (D1) propose-milestones assist** (enabled, `2726ed1` — the 4th real `AssistSpec`; FIRST FK-vocab-setting assist), **US-LLM-13 (C1) suggest-feedback-channels assist** (enabled, `f71b0e1` — the 5th), **US-LLM-12 (B2) bind-criteria-to-channels assist** (enabled, `76d10be` — the 6th; FIRST nested-binding assist), **US-LLM-17 (F2) propose-responsible-division assist** (enabled, `c336a37` — the 7th; FIRST per-step-nested-child assist), **US-LLM-18 (F3) propose-timelines assist** (enabled, `8fc960d` — the 8th), **US-LLM-19 (F4) propose-resources+substeps assist** (enabled, `db338ee` — the 9th; FIRST to fill TWO child types per step + FIRST multi-row LISTS), and **US-LLM-20 (F5) propose-evidence assist** (enabled, `d6d61d8` — the 10th real `AssistSpec`; the LAST per-step gate element; a FUSION of F4's multi-row fill + F2's FK-vocab resolve). **Static gate GREEN + verified as of the latest code commit** (`b7a0693` tracking-only for the workshop bootstrap — code-state unchanged from `77fce3b`; prior `77fce3b`, DEC-38 implementation — `PlanPredecessor` typed with ContinuationKind + translated rationale — mypy **297** unchanged, pytest **1500** (+7 from baseline 1493: 1 default + 4 parametrize × 1 round-trip + 1 blank + 1 translation), `make test-js` **42/42** unchanged, `plans/0041_planpredecessor_continuation_type_and_more` auto-generated [1 AddField continuation_type CharField max_length=16 choices default='extension' + 3 AddFields rationale + _en + _zh_hans TextField blank=True, reversible, depends on 0040_year_group_fks]; dev-DB applied: REVISION-typed PlanPredecessor round-trip + default=extension on a second probe edge confirmed via shell, probes cleaned up, count back to 0; the structural Plan↔predecessor edge now bears its WASC evidence semantics natively; prior `bc6bf8a`, correction over `fa3a1f0` — `Position.scope_summary` swapped from TextField to CharField(max_length=500) for strict Division parity — mypy **297** unchanged, pytest **1493** (+4 over 1489 baseline from the 4 new fa3a1f0 tests), `make test-js` **42/42** unchanged, `school/0056_position_scope_summary` regenerated [3 AddFields: scope_summary + _en + _zh_hans, all CharField(500), reversible, depends on 0055]; dev-DB applied: 46 existing Positions all have blank scope_summary default; round-trip via shell-edit confirmed; the two responsibility-atom containers now share container shape (prose CharField(500) + ordered atoms); earlier `fa3a1f0`, Position.scope_summary added per plan's explicit TextField declaration — orchestrator paused before dev-DB apply on the user's surfaced field-type discrepancy (plan rhetoric vs declaration contradicted; Division's is CharField(500) not TextField); fa3a1f0's migration was never applied to dev DB; earlier `0b475cc`, importer `TITLE_TO_POSITION` extended for the Innovation Center seed + `test_real_csv_dry_run` flipped to assert 0 unmapped errors — mypy **297** unchanged, pytest **1489** unchanged (assertions replaced, not added), `make test-js` **42/42** unchanged, no migration (importer mapping + test docstring/assertion-only); dev-DB importer re-run: `persons_created=1, persons_reused=55, holdings_created=2, holdings_deduped=73, errors=0`; Axel Chen landed (Ge Chen / Axel, email `chenge@chiway-repton.com`) with TWO Holdings (Biology Teacher / Sciences + Director of Future Scholar Innovation Center / Future Scholar Innovation Center); D7 roster owed-item now FULLY closed (56/56 logical CSV rows imported); earlier `06f9ba2`, Future Scholar Innovation Center Division + Director Position seeded — mypy **297 source files** (+1 test module), pytest **1489** (+4), `make test-js` **42/42** unchanged, `school/0055` [1 top-level Division + 1 leader Position under it, single migration, idempotent + reversible]; dev-DB applied: tenant top-level Divisions 7→8, Positions 45→46; earlier `9ec2a97`, staff roster importer landed — US-ONB-4 enabled — mypy **296 source files** (+ new command module + test + 2 init markers — IMPL-reported count), pytest **1485** (+8 new), `make test-js` **42/42** unchanged, no migration (`users/management/commands/import_staff_roster.py` + 8 tests + 2 init markers; CSV → User+Person+Holding per row; idempotent; per-row atomic; strict-fail on unmapped Division/atom; `--dry-run` flag); dev-DB applied: 55 Persons + 55 Users + 73 Holdings created on the real run; idempotent re-run = 0 new; 1 row fails (Axel Chen — Innovation Center gap, known); earlier `e10ddc5`, 25 CSV-implied Positions pre-seeded — mypy **292** (+1 test file), pytest **1477** (+4), `make test-js` **42/42** unchanged, `school/0054` [25 staff-kind Positions across 13 Divisions, idempotent + reversible]; earlier `470b320`, CSV-controlling rename Dorm Supervisor → Dormitory Teacher — mypy **291**, pytest **1473** unchanged (pure rename), `make test-js` **42/42** unchanged, `school/0053` [in-place UPDATE preserving pk, reversible]; earlier `f2218af`, 3 additional Positions — mypy **291** (+1 test file), pytest **1473** (+4), `make test-js` **42/42** unchanged, `school/0052` [Art HOD + College Counseling Coordinator + Dorm Supervisor, idempotent + reversible]; earlier `71e5fff`, 4 additional Divisions — mypy **290** (+1), pytest **1469** (+5), `make test-js` **42/42** unchanged, `school/0051` [4 Divisions: 2 nested under C&T + 2 top-level service units, idempotent + reversible]; earlier `b0ff1fd`, DEC-36 Director-of-X seed — mypy **289** (+1), pytest **1464** (+2), `make test-js` **42/42** unchanged, `school/0050` [4 Director Positions, idempotent + reversible]; earlier `463054a`, year-leader split — mypy **288** (+1), pytest **1462** (+3), `make test-js` **42/42** unchanged, `school/0049` [retire generic + seed 4 year-scoped, reversible]; earlier `6468ed1`, DEC-35 HOD seed — mypy **287** (+1), pytest **1459** (+2), `make test-js` **42/42** unchanged, `school/0048` [data migration: 5 HOD Positions, idempotent + reversible]; earlier `dbe1685`, Position.kind — mypy **286**, pytest **1457** (+4), `make test-js` **42/42** unchanged, `school/0047` [AddField with default STAFF]; earlier `ea0b5a6`, YearGroup model + per-element FKs — mypy **286** (+3), pytest **1453** (+15), `make test-js` **42/42** unchanged, four migrations [school/0044+0045+0046 + plans/0040]; earlier `363454f`, project-wide modeltranslation fallback fix — mypy **283**, pytest **1438**, `make test-js` **42/42** unchanged, ONE settings line + 3 tests, fixes 32 of 47 `__str__` methods at once; earlier `51ff5cd`, locale-neutral Person name structure — mypy 282, pytest 1435, school/0043 [6 AddField + 3 RemoveField + 1 AlterModelOptions]; prior `622aa35`, US-LLM-27 responsibility-fulfillment — mypy **282**, pytest **1431**, `make test-js` **42/42** [+4 apply-responsibilities], `ai/0017` [new propose-responsibilities template via get_or_create]; prior code commit `dda9291`, US-LLM-26 A3-rationale — mypy **281**, pytest **1394**, `make test-js` **38/38**, `ai/0016` [guarded reversible template-body UPDATE — the prompt-template-evolution pattern]; earlier `a38c561`, DISC-22 position grain — mypy **281**, pytest **1383**, `make test-js` **37/37**, school/0041–0042 + plans/0038–0039 [PositionResponsibility + Assignment.position + ActionStepPositionResponsibility + grants]; earlier `867be08`, DEC-32 Layer 2b — mypy **277**, pytest **1338**, plans/0036–0037 [six alignment M2Ms → through-models+rationale via SeparateDatabaseAndState data-preserving migration; A3 apply reworked to formsets; multi-select retired; school_admin grant], `make test-js` **37/37** [−5 multi-select +7 alignment-formsets]; prior code commits `f1b9c95` DEC-32 L2a [plans/0034–0035], `62a8caa` L1 [school/0038–0040], `e224d9c` G1 [ai/0015], `34f7bf0` A3, `dd64f9d` A2, `7217045` DISC-21, `d30cd59` JS harness). `make test-js` **37/37**: `manage.py check` 0; `makemigrations --check` no changes (`ai/0012` is a hand-written DATA migration); `ruff` baseline-only (the 6 pre-existing: 5× N803 in school/0027, 1× F841 in test_seed_positions — `ai/`+`planner/` add zero) + `ruff format` clean; `mypy` clean (**277 source files**); `pytest` **1338 passed**; **`make test-js` 37/37** (NEW gate layer — DEC-31: jsdom@27.0.1 + Node-built-in `node --test` behavioral suite over the client apply/navigator functions + `node --check` all wizard modules; +2 DISC-21 `for`/nested-id-reindex, +3 G1 review-loop, +7 DEC-32-L2b alignment-formsets; the A3 multi-select tests RETIRED in L2b). **First npm footprint** (dev/test-only): `school-improvement-plans/package.json` (`"type":"module"` at the project root) + committed `package-lock.json` + gitignored `node_modules/` (run `npm ci` once in a fresh tree before `make test-js`); the browser runtime is UNCHANGED (native `<script type="module">`, no bundler; jsdom never ships to the client). Apps: users/school/accreditation/plans/`ai`/`planner`/`devmocks`. Latest migrations: school `0040` (DEC-32 L1: 0038 models+translation, 0039 SLO-reseed+vision-seed, 0040 school_admin grant), plans `0037` (DEC-32 L2a: 0034 ActionStepResponsibility+PlanGuidingClause through-models, 0035 grant; L2b: 0036 six alignment M2Ms→through-models+rationale [SeparateDatabaseAndState data-preserving], 0037 grant), users `0004`, **ai `0015`** (`ai/0015_seed_propose_review_loop_template`; applied+verified on dev DB; **13 assist templates** [+review-loop]: narrative/action-steps/success-criteria/milestones/feedback-channels/measurement-bindings/assignments/timelines/step-resources/evidence/accreditation-standards/domain-alignment). (`ai` = superuser-only operational infra; `planner` = the create-plan front-end — **13 real assists now — the per-element gate-required set is COMPLETE**: A1 narrative + **A2 standards + A3 domain-alignment M2Ms** (basics), B1 criteria + B2 bind-channels (criteria), C1 feedback-channels (feedback), D1 milestones, F1 action-steps + **F2 responsible-division + F3 timeline + F4 resources+substeps + F5 evidence** (steps), **G1 review-loop — communications + review-events(+inputs) + revision-rules** (review); **EVERY `_check_promotion_gates` predicate now has a backing assist** → the DEC-29 orchestration trio (US-LLM-24 heuristic → US-LLM-25 gap-closure → US-LLM-23 whole-draft) + US-UI-6 (admin promote) is next. **the per-step gate elements are also COMPLETE** — each step has assignment+timeline+resource+evidence assists; each control labelled per-spec; the reusable **per-step-child apply navigator** (`stepChildFormset`) + `addRows`/`directRows` serve F2/F3/F4/F5 [F4 added the multi-row pattern; F5 fused it with F2's resolve over the plain plan-owned+M2M evidence formset]. `AssistSpec` machinery: parse → optional `resolve` [FK-vocab codes/names → school-scoped pks, incl. F5's nested steps→evidence walk] → `AssistResult(prefill, note)` → client add-then-fill / `preview` modes [`text`/`fields`/`bindings`/`assignments`/`timelines`/`resources-substeps`/`evidence`/`review-loop`/`alignment-formsets`] over the model-is-contract POST; **whole-form `draft_state`** (B2 fix — every assist now grounds on the full in-progress plan, correcting a prior per-step under-grounding); capture saves Proposed via plain ORM; per-step Evidence sub-formset (US-UI-3) + same-session milestone/phase `*_index` linking (US-UI-5); `devmocks` = DEBUG-only mock launcher at `/mocks/`.)
- **Phase 15 data population (in progress):**
  - **WASC accreditation standards** — `accreditation/0003_seed_wasc_standards.py` (`c397785`): 3 categories + 12 standards, verbatim from `sources/wasc-standards-list.txt` (`7e882c8`). Global/invariant. Verified 3+12.
  - **Tenant School + FramingVocabulary** — `school/0006_seed_school.py` + `school/0007_seed_framing_vocabulary.py` (`96500e2`): seeds the tenant `School` (slug **`chiway-repton-xiamen`** — the anchor every future school-scoped seed references) and the 4 framings (student/teaching/parent/schoolwide-focused). Source: `web/data/plan-data.js` (canonical design SOT). English-only (zh-hans deferred, UI falls back). `default_priority_tier` null → Phase-14 priority-deviation gate dormant for these. Verified in dev DB.
  - **StakeholderGroup + AreaForImprovement** — `school/0008` + `school/0009` (`43be661`): 6 stakeholder groups (keyed on label; source `sh-*` ids not carried — no code column) + 7 AFIs (source `afi-*` ids carried as code). Two of the "ready-clean" datasets. Verified 6 + 7 in dev DB. (DEC-5 in seed-round plan.)
  - **PriorityTier + MissionArea + PlanningMethod+Step** — `school/0010` + `0011` + `0012` (`64c68eb`): 3 priority tiers (high/medium/low, key (school,code)) + 3 mission areas (`ma-*`, key (school,code), no source description) + 4 planning recipes with 20 ordered steps (method key (school,name) — no code column; step key (method,order); role_hint dropped). The remaining clean datasets — all clean vocabularies now seeded. Verified 3+3+4 methods/20 steps in dev DB. (DEC-6.) **Tracked follow-up:** `PlanningMethod.applicable_subject_kinds` (M2M → SubjectKind) seeded empty — SubjectKind is D4-blocked; wire once it is seeded.
  - **Established:** FramingVocabulary = a plan's primary AUDIENCE/beneficiary focus (who it's for), not a strategic theme; recorded in `data/reference-data-spec.md` §12. **Seed source found:** `web/data/plan-data.js` is the structured canonical registry for the remaining school-specific vocabularies (assessed as a viable PARTIAL source — clean for ~7 vocabularies; gaps: Cycle dates, Role→Department links, Frequency synthesis, provisional SLOs/departments, English-only).
  - **Representational gaps → `data/source-model-gaps.md`**: factual catalog of where the models do not hold what the sources/web prototype contain — categorised by kind of difference (lossy / needs-value / mismatch / no-model / capability), no intent or scope verdicts. Includes the WASC action-plan-table columns the model lacks (ActionStep status, ActionStep assessment, ActionStep↔Communication link) and the Evidence-auditability capability gap (no artifact payload/location; no status-change audit log). These are recorded as work items, not classified as in/out of scope.
  - **Model concepts → `docs/domain-model.md`**: conceptual reference (what each model means) — established entries for FramingVocabulary, Department, Role, StakeholderGroup, SubjectKind, PlanningMethod, PlanningStep; the rest are stubs to document when established. Eventual basis for the app's documentation.
  - **PLANNING SOURCE for this data round → `data/seed-round-plan.md`** (read it before continuing seed work). It carries the full source↔model conflicts, the per-model status table (SEEDED / blocked-on-decision), the decisions log (DEC-1..DEC-6 made), and the open decisions (D1 cycle dates · D2 role→department · D3 frequency set · D4 subject-kind mapping · D5 provisional SLOs · D6 provisional departments · D7 user accounts). Append new `DEC-N` there as decisions land. The clean datasets (FramingVocabulary, StakeholderGroup, AreaForImprovement, PriorityTier, MissionArea, PlanningMethod+Step) are all SEEDED; the remainder is blocked on D1–D6.
- **Phase 0–14:** GO.
- **Post-Phase-15 remodel (`057f5ed`, DISC-14-0521-A):** `subject_kind` + `planning_method` relocated from `Plan` to `ActionStep` (per-step classification; a plan is a bundle of differently-kinded steps). The kind→method promotion gate is now evaluated per step in `_check_promotion_gates`. `Plan` carries no kind column/property. Migration `plans/0017_*` applied (lossless — 0 Plan rows carried values). The 32-candidate `subject-kind-candidates.md` RPM column is now a per-step gate.
- **Milestone tier (`e824283`, DISC-15-0521-A RESOLVED):** new `Milestone` model + `Phase.milestone` (SET_NULL, same-plan `clean()`) → hierarchy `Plan → Milestone → Phase → ActionStep`. `Milestone` carries `target_date` (aspiration) + `status`/`reached_on`/`reached_by` stamped by `mark_milestone_reached` (per-status CheckConstraint). The 3 timing carriers (FeedbackChannel/Communication/ReviewEvent) gained `timing_kind` (`TimingKind`) + `milestone` FK (PROTECT); `frequency`/`scheduled_date` now nullable; per-kind CheckConstraints. Migrations `plans/0018` (schema) + `plans/0019` (Layer-A perms). Applied to dev DB; verified.
- **Phase 15 seed scope:** SEEDED — WASC standards (standards list); FramingVocabulary, StakeholderGroup, AreaForImprovement, PriorityTier, MissionArea, PlanningMethod+Step (from `plan-data.js`); **ImprovementType (32, seeded `5e5d4cd` as `SubjectKind`, renamed `19cc102`/DEC-14; DEC-7 — resolves D4)** from `data/subject-kind-candidates.md`. `PlanningMethod.applicable_improvement_types` M2M wired (`5d04573`, DEC-8). Frequency (4), Division (5 top-level, seeded as `Department`/renamed `d5a2ad2`, + 5 subject departments under C&T `school/0035`/DEC-15), LearnerOutcome (4 SLOs), 3 division-owned Policies seeded (`af5ed39`, DEC-9 — D3/D5/D6). **Cycle (2025-26, 2026-27) seeded (`cc028a3`, DEC-10 — resolves D1).** Remaining unseeded: **Role** (D2 — role→department mapping absent; also gates the 4th policy `pol-slos`), **User** (D7). Every other reference vocabulary is seeded. See `data/seed-round-plan.md`.
- **Naming + altitude (DISC-17 — FULLY RESOLVED):** **DEC-13 DONE (`d5a2ad2`, Pass A):** org-unit model `Department`→`Division` ("School Division") + all targeting FK fields `*_department`→`*_division` (school+plans), non-destructive `RenameModel`/`RenameField` split (school/0029–0031, plans/0022–0023), seeded rows/PKs preserved, 4 orphan `*_department` perms deleted, applied + verified. A **Subject Department** = a `Division` nested under C&T (no new model; seeded later with names). **DEC-14 DONE (`19cc102`, Pass B):** `SubjectKind` → **`ImprovementType`** (type of improvement initiative — selects a `PlanningMethod`; distinct from `AreaForImprovement` = area improved); **altitude moved `ActionStep` → `Milestone`** (`Milestone.improvement_type` + `Milestone.planning_method`; gate per-milestone in `services.py`, service-level — no DB constraint); M2M `applicable_subject_kinds`→`applicable_improvement_types`; granted `*_subjectkind` perm codenames renamed in place → `*_improvementtype` (grants preserved). Migrations school/0032–0034, plans/0024; 32 types + M2M wiring preserved, applied + verified. Conception: a teacher = a `Person` holding a `Teacher` `Position` under a subject department; HOD = that subject dept's `head`.
- **Materialization principle (DEC-18, standing):** `web/canonical.html` is a **required output materialization** (formal WASC schoolwide action-plan doc; audience = visiting committee/accreditors; fixed WASC-sample shape), NOT a prototype to align to the model. The model must be rich enough to **materialize** canonical (report/export view → that exact shape from stored data) AND drive the staff views; direction is model → rendering. canonical keeps its WASC-audience vocabulary and is never relabeled to db field names nor edited down — where it shows a datum the model can't produce, the **model** changes (gap → `source-model-gaps.md`, mandate-007). DEC-16/17 were already instances. **`web/working.html` + `web/wizard.html` get the SAME treatment as `mockups/a–e`** (align rendered language + capabilities to db terms). `web/data/plan-data.js` is the shared sample/import source (canonical reads it too) — existing prototype keys are NOT renamed. Persisted: framing at head of `data/source-model-gaps.md` + DEC-18.
- **Model-faithful sample + output-frozen canonical (DEC-19, standing):** `web/data/plan-data.js` is to be made **EXACTLY faithful to the Django modeling** (the JS stand-in for stored plan data: `divisions` parent/head; `Milestone` tier Plan→Milestone→Phase→ActionStep with per-milestone improvement_type/planning_method; actors division+optional person, Role retired; per-step status/assessment/communications/impediments; target-kind SuccessCriterion, no standalone KPI). Single model-faithful sample SoT. `web/canonical.html` stays an **output-frozen** view — its WASC shape held constant, does NOT display elements its shape omits (projects model data DOWN); its renderer is updated to read the model-faithful keys while preserving output, guarded by a **golden-output check** vs the WASC sample (`sources/`). The "original prototype" divergence record is pinned to a git SHA in source-model-gaps.md. working/wizard read the same data, show milestones/divisions natively. Reframe: canonicity is a property of canonical's RENDERER, not of plan-data.js's vocabulary. Dissolves the two working/wizard STOP-flags. **IMPLEMENTED 2026-05-23:** plan-data.js rebuilt model-faithful + canonical re-pointed output-frozen (`cf09618`; golden contract `web/data/canonical-golden.json`, zero shape diff); working.html + wizard.html switched to native structural keys (`ca8ad5c`; wizard gained a Milestone step at the improvement_type/planning_method altitude); prototype pinned at `529b3d0` in source-model-gaps.md; tracking `<this commit>`. All web JS passes node --check; no Django code touched.
- **Architecture orientation (DEC-20, standing — no direction chosen):** the zero-API/views posture so far was a deliberate FIRST FOCUS on data modeling, not a permanent exclusion. **Settled invariant:** the **model is the contract** — views, forms/formsets, the create-plan wizard, AND any AI author-assist all write through the same model invariants (CheckConstraint/clean()) + `services.py`; the AI gets no privileged write path (it proposes data the same constraints validate; seeded reference vocabularies are its grounding + guardrails). A reference landscape of canonical Django elements is captured in DEC-20 (request/response layer; AI-specific = background tasks + async streaming + an `ai` service; cross-cutting = mgmt commands, FileField for §H, permission mixins; keep-out = signals/DRF/SPA/2FA per standing stances). **UNCHOSEN:** front-end direction (5 mockups are the input) + every tech choice. **Flagged early-shaping element:** the AI call's slowness forces off-request work (background-task + async-streaming) which shapes settings/deploy (a worker) + the wizard interaction. See DEC-20 in `data/seed-round-plan.md`.
- **LLM reuse source chosen (DEC-21, standing — likely next build):** harvest the AI author-assist elements from `django-llm-template` (`gh:davidorex/django-llm-template`; local `/Users/david/Projects/digitalocean/django-llm-template/template/core/`) — a same-author Copier template, taken as a HARVEST source (not whole-template; this project diverged on auth + API). Lands as Django **models** here: `LLMProviderConfig`, `PromptTemplate`, `LLMCall` (audit) + the `core/services/llm/` provider-abstracted service (`get_llm`, `.complete()`/`.stream()`, anthropic+others, httpx) + `prompt_sanitizer.py` (OWASP LLM01) + the `LLM_*` env settings (default Anthropic claude-sonnet-4.5). **Decoupled from Celery/Redis** (service is infra-independent; wizard path = streaming async+SSE, no extra infra; worker is a later separate choice, `django.tasks`-on-Postgres avoids Redis). NOT reused: allauth (conflicts built onboarding), DRF (no-API), crispy-forms, the scaffold. Adapt: dedicated `ai`/`llm` app, project base-model/Meta, English-only (no modeltranslation), decide multi-school tenancy on `LLMCall`/`PromptTemplate`, perms app-admin/superuser only, wire `PromptTemplate` grounding to the seeded vocabularies. Full inventory in DEC-21. **IMPLEMENTED `108e90f`** (the LLM service slice): the `ai` app — `LLMProviderConfig`/`PromptTemplate`/`LLMCall` models, `ai.services.llm` (get_llm/get_llm_logged/LoggedLLMProvider + httpx provider registry, anthropic default), `ai.services.prompt_sanitizer`, superuser-only admin, `LLM_*` settings, `httpx` dep, `ai/0001_initial` applied + verified (no group holds ai perms). Gate green (mypy 221, pytest 686). NOT built (later slices): views/wizard, Celery/SSE, AI-prompt/grounding, and a `school`/`plan` tenancy FK on `LLMCall` (additive when the wizard wires AI calls to a plan).
- **Front-end conventions established + applied (DEC-22, standing):** `FRONTEND-CONVENTIONS.md` is the precedent for `mockups/` + `web/` (NOT canonical.html [output-frozen] / plan-data.js [shared classic]) — native ES modules (one `main.js` entry + data/i18n/format/render/events submodules) + CSS partials (tokens→layout→components), no god files, disk layout approaching Django templates/static, ESM-needs-http preview. Applied across mockups/a–e + web/working + web/wizard: inline assets externalized + monoliths modularized (`6d65ae4`), then **naming standardized** (`bc86b57` — CSS custom props → `--<category>-<name>`, ids → kebab, i18n keys → snake_case; data-contract values not renamed). Zero render change throughout (verified by headless-Chrome byte-identical screenshots; all 45 JS modules node --check). The conventions doc's "Naming conventions" section is the standard the eventual Django template suite(s) inherit. Front-end direction still UNCHOSEN — these are decision input + a clean, incorporable base.
- **Front-end design precedents (standing, front-end UNCHOSEN):** THREE paired docs the chosen front-end inherits — `FRONTEND-CONVENTIONS.md` (structure/naming: ES modules + CSS partials + naming standard, DEC-22) and `docs/ia-visibility-envisioning.md` (behavior/visibility **initial envisioning**, DEC-23: default-to-relevance + drill-don't-dump + exception-first + Now·Next·Later + altitude-appropriate density [Division/Position/Person] + one faceted home + pull-not-push [no notifications]) + `docs/suite-selector-envisioning.md` (DEC-24: the dev-time harness to run 2–3 real Django template+static suites over the same views with a runtime selector — canonical Django pattern, no dep [session + context processor + `get_template_names()` suite prefix + `static/<suite>/`]; mockups map 1:1 to suites; collapses to a single-suite app at finalization. **FIRST SUITE SEEDED (DEC-25): `e-wizard` = the chosen plan-creation view; a suite is many views, e is only creation. Build view-by-view — model-ready now: creation[e], Global/home, Division, all-plans progress, all-plans blockers; BLOCKED on §I: Position/overbooking/conflicts/AI; Calendar partial. Multi-suite comparison is a later use of the selector. FRONT-END BUILD GREENLIT (DEC-26): build the e-wizard plan-CREATION view as real Django FIRST (templates/wizard/ + static/wizard/, plain URL, hardcoded suite prefix); switcher deferred until a 2nd surface exists; suite-namespaced layout adopted day one. Single superuser/author covers creation ('all positions' = author+assign as data); scoped per-position lenses are later (partly model-blocked). PLAN APPROVED, IMPL IN FLIGHT (DEC-27): new `planner` app — SHARED canonical Django forms (ModelForms + nested formsets + per-kind clean() mirroring CheckConstraints + school-scoped FK querysets); a suite = presentation skin (template inheritance + per-suite static), first suite `wizard` skinned from mockups/e-wizard. Mechanism = Option C (user): one server-rendered page, e-suite JS paginates (progressive enhancement, works w/o JS), ONE atomic POST → Proposed plan. Minimal+extensible (steps.py registry + shared partials + blocks). Capture = plain ORM (no services). Resolves US-UI-1 (structured Timeline). Deferred (additive): promote-to-Active, durable draft, suite-selector, dependency capture, Position-as-responsible, Evidence payload. formtools rejected (nested formsets/commit-at-end). **BUILT `1f5c815`:** the `planner` app (forms.py canonical ModelForms + per-kind clean() mirrors + school-scoped FK querysets; formsets.py nested + save_nested; steps.py registry; views.py one-page/one-atomic-POST PlanCreateView + DraftList/Detail; shared templates/planner/* + wizard suite skin templates/wizard/* + static/wizard/{css,js} progressive-enhancement [works JS-off]; tests). No models/migrations. Gate green: check 0, makemigrations clean, ruff planner-clean, mypy 235, pytest 720(+34); 4 JS node-check pass. Capture=plain ORM (no services), stamps author/school/lifecycle=proposed. US-UI-1 → enabled. Note: intra-graph refs (MeasurementChannel/ReviewEventInput→FeedbackChannel, RevisionRule.trigger→ReviewEvent) captured by create-order index, resolved server-side post-save (fine for create; edit/resume must switch to pk refs). DEFERRED (additive): promote-to-Active, durable draft/resume, suite-selector, ActionStepDependency capture, Position-as-responsible (§I), Evidence payload (§H.1). Remaining check: human browser create-a-plan via runserver.**). Plus `docs/model-surface-audit.md` (code-grounded model inventory + candidate-view readiness) and `data/source-model-gaps.md` §I (the capability gaps the BLOCKED views/IA lean on: `Assignment.position`, capacity/overbooking, RRULE recurrence, ActionStep↔Position, cross_plan_ref integrity). Candidate views + actor-altitude needs were thought out (Division/Position/Person; the crux gap = responsibility at the Position altitude / `Assignment.position`); not built.
- **Mockups aligned (`86f4daa`, log `f1c124f`):** the 5 plan-creation mockups `mockups/a–e` aligned to current model reality (status = real audited `ActionStep.status`; blocked = real `ActionStepImpediment` orthogonal to status; `assessment` + per-step `communications` surfaced; d-dashboard dropped non-model `_weight`; terminology already current). First parallel run was cut off mid-edit by transient rate-limiting (a-outline/c-kanban partial diffs restored); all five re-run from clean. e-wizard scope judgment: status added as a creation field, impediment documented in README but NOT given creation UI (execution-time surface).
- **web/working.html + web/wizard.html aligned (`6db39dd`, log `4f2f0e6`):** labels/capabilities aligned to current model (Department→Division incl. view-mode values; Role-actor→Division head/Person labels; subject_kind→"Improvement type" label; KPIs→target-kind SuccessCriterion; status normalized +"(audited)"; wizard action-step gained status/assessment/per-step communications; cadence→frequency/timing; no impediment UI in the creation wizard). plan-data.js + canonical.html verified untouched; JS valid. Both agents STOP-flagged two structural mismatches → resolved + BUILT by **DEC-19**: (1) `web/data/plan-data.js` rebuilt exactly model-faithful + `window.PlanHelpers` re-pointed (`cf09618`); (2) `web/canonical.html` renderer re-pointed OUTPUT-FROZEN, golden contract `web/data/canonical-golden.json`, zero shape diff (`cf09618`); (3) `web/working.html`/`web/wizard.html` switched to native structural keys — milestone tier, divisions parent/head, structured assignments, impediments; wizard gained a Milestone step (`ca8ad5c`); (4) prototype pinned at `529b3d0` in `source-model-gaps.md`. **All web views now read one model-faithful sample; canonical stays the output-frozen WASC view.** Remaining: wizard's Timeline capture is labeled v1 free-text (`timeline_text`) — recorded as **US-UI-1** (pending required work: a structured kind+dates control writing the model's `Timeline` shape; the model+admin already support it, `f5a02ce`; gap is the create-plan interface). Per-phase human browser pass of the four web views is part of the batched post-Phase-15 verification.
- **Open discoveries:** **DISC-19-0523-A** (standing gotcha; refined): `school/0036` is a ONE-TIME `school_admin` grant whose `create_permissions`-on-live-registry side-effect fires only on a FRESH build — so every NEW plans model after 0036 must set `school_admin`'s grant EXPLICITLY in its own migration: **view-only + strip mutation** for an append-only audit log (plans/0027, plans/0029) or **full CRUD granted explicitly** for an editable child (plans/0032 — after plans/0031 wrongly relied on 0036 and under-granted on the dev DB). Never rely on 0036 for already-migrated DBs; fresh-build perm tests don't catch the divergence. **Verified:** a dev-DB reconciliation sweep confirms `school_admin`'s per-plans-model grants are fully consistent (no remaining divergence). The convention is now recorded as `phases/PHASE-LAUNCH-CHECKLIST.md` item **2a** (binding on every future school-scoped model). Guards = the convention + a periodic reconciliation sweep; optional future structural cure = an idempotent `post_migrate`/mgmt-command reconciliation from a declared posture map (not required while the convention holds). DISC-18 RESOLVED (per-school-admin foundation `391b893`). DISC-17 RESOLVED (`d5a2ad2`,`19cc102`); DISC-14 `057f5ed` (refined by DEC-14); DISC-15 `e824283`; DISC-16 `048cf71`/`6f7813d`/`98576d1`.
- **LLM author-assist envisioning (DEC-28, persisted; NOT built, no path chosen):** user-commissioned agent think-out of how the model assists a human create a draft plan → `docs/llm-assist-envisioning.md`. The "where + how" companion to DEC-21 (the built `ai` app). Codebase-grounded (at `be40e59`): ~16 assists mapped onto the real `planner/steps.py` seven steps, each with input context + a concrete `ai.PromptTemplate` (slug + variables + strict JSON OUTPUT CONTRACT) + the exact model fields it proposes + its validated re-entry (DEC-20: AI proposes editable prefills → human accepts/edits → ONE atomic POST → every `clean()`/CheckConstraint fires; no privileged write). Flagship = F1 (method-recipe `PlanningStep.template` → concrete action steps). Shared `ai/services/grounding.py::build_grounding()` assembles seeded-vocab + WASC corpus + draft serialization, per-assist sliced for token budget. Additive `ai` fields flagged (LLMCall school/plan FK, PromptTemplate schema field); leans on named gaps (Assignment.position §I, Person roster D7, no RRULE §I, Evidence/dependency not in wizard, off-request streaming infra). Exclusions honored (no KPIs/notifications/hooks). Next: user decides which assists (if any) to build → own plan→impl. **Acceptance surface PERSISTED (user-confirmed 2026-05-25):** the `llm-user` actor + `US-LLM-1..23` (foundation US-LLM-1..5 incl. the model-is-contract re-entry seam, grounding, off-request streaming, audit/sanitize/attribution [US-LLM-4 carries the additive nullable `LLMCall.school`/`plan` FK migration], managed PromptTemplate; + the 18 per-assist rows US-LLM-6..23) and the two capture prerequisites `US-UI-3` (Evidence link/label — §H.1 payload stays a separate open gap) / `US-UI-4` (within-plan dependency) — all in `phases/00-preamble.md` + `phases/US-STATUS.md`, Status=pending. Confirmed forks: series `US-LLM` + actor `llm-user`; prereqs as US-UI-3/4; full set; LLMCall FK + evidence link-only. F5 (US-LLM-20) blocked on US-UI-3. **FOUNDATION BUILT — US-LLM-1..5 → enabled (`7553a06`; plan `~/.claude/plans/gleaming-skipping-emerson.md`, approved).** Stream-under-WSGI fork chosen (user). Shipped: `ai/services/grounding.py::build_grounding(school, draft_state, include=)` (US-LLM-2; "active cycle" derived by date — no model flag); `LoggedLLMProvider` now persists optional `prompt_template`/`school`/`plan`, migration `ai/0002_llmcall_tenancy_fks` (nullable FKs, applied+verified) (US-LLM-4); promoted `ai/services/llm/testing.py::FakeLLMProvider` + `fake_llm` fixture; `planner/assist.py` `AssistSpec`+`REGISTRY` (EMPTY in prod — registers ZERO real specs) + parse→prefill (US-LLM-1 server); `initial` threaded through `PlanCreateView` (prefill seam, reused by future draft-resume); `AssistStreamView` (`planner:assist`, LoginRequired+school) returning `StreamingHttpResponse(text/event-stream)` over the existing sync `stream()`, `sanitize_refinement` on the seed, terminal `event: result` carrying `spec.parse()` prefill (US-LLM-3); client `ai-assist.js` (ES module, `fetch`-streaming, json_script `planner-assist-specs`, CSP-safe) + shared `_assist.html` + `assist.css` (US-LLM-1 client). **Empty registry ⇒ wizard renders unchanged** (test-asserted). Re-entry proven: a reference spec's parse→prefill validates+saves through the normal atomic POST (test). PromptTemplate model+admin already satisfied US-LLM-5 (no real templates seeded). Gate verified independently (mypy 247, pytest 765 [+20]); migration applied to dev DB. **No browser-visible assist in the foundation phase** (no real spec) — first browser pass rides US-LLM-6. Out of scope held: no ASGI/deploy change, no real prompts, Evidence location field still separately slated. **A1 BUILT — US-LLM-6 → enabled (`92657ae`; the FIRST real `AssistSpec`).** User decision: strict-JSON output contract (no envisioning deviation) but parsed prose rendered straight into the `basics` form fields (a new per-spec `AssistSpec.preview="fields"` mode: "drafting…" status during the stream, fields filled on the terminal `result`; default `"text"` live-token pane retained for future assists). Shipped: data migration `ai/0003_seed_plan_narrative_draft_template` (idempotent `get_or_create` global PromptTemplate `plan-narrative-draft`, admin-editable after — US-LLM-5; applied+verified, 2719-char body, 5 keys); `planner/specs.py` (the A1 `AssistSpec` key=`narrative-draft`, target_step=`basics`, `parse_narrative` robust JSON [strips ```json fences, raises on malformed] → the 5 EN prose fields; EN only, zh-hans = US-LLM-7) registered idempotently via `planner/apps.py::ready()`; `AssistSpec.preview` field serialized in `_build_assist_specs`; `ai-assist.js` `"fields"` mode + status. **REGISTRY now NON-EMPTY in prod ⇒ the `basics` step shows the A1 "Draft with AI" control** (the foundation's empty-registry test was adjusted to save/clear/restore REGISTRY; create happy-path still green with the control present — the seed input does not POST as a form field). Re-entry proven (prefill merges into the normal POST → Plan saves the prose). Gate verified independently (mypy 249, pytest 782 [+10]); `ai/0003` applied to dev DB. **PENDING — first BROWSER pass (human step, rides US-LLM-6): needs a configured provider key (`LLM_API_KEYS`/`LLMProviderConfig`); runserver → author w/ school → basics → seed → draft → fields populate → edit → save. US-LLM-6 stays `enabled` (built + automated-verified); flips to `vivified` only after that human end-to-end with a live model.** NEXT (user decides): the browser pass for A1, and/or the next per-assist story (US-LLM-7..23) or the US-UI-3/4 capture prereqs — each its own plan→impl.
- **Claude Design evaluation (research persisted; DECISION OPEN, no work in flight):** the user is weighing making **Claude Design** (`claude.ai/design`, Anthropic Labs preview) the surface where the **suite of front-end views is refined and finalized**. Three research passes (general fit + CSP-export-structure probe + repo/branch/worktree-ingestion probe) are fully persisted in **`docs/claude-design-research.md`** (confidence-tagged; no real export byte was publicly inspectable, so §4 export findings are INFERRED from Anthropic's frontend cookbook + adjacent artifacts). Established: the framing fits the repo's existing materialization pattern (suite=skin DEC-25/27; committed-artifact-as-design-of-record DEC-18/19) — Claude Design substitutes for the *hand-authored mockup layer*, not a new architecture. Hard facts: export default is CSP-hostile (`<style>`/`<script>` blocks + CDN Tailwind + Google Fonts) → a transform layer is ALWAYS required; cost is heavy-per-cycle by default, reducible to mechanical (externalize blocks to files, matching FRONTEND-CONVENTIONS) only if the generation prompt is locked CSP-friendly; bilingual is unfinalizable in Design (EN-only; 中文 only Django-side via `{% trans %}`); branch targeting in Design's repo-ingest is unconfirmed → **worktree-folder drag-drop is the deterministic state-pin**. **GATING action before any adoption (per mandate-004, the one unresolved fragility): generate ONE real export from the user's account + inspect it against the app CSP** (~5 min, hands-on; the user does this, agents can't reach `claude.ai/design`). No DEC yet (no decision made); revisit when the user chooses.
- **BUILT (`edf4840`, US-UI-3) — the hand-authored `Evidence.location` field:** `TextField(blank=True)` (not translated), migration `plans/0033_evidence_location` (applied+verified), surfaced in admin + captured in the planner per-step Evidence sub-formset. The §H.1 payload in its hand-authored form; the fuller link/upload storage decision stays deferred (still §H.1-open) and composes alongside `location` later. Gives **US-LLM-20** (evidence assist) a real target field. (Was a slated plan→impl step; now done.)
- **NORTH-STAR → DEC-29 (AI-orchestrated promotion-ready complete draft; user think-out + agent coverage analysis, 2026-05-25):** target = a school-admin writes ONLY current+desired state → the `llm-user` sequences a heuristic and proposes the WHOLE `Plan` graph → a fully completed DRAFT → the admin ALONE flips draft→active. **Verdict: not met today; the spine exists, the bulk is unbuilt, and a recorded artifact was DEFECTIVE.** Key finding: **US-LLM-23/H2's recorded sequence (`A1→A2→A3→D1→F1→B1→C1→G1`) was provably partial** (omits E1/F2/F3/F4/F5/A4, all gate-required) → a draft from it would FAIL `_check_promotion_gates`; now corrected. Four confirmed forks: (1) draft must be **promotion-READY** → needs a gap-closure pass; (2) **minted US-LLM-24** (dependency-aware sequencing heuristic over ALL gate-required elements) **+ US-LLM-25** (pre-submit gap-closure pass), **US-LLM-23 corrected** to the orchestration running 24→25, input = admin's current+desired (framing/priority/type AI-proposed); (3) admin-flip on the **plan surface** → **minted US-UI-6** (admin-only promote → `advance_plan_status`, `change_plan`+own-school; DEC-27 had deferred it); (4) **division-only responsibility = complete** (D7/§I named-person is later, off critical path). **Minimal closure set (hard blockers, each own plan→impl):** US-UI-3 (evidence, gate-required) · US-UI-5 (milestone/phase) · the per-element assists US-LLM-7..21 · US-LLM-24 · US-LLM-25 · US-LLM-23 · US-UI-6. Soft/excluded: US-UI-4, D7 roster, §I `Assignment.position`, RRULE, Evidence payload, KPIs/notifications/hooks. Persisted: DEC-29 (seed-round-plan.md) + `phases/00-preamble.md` (US-LLM-23 corrected, US-LLM-24/25, US-UI-6) + `phases/US-STATUS.md` (pending) + `docs/llm-assist-envisioning.md` H2 annotated. Nothing built this turn (analysis only). Model-is-contract preserved (AI proposes editable prefills, never flips lifecycle).
- **NEW CAPABILITY → DEC-30 (draft review — stakeholder commenting; user think-out, 2026-05-25):** during the draft phase (`lifecycle=proposed`), a set of school users **routed by the plan's type/recipe** may **comment on the draft itself** (feedback to inform the admin before the US-UI-6 flip). Terminology pinned: "type"=`ImprovementType`, "recipe"=`PlanningMethod` (per-`Milestone`, DEC-14). DISTINCT from operational `FeedbackChannel`/`Communication`/`ReviewEvent`. **Four confirmed forks:** (1) routing = a configured **ImprovementType/PlanningMethod → audience mapping** (`DraftReviewerRule`); a draft's commenters = the **union across its milestones'** type/recipe rules; (2) reviewer identity = **org structure** (Position/Division holders, via Holding/Person.user); (3) comment scope = **plan + elements** (`DraftComment` carries a generic target); (4) **advisory only** — does NOT gate the flip (DEC-29 unchanged); no acknowledgment/resolution (exclusion). **OPEN sub-question (to think out further, 2026-05-25):** the element-comment **atomicity/granularity** — per step? per element (which types: milestone/phase/criterion/feedback-channel/…)? — is unresolved; the `DraftComment` generic-target shape settles when it is ("plan + elements" is directional, not final). New models (own plan→impl, NOT built): `DraftComment`, `DraftReviewerRule`. Stories (atomic, `user`-actor only, no `llm-user`): **US-REV-1** (admin configures the rule), **US-REV-2** (reviewer comments, plan/element), **US-REV-3** (author/admin sees comments while reviewing; advisory/non-gating), **US-REV-4** (pull discovery of drafts-to-review). **Dependency:** live commenting needs the `Person` roster + `User` links (**D7**); the mapping+models build/test independently. Exclusions held (no notifications [pull], no ack/resolution, no KPIs/hooks). Persisted: DEC-30 (seed-round-plan.md) + `phases/00-preamble.md` "Draft review — stakeholder commenting (US-REV)" section + `phases/US-STATUS.md` (US-REV-1..4 pending). Not built; `domain-model.md` entries land when the models are built.
- **NEW DESIGN → DEC-32 (guiding-principle atoms + stored principle-linkages carrying rationale = the accreditation-evidence model; user think-out, 2026-05-26; SETTLED for the atom/linkage core, NOT built):** enriches the DEC-29 orchestration grounding. A plan = current→desired transformation (by success criteria); each division fosters purview-appropriate change; the LLM reasons over structured **grounding atoms** to decompose rationally, and **every plan-element↔atom linkage carries a `rationale` that IS the WASC committee's evidence** (+ feeds the canonical materialization, DEC-18). **Atoms:** (1) **SLOs = the canonical 7** (Engaged/Reflective/Compassionate/Confident/Innovative/Responsible/Bilingual Communicators) → a `LearnerOutcome` RESEED, NO schema change [Confident description given; **6 OWED by user**]; (2) **Vision+Mission = first-class clause atoms** (new kind=vision|mission prose+ordered-clause model, `zh_hans` first-class; mission given+atomized; **verbatim VISION text OWED by user**; SLO/mission overlap left un-reconciled as genre-inherent); (3) **Division responsibilities atomized** → new `DivisionResponsibility` replacing the single `scope_summary` blob (atoms single-division-owned; "overlap" = concern-level co-recruitment). **Five stored principle-linkages, each +rationale (uniform, mirroring A2's `PlanAccreditationStandard`):** standards (EXISTS); **SLOs → ENTAILED UPGRADE of A3's plain `learner_outcomes_targeted` M2M → through-model+rationale**; mission_areas → same; vision/mission clauses → NEW plan-level through-model; division responsibilities → NEW `ActionStepResponsibility` (step↔responsibility+rationale, M2M). **mandate-007:** the evidence principle makes A3's just-shipped plain SLO/mission-area M2Ms under-built → recorded as an entailed rationale-upgrade. Baked: division-coherence (step's fulfilled responsibility ∈ owner_division); principle-rationales OPTIONAL/non-breaking by default; EN-first rationale EXCEPT vision/mission content (zh_hans first-class); recruitment inferential; model-is-contract. **Open:** which other A3 M2Ms get rationale (AFI plausibly yes; stakeholder/policies no — user decides); scope_summary fate; MissionArea-vocab vs mission-clauses; the grounding emitters + assist(s); **US stories to author when sequenced**. **Cross-plan coherence** (the side-consideration's other, less-developed strand) recorded as a direction: draft-time detection of action-step/timeline/division-purview overlaps + timing/workflow conflicts vs existing active plans, surfaced on the draft-review surface (pull-flag, not push) — own modeling/stories when scoped. Persisted: DEC-32 (seed-round-plan.md) + LOG. **Content OWED by user:** verbatim vision statement + 6 SLO descriptions. **NEXT FOCUS (user-flagged):** think out the STEPS by which the LLM creates a draft *within the wizard* (DEC-29 orchestration mechanics — US-LLM-23/24/25), then pick best next build step. **DEC-32 build STARTED: Layer 1 (the reference/atom foundation) BUILT `62a8caa`** — DivisionResponsibility + GuidingStatement/StatementClause + the SLO reseed (7) + vision seed + admin + grounding + `school_admin` grant; US-UI-11/12 enabled; school/0038–0040 applied+verified on dev DB. **DEC-32 Layer 2 COMPLETE (2a `f1b9c95` + 2b `867be08`).** L2a = the two ADDITIVE through-models (`ActionStepResponsibility` step↔responsibility+rationale [per-step capture] + `PlanGuidingClause` plan↔vision/mission-clause+rationale [basics capture]). L2b = ALL SIX of A3's plain plan-level alignment M2Ms upgraded to rationale-bearing through-models (`PlanLearnerOutcome`/`PlanMissionArea`/`PlanAreaForImprovement`/`PlanStakeholderImpact`/`PlanPolicyEstablished`/`PlanPolicyRevised`, mirroring `PlanAccreditationStandard`) via a `SeparateDatabaseAndState` data-preserving migration (no edges on dev → copy a no-op; populated path tested); the A3 apply reworked multi-select→`applyAlignmentFormsetsPrefill` (`preview="alignment-formsets"`; parse/resolve unchanged); the multi-select path RETIRED; six PlanForm formsets + admin + school_admin grant; gate/grounding unchanged; plans/0034–0037 applied+verified. **So the full DEC-32 reference + linkage substrate is built** — atoms (L1) + the rationale-bearing principle-linkage through-models (standards [A2] + SLO/mission/AFI/stakeholder/policies [L2b] + vision/mission-clause + ActionStepResponsibility [L2a]). No US-row flip (the linkages are the substrate; US-LLM-9/A3 stays enabled, apply reworked). **US-LLM-26 BUILT `dda9291` → enabled** (the A3 assist now PROPOSES the per-relation rationale — object contract/parse/resolve `{label,rationale}`→`{pk,rationale}` + the apply fills the EN `-rationale` field + `ai/0016` guarded reversible template-body UPDATE establishing the prompt-template-evolution pattern; NO model/schema change). US-LLM-27 BUILT `622aa35` → enabled** (responsibility-fulfillment assist proposing `ActionStepResponsibility`/`ActionStepPositionResponsibility` + rationale over the DISC-22 division+position inventory, both grains, statement+owner→pk resolve; `ai/0017`; end-to-end exercise content-gated on seeded responsibility atoms [admin-authored/owed]). **The per-element evidence assist layer is now COMPLETE (US-LLM-26 + 27).** **NEXT FOCUS = the orchestration trio** (US-LLM-24 dependency-aware sequencing heuristic → US-LLM-25 pre-submit gap-closure → US-LLM-23 staged whole-draft runner, DEC-29/DEC-33) + stored drafting-stage substate + US-UI-6 (admin promote → `advance_plan_status`), then US-LLM-27 (responsibility-fulfillment assist), then the orchestration trio (US-LLM-23/24/25 staged). **RESOLVED + BUILT (`a38c561`) — DISC-22-0526-A (user-surfaced 2026-05-26): responsibility was division-grained but role-borne** — `Assignment` has no `position` target + DEC-32's `DivisionResponsibility` atoms hang off Division only, so a Position (dorm-supervisor vs YGL) can't carry its own responsibility atoms → role-specific responsibilities within a division aren't articulable. Resolution SETTLED (user 2026-05-26) — **general policy: each division AND position can be/IS responsible for its part; the model must expose the division+position responsibility INVENTORY as grounding the orchestration allocates to meet the plan's end-state criteria.** Shape: dedicated `PositionResponsibility` (FK position, statement, order; polymorphic owner REJECTED) PARALLEL to the built `DivisionResponsibility` (untouched); `Assignment.position` added with the `responsible` constraint = **exactly one of {division, position}** (option i; person optional; division derivable from `Position.division`), `participant` also admits position; extend the `ActionStepResponsibility` linkage to reference EITHER grain; grounding emits positions + their atoms as the inventory. Purely additive. **Its OWN increment, BUILT BEFORE US-LLM-27** (US-LLM-24/27 allocate the inventory). Faithful to DISC-16 (no year-group/homeroom sub-divisions). DISC-22 BUILT `a38c561` (PositionResponsibility + Assignment.position [exactly one of {division, position}] + ActionStepPositionResponsibility [a SECOND dedicated through-model] + inventory grounding + grants; school/0041–0042, plans/0038–0039; applied + verified on dev DB; `resolved_by` filled). **US-LLM-27 now UNBLOCKED.** Then the assists US-LLM-26 (A3 rationale — independent) / US-LLM-27 (responsibility-fulfillment — now reasons over the position grain), then the orchestration trio (US-LLM-23/24/25 staged). OWED by user: 6 SLO descriptions + the mission statement + the division-responsibility atoms (authored via admin).
- **NEW DESIGN → DEC-33 (staged whole-draft orchestration + per-stage commentary + draft/stage PDF export; user think-out, 2026-05-26; captured, NOT built):** refines DEC-29 from a single auto-shot into a **staged, human-checkpointed build** — US-LLM-24's heuristic runs as ordered STAGES (LLM drafts a pass → admin reviews/edits → CLOSES → next pass grounded on the confirmed prior); admin decides at EVERY stage boundary; whole artifact stays `proposed` until the single admin promote (US-UI-6). **Corrected pass order: division-responsibility recruitment BEFORE milestones** (who/what-sphere precedes the time-phased arc; reasoning order ≠ storage order). Roles: US-LLM-23 = staged runner, US-LLM-24 = stage order, US-LLM-25 = final gap-closure stage. **Stored drafting-stage sub-state DECIDED (option b)** — orthogonal to lifecycle; chosen stored because stage-comments + stage-export need a referenceable stage + the build must be resumable/auditable. **DEC-30 extended:** stage added as a comment-target LEVEL ({plan, stage, element}) — additive, does NOT resolve DEC-30's element-atomicity sub-question; + optional per-stage feedback loop (advisory). **NEW: draft/stage PDF export** (operationalizes DEC-18 for the draft phase) — whole-draft + stage-scoped, via canonical or a lighter review template; mechanism OPEN + deploy-adjacent. Rests on DEC-32 atoms/linkages (precondition). Persisted: DEC-33 (seed-round-plan.md) + LOG. **Stories PARTICULARIZED** (user "yes persist all of that") into `phases/00-preamble.md` + `phases/US-STATUS.md` (all `pending`): refined US-LLM-23 (staged runner) / US-LLM-24 (division-responsibility BEFORE milestones; checkpointed stages) / US-LLM-25 (terminal gap-closure stage) / US-REV-2,3 (stage comment level) / US-UI-6 (end-of-staging gate); NEW US-UI-7 (resume staged draft), US-UI-8 (close/advance stage), US-UI-9 (export whole draft PDF), US-UI-10 (export stage PDF), US-UI-11 (edit DivisionResponsibility atoms), US-UI-12 (edit vision/mission + clauses), US-LLM-26 (rationale-bearing principle alignments + A3 M2M→through-model upgrade), US-LLM-27 (per-step `ActionStepResponsibility` + rationale), US-REV-5 (stage-level comment + per-stage feedback loop). Build items (no US rows; in DEC-32/33): stored drafting-stage sub-state, PDF render path, SLO reseed, the through-models. Open: PDF renderer/template; stored-stage shape; DEC-30 element-atomicity. OWED by user (DEC-32): verbatim vision + 6 SLO descriptions. NEXT: finalize the US-LLM-24 staged sequence + pick best next BUILD step (DEC-32 reference/linkage layer = the precondition).
- **DECISION → DEC-35 (HOD = a `Position` under the subject-dept `Division`, complementary to `Division.head`; user 2026-05-26; NO model change; seed + content follow):** resolves the head-vs-position question surfaced after DISC-22. `Division.head` is a bare org-leader pointer (no responsibility atoms; `Assignment` has no "head" target — only division/position per DISC-22), so an HOD modeled only as `head` cannot enter the DISC-22 division+position responsibility INVENTORY nor be a step's responsible party at the HOD grain. Decision: the HOD is a `Position` ("Head of Department" seat) under each subject-dept `Division` (children of C&T), carrying `PositionResponsibility` atoms (= its job description, DEC-29) and `Assignment.position`-assignable so US-LLM-24/27 allocate it. COMPLEMENTARY: `head` stays the rollup leader-pointer (`people_in_subtree`, `Person.heads`); the HOD Position is the responsibility-bearing seat; a dept leader is linked BOTH ways (head FK + a `Holding` of the HOD Position). **NO model/migration change** — Position + PositionResponsibility + Assignment.position + grounding positions-nesting all exist (DISC-22 `a38c561`); the grounding inventory picks the HOD up automatically once its Position rows + atoms exist. Supersedes the `docs/domain-model.md` Position-entry prior art ("an HOD is not a position — it is the head"), corrected this turn. **Follow-on build (own plan→impl; orchestrator-direct seed):** a data migration creating an HOD `Position` under each of the 5 seeded subject depts (Business and Economics/English/Humanities/Math/Sciences), keyed `(division, label)`; its `PositionResponsibility` atoms = admin-authored content (OWED). Seed details to settle at seed time (content, not structure): label convention (uniform "Head of Department" vs per-subject) + shared-role vs per-dept atoms. People-linking (head FK + HOD Holding) needs the roster — **D7-blocked**; the Position+atoms seed independently. Scope narrow (the HOD/subject-dept case named; other division heads getting positions = user's call when it arises). No new US row minted (mechanism + admin editing exist). Persisted: DEC-35 (seed-round-plan.md) + domain-model.md + LOG. **FUTURE-FOCUS DESIGN DOC → `docs/hod-default-responsibilities.md`** (user-directed 2026-05-26): the HOD "goes-without-saying" default responsibility set derived from the modeled universe, split across the two DISC-22 grains (division-grain `DivisionResponsibility` defaults: curriculum/teaching delivery, assessment+evidence, dept policies, subject feedback; position-grain `PositionResponsibility` defaults: lead/develop staff, accountable-for-division-atoms, own dept plans + be the responsible party, represent dept in review/revision, curate accreditation evidence) + the EXCLUSION of re-storing already-first-class atoms (SLOs/standards/vision-mission/mission-areas/AFIs — already carried by their plan-grain rationale linkages) + a hard finding (defaults must be MATERIALIZED stored atoms, not computed — DEC-32's linkage FK requires a real row) + the materialization mechanism (canonical defaults defined once, idempotently seeded per subject dept, per-dept overridable) + THREE open forks (1: how the system recognizes an HOD position — label-convention vs structural vs a role/type marker [the load-bearing one; bears on DEC-35's "no model change"]; 2: default-text evolution [get_or_create won't update — needs an update path that won't clobber admin edits]; 3: the grain split itself). Substance + forks NOT yet user-validated; on validation → a DEC (likely extending DEC-35) + the materialization seed as its follow-on plan→impl (fork 1 first).
- **NEW DESIGN → DEC-34 (cross-plan coherence — draft-time overlap + timing/workflow-conflict detection; user think-out, 2026-05-26; direction recorded, NOT designed/built):** promoted from a DEC-32 sub-paragraph to a first-class DEC (user: "flagged = lost if not persisted"). At draft time, detect a new plan's overlaps + timing/workflow conflicts vs the school's EXISTING active plans PER DIVISION (concern+timing+division-load level), and surface on the draft-review surface (pull-flag per DEC-23/30, NOT push) for resolution before promotion. Beyond DEC-29 (single-plan) + DEC-30 (commenting); needs its own think-out (detection model; where it runs; user+llm-user stories) before build. Persisted: DEC-34 (seed-round-plan.md) + LOG.
- **RESOLVED — US-UI-5 (DISC-20-0525-A) BUILT (`3aa780b`):** the create wizard now links same-session milestones/phases. The five FKs (`Phase.milestone`, `ActionStep.phase`, `FeedbackChannel`/`Communication`/`ReviewEvent.milestone`) became form-only `*_index` fields resolved in `save_nested` against new `milestones_by_index`/`phases_by_index` (extending the `channel_index`/`trigger_index` pattern); `milestone-phasing.js` (CSP-safe ES module) populates the index selects from same-session row labels live; `_clean_timing`/`ReviewEventForm.clean` read `*_index`; a milestone-timed carrier with a bad/missing index → form error (HTTP 200, no IntegrityError); optional hierarchy links stay optional (blank index = no link). **No model change/migration.** The `Plan→Milestone→Phase→ActionStep` hierarchy (US-17, US-ext-milestone) + milestone-anchored timing are now capturable at creation. Gate green (mypy 251, pytest 791 [+5]). DISC-20-0525-A `resolved_by` filled. (The other two pulldown-audit findings were operational [superuser `school=None`] and a known data gap [D7 person roster].)
- **Operational note (dev):** the create UI is school-scoped; a dev superuser created via `createsuperuser` has `User.school=None` → ALL reference dropdowns render empty + the assist endpoint 403s. Set the user's `school` to `chiway-repton-xiamen` (admin) to exercise `/en/planner/create/`.
- **Next (user decides — no work in flight; awaiting direction at the next session):**
  - **Front-end direction choice.** Five plan-creation mockups exist at `mockups/{a-outline,b-timeline,c-kanban,d-dashboard,e-wizard}/` (one per UX-research §4 direction; open each `index.html` in a browser). They are decision input — **the user has not chosen a direction**, and no front-end build has started. The front-end is the prerequisite for end-user login/use ("no one logs in before the front end").
  - **US-ONB-4 admin CSV bulk import** — the remaining onboarding story (header contract: `user-import-stub.csv` = first_name,last_name,chinese_name,email). Can reuse the email+code verify mechanism for CSV users' first login; + the optional "Create linked Person" admin action.
  - **Remaining school-specific data:** subject-department people/holdings/heads (roster pending); User accounts (D7). (Subject departments themselves SEEDED `school/0035`/DEC-15; Role/D2 dissolved — `Role` retired, responsibility = `Division` + optional `Person`.)
  - **Post-Phase-15:** human end-to-end walkthrough (US-1..22 + US-ext + US-ORG + US-ONB, advance proposed→active, EN↔简体中文 grouped-field check) → US-STATUS `enabled`→`vivified` flips → close-out. Execution tracking is now mostly built (DEC-16): `ActionStep.status` audited + `Evidence.status` audited (§B + §H audit-half DONE); the remainders are US-ORG-19's rollup *view* (read-side, downstream) and §H's Evidence artifact payload/location half (separate storage decision).
- **Phase 12 ↔ 14 gate split: CLOSED.** Phase 14 IMPL (`68baef7`) appended all three deferred predicates to `_check_promotion_gates` (`student_impact_framing` non-empty; `planning_method` when `subject_kind.requires_planning_method`; priority-rationale-on-deviation via `plan.clean()` folded into the aggregate) AND extended `test_services_advance_plan_status.py` (+135 lines; `_complete_plan` builder now sets `student_impact_framing` so the baseline still passes). Verified in the diff before recording GO. US-22's promotion gate is now complete.
- **i18n presentation decision (Phase 13):** translated fields render GROUPED via `TranslationAdmin`, NOT JS tabs. Do not reintroduce `TabbedTranslationAdmin` (the Phase 13 test guards against a CDN jQuery-UI URL reappearing). New Phase-14 translated Plan fields tab into the grouped admin automatically.
- **US-STATUS:** US-ext flipped `pending → enabled` against commit `68baef7`. Cumulative enabled: US-1..US-15, US-16a/b/c/d/e/f/g/h, US-17..US-22, US-ext (30 of 31 of the original rows). Only the US-16 umbrella row remains pending (its sub-items US-16a..h are all enabled). **Pending stories:** US-ORG-12..17 + US-ORG-19 (front-end visibility/rollup — US-ORG-19's rollup *view* downstream; status data layer exists); **Enabled:** US-ORG-20 (blocked = stored `ActionStepImpediment`, `cc79349`); US-ORG-18 (audited per-step status + evidence attach, `cba276f`); US-ONB-3 + US-ONB-5 (`391b893`); US-ONB-1 + US-ONB-2 (`9d637ae`). **Pending US-ONB:** US-ONB-4 (admin CSV bulk import).
- **Runtime:** postgres up; volume preserved.
- **Branch:** `main`, unpushed (last user push `f0d5c67`). For the live count run `git rev-list origin/main..HEAD --count` — not hard-coded here, since a hard-coded number drifts on every commit (the same reason the per-commit ledger was retired).
- **HEAD:** `5de0381` (the five plan-creation mockups). Run `git log --oneline -30` for the authoritative recent ledger; the post-Phase-15 architecture work (DISC-17 renames, subject-dept seed, per-school-admin foundation Slices 1–6, user-onboarding flow, mockups) is the run since `98576d1`. (Earlier infra note: `c8b53ef` set pyright `typeCheckingMode: off`; mypy remains the type gate.)

## Commit ledger

**The authoritative, always-current commit ledger is `git log` itself** — run `git log --oneline -40` (or `git log --stat` for detail). This file no longer hand-duplicates the full per-commit table: a copy of an append-only log structurally cannot stay current (it can never record its own commit, and out-of-band commits slip until the next refresh), so that table was the only drift surface here. What follows is the LOW-CHURN synthesis a fresh session cannot reconstruct from `git log` alone: a per-phase key-commit map (grows one block per phase) + the standing cross-cutting decisions. Commit *messages* carry the forensic detail (the mandatory commit pattern); `git log` + the synthesis below + the phase MDs + US-STATUS + discoveries + memory = full reconstruction.

### Per-phase key commits (prep → IMPL → cleanup → tmp-track → state-refresh)

| Phase | Deliverable | Key SHAs |
|---|---|---|
| 0 | foundation scaffold | prep `299eedd` · IMPL `1846640` · test/smoke `bcc060f` · tmp `5280bb2` · state `1c3df8d` |
| 1 | users app (email-as-username) | prep `15e8810` · IMPL `a052b99` · state `231ef89` · tmp `889ffcd` · retro-tests `fcb576a` |
| 2 | school app (10 reference entities) | prep `1f462ad` · IMPL `a35a373` · retro-tests `846c765` · tmp `dd40cb5` · state `fb8e19b` |
| 3 | accreditation app (global WASC data) | prep `edd47ab` · IMPL `7ffd882` · tmp `17a04c1` · state `5931ff3` |
| 4 | school consultant vocabularies (5 models) | prep `6938d82` · IMPL `3920959` · cleanup `aa3eed8` · state `9089b2d` |
| 5 | plans scaffold + choices | IMPL `5a2d145` · tmp `c0b5746` |
| 6 | Plan core + domain M2Ms | prep `5558676` · IMPL `35a2653` · tmp `563cc87` · state `683ffbb` |
| 7 | SuccessCriterion/Measurement/FeedbackChannel | prep `bff40a2` · IMPL `56b2b38` · tmp `1daf955` · state `414b6cc` |
| 8 | Phase + ActionStep skeleton | prep `61a6d61` · IMPL `cfeba91` · cleanup `f49c1ec` · state `86957fb` |
| 9 | ActionStep children (5 models) | prep `19ae166` · IMPL `f5a02ce` · tmp `80b1c98` · state `2859439` |
| 10 | Outputs (Communication, Evidence + M2M) | prep `830ea8a` · IMPL `b07218f` · cleanup `312e05b` · state `2f87140` |
| 11 | Review loop (ReviewEvent/Input/RevisionRule) | prep `49a0249` · IMPL `c7649e8` · tmp `5ff3496` · state `7055f81` |
| 12 | promotion service + PlanStatusTransition | prep `e763262` · IMPL `2ae1c58` · cleanup `1416bd2` · state `ebb7eee` · admin-action test `ecb28dc` |
| 13 | i18n init (grouped TranslationAdmin + .po) | prep `17e5093`→`6e4d641` · IMPL `b1c84ce` · tmp `2a41eb8` · state `955c455` |
| 14 | WASC extension fields + gate-split close | prep `0b004fe` · IMPL `68baef7` · tmp `a09f50f` · state `88e3f56` |
| 15 | final validation (in progress) | static gate GREEN (run, no commit) · WASC-standards seed `c397785` · seed source `7e882c8` · reference-data spec `fdbbdc4` · School + FramingVocabulary seed `96500e2` · seed-round plan `2c6a3a6` · StakeholderGroup + AFI seed `43be661` · source-model-gaps `cfe4168` (+ evidence-auditability `a7272e7`, factual-language pass `d329601`/`a7efcd5`) · domain-model `21113a5` · tracking spine `d3b062e` · PriorityTier/MissionArea/PlanningMethod+Step seed `64c68eb` |

### Standing cross-cutting commits (decisions + resolutions)

- **DISC resolutions:** DISC-01 (ghcr.io→PyPI uv) `0d54ffe`; DISC-02-0519-A (drop django-stubs) `a68ba12`+`f0d5c67`; DISC-02-0519-B (6 translation regs) `c96d7aa`; DISC-03 (permission-seed `create_permissions`-first) `5be0231`; DISC-09 (ActionStepDependency.depends_on → PROTECT) `5daabef`. (DISC-05 resolved naturally at Phase 6 `35a2653`.)
- **Conventions/infra:** PHASE-LAUNCH-CHECKLIST `59c1cb5`; batched-verification convention `6f3e6ed`; impl-template ruff-format requirement `d08d54a`; in-phase admin-behavior-test convention `1ba6f57`; 2FA removal `80f46ae`; role/db rename → socrates `671fb0c`; pyrightconfig `7e97420` then typeCheckingMode-off `c8b53ef`.
- **Out-of-band (no main commits):** the 2-agent jQuery-UI offline-sourcing research (Phase 13) ran in isolated worktrees; findings drove the tabs→grouped relaxation; worktrees cleaned.

> Maintenance rule: append one phase block per phase + new standing-decision lines as they land. Do NOT reproduce the per-commit `git log`. After any commit, `git log --oneline` is the current ledger; this synthesis is refreshed at phase/decision boundaries.

## Last subagent invocations (most recent first)

- **`68baef7`** (Phase 14 IMPL): WASC-consultant extension fields. Added 9 fields to `Plan` in `plans/models/plan.py` (translated `theme`/`priority_rationale`/`student_impact_framing`/`provenance` all `blank=True, default=""`; FKs `framing_focus` [related_name plans_primary], `priority_tier`, `subject_kind`, `planning_method` all null/blank/PROTECT; M2M `secondary_framings` [related_name plans_secondary]) + `Plan.clean()` (priority-rationale-on-deviation). `PlanT` extended to 8 translated fields. **Appended the three deferred promotion-gate predicates** to `_check_promotion_gates` in `plans/services.py` (student_impact_framing non-empty; planning_method when subject_kind.requires_planning_method; priority-rationale via `plan.clean()` folded into the aggregate) — gate split CLOSED; verified in diff. `plans/admin/plan.py` fieldsets extended (Framing + Priority groups, provenance under Problem statement) with autocomplete/filter_horizontal as list literals. Migration `0016_*` generated non-interactively (the `default=""` on the new text fields made the existing-table ALTER non-interactive) + applied via superuser DSN. Tests: extended test_plan.py, new test_plan_clean.py, extended test_services_advance_plan_status.py (+135 lines; `_complete_plan` now sets student_impact_framing). 320 tests pass; all gates clean; IMPL ran ruff format proactively (no orchestrator cleanup). No DISCs.
- **`b1c84ce`** (Phase 13 IMPL): i18n initialization, GROUPED presentation. Converted 25 translated-field admins (15 school + 10 plans, set derived from the actual translation.py files) to `modeltranslation.admin.TranslationAdmin`; translated-field inlines (`PlanAccreditationStandardInline`, `SubStepInline`, `PlanningStepInline` → `TranslationTabularInline`; `RevisionRuleInline` → `TranslationStackedInline`); stock bases retained for the 6 non-translated admins + their inlines. Deliberately NOT the tabbed bases (no jQuery UI / no CDN — see i18n decision below). Rewrote `list_display`/`list_filter`/`fieldsets` tuple→list literals on every converted admin (the mypy `list[Any]` rule from subclassing a modeltranslation base). `makemessages -l en -l zh_Hans` from all 4 app dirs → 8 tracked `.po`; `compilemessages` exits 0. Added `plans/tests/test_admin_translation_fields.py` (3 Client tests: per-language inputs render on a school admin, a plans admin, and a translated-field inline; AND no `ajax.googleapis.com`/`jquery-ui` CDN URL present — guards the dependency from reappearing). No migrations (admin-only edits). 302 tests pass; all 7 gates + compilemessages green. No DISCs.
- **`2ae1c58`** (Phase 12 IMPL): plan promotion service. `plans/services.py`: `advance_plan_status(plan, target_status, by_user)` (matrix-gated; promotion gates on the two transitions into `active`; writes a `PlanStatusTransition` audit row per successful move; saves `plan.lifecycle`), `_check_transition_allowed` (the 6-path matrix; ValidationError otherwise; `closed` terminal), `_check_promotion_gates` (aggregates ALL failures into one ValidationError; implements only Phase-12 predicates — the three Phase-14 conditionals deliberately omitted per the gate split), `promote_proposal_to_plan` stub. `PlanStatusTransition` model in `plans/models/plan.py` (plan CASCADE, by_user PROTECT, auto_now_add `at`). Migrations `0014_planstatustransition.py` + `0015_extend_permission_groups_phase_12.py` (Layer A; view-only for all three groups — audit-semantics exception). Admin: `advance_plan_status_action` with an intermediate `target_status` confirmation page (`plans/templates/admin/plans/advance_plan_status.html`), wired into `PlanAdmin.actions`; per-plan ValidationErrors surfaced without aborting selection. Selectors: 3 NotImplementedError stubs. 2 test files (49 new tests; full matrix + audit-row writes + every Phase-12 gate predicate incl. aggregation + `*_all` flag shortcuts). mypy accommodation: `_ALLOWED_TRANSITIONS` keys/values are `str(...)`-coerced TextChoices members (no-django-stubs infers bare member as the (value,label) tuple). Post-IMPL cleanup (`1416bd2`): orchestrator ran `ruff format` on 5 IMPL files (actions, migration 0014, selectors, services, the service test) — the IMPL ran `ruff check` but not `ruff format`, so formatting drift went uncaught locally; format-only, 293 tests unchanged. NO DISCs. Gate green after cleanup.
- **`c7649e8`** (Phase 11 IMPL): `ReviewEvent` in `plans/models/review.py` (plan FK CASCADE, translated label, scheduled_date DateField required, translated scheduled_note TextField blank=True, responsible_department + responsible_role two-nullable-FK PROTECT polymorphism + `reviewevent_responsible_exactly_one` CheckConstraint, inputs M2M through `ReviewEventInput`). `ReviewEventInput` pure through-model (review_event CASCADE + feedback_channel CASCADE). `RevisionRule` (plan FK CASCADE, trigger FK PROTECT to ReviewEvent, translated condition + action TextFields). Translation: ReviewEventT(label, scheduled_note), RevisionRuleT(condition, action). Admin: ReviewEventAdmin with ReviewEventInputInline + RevisionRuleInline; standalone ReviewEventInputAdmin and RevisionRuleAdmin per admin-package spec. Migrations: `0012_reviewevent_revieweventinput_reviewevent_inputs_and_more.py` (auto-generated, ruff-formatted by IMPL proactively) + `0013_extend_permission_groups_phase_11.py` (Layer A; DISC-03 idiom). 3 per-model test files (19 new tests). Object-graph property documented in `test_revisionrule_cascades_when_plan_deleted_without_protected_trigger`: a naive `plan.delete()` attempts to cascade the trigger ReviewEvent, which RevisionRule.trigger PROTECT blocks — admissible delete order is rules → events → plan; test documents this property of the chosen PROTECT-on-trigger policy. 244 tests pass; all gates clean (IMPL ran ruff format proactively this round — no orchestrator cleanup needed).
- **`b07218f`** (Phase 10 IMPL): `Communication` in `plans/models/output.py` (plan FK CASCADE, audience FK PROTECT to StakeholderGroup, translated channel, frequency FK PROTECT, owner_department + owner_role two-nullable-FK PROTECT polymorphism + `communication_owner_exactly_one` CheckConstraint). `Evidence` in same file (plan FK CASCADE, translated label, owner polymorphism + `evidence_owner_exactly_one` CheckConstraint, status from EvidenceStatus.choices defaulting to PLANNED with free transitions, retention_until nullable DateField). `ActionStep.evidence_artifacts` M2M added to existing `plans/models/action.py` per MD line 25 directive; `filter_horizontal=("evidence_artifacts",)` added to `ActionStepAdmin`. Translation: CommunicationT(channel), EvidenceT(label). Admin: CommunicationAdmin, EvidenceAdmin with list_filter including status, no transition gating. Migrations: `0010_evidence_actionstep_evidence_artifacts_communication_and_more.py` (auto-generated; 2 tables + M2M through-table + both CheckConstraints) + `0011_extend_permission_groups_phase_10.py` (Layer A; DISC-03 idiom). 2 per-model test files (17 new tests). No DISCs filed. Post-IMPL cleanup (`312e05b`): ruff format on `0010_*` (7th instance of auto-generated-migration format-drift pattern per checklist) combined with tmp/ track. 225 tests pass; all gates clean.
- **`5daabef`** (DISC-09-0520-A resolution subagent): one targeted change. `ActionStepDependency.depends_on` FK rule changed from `on_delete=models.SET_NULL` to `on_delete=models.PROTECT` in `plans/models/action.py`; auto-generated migration `plans/0009_alter_actionstepdependency_depends_on.py` applied; test renamed to `test_actionstepdependency_depends_on_protect_blocks_target_delete` with assertion changed from `IntegrityError` to `ProtectedError`; introspection test now asserts `PROTECT`; module docstring + field comment rewritten to describe PROTECT semantics and reference DISC-09-0520-A; `phases/phase-09-action-step-children.md` line 46 updated from `on_delete=SET_NULL` to `on_delete=PROTECT` so MD agrees with as-shipped code. Verification: 208 tests pass, all gates clean. Resolves DISC-09-0520-A — same observable behavior (delete blocked when within-plan dep references the target) via the FK rule that names what it does, no longer dependent on a constraint-cascade race.
- **`f5a02ce`** (Phase 9 IMPL): 5 new ActionStep child models in `plans/models/action.py`. `SubStep` (action_step CASCADE, translated description, ordering). `Assignment` (3-way polymorphic owner over department/role/stakeholder_group with PROTECT per Phase 7 precedent + 3-disjunct exactly-one CheckConstraint; kind from AssignmentKind.choices). `Timeline` (kind from TimelineKind.choices; date/from_date/to_date nullable DateFields + note TextField; 4-arm per-kind CheckConstraint encoded as Q() disjunction). `RequiredResource` (kind from ResourceKind.choices; note TextField). `ActionStepDependency` (action_step CASCADE as dependent, depends_on nullable SET_NULL as within-plan target, cross_plan_ref regex-validated CharField; exactly-one-of-target + no-self-loop CheckConstraints). Admin: 5 inlines registered on `ActionStepAdmin` + 5 standalone ModelAdmins. Migrations `0007_*` (auto-generated; modeltranslation columns for SubStep.description) + `0008_extend_permission_groups_phase_9.py` (Layer A; DISC-03 idiom). 5 per-model test files (53 new tests). Departure: IMPL test-pinned the SET_NULL/exactly-one contradiction via `test_actionstepdependency_depends_on_set_null_on_delete_then_exactly_one_violated` (encodes IntegrityError-on-cascade as the intended invariant) rather than logging a DISC — orchestrator subsequently logged the architectural-debt DISC-09-0520-A for forensic record so user can decide whether the as-shipped invariant is desired. 208 tests pass; all gates clean (no post-IMPL cleanup needed — IMPL ran ruff format proactively this round).
- **`cfeba91`** (Phase 8 IMPL): `Phase` in `plans/models/phase.py` (plan FK CASCADE, translated label, order IntegerField, `Meta.ordering=["order"]`); `ActionStep` in `plans/models/action.py` (plan FK CASCADE, phase FK nullable SET_NULL per US-17 "optionally groups", translated description, order). Admin: `PhaseAdmin` with `raw_id_fields=("plan",)`; `ActionStepAdmin` with `raw_id_fields=("plan","phase")` — skeleton (no inlines until Phase 9 per MD step 5). Migrations: `0005_phase_actionstep.py` (auto-generated; modeltranslation per-language columns materialized) + `0006_extend_permission_groups_phase_8.py` (Layer A; DISC-03 idiom). 2 per-model test files (15 new tests). No DISCs filed. Post-IMPL cleanup (`f49c1ec`): ruff format on `0005_phase_actionstep.py` + `test_actionstep.py` (standard auto-generated-migration drift pattern per checklist § Post-IMPL cleanup; 5th instance). 155 tests pass; all gates clean after cleanup.
- **`56b2b38`** (Phase 7 IMPL): `FeedbackChannel` (two-nullable-FK owner polymorphism + exactly-one CheckConstraint), `SuccessCriterion` + `Measurement` + `MeasurementChannel` (target-fields-when-applicable CheckConstraint). 140 tests.
- *(Phases 1–6 IMPL + DISC-01/02/03 resolutions: see Commit history table above; detail trimmed here per state-refresh template.)*

## Open decisions awaiting user direction

1. **Push the unpushed commits to `origin/main`** (last user push was `f0d5c67`; branch is now ~27 ahead — all Phase 3–13 IMPL commits, DISC resolutions, the PHASE-LAUNCH-CHECKLIST, the two-agent jQuery-UI research outcome, plus state-refresh commits).

Nothing else open. All discoveries (DISC-01-0519-A, DISC-02-0519-A, DISC-02-0519-B, DISC-03-0519-A, DISC-05-0520-A, DISC-09-0520-A) are resolved. The Phase 13 "language tabs → grouped" relaxation was a user-approved spec change, not a DISC.

## Verification convention (binding from 2026-05-20)

Per-phase human browser-create verification is **deferred** until all phases (0 through 15) commit. The per-phase gate is automated only: static checks (`manage.py check`, `ruff`, `mypy`, `pytest`) + per-model `tests/test_<model>.py` coverage. After phase NN's gate is green the orchestrator proceeds directly to phase NN+1; no Part B human pause between phases.

A single post-Phase-15 verification pass batches:
- **Initial-data migrations** seeding known reference data from mock-ups and planning documentation (WASC accreditation standards from `sources/extracted/`, learner outcomes, school departments, mission areas, planning methods, framings, priority tiers, subject kinds).
- **End-to-end create-plan walkthrough** against populated data (US-1 through US-22 + US-ext).
- **US-STATUS** rows flip `enabled` → `vivified` after end-to-end exercise during this pass.

Phase 0–3 already-committed work is unchanged by this convention; their "Phase N GO" labels stand on the automated gate. Phase 3 Part B (browser-create AccreditationCategory + AccreditationStandard) is no longer pending — folded into the post-Phase-15 pass.

## Pending orchestrator actions

Phase 15 is IN PROGRESS. Reference-data population is nearly complete — seeded: WASC standards, School, FramingVocabulary, StakeholderGroup, AFI, PriorityTier, MissionArea, PlanningMethod+Step (+M2M), ImprovementType (32; renamed from SubjectKind, `19cc102`), Frequency, Division (5; renamed from Department, `d5a2ad2`), LearnerOutcome (SLOs), 3 division-owned Policies.

**Immediate next (user-directed — all decided, build pending; one plan→impl):**
1. **`Department` → "School Division"** (model `Department`→`Division`, verbose name, FKs/admin/seeds/translation/perms) + introduce **Subject Department** for academic units under a division (DEC-13).
2. **`SubjectKind` → `ImprovementType`** (model + all refs/seeds/candidate-doc/US/tests) AND **move it (and `planning_method`) off `ActionStep` onto `Milestone`** — `Milestone.improvement_type` + `Milestone.planning_method`, relocate the `requires_planning_method` gate predicate to per-milestone (DEC-14, refines DISC-14).
Decisions persisted (DISC-17 / DEC-13 / DEC-14 / domain-model); only the build remains.

Remaining:
1. **DISC-16 — org-structure remodel: model + structural data DONE.** Built: `Person`, `Department.parent`+`head`, responsibility = Unit + optional Person, `Role` retired (`048cf71`); `Position` (named seat) + `Holding` (`Person↔Position` M2M) replacing `Membership` (`6f7813d`/`00c5dea`, DEC-12). Seeded: authoritative **5 departments** + **5 vacant positions** + policy repoints (`98576d1`). US-ORG-1..11 enabled. **Remainder (sequenced, in scope):**
   - (a) **Subject departments + people data** — subject-dept child units under C&T (HOD = each one's head; Teacher positions) + `Person`/`Holding`/`head` population. Needs the user's subject-dept names + roster. Data step.
   - (b) **Execution tracking — DONE (DEC-16, `deca4d5`/`cba276f`/`c54f8046`):** audited `ActionStep.status` + per-step `assessment` + `communications` (§B resolved) and audited `Evidence.status` (§H audit-half resolved). US-ORG-18 enabled. Remainders: US-ORG-19's rollup *view* (read-side, downstream w/ the front-end) + §H's Evidence artifact payload/location (separate storage decision).
   - (c) **Front-end visibility (US-ORG-12–17,19):** personalized/permission-scoped home + head roll-up — front-end build informed by `docs/ux-prior-art-research.md`.
2. **Open decisions** remaining: **D7** user accounts + `User`↔`Person` data (the link is built; accounts/links are data). (D1–D6 resolved; **D2 dissolved** — Role retired.)
3. **DISC-15 — Milestone tier: DONE** (`e824283`).
4. **Human end-to-end walkthrough** (Phase 15 steps 2–3, user-performed): US-1..US-22 + US-ext, create one Plan, advance proposed→active via the `Advance plan status` action (MUST exercise the advance-status intermediate page — the one custom-template affordance), switch EN↔简体中文 observing grouped per-language fields + translated choice/Meta labels. (Best sequenced after the DISC-16 remainder so the walkthrough exercises the real org structure.)
5. **US-STATUS `enabled`→`vivified`** for rows the walkthrough confirms; close the US-16 umbrella row.
6. **Project close-out** state refresh.

`templates/impl-static.md` carries a `<pre_commit_checks>` block requiring the IMPL to run the full static suite — including `ruff format .` (write fixes), not just `ruff check` — before committing (added after Phase 12 left 5 files unformatted; Phase 13 IMPL came back format-clean, so it's working). Orchestrator-side post-IMPL ruff cleanup should now be rare exception-handling, not routine.

## Phase 0 verification artifacts (kept for reference)

- `cd school-improvement-plans && docker compose up -d postgres` brings postgres up on host port 5433.
- `pg_isready -h localhost -p 5433 -U postgres -q` checks readiness.
- `uv run python manage.py migrate` applies migrations (no env-var prefix needed; `local.py` carries dev defaults).
- `uv run python manage.py createsuperuser` (interactive — human runs this step).
- `uv run python manage.py runserver 8008` starts the dev server.
- `curl http://localhost:8008/health/` returns `200`.
- `curl http://localhost:8008/admin/` returns `302` (i18n redirect to `/en/admin/`). `/en/admin/login/` returns `200` (Django's stock admin login). Login → admin index.

## Known issues / discoveries (state of `phases/discoveries.md`)

- **`DISC-01-0519-A`** (Phase 1, verification-failure, **resolved**) — docker build failed at `COPY --from=ghcr.io/astral-sh/uv:0.9.11` with TLS handshake timeout. Root cause: ghcr.io structurally unreachable from this network. Resolved by commit `0d54ffe` (PyPI install).
- **`DISC-02-0519-A`** (Phase 2, architectural-debt, **resolved**) — django-stubs manager resolution failures on `models/` package layout. Resolved by commits `a68ba12` + `f0d5c67` (django-stubs removed).
- **`DISC-02-0519-B`** (Phase 2, artifact-conflict, **resolved**) — 5 missing modeltranslation registrations vs Phase 2 MD. Resolved by commit `c96d7aa` (all 6 fields registered; migration school/0003 adds per-language columns).
- **`DISC-03-0519-A`** (Phase 3, architectural-debt, **resolved**) — permission-group seed migrations read empty Permission rows on fresh-DB single-shot migrate. Phase 3 fixed accreditation/0002; school/0002 retrofitted via commit `5be0231`.
- **`DISC-05-0520-A`** (Phase 5, artifact-conflict, **resolved**) — Phase 5 MD's "admin shows empty Plans section" verification clause now satisfied. Phase 6 commit `35a2653` registers `Plan` with PlanAdmin + 2 inlines, so the admin index renders a "Plans" heading naturally; the literal-vs-charitable reading is moot.
- **`DISC-09-0520-A`** (Phase 9, architectural-debt, **resolved**) — `ActionStepDependency.depends_on` changed from `SET_NULL` to `PROTECT` per user direction in commit `5daabef`. The observable "delete blocked while target depended upon" behavior is preserved; the mechanism is now the FK rule that names what it does (raises `ProtectedError`) rather than a constraint-cascade race (raised `IntegrityError`). Mirrors the project's PROTECT convention at `Plan.author`, `FeedbackChannel.owner_*`, `Assignment.*`.
- **`DISC-21-0525-A`** (Phase 15, architectural-debt, **resolved** `7217045`) — `reindexRow` reindexed a cloned formset row's `name` but not its `id_`-prefixed `id`/`for` (the `^(${prefix})-` regex couldn't match past Django's `id_` auto-prefix) → duplicate ids + mis-targeted labels on every JS-added row (the "+ Add" control + the F4/F5 multi-row assists). Surfaced by the DEC-31 jsdom harness; user directed the fix as the next step (canonical loop). Resolved by widening the regex to `^(id_)?(${prefix})-(\d+)-` (replacement `$1$2-${newIndex}-`): `$1` captures the optional `id_`, only the leading index segment rewrites (nested own index stays 0). JS-only; apply functions untouched (select by `[name$=]`). The harness now asserts the fix (flipped id assertion + new `for`-reindex + nested-id-reindex tests; `node --test` 25→27; a local revert-probe to the old regex made them fail). Gate: make test-js 27/27, mypy 260, pytest 1079.
- **Phase 13 i18n spec relaxation (not a DISC)** — the Phase 13 verification clause "language tabs in admin" was changed to "grouped per-language fields" (`TranslationAdmin`, not `TabbedTranslationAdmin`) by user decision (prep `6e4d641`, IMPL `b1c84ce`). The tabbed widget needs jQuery UI, which modeltranslation defaults to `//ajax.googleapis.com` — unreachable on this build network (DISC-01) and blocked in China. A 2-agent worktree research+POC pass proved both offline-sourcing paths (vendor the asset; add a PyPI package) work, but both exist only to feed a JS widget that at two languages is worse UX than grouped. Grouped needs zero jQuery UI / CDN / vendoring / dependency. The Phase 13 test guards against any CDN jQuery-UI URL reappearing. **Do not reintroduce `TabbedTranslationAdmin`.**
- US-STATUS: US-1..US-15 + US-16a/b/c/d/e/f/g/h + US-17..US-22 + US-ext flipped to `enabled` (US-1..US-12 against `35a2653`; US-13/14/19 against `56b2b38`; US-15/16a/17 against `cfeba91`; US-16b/c/d/e/g/h against `f5a02ce`; US-16f/18 against `b07218f`; US-20/21 against `c7649e8`; US-22 against `2ae1c58`; US-ext against `68baef7`). 30 of 31 rows enabled. Only the US-16 umbrella row remains pending (its sub-items US-16a..h are all enabled). US-22's gate is now COMPLETE (gate split closed by `68baef7`). Rows flip to `vivified` only during the post-Phase-15 batched verification pass.

## Update protocol (orchestrator)

This file is the SYNTHESIS layer. The continuous commit ledger is `git log` (never lags, zero maintenance) — do not duplicate it here. The exact chronological record is `ORCHESTRATOR-LOG.md` (append-only). The orchestrator updates persistence:
- **Append to `ORCHESTRATOR-LOG.md` at the moment each event happens** (definition of done for any state change): one line per commit, user direction, decision, established definition, recorded gap, or correction. Append-only — never rewrite or delete a prior line. This is the drift-proof layer; do it before, not after, refreshing this synthesis. No hooks — this is orchestrator discipline.
- **Proactively persist conversational establishments**: when a definition, decision, or gap is settled in conversation, land it that turn — in its home doc (`docs/domain-model.md`, `data/seed-round-plan.md`'s DEC log, `data/source-model-gaps.md`) and as an `ORCHESTRATOR-LOG.md` line — rather than carrying it only in working memory.
- **At every phase/decision boundary** (phase GO, DISC resolution, a user-directed decision, a convention/config change): update `Current position`, the per-phase commit map / standing-decisions list, `Last subagent invocations`, `Open decisions`, `Pending orchestrator actions`, and `Known issues / discoveries`; bump `Last updated`.
- **Before ending a session**: refresh `Last updated` and confirm `Pending orchestrator actions` reads as the literal next move.
- After every user direction that resolves an open item, remove it from `Open decisions`.

Continuous-persistence invariant: at any instant, a fresh session reconstructs full state from `git log` (ledger, never lags) + `ORCHESTRATOR-LOG.md` (append-only event spine, never rewritten) + this synthesis (current as of the last boundary) + the phase MDs + `phases/US-STATUS.md` + `phases/discoveries.md` + the memory store. Because every action is a forensic commit (the mandatory commit pattern) AND an append-only log line, nothing is ever lost even if this synthesis trails — the trailing events are fully described in their commit messages and log lines. Do NOT hard-code the ahead-count or reproduce per-commit rows here; those are the drift surfaces that were removed.

A new session reads this file first, then the `ORCHESTRATOR-LOG.md` tail + `git log --oneline -20` + `git status` to pick up anything past the last synthesis boundary.
