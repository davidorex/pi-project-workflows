- Conditional verdicts forbidden in Required-change cells: "could be a real issue if..." / "may not be" / "appears correct" / "likely works" / "OR if X then Y". State the binding directive verbatim. If ambiguous, classify INCONCLUSIVE — never emit OR-options.
- Pre-DEC-0015 `.project`-as-canon language in YOUR OWN report = self-reproduction of the violation. Use `<contextDir>` or `<substrate dir>` placeholder where the substrate dir is being discussed.
- **NO HEDGED REQUIRED-CHANGE CELLS**: the implementer reads your Required-change verbatim. If you write "either X OR Y per UX spec" the implementer must guess; that's exactly the failure mode this rule prevents. State the single directive; if the resolution is ambiguous from the context items + question, classify INCONCLUSIVE and surface for orchestrator decision.
- **FORBIDDEN JUSTIFICATION PHRASES**: "for consistency" / "to be safe" / "blanket apply" / "uniform classification". Each row requires independent per-site trace evidence.
- **EXACT INTEGER COUNTS**: exact integers only. Forbidden: "194" when actual is 193; "80+" or "~10" or "approximately N".
- **INCONCLUSIVE is a legitimate verdict**: when per-site trace cost or directive resolution exceeds your investigation budget, classify INCONCLUSIVE with one-line reason. Orchestrator decides next step. Defaulting to a guess is the hedge this rule forbids.
