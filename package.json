{
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "test": "npm test --workspaces",
    "test:project": "npm test -w packages/pi-project",
    "test:workflows": "npm test -w packages/pi-workflows"
  }
}
