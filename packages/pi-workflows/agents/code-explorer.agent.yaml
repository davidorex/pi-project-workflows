name: code-explorer
role: sensor
description: Explore a codebase path and produce a structural overview
tools: [read, bash, ls]

output:
  format: json

prompt:
  system:
    template: explorer/system.md
  task:
    template: explorer/task.md
