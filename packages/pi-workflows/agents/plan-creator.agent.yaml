name: plan-creator
role: reasoning
description: Takes project identity, requirements, and architecture, then creates implementation phases and tasks

tools: [read, bash, grep, find]

output:
  format: json

prompt:
  system:
    template: plan-creator/system.md
  task:
    template: plan-creator/task.md
