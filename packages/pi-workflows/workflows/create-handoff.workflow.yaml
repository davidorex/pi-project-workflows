name: create-handoff
description: Capture current work context for session resume
version: "1"

steps:
  load-blocks:
    block:
      read: [project, issues, decisions, tasks]
      optional: [project, issues, decisions, tasks]
    output:
      format: json

  load-phases:
    block:
      readDir: phases
    output:
      format: json

  load-git:
    command: |
      node -e "
        const { execSync } = require('child_process');
        const git = {};
        try {
          const lines = execSync('git log --oneline -10', { encoding: 'utf-8' }).trim().split('\n').filter(Boolean);
          git.recent_commits = lines.map(line => {
            const spaceIdx = line.indexOf(' ');
            return { sha: line.slice(0, spaceIdx), message: line.slice(spaceIdx + 1) };
          });
        } catch (e) {
          git.recent_commits = [];
          git.commits_error = e.message || 'git log failed';
        }
        try {
          git.changed_files = execSync('git diff --name-only HEAD~3 2>/dev/null || echo \"\"', { encoding: 'utf-8' }).trim().split('\n').filter(Boolean);
        } catch (e) {
          git.changed_files = [];
          git.files_error = e.message || 'git diff failed';
        }
        console.log(JSON.stringify(git));
      "
    output:
      format: json

  assemble-state:
    command: |
      node -e "
        const blocks = JSON.parse(process.argv[1]);
        const phases = JSON.parse(process.argv[2]);
        const git = JSON.parse(process.argv[3]);
        console.log(JSON.stringify({
          project: blocks.project,
          phases: phases,
          issues: blocks.issues,
          decisions: blocks.decisions,
          tasks: blocks.tasks,
          recentCommits: git.recent_commits || [],
          changedFiles: git.changed_files || [],
          blocks_status: {
            project: blocks.project != null,
            issues: blocks.issues != null,
            decisions: blocks.decisions != null,
            tasks: blocks.tasks != null,
            phases: phases.length > 0,
            git_commits: !git.commits_error,
            git_files: !git.files_error
          }
        }));
      " '${{ steps.load-blocks.output | shell }}' '${{ steps.load-phases.output | shell }}' '${{ steps.load-git.output | shell }}'
    output:
      format: json

  capture:
    agent: handoff-writer
    input:
      project_state: ${{ steps.assemble-state.output }}
      path: "."
    output:
      format: json
      schema: block:handoff
    retry:
      maxAttempts: 2

artifacts:
  handoff:
    path: .project/handoff.json
    from: steps.capture.output

completion:
  message: |
    Handoff captured to .project/handoff.json. Resume context is available for the next session.
